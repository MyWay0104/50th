# P1 빌더 메모 (2026-09-18)

1. 검사: `check_fragment` 통과(15장면) · `deck-rules.json` 금지 패턴 자체 점검 0건 · 1920×1080 미리보기로 넘침 실측.
2. **LAB01~03은 현재 CSS로는 넘친다**(LAB01 +180px · LAB02 +96px · LAB03 +47px — 완료 조건·막히면이 S08보다 길고 `.when` 줄이 더해짐). S08에서 복제된 규칙은 `time-band` 두 줄뿐이다. → **CSS 요청**: `blocks/P1-css-request.css`(검증본, 붙이면 세 장면 모두 952 안). 붙이지 않으면 문안을 줄여야 한다.
3. LAB01: 열쇠 지도 축소판은 콘티의 대체 지시대로 개념 칩 줄 오른쪽 끝에 둠(요청 CSS에서 한 줄). LAB03: 관찰 세 가지 체크 카드는 뺌(설명문이 같은 세 가지를 말하고, 넣으면 오른쪽 칸이 190px 더 필요).
4. E01: 가로 점선 영역 세 개 + 열쇠 지도는 어떤 격자에서도 100px 이상 넘쳐, **왼쪽을 세로 세 층 상자(`wide-right`)** 로 바꿈. 층 이름표는 상자 제목에 합침("내 PC · 실습 코드"), ⇄ 화살표 없음, 왼쪽 캡션 앞 절은 콘티 지시대로 뺌, 코드 위치 줄은 왼쪽 아래. 열쇠 지도 글자는 그대로. 여유 13px. S23(P3)이 같은 그림을 쓰려면 세로 모양을 따라야 한다.
5. T01: 요지 2줄 + 설명문 3줄이면 세 열이 160px 넘쳐 **설명문(.explain) 세 문장을 화면에서 뺌**(노트에 같은 내용 있음). 그래도 여유 0px(둘째 열 환각 정의 2줄).
6. N01: 코드가 808px 칸에 안 들어가 `wide-right`. 첫 줄은 8줄·폭 제한 때문에 주석을 윗줄로 올리고 가상 이름을 `MODEL` · `GATEWAY`로 줄임. 번호 칩(① ② ③)은 코드 주석과 겹쳐 뺌, "변수만 바꿉니다" 칩은 한 줄에 안 들어가 뺌, 코드 위치 줄은 왼쪽. 파이프 라벨은 `has-label`이 상자를 덮어 도식 아래 캡션("세로 막대 | · 앞의 출력이 뒤의 입력")으로. `{topic}` 주황 표시는 코드 안에서만(`.hl`은 `.code-body` 전용).
7. S04: 제목 옆 아이콘용 CSS가 없어 두뇌 아이콘을 한 줄 요지 앞 `.inline-icon`으로 둠. **CSS 요청(선택)**: 제목 왼쪽 아이콘 배치.
8. S07: 눈금은 `.scale-line` 한 줄("0 · 비슷한 답" ─ "다양한 답 · 2"). 눈금 아래 좌우 라벨 줄은 CSS가 없어 양 끝 글자에 합침. L01: B 카드의 ② ③ 주황 표시는 `.hl`이 코드 전용이라 글자만, 격자는 1:1.
9. G01: `.panel-title`과 "준비 · 01" 표시가 겹치지 않게 띠를 `.dg.is-sep`로 감쌈(겹침 없음, 아래 끝 870). 캡션 두 줄은 `.stack.is-fig`.
10. 높이 빠듯: T01(0px) · E01(13px) · N01 · LAB01(요청 CSS 적용 시 0px). 노트는 강사 스크립트 + 전환 문장(예상 질문·메모·소요시간은 옮기지 않음).
