# P4 · APP 조각 메모 (2026-09-18 · 1920×1080 크롬 렌더로 높이 확인)

1. **G02 · `li.axis-prep`("준비 · 01")를 뺐다.** 띠 위 84px로 튀어나와 한 줄 요지와 겹치고(본문 흐름의 띠 `margin-top` 48px), 자리를 만들면 세로가 넘친다(콘티 part1 "거슬리면 G02에서만 뺀다" 적용). 되살리려면 `ol` 첫 자식으로 `<li class="axis-prep"><img class="axis-icon" src="assets/img/icons/key.svg" alt="준비 아이콘"><span data-editable="true">준비 · 01</span></li>` + CSS 요청 ①②. 정거장 구조는 G01 사양 그대로(`.axis-tile → .axis-label → .dg-sub → .chip`, 칩 글자 "실습 02 · 03" …).
2. **G02 · 넘침 대비 ①②를 모두 썼다**: 카드 묶음 라벨과 성과 카드의 작은 글씨 네 줄을 화면에서 빼고, 네 줄의 낱말은 노트에 한 문장으로 옮겼다(그래도 남는 여유 26px).
3. **S39 · 띠 캡션 "어느 쪽이든 …"을 화면에서 뺐다**(노트에는 있다). 콘티의 넘침 대비 ①②(설명문 셋째 문장 삭제 · 배지를 윗줄로)를 다 써도 58px 넘쳤다. 띠 상자는 `inline-icon + span(글자 + dg-sub)` 가로형, 띠와 세 카드는 `.stack` 하나에 넣었다. 첫째 카드 배지도 같이 윗줄로 옮겼다. **여유 0px**(카드 아래 여백 39px).
4. **LAB08 · 콘티의 넘침 대비 배치**(불릿 두 줄 삭제 · 왼쪽 소단계 2×2 · 오른쪽 완료 조건 + 막히면). 작은 글씨는 `span.when.dg-sub`. "질문할 때 보여 줄 세 가지"는 칩 줄 맨 앞 `p.dg-caption`(따로 한 줄이면 15px 넘침). `.min`은 넣었지만 LAB08 별칭 CSS가 숨겨 번호가 안 보인다.
5. **M01 · 제목을 P4-8 첫째 줄임으로**(전체 이름은 두 줄). "오전에 만든 장비 조회 Agent"는 안내 줄 앞머리로.
6. **A05 · 열쇠 아이콘은 넣지 않았다**(`.step-icon`이 절대 위치라 둘이 겹친다 · 콘티 "자리가 없으면 글자만"). ① 예시는 긴 문안 그대로 세 줄에 들어간다.
7. **콘티에 없는 낱말 치환 3건(검사 규칙 때문)**: S25 화면 `"해당 장비 없음"` → `"장비 없음"`(deck-rules `해당 장(?!면)`의 오탐 — 정규식을 `(?![면비])`로 고치면 되돌려도 된다) · S30 노트 "통째로 복사" → "그대로 복사" · C04 노트 "별개로" → "는 따로".
8. D07 노트는 강사 스크립트 두 문장만(전환 문장 제외 · 브리프 3절). S40 · S43 · S44 · C02 · S28 · S30 · C04는 콘티 쌍 그대로.
9. **CSS 요청**: ① `.scene:not(.title-scene) > .axis-strip:has(> .axis-prep) { margin-top: 108px; }`(G01도 같은 겹침) ② G02에 ①을 쓰려면 `[data-scene-id="G02"] > .explain.is-closing { margin-top: 32px; }` ③ `.compare-kicker:has(> .concept-badge)`의 줄 높이 고정(S39 셋째 카드 제목선이 7px 높다) ④ LAB08 번호를 보이려면 별칭 규칙의 `.min { display: none }`에서 LAB08 제외 ⑤ `.time-block .what .when`을 작은 글씨로(지금은 `.dg-sub`를 함께 줘서 해결).
10. **높이가 빠듯한 장면**: S39(0px) · G02(26px) · LAB08(오른쪽 칸 24px) · M01(제목 오른쪽 110px). **유지 장면 C03은 원본 그대로인데 오른쪽 표가 86px 넘친다**(right 1886 > 1800 · 기존 문제, 고치지 않았다).
