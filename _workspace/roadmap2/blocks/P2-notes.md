# P2 빌드 메모 (2026-09-18 · 13장면 · check_fragment 통과 · 2차: 장면 전용 압축 CSS 반영)

실측 방법: `index.html`의 `#scene-styles` 뒤에 `blocks/P2-css-request.css`를 붙인 임시 페이지를 헤드리스 크롬 1920×1080으로 쟀다. **세로 넘침 0(모든 장면 아래 끝 y ≤ 894, 출처 줄 위), 소단계 칸의 가로 삐져나옴 0.** LAB04 · LAB04b · S15 · S16은 이 CSS가 있어야 맞는다(CSS 없이 재면 LAB04 1084 · LAB04b 1063 · S16 905 · S15는 두 열이 눌림).

1. **아래 끝 y(본문 → 출처 줄 894)**: D03 · D04 878(출처 없음 · 한계 952) · S10 871 · V01 · V02 894(원본 그대로 · 본문이 남는 높이를 채움) · R01 864 · LAB04 왼쪽 884 / 오른쪽 894 · S14 835 · T02 876 · S15 875 · S16 877 · LAB04b 왼쪽 851 / 오른쪽 894 · N02 871.
2. **LAB04 — 되살린 것**: 개념 칩 줄 ①~⑤(③ 앞 열쇠 아이콘) · `tb-icon` 다섯 · `.when`(실습자료 4-1 / 4-2) · 카드 아래 작은 글씨({context} · {question}) · 출처 줄. 콘티에서 빠진 것 없음. 5칸은 `time-band` + CSS로 같은 폭(칸 156px), 줄 나눔은 `.nowrap`으로 고정, 코드 카드는 눌리지 않게 `.dg`로 감쌌다.
3. **LAB04b — 되살린 것**: 개념 칩 줄 ①~④ · 열쇠 지도 칩 한 줄(셋째 강조 + 꼬리) · `tb-icon` 넷 · 출처 줄. **여전히 뺀 것**: `.when` 줄(첫 칸 폭 151px에서 "실습자료 4b-1"이 두 줄로 접혀 +68px) · 메시지 목록 카드의 system 줄(콘티가 먼저 빼라고 한 줄 · 오른쪽 열 여유 0). 칸 폭은 글자 길이에 비례(`flex: 1 1 auto`) — 넷째 칸 문안이 길어 같은 폭으로는 세 줄이 된다.
4. **S15 — 되살린 것**: 설명문 첫 문장 "Tool은 두뇌의 손발입니다."(`p.explain` 한 줄). **여전히 뺀 것**: 설명문 둘째 · 셋째 문장 · 캡션 · 왼쪽 열 셋째 줄("어디서 나온 값인지 알 수 없음"). 띠는 `inline-icon` 낮은 상자, "개념 예시" 배지는 띠 앞, "질문" 상자에 `message-square` 아이콘.
5. **S16 — 되살린 것**: 코드의 `bind_tools` 줄(카드 ① 주석 · 코드 다섯 줄). **여전히 뺀 것**: 설명문 · 되돌이 화살표 줄("호출 요청이 있으면 다시 ②") · 계산 띠의 화살표와 "답변" 상자. 계산 예시는 코드 카드 오른쪽 세로 칩 셋. 캡션(C-8 통일 구절) · `return a / b` · ⓐⓑⓒ 유지.
6. **S10**: `.dg-zone` 두 개를 쌓으면 145px 넘쳐, 구역 상자 없이 `.dg-row.stretch` 두 줄 + 줄 머리 `row-icon`(database · search) + `concept-badge`("미리 준비" / "질문할 때"). CSS 불필요.
7. **T02**: 설명문은 셋째 문장만(첫째 · 둘째는 세 열과 같은 말). **N02**: 설명문 한 문장을 빼고, 열쇠 지도 칩 줄을 오른쪽 열 안(두 줄로 접혀 98px 넘침)에서 두 열 바로 아래 한 줄로 옮김. 꼬리는 세로 정렬 때문에 `.dg-caption`. **R01**: `.stack.is-fig`(간격 12px). **S14 · V01 · V02**: 콘티의 치환 쌍 그대로.
8. **CSS 파일**: `blocks/P2-css-request.css` — 선택자는 모두 `.scene[data-scene-id="…"]`로 시작, 글자 크기 최소 24px. LAB04 · LAB04b: 설명문 28px · 격자 위 여백 16px · 열 안 간격 12px · 소단계 띠(열 간격 20px · 화살표 위치 · 번호 원 44px · 글자 24px · 아이콘 28px · 위 정렬) · 칩 24px · 완료 28px · 막히면 24px · 자리표시 여백과 글자 24px · 코드 카드 24px. S15: 두 열 여백 24×36 · 목록 간격 · 띠 상자 여백. S16: 카드 여백 20×32 · 코드 여백 18×28 · 간격 16px.
9. **노트**: D03 · D04는 표지 규약(두 문장)에 맞춰 콘티의 전환 문장을 붙였고, LAB04 · LAB04b 노트 끝에 X-13의 손가락 질문(전환 문장)을 넣었다(S14 · N02 노트가 그 답의 확인으로 시작하므로).
10. **빠듯한 곳**: LAB04 왼쪽 열 여유 10px · LAB04 · LAB04b 오른쪽 열 여유 0(자리표시가 남는 높이를 흡수) · S16 17px · T02 18px · S15 19px.
