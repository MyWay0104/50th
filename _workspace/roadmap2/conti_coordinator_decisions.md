# 콘티 v1 · 코디네이터 결정 (2026-09-18 밤)

콘티 4묶음(`docs/edu-conti-v1-2026-09-18-part1~4.md`) 말미의 「확인 필요 사항」과 CSS 요청에 대해 메인 에이전트(코디네이터)가 내린 결정이다. 사용자는 부재 중이며 의사결정 권한을 위임했다. 콘티 확정(T3)과 빌드(T5)는 이 문서를 확정 입력으로 쓴다. "사내 확인"으로 분류된 질문은 `docs/handoff-inhouse-claude-lab-check.md`에 문항으로 넣었고, 답이 올 때까지는 콘티가 고른 "확인된 범위만 말하는" 문안을 쓴다.

## 1. 전 묶음 공통

| # | 항목 | 결정 |
|---|---|---|
| C-1 | 비유 축 아이콘 파일(전 묶음 같은 파일) | 두뇌 `brain.svg` · 책 `book-open.svg` · 손발 `hand.svg` · 조종 `joystick.svg` · 내 PC의 에이전트 `laptop.svg` · 적용 `rocket.svg` · 열쇠 `key.svg` · 문 `door-open.svg`. `joystick` · `laptop` · `door-open`은 기존 아이콘과 같은 판본(lucide-static 0.460.0, ISC)으로 받아 `assets/img/icons/`에 넣었다. **P3가 임시로 쓴 `wrench`(손발) · `bot`(조종) · `monitor`는 위 파일로 바꾼다.** 기존 장면의 의미가 다른 아이콘(S14의 `wrench` 등)은 그대로 둔다 |
| C-2 | 진행 띠 클래스 이름 | 묶음마다 다른 이름(`.axis-band` / `.icon-track` / `.axis-strip`)을 제안했다. **UI 사양(`_workspace/roadmap2/slide_ui/00_global_css_patch.md`)이 정한 이름 하나로 통일**하고 빌더 브리프에 고정한다. 콘티의 구성 설명(칸 이름 · 상태 세 가지 · 실습 번호 칩)은 P1의 「공통 부품」이 기준이다 |
| C-3 | 표지 큰 제목의 줄 나눔 | `.nowrap` 두 덩어리로 나눈다(`<br>` 금지). 전 표지 공통 |
| C-4 | 출처 줄의 vLLM 문서 이름(S07 · A05) | 유지한다. R2-17은 층 이름에 관한 결정이고 출처 줄은 공식 문서 이름을 적는 자리다 |
| C-5 | 출처 줄의 문서 이름 표기 | 덱의 현재 이름("… 가이드", "… 문서")을 그대로 쓴다 |
| C-6 | "Tool"(주제 이름)과 "도구"(낱낱의 함수) | 스토리보드의 쓰임새대로 함께 쓴다 |
| C-7 | "검색 증강 생성(RAG)" 병기 위치 | S10 한 곳. T01은 "RAG"만 쓴다 |
| C-8 | "목록"이라는 낱말 | "모델은 이번에 보낸 목록만 봅니다"로 통일(S16 · S30) |
| C-9 | 중심 메시지 표기 | "코드·데이터·사람" 가운뎃점 붙여쓰기, 글자 그대로(G02 · N02) |
| C-10 | 사이클 낱말 | N03은 "생각 → 행동 → 관찰"(ReAct), S22는 "관찰 → 판단 → 행동"(현 덱)으로 두고, N03 목록 첫 줄이 둘을 잇는다. 교차 검수가 통일을 권하면 T3에서 디자이너가 정한다 |

## 2. 묶음별

| # | 장면 | 결정 |
|---|---|---|
| P1-2 | G01 · G02 | **표지와 같은 진행 띠 `ol.axis-strip`(밝은 배경) 6칸 + 그 아래 실습 번호 칩 줄**로 그린다. "준비 · 01"은 `axis-prep` 변형. 콘티가 제안한 9열 격자(`.axis-grid`)는 새 CSS와 넘침 위험이 커서 채택하지 않았다(같은 그림을 G01 · 표지 · G02가 반복하는 효과는 그대로다) |
| P1-3 | N01 | `{topic}`은 일반 낱말이라 그대로. 연결 줄의 이름은 가상 이름 |
| P1-4 | L01 | 긍정봇의 마무리 한마디를 화면에 그대로 쓴다(실습자료와 같은 이름 · 같은 문구라 수강생이 헷갈리지 않는다) |
| P1-8 | S04 | 한 줄 요지를 비유 고정 문장으로 바꾸고 원래 요지는 설명문 첫 문장으로 내린다 |
| P1-9 | E01 | 게이트웨이 상자에 사용량 · 한도는 적지 않는다(LAB06이 맡는다) |
| P1-10 | LAB02 · LAB03 | "막히면"의 "준비된 출력"은 강사가 미리 받아 두는 것으로 본다(아침 확인 항목) |
| P2-1 | S10 | 두 줄 도식(윗줄 ①②③④ 저장 / 아랫줄 질문 → ④ 검색 → ⑤ 답변). 새 CSS 없이 `.dg-row.stretch` 두 줄로 빌드하는 대체안을 기본으로 한다 |
| P2-4 | 비유 아이콘 교체 범위 | 재작성 · 신설 장면만 |
| P2-5 | V02 | 노트의 "파일로 저장…" 문장을 뺀다 |
| P2-11 | N02 | 실제 오류 화면 자리표시는 두지 않는다 |
| P2-12 | N02 출처 | Python 공식 문서와 RFC 9110은 공식 문서 · 표준이므로 허용 |
| P2-14 | LAB04 | 강조 칸을 4번(검색하고 점수 보기)으로 옮긴다 |
| P2-15 | S16 | 넘치면 설명문 둘째 문장 → 계산 예시의 질문 상자 순서로 뺀다. `return a / b` 줄은 남긴다 |
| P3-1 | S21 · S22 | 두뇌는 아이콘과 라벨 "두뇌 · LLM"만 쓰고 고정 문장의 뒷부분("아직 손발이 없습니다")은 쓰지 않는다 |
| P3-3 | LAB05 | 개념 칩 ④를 "입력 → Agent → 화면의 연결"로 바꾼다 |
| P3-6 | S22 출처 | "Kaggle Agents 백서"를 뺀다(공식 문서 · 논문만 남긴다) |
| P3-7 | N03 | "읽기 전용"은 "(일반 원칙)" 표기 유지 |
| P3-12 | G03 | 준비물 칩 추가는 필수, 상자 추가는 폭이 되면 |
| P3-13 | LAB05 | 화면은 가상 장비 ID, 노트에 "실습에서는 실습자료의 장비 ID를 넣습니다" 한 문장 |
| P4-1 | S43 | 제목에서 "3단계"를 뺀다 → "자유 실습 · 검증과 기록" |
| P4-3 | S39 | 화면 유지. 자료 범위는 강사가 말로 안내 |
| P4-5 | S28 | 유지. "공식 문서의 네 단계"라고 말하지 않는다 |
| P4-6 | G02 띠 | G01과 같은 방식("준비 · 01" 포함 여부도 G01을 따른다) |
| P4-8 | M01 | 제목이 두 줄이 되면 "선택 과제 · 장비 조회 Agent에 결과 없음 안내 붙이기"로 줄인다 |
| P4-10 | 준비된 CSV | 강사가 따로 준비하는 가상 CSV로 본다(아침 확인 항목) |

## 3. CSS 요청 처리 (T4에서 반영)

| 요청 | 처리 |
|---|---|
| 진행 띠(밝은 배경 · 반전 배경, 상태 3종, 실습 번호 칩) | 채택 · 필수. UI 사양의 이름으로 |
| 진행 띠 클래스 이름 | **`.axis-strip` · `.axis-stop` · `.axis-tile` · `.axis-icon` · `.axis-label` · `.axis-prep`, 상태 `is-done / is-current / is-todo`** 로 확정(UI 사양 E절). 콘티의 `.axis-band` · `.icon-track` · `.is-now` · `.is-next`는 이 이름으로 읽는다 |
| `.axis-grid`(9열 격자 · Part 묶음 줄) | 채택하지 않음. `axis-strip` + 칩 줄로 대체 |
| 표지용 제목 크기 변형 | 채택: `.hero-title.is-section`(88px) |
| 썸네일 네 모서리 둥글림 | 채택(R2-20) |
| `.dg-box.is-dashed`(점선 테두리, E01 셋째 열쇠) | 채택 |
| `.scale-line`(S07의 0~2 눈금) | 채택(작은 규칙) |
| `.compare-grid.cols-3.is-mid-wide` (N03) | 채택 |
| S10 다섯 열 격자 · `.dg-icons` · `.dg-link` · "또는" 구분 · G02 카드 여백 변형 | 보류. 콘티의 대체안(새 CSS 없음)으로 빌드하고, 넘침이 나오면 그때 추가 |
| `[data-scene-id="S13"]` · `["S19"]` 전용 규칙 | 선택자를 LAB04 · LAB04b로 옮긴다. S19의 "네 층" 도식 규칙은 지운다 |

## 3-1. 교차 검수 뒤 결정 — 묶음 사이에 걸친 것 (T3에서 네 묶음이 똑같이 적용)

근거: `_workspace/roadmap2/review_conti_llm_professional.md`, `_workspace/roadmap2/review_conti_lecture_expert.md`. 두 보고서의 **높음 · 중간 지적은 모두 반영**한다. 아래는 묶음 사이에서 한쪽으로 정해야 하는 것의 확정값이다. 확정 사실(R2)을 뒤집자는 "제안" 6건은 채택하지 않는다(차선 문안만 쓴다).

| # | 항목 | 확정 |
|---|---|---|
| X-1 | Agent 사이클 낱말 | N03과 S22 모두 **"판단 → 행동 → 관찰"** 로 통일한다. N03 노트에서 "ReAct 논문은 판단을 '생각'이라고 부릅니다"를 한 번만 말한다. S16의 네 카드는 호출 순서(도구 알려 주기 → 호출 요청 → 실행 → 결과 전달)이고, 노트에서 "호출 요청이 모델의 판단, 실행이 행동, 결과 전달이 관찰"이라고 한 번 잇는다. 끝나는 조건의 말은 어디서나 **"호출 요청이 없으면 끝납니다"** |
| X-2 | 401의 고정 구절 | **"401은 열쇠 문제입니다. 어느 문에서 났는지 보고 그 열쇠를 확인합니다."** LAB04b · N02 · LAB05 · A05에서 같은 구절을 쓰고 장면마다 꼬리만 붙인다(예: "셋째 열쇠는 만료되기도 합니다"). "401 = 토큰 만료"라고 단정하지 않는다 |
| X-3 | 낱말 '목록' | N01 설명문에 "역할이 붙은 메시지를 차례로 담은 **메시지 목록**"이라는 낱말만 심는다(원리는 말하지 않는다). S16 캡션은 "메시지 목록(질문과 지금까지의 요청 · 결과)"으로 풀어 쓴다. S16 코드 주석 번호는 ⓐⓑⓒ로 바꿔 카드 ①~④와 겹치지 않게 한다. LAB04b의 대체 도식은 역할이 보이는 "목록이 자라는 모습"으로. N01의 "assistant 역할은 Tool 실습에서 봅니다" 약속은 LAB04b 도식에서 지킨다 |
| X-4 | RAG 병기 | "검색 증강 생성(RAG)"은 S10 한 곳. T01은 "RAG"만 |
| X-5 | "세 층" | 연결의 그림(E01 · S23)만 **"세 층"**(내 PC · 환경 파일/설정 파일 · 사내 LLM 게이트웨이)이라 부른다. 앱의 그림(S29 · S21 · LAB05)은 **"화면 · 처리 · 데이터 세 부분"** 이라 부른다 |
| X-6 | 열쇠 지도 축소판 | P1 「공통 부품」의 모양(칩 한 줄)이 기준이다. P2의 상자 셋은 칩 한 줄로 바꾼다 |
| X-7 | CLAUDE.md의 자리 | **Context**(C04 기준). S30 노트는 "작업 기록 파일이 Memory, CLAUDE.md는 지침이라 Context"로 고친다. S21의 Memory 칩은 "오후에 채울 자리" |
| X-8 | 손들기 칩의 아이콘 | `users.svg`로 바꾼다(`hand.svg`는 손발 비유 전용) |
| X-9 | S15 한 줄 요지 | "정확히 계산하지도 못합니다"를 빼고 **"계산이 맞아 보여도 맞았는지 보장하지 못합니다"** 방향으로. LAB04b의 관찰은 "값이 어디서 나왔는지 확인할 수 있는지"를 비교하게 |
| X-10 (개정) | **고정 문장의 표현을 "읽히지 않습니다" → "읽게 하지 않습니다"로 바꿨다**(빌드 후 내용 검수 지적 1~4). 피동형은 "Claude Code가 열쇠 파일을 알아서 피한다"는 틀린 안심으로 읽힌다. 우리가 하는 행동으로 말한다. 노트에는 "도구가 알아서 막아 준다고 가정하지 않습니다"를 더한다. 아래 원래 결정의 나머지는 그대로다. 콘티 part3 · part4의 같은 문장도 이 표현으로 읽는다 |
| X-10 | **LAB07 환경 파일 안전** | 요청문 예시에 **"환경 파일은 열지 마세요"** 를 넣는다. 관찰 문구와 노트에 "환경 파일에는 열쇠가 들어 있으므로 읽히지 않습니다. 화면을 함께 볼 때도 열지 않습니다"를 더한다. 승인 요청은 "명령 실행과 파일 수정에서 뜹니다"로 고쳐 쓴다(파일 읽기에는 뜨지 않을 수 있다). S23에 "읽은 파일 내용은 모델 쪽으로 전송됩니다 → 열쇠가 든 파일은 읽히지 않습니다" 한 줄 |
| X-11 | 비상 사다리의 셋째 열쇠 | LAB05 노트 첫머리에 "셋째 열쇠 칸이 채워져 있는지 먼저 보고, 비었으면 지금 받습니다". LAB04b 메모의 절감 효과는 −3 안팎으로 고친다 |
| X-12 | R01 카드 번호 | 실습자료 · S10 · LAB04와 같은 **② 나누기 ③ 임베딩 ④ 저장 · 검색**. 노트의 "오늘 실습은 ③까지 직접 합니다"는 "오늘 실습에서 ②부터 ⑤까지 모두 직접 합니다"로 |
| X-13 | 답이 화면에 먼저 보이는 확인 질문(S07 · S14 · T02 · N02 · N03) | S17에서 쓴 방법대로, **앞 장면의 전환 문장에서 먼저 묻고** 해당 장면에서 답을 확인한다 |
| X-14 | 실습 직전 새 용어 3개 초과(E01 · N01 · S16 · N02 · N03 · S21 · S23) | 콘티가 "넘치면 뺄 것"으로 적어 둔 항목을 **지금 뺀다**(노트로 내린다) |
| X-15 | LAB04 · LAB05 | LAB04의 대체 도식은 S10과 겹치지 않게 **"⑤의 근거 프롬프트 · 개념 예시" 세 줄 카드**로(근거로만 답하기 · 추측하지 않기 · 없으면 확인하기 어렵다고 답하기). S10 노트에 "N01의 템플릿 변수 자리에 찾은 조각이 들어갑니다" 한 문장. LAB05 개념 칩은 "호출 기록" · "반복 상한 · 입력 이스케이프" · "화면 · 처리 · 데이터"로 다시 짜고, 입력 이스케이프 관찰 문장을 하나 더한다 |
| X-16 | S22 "웹 채팅은 말만" | 단정을 누그러뜨린다: "웹 채팅은 주로 대화로 답하고, Claude Code는 내 PC의 파일과 명령을 도구로 씁니다" |
| X-17 | R2-14 "손으로"의 뜻 | LAB04b · T03 노트에 "여기서 '손으로'는 반복문을 우리가 직접 코드로 짜서 돌렸다는 뜻입니다"를 넣는다. T03의 "아직 안 되는 것"은 "반복을 멈추는 장치"가 아니라 **"실제 데이터를 읽는 손발, 분명한 멈춤 조건, 누구나 쓰는 화면"** 처럼 04b에 상한이 없다고 단정하지 않는 표현으로 |
| X-18 | 강사 진행표 | 덱 밖 문서 `docs/instructor-runsheet.md`(한 장: 점검 지점 셋, 비상 사다리 4단, 바꿔 말할 문장)를 메인 에이전트가 만든다. 화면 · 노트에는 넣지 않는다 |

## 4. 아침에 사용자가 확인할 것 (강사만 아는 것)

1. LAB02 · LAB03 "막히면"에 적힌 "준비된 출력"을 미리 받아 둘 수 있는지.
2. 자유 실습용 "준비된 가상 CSV" 파일을 준비하는지.
3. 실습 01에 실제로 걸리는 시간(오전 시간 추정의 가장 큰 변수).
4. 자유 실습에서 오전 Agent에 업무 질문을 던질 때 허용할 자료 범위.
