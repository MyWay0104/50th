# 마지막 시각 검토 · 30–57쪽

- 기준: `scratchpad/final/visual_brief.md`
- 대상: `shots/slide-30.png` ~ `slide-57.png` 28장 (1920×1080 실제 렌더)
- 좌표는 스크린샷 기준 px 추정값이다.

## 장면별 판정

- 30쪽 · 통과
- 31쪽 · 수정 필요 — 자리표시 상자 조건 줄에서 "1600×900 이상"만 다음 줄로 떨어짐
- 32쪽 · 수정 필요 — 코드 상자 안 한글 낱말 사이가 두 칸처럼 벌어짐(고정폭 글꼴)
- 33쪽 · 수정 필요 — 윗줄 카드 간격(약 22px)과 아랫줄 자리표시 간격(약 42px) 불일치
- 34쪽 · 수정 필요 — 막히면 상자에서 "모델 / 호출"이 끊기고, 오른쪽 칸이 왼쪽보다 약 80px 일찍 끝남
- 35쪽 · 통과
- 36쪽 · 수정 필요 — 부제 끝 마침표(30–35쪽·부록 부제에는 없음)
- 37쪽 · 수정 필요 — 부제 끝 마침표, 상태 점에 주황·초록이 추가 강조색으로 쓰임
- 38쪽 · 수정 필요 — 부제 끝 마침표
- 39쪽 · 수정 필요 — 막히면 상자에서 "Claude / Code"가 두 줄로 끊김
- 40쪽 · 수정 필요 — 부제 끝 마침표, "□ 자료 없음"의 □가 멀리서 깨진 글자처럼 보임
- 41쪽 · 수정 필요 — 부제 끝 마침표
- 42쪽 · 통과
- 43쪽 · 수정 필요 — 막히면 상자에서 "CSV / 과제"가 두 줄로 끊김
- 44쪽 · 수정 필요 — 시간 카드 3장 중 1장만 한 줄이고, 2장은 시각이 둘째 줄로 떨어짐
- 45쪽 · 수정 필요 — 막히면 상자에서 "완료합니다" 한 낱말만 둘째 줄로 떨어짐
- 46쪽 · 수정 필요 — 출처 줄이 다른 장면(y≈933)보다 약 32px 아래(y≈965)에 있고, 질문 카드 3장 폭이 제각각
- 47쪽 · 수정 필요 — 부제 끝 마침표
- 48쪽 · 수정 필요 — 수식 "QK^T"에 캐럿 문자가 그대로 보임
- 49쪽 · 통과
- 50쪽 · 통과
- 51쪽 · 통과
- 52쪽 · 수정 필요 — 2번 카드에서 "실습 / 가이드"가 두 줄로 끊김
- 53쪽 · 수정 필요 — 카드 둘째 줄이 가운뎃점(·)으로 시작하고, 5번 카드만 한 줄
- 54쪽 · 수정 필요 — 조건부 Edge 화살표가 분류 노드 아래가 아니라 왼쪽(START 아래)에 있음
- 55쪽 · 통과
- 56쪽 · 수정 필요 — 도식 묶음 상자가 점선이라 자리표시 상자처럼 보임(54쪽도 같음)
- 57쪽 · 수정 필요 — 오른쪽 승인 카드 캡처 글자가 약 12–14px라 강의실 뒤에서 읽을 수 없고, 캡션이 그림과 떨어져 있음

## 수정 요청

| 번호 | 쪽 | 요소 | 현재 상태(보이는 그대로) | 제안 | 심각도 |
|---|---|---|---|---|---|
| 1 | 57 | 오른쪽 승인 카드 캡처 이미지 | 이미지 폭 약 808px, 안의 글자("쉘 명령 실행 승인이 필요합니다", JSON, 승인·거부 버튼)가 약 12–14px라 뒤에서 읽을 수 없음. 원 논문 그림 예외(7·48쪽)에 해당하지 않음 | 캡처를 제목·command 줄·승인/거부 버튼만 남기게 잘라 약 1.6배로 키우거나(글자 20px 이상), 같은 카드를 HTML로 다시 그려 글자를 24px 이상으로 만든다 | 중간 |
| 2 | 54 | 왼쪽 도식 "↓ 조건부 Edge" 화살표 | ↓가 x≈316(START 상자 오른쪽 아래)에 있어 분류 노드(가운데 x≈622)에서 나오는 선으로 읽히지 않음 | ↓와 설명 줄을 분류 상자 가운데(x≈622) 아래로 옮기거나, 설명 줄 왼쪽 시작을 분류 상자 왼쪽 끝(x≈342)에 맞추고 ↓를 x≈622에 둔다 | 중간 |
| 3 | 39 | 막히면 상자 | "…붙여 넣고, Claude / Code가 안 되면…"처럼 제품 이름이 줄 끝에서 끊김 | "Claude Code"에 줄바꿈 금지(`white-space:nowrap` 또는 `&nbsp;`)를 걸거나, 문구를 "Claude Code가 안 되면 짝 화면으로, Skill이 안 되면 절차를 요청문으로 붙여 넣습니다"처럼 순서를 바꾼다 | 중간 |
| 4 | 52 | 2번 카드 "패키지" 본문 | "설치 판본을 실습 / 가이드와 비교"로 "실습 가이드"가 두 줄로 끊김 | "설치 판본을 / 실습 가이드와 비교"로 `<br>` 위치를 "판본을" 뒤로 옮긴다 | 중간 |
| 5 | 46 | 출처 줄 "근거: 사용자 제공 강의안·시간표" | y≈965로 다른 장면(y≈933)보다 약 32px 아래, 꼬리말과 간격이 48px로 좁음 | 다른 장면과 같은 위치(y≈933)로 올린다(인용 장면 전용 여백·위치 값을 공통 `.source` 값에 맞춤) | 중간 |
| 6 | 44 | 오른쪽 시간 카드 3장 | "첫 실행 ~16:35"는 한 줄, "다른 입력 / ~16:55"와 "멈춤·정리 / ~17:05"는 시각이 둘째 줄로 떨어져 같은 종류 카드의 줄 수가 다름 | 세 장 모두 이름 뒤에 `<br>`를 넣어 "첫 실행 / ~16:35" 형태 두 줄로 통일한다 | 중간 |
| 7 | 45 | 막히면 상자 | "…오류를 적는 것으로 / 완료합니다"로 한 낱말만 둘째 줄로 떨어짐 | 문구를 "결과가 안 나오면 멈춘 단계와 오류를 적고 끝냅니다"로 줄여 한 줄에 맞추거나, "멈춘 단계와 오류를 / 적는 것으로 완료합니다"처럼 의미 단위로 끊는다 | 중간 |
| 8 | 53 | 다섯 단계 카드 본문 | "날짜 기준·단위 / ·결측값 문서화", "정답 아는 입력 / ·실패 상황"처럼 둘째 줄이 가운뎃점으로 시작, "읽기 범위· / 허용 테이블"은 줄 끝에 점이 남음. 5번 카드만 한 줄 | 모든 카드에 `<br>`로 끊는 곳을 정하고 점은 줄 끝에도 줄 머리에도 두지 않는다: "읽기 범위 / 허용 테이블", "날짜 기준·단위 / 결측값 문서화", "정답 아는 입력 / 실패 상황", "응답 시간 / 호출량도 함께", "누가 무엇을 / 바꿨는지" | 중간 |
| 9 | 31 | 자리표시 상자 조건 줄 | "…실행일 표기 · / 1600×900 이상"으로 끝 두 낱말만 다음 줄로 떨어지고 줄 끝에 점이 남음 | "조건: 모델명·주소·키 가리기 / 실행일 표기 · 1600×900 이상"으로 `<br>` 위치를 "가리기" 뒤로 옮긴다 | 낮음 |
| 10 | 32 | 두 코드 상자 안 한글 | "## 저장소   성격", "결정:  무엇을  하기로  했나"처럼 한글 낱말 사이가 두 칸 가까이 벌어짐(고정폭 글꼴의 공백 폭) | 한글만 있는 예시 줄은 본문 글꼴(비고정폭)로 쓰거나 `word-spacing:-0.3em` 정도로 줄인다 | 낮음 |
| 11 | 33 | 윗줄 질문 카드·아랫줄 자리표시 상자 | 윗줄 카드 사이 약 22px, 아랫줄 자리표시 사이 약 42px로 같은 화면에서 간격이 다름 | 윗줄 간격을 40px로 늘려 아랫줄과 맞춘다(또는 아랫줄을 22px로 줄인다) | 낮음 |
| 12 | 34 | 오른쪽 칸 막히면 상자·코드 위치 줄 | "앱의 모델 / 호출이"로 끊기고, 오른쪽 칸이 y≈780에서 끝나 왼쪽 완료 조건 상자(y≈858)보다 약 80px 짧음 | "앱의 모델 호출이 막히면"을 한 줄에 두도록 `<br>`를 "화면으로," 뒤에 넣고, 오른쪽 칸 아래 끝을 왼쪽에 맞추려면 막히면 상자 위아래 여백을 약 20px씩 늘린다 | 낮음 |
| 13 | 36·37·38·40·41·47 | 파란 밑줄 부제 | 부제 끝에 마침표가 있음("…다르다.", "…나눈다." 등). 같은 범위의 30–35쪽, 48–57쪽 부제에는 마침표가 없음 | 여섯 장의 부제 끝 마침표를 지운다 | 낮음 |
| 14 | 37 (57) | 상태 점 | "검토 전" 주황 점, "검토됨" 초록 점(57쪽 승인 초록·거부 주황도 같음)으로 파랑 1색 규칙 밖의 색이 쓰임 | 의도한 상태색이면 두 장면에서만 쓰는 예외로 DESIGN 기준에 적어 두고, 아니면 검토 전 = 회색 점, 검토됨 = 파랑 점으로 바꾼다 | 낮음 |
| 15 | 40 | 가운데 카드 아래 표시 "□ 자료 없음" | □가 작고 가늘어 멀리서 글자 깨짐(네모)처럼 보임. 원문(`index.html`)에는 의도한 빈 상자 기호로 적혀 있음 | 글자 기호 대신 CSS로 14×14px 테두리 상자를 그리거나, 기호를 빼고 "빈칸 = 자료 없음"으로 적는다 | 낮음 |
| 16 | 43 | 막히면 상자 | "…준비된 CSV / 과제로 바꿉니다"로 "CSV 과제"가 끊김 | "준비된"과 "CSV 과제"를 `&nbsp;`로 묶어 "준비된 CSV 과제로 바꿉니다"가 둘째 줄에 함께 가게 한다 | 낮음 |
| 17 | 46 | 질문 카드 3장 | 폭이 약 355·453·389px로 제각각(글자 길이를 따라감) | 세 장을 같은 폭(약 415px)의 3칸 격자로 맞추고 글자는 왼쪽 정렬 유지 | 낮음 |
| 18 | 48 | 왼쪽 두 번째 항목 수식 | "softmax(QK^T / √d) · V"에서 ^가 캐럿 문자로 보임 | "softmax(QKᵀ / √d) · V"로 바꾸거나 `QK<sup>T</sup>`로 위첨자 처리 | 낮음 |
| 19 | 54·56 | 도식 묶음 상자 | State 묶음(54쪽), 하네스·Deep Agents 묶음(56쪽)이 점선 테두리라 `[IMG-PLACEHOLDER]` 자리표시 상자와 같은 모양으로 보임 | 도식 묶음은 1px 실선(카드와 같은 선 색) 또는 옅은 바탕색으로 바꿔 자리표시와 구분한다 | 낮음 |
| 20 | 57 | 캡처 이미지 위치·캡션 | 이미지 윗변이 y≈476으로 왼쪽 첫 줄 상자 윗변(y≈447)보다 약 29px 낮고, 캡션이 이미지 아래 약 70px 떨어진 y≈765에 있음. 캡션 첫 줄이 "버튼 ·"으로 점에서 끝남 | 이미지 윗변을 y≈447에 맞추고 캡션을 이미지 아래 16–20px로 붙인다. 캡션은 "사내 배포 패키지 예시 화면의 승인 카드 · 수강생 실습 화면 아님"으로 줄여 한 줄로 만든다 | 낮음 |
| 21 | 57 | 왼쪽 흐름 "사람 결정" 아래 ↓ | ↓가 x≈819에 하나만 있어 거부 상자 오른쪽 위로만 이어지고 승인 쪽 갈래가 보이지 않음 | ↓를 두 상자 사이 가운데(x≈524)로 옮기거나, 승인·거부 각 상자 위에 짧은 ↙ ↘ 두 개로 갈래를 보인다 | 낮음 |

수정 필요: 높음 0 · 중간 8 · 낮음 13
