# v0.5 r3 시각 검토 · 1–25쪽 (최종 확인)

- 기준: `_workspace/roadmap1/review_r3/snaps/slide-01.png`–`slide-25.png`(1920×1080 실제 렌더)를 한 장씩 열어 봤다. 2회차 보고서(`v0.5-r2-visual-01-25.md`)의 요청 17건이 해결됐는지와 수정으로 생긴 회귀를 먼저 확인했고, 원인은 `topics/sk-hynix-ai-agent-guide-edu/index.html`에서 찾았다. 원본은 고치지 않았다.
- 범위: 1–25쪽(장면 ID 대응은 `_workspace/roadmap1/scene_map_v0.5.md`). 결함으로 보지 않은 것: 주황 단일 강조·1쪽 반전, `[IMG-PLACEHOLDER]` 자리표시, 2·22쪽의 시각 표기, 3쪽 YouTube 출처, 10·14·28쪽 녹색 상태 점(2회차 #16 유지 결정), 2쪽 오후 행 빈 칸(#15 유지 결정).
- 이번 회차는 **높음·중간만** 새 요청으로 올리고, 낮음은 "남은 미세 조정"에 3줄로 묶었다.

## 2회차 반영 확인

| 번호 | 쪽(장면 ID) | 2회차 요청 | 판정 | 비고 |
|---:|---|---|---|---|
| 1 | 25쪽(S26) | 막히면 상자 하단이 출처 줄과 겹침 | 해결 | 칩 줄을 오른쪽 열(자리표시 아래 "막힌 단계에 손을 들어 알려 주세요" + 손 아이콘 + 칩 5개)로 옮김. 막히면 상자는 한 줄(y≈757–828), 출처(y≈933)와 분리 |
| 2 | 02쪽(G01) | 6번 카드만 2줄이라 높이 어긋남 | 해결 | `align-items: stretch` 적용, 6·7·8 카드 모두 y≈635–855. 6번 문구는 2줄 그대로지만 번호·아이콘 위치 일치 |
| 3 | 14쪽(S14) | `.fill`로 카드 안 170–200px 빈 면 | 해결 | `.fill` 제거 + padding 48px. 카드가 내용 높이(y≈405–775)로 돌아옴 |
| 4 | 19쪽(S29) | 코드 카드 안 190px 빈 면 | 해결 | `align-items: start`. 코드 카드 y≈405–735, 자리표시 405–745 |
| 5 | 23쪽(S22) | 두 카드 안 190px 빈 면 | 해결 | 14쪽과 같은 방법, 카드 y≈405–775 |
| 6 | 17쪽(S19) | 막히면 상자를 왼쪽 열로 | 해결 | 왼쪽 `.done-box` 아래(y≈805–875). 9·13·17·20·25쪽 모두 왼쪽으로 통일 |
| 7 | 21쪽(S21) | 하네스 칩 줄 2줄·`+` 허공 | **미해결** | 셋째 칩을 "Context · 지침·자료"로 줄이고 gap 8px을 줬지만 여전히 "… [Memory · 대화 이력] +" 에서 끊기고 "[Context · 지침·자료]"만 둘째 줄(요청 표 #2) |
| 8 | 17쪽(S19) | "필요 / 없는" 분리 | 해결 | 문안 "정답을 아는 질문과 도구 없이 / 답할 질문을 구분해 설명합니다" — 어절 단위로 자연스럽게 나뉨 |
| 9 | 19쪽(S29) | "가리기" 고아 | 해결 | 둘째 줄이 "모델명·주소·키 가리기"로 함께 내려감 |
| 10 | 03쪽(G00) | "넣었습니다." 고아 | 미해결(낮음) | 문안을 "…설명과 확인이 있습니다."로 바꿨으나 이번엔 "있습니다."가 둘째 줄 고아 |
| 11 | 13쪽(S13) | ④ 칩 혼자 둘째 줄 | 해결 | 네 칩 한 줄(y≈540–592) |
| 12 | 20쪽(S20) | "봅니다" 고아 | 해결 | "…계속 막히면 / 강사 시연으로 진행합니다" 두 줄, 단어 하나 고아 없음 |
| 13 | 25쪽(S26) | "손들기" 고아·배지 없음 | 해결 | 1번과 함께 해결(칩 줄이 오른쪽 열 캡션+칩으로 분리) |
| 14 | 17쪽(S19) | 오른쪽 도식 위아래 90px 띠 | 부분 해결 · 회귀 | `justify-content: flex-start`로 제목 아래에 붙였지만 이제 도식이 y≈620에서 끝나 오른쪽 열 아래 약 255px이 빈 면(요청 표 #3) |
| 15 | 02쪽(G01) | 오후 행 오른쪽 2칸 빈 면 | 제외 | 사용자 결정 유지 |
| 16 | 10·14쪽 | 녹색 상태 점 | 제외 | 사용자 결정 유지 |
| 17 | 04쪽(S02) | 세 도구 카드 안 아래 110px 여백 | 부분 해결(낮음) | `justify-content: center`를 넣었으나 카드 내용(y≈640–800)이 카드(620–910) 중앙(765)보다 위에 있음. `.dg-box`가 flex column이 아니면 효과 없음 |

## 장면별 판정

- 01쪽 · 통과
- 02쪽 · 통과 (6번 문구 2줄은 카드 높이가 맞아 문제 없음)
- 03쪽 · 통과 — "있습니다." 고아(낮음)
- 04쪽 · 통과 — 카드 안 내용이 위로 쏠림(낮음)
- 05쪽 · 통과
- 06쪽 · 통과
- 07쪽 · 통과
- 08쪽 · 통과
- 09쪽 · 통과
- 10쪽 · 통과
- 11쪽 · 통과
- 12쪽 · 통과
- 13쪽 · 통과
- 14쪽 · 통과 — 카드 아래 약 150px 여백(낮음)
- 15쪽 · 통과
- 16쪽 · **수정 필요** — 설명문이 3줄이 되어 코드 카드 하단이 출처 줄을 덮음(높음, 회귀)
- 17쪽 · 수정 필요 — 오른쪽 네 층 도식 아래 약 255px 빈 면(중간, 회귀)
- 18쪽 · 통과
- 19쪽 · 통과 — 두 카드 아래 약 180px 여백(낮음)
- 20쪽 · 통과
- 21쪽 · 수정 필요 — 하네스 칩 줄 2줄·`+` 첫 줄 끝(중간, 미해결)
- 22쪽 · 통과
- 23쪽 · 통과 — 카드 아래 약 150px 여백(낮음)
- 24쪽 · 통과
- 25쪽 · 통과 — 좌우 열 하단선 828/893으로 65px 차(낮음)

## 수정 요청

| 번호 | 쪽(장면 ID) | 요소 | 현재 상태 | 제안 | 심각도 |
|---:|---|---|---|---|---|
| 1 | 16쪽(S16) | `p.explain` → `.stack` > `.code-card` ↔ `p.source` (index.html 1807행·1828–1832행·1834행) | `.explain`이 3줄("…이번 도구는 인자가 없어 모델은 '부를지 말지'만 / 정합니다.")이 되면서 `.scene > .stack`(margin-top 56, gap 24)이 약 40px 아래로 밀렸다. `.stack{min-height:0}`은 줄어들지만 `.scene[data-scene-id="S16"] .code-card{flex-shrink:0}`(1332행) 때문에 코드 카드가 스택 밖으로 삐져나와, 카드 하단(y≈925)이 `.source{margin-top:auto; padding-top:24px}` 위에 겹친다. 출처 "출처: LangChain Tools · OpenAI Function calling 가이드 · 코드는 개념 예시"(y≈933) 윗부분이 카드에 가려 읽을 수 없다. 1회차 #1(코드 카드 잘림)의 재발 | (1) 문안을 2줄로: `<p class="explain">프로그램이 도구의 이름·설명·입력 형식을 알려 주면 모델은 부를 도구 이름을 담은 호출 요청을 돌려줍니다. @tool은 docstring이 곧 도구 설명이라 모호하면 잘못 고릅니다. 이 도구는 인자가 없어 모델은 '부를지 말지'만 정합니다.</p>` (2) 문안 유지가 필요하면 CSS로 48px 회수: `.scene[data-scene-id="S16"] > .stack { margin-top: 24px; gap: 16px; }` (3) 어느 쪽이든 `flex-shrink: 0`은 두되, 재발 방지로 `.scene[data-scene-id="S16"] .explain { max-width: 1680px; }`처럼 줄 폭을 넓혀 두 줄을 보장 | 높음 |
| 2 | 21쪽(S21) | `.split-panel` > `.chip-row` (1979–1987행) | "[하네스] = [Tools · 조회 도구] + [Memory · 대화 이력] +" 첫 줄, "[Context · 지침·자료]" 둘째 줄. 배지+`=`+칩 3개+`+` 2개 폭 합(≈960px)이 패널 내용폭(≈830px)을 gap 8px로도 약 130px 넘쳐 축약·gap 조정으로는 해결되지 않음 | (1) 최소 조치 — `+`를 뒤 칩 안으로 넣어 어디서 접혀도 기호가 허공에 남지 않게: `<span class="chip accent">Tools · 조회 도구</span><span class="chip accent">+ Memory · 대화 이력</span><span class="chip accent">+ Context · 지침·자료</span>` (`.dg-arrow` `+` 두 개 삭제). 둘째 줄이 "+ Context …"로 시작해 수식이 이어져 보임 (2) 한 줄로 만들려면 왼쪽 패널을 넓힘: `.scene[data-scene-id="S21"] .split-grid.wide-left { grid-template-columns: 1.7fr 1fr; }` + 칩 `padding-inline: 14px` — 오른쪽 2블록은 글자가 짧아 폭이 줄어도 한 줄 유지 (3) 두 줄을 의도로 두려면 `.scene[data-scene-id="S21"] .split-panel .chip-row { row-gap: 10px; }`로 줄 간격만 정리(현재 둘째 줄이 첫 줄에 바짝 붙음) | 중간 |
| 3 | 17쪽(S19) | 오른쪽 `.stack` > `.panel-title` + `.dg` (1861–1867행) | 2회차 #14 반영으로 `.dg`가 `flex: 0 0 auto; justify-content: flex-start`(1325행)가 되면서 2×2 도식이 y≈425–620에서 끝나고, 왼쪽 열은 y≈875까지 이어져 오른쪽 열 아래 약 255px이 빈 면. 2회차의 "위아래 90px 띠"가 "아래 255px 빈 면"으로 옮겨진 회귀 | `.dg`가 남은 높이를 채우도록 되돌리되 상자 자체를 늘림: `.scene[data-scene-id="S19"] .practice-grid > .stack > .dg { flex: 1; justify-content: flex-start; gap: 20px; }` + `.scene[data-scene-id="S19"] .practice-grid > .stack > .dg > .dg-row.stretch { flex: 1; }` + `.scene[data-scene-id="S19"] .practice-grid > .stack > .dg .dg-box { min-height: 0; height: 100%; }` — 네 상자가 왼쪽 열 높이(y≈425–875)를 2행으로 나눠 채움. 상자가 너무 커 보이면 `.dg-box`에 `max-height: 190px`을 두고 `.dg { justify-content: space-between; }` | 중간 |

## 남은 미세 조정

- 3쪽 "있습니다." 둘째 줄 고아(`<span class="nowrap">확인이 있습니다.</span>`), 4쪽 세 도구 카드 내용이 카드 중앙보다 위(`.dg-box`에 `display:flex; flex-direction:column; justify-content:center`가 실제로 걸리는지 확인).
- 14·19·23쪽 카드 아래 150–180px 여백(카드가 내용 높이로 돌아온 결과이며 출처 줄과 분리돼 있어 유지 가능), 25쪽 좌우 열 하단선 65px 차(`.placeholder{min-height:200px}`를 왼쪽 열 높이에 맞춰 조정).
- 16쪽 문안 수정 시 "정합니다." 고아도 함께 사라지는지 확인.

수정 필요: 높음 1 · 중간 2 · 낮음 5
