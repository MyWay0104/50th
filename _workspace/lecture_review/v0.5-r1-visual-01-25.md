# v0.5 r1 시각 검토 · 1–25쪽

- 기준: `_workspace/roadmap1/review_r1/snaps/slide-01.png`–`slide-25.png`(1920×1080 실제 렌더)를 한 장씩 열어 정렬·치우침·글자·가독성·하우스 룰·일관성을 봤다. 원인은 `topics/sk-hynix-ai-agent-guide-edu/index.html`의 마크업·CSS에서 확인했고 원본은 고치지 않았다.
- 범위: 1–25쪽(장면 ID 대응은 `_workspace/roadmap1/scene_map_v0.5.md`). 결함으로 보지 않은 것: 오프화이트·주황 단색, 헤어라인 카드, 푸터, 쪽번호 없음, `[IMG-PLACEHOLDER]` 자리표시, 2·22쪽의 시각 표기, 실습 안내판의 개념 칩.
- 심각도: 높음 = 잘림·겹침·깨짐·오탈자·요청 위반 / 중간 = 눈에 띄는 정렬 이상·큰 빈 공간·어색한 줄바꿈 / 낮음 = 미세 조정.

## 장면별 판정

- 01쪽 · 통과
- 02쪽 · 수정 필요 — 오후 행 카드 폭이 오전 행과 달라 6번 카드만 넓다(낮음)
- 03쪽 · 수정 필요 — 설명문 둘째 줄에 "들어 있습니다."만 남음, 출처가 YouTube 영상(낮음)
- 04쪽 · 수정 필요 — 카드 아래 약 230px 빈 공간, "개인 기록" 배지 위치 어색(중간·낮음)
- 05쪽 · 통과
- 06쪽 · 통과
- 07쪽 · 통과
- 08쪽 · 수정 필요 — 강조 상자 둘째 줄에 "따라옵니다"만 떨어짐(중간)
- 09쪽 · 수정 필요 — 번호 블록 폭이 글자 길이대로 제각각, 13·17·25쪽의 등폭 블록과 다름(낮음)
- 10쪽 · 수정 필요 — 카드 아래 약 185px 빈 공간(중간)
- 11쪽 · 통과
- 12쪽 · 수정 필요 — ② 카드 제목 둘째 줄에 "가까움"만 떨어짐, 출처 괄호 앞 띄어쓰기 없음(중간·낮음)
- 13쪽 · 수정 필요 — "막히면" 상자가 오른쪽(9·17·25쪽은 왼쪽), ④ 칩만 둘째 줄로 떨어지고 문장형 "없다"(중간·낮음)
- 14쪽 · 수정 필요 — 카드 아래 약 170px 빈 공간(중간)
- 15쪽 · 수정 필요 — 카드 아래 약 185px 빈 공간(중간)
- 16쪽 · 수정 필요 — 코드 카드 하단이 잘려 docstring 줄이 테두리에 닿음, 설명문 셋째 줄에 "말지'뿐입니다."만 남음(높음·중간)
- 17쪽 · 수정 필요 — 오른쪽 설명 캡션이 24px 회색 두 줄(가독성), 완료 조건 "필요 / 없는" 분리, 좌우 하단선 불일치(중간·낮음)
- 18쪽 · 통과
- 19쪽 · 수정 필요 — 카드 아래 약 185px 빈 공간, 자리표시 줄 끝에 "·"만 남음(중간·낮음)
- 20쪽 · 수정 필요 — "막히면" 상자가 오른쪽(13쪽과 같고 9·17·25쪽과 다름), 좌우 하단선 불일치(중간·낮음)
- 21쪽 · 수정 필요 — 왼쪽 패널 캡션이 패널 테두리 위에 겹침, 하네스 칩 3개가 세로로 흩어지고 "+"가 허공에 뜸, 오른쪽 열 아래 약 270px 빈 공간(높음·중간)
- 22쪽 · 통과
- 23쪽 · 수정 필요 — 카드 아래 약 170px 빈 공간(중간)
- 24쪽 · 통과
- 25쪽 · 수정 필요 — 자리표시 둘째 줄에 "화면"만 떨어짐, "막힌 단계에 손들기" 구성이 다른 안내판과 다름(낮음)

## 수정 요청

| 번호 | 쪽(장면 ID) | 요소 | 현재 상태 | 제안 | 심각도 |
|---:|---|---|---|---|---|
| 1 | 16쪽(S16) | `.code-card` > `pre.code-body` | 코드 카드가 아래로 눌려 마지막 줄 `"""장비별 현재 위치와 진행 공정"""`이 카드 하단 테두리에 닿음(하단 padding 24px이 사라짐). 원인: `.explain`이 3줄로 늘어난 뒤 `.stack{min-height:0}` 안에서 `.code-card`가 flex-shrink로 줄어듦 | (1) `.explain` 문장을 2줄로 줄인다 — 예: "프로그램이 도구의 이름·설명·입력 형식을 알려 주면 모델은 쓸 도구를 돌려줍니다. @tool은 docstring을 도구 설명으로 쓰므로 설명이 모호하면 도구를 잘못 고릅니다. 이번 도구는 인자가 없어 모델은 '부를지 말지'만 정합니다." (2) 보험으로 CSS에 `[data-scene-id="S16"] .code-card { flex-shrink: 0; }` 추가 | 높음 |
| 2 | 21쪽(S21) | `.split-panel` > `p.dg-caption` | 캡션 "주황 테두리: 오후 시연과 선택 과제의 대상(예: 결과 없음 안내)"의 글줄이 패널 하단 헤어라인(y≈893)과 정확히 겹쳐 잘려 보이고, 바로 아래 출처 줄(y≈933)과 붙음. 원인: `.chip-row` 칩 3개가 각각 한 줄씩 차지해 패널 내용이 패널 높이를 넘침 | (1) `Memory · 대화 이력(오전엔 비워 둔 자리)` 칩을 `Memory · 대화 이력`으로 줄여 세 칩이 한두 줄에 들어가게 한다(괄호 내용은 발표자 노트에 이미 있음). (2) `p.dg-caption`을 `.split-panel` 밖, `.split-grid` 다음 형제로 옮겨 그림 아래 캡션 위치를 5·24쪽과 맞춘다 | 높음 |
| 3 | 21쪽(S21) | `.split-panel` > `.chip-row` | "하네스 = [Tools] + / [Memory …] + / [Context]"가 세 줄로 흩어지고 `+` 기호가 각 줄 끝 허공에 떠 수식이 읽히지 않음 | 2번 수정으로 칩 길이가 줄면 한 줄에 들어옴. 그래도 두 줄이면 `dg-arrow` `+`를 칩 앞으로 옮겨 "= [Tools] [+ Memory] [+ Context]" 순으로 배치 | 중간 |
| 4 | 21쪽(S21) | 오른쪽 `.dg`(2블록) | 블록 두 개가 y≈405–660에 몰리고 그 아래 약 270px이 빔. 왼쪽 패널(405–893)과 높이 차가 큼 | 오른쪽 `.dg`에 `align-self: center`(예: `.split-grid > .dg.is-vcenter`) 또는 블록 사이 `↓` 화살표 간격을 늘려 세로 중심을 왼쪽 패널과 맞춘다 | 중간 |
| 5 | 16쪽(S16) | `p.explain` | 3줄째에 "말지'뿐입니다."만 남음(고아 어절). `<span class="nowrap">함수 설명문(docstring)을</span>`이 둘째 줄 앞부분을 통째로 밀어 줄바꿈이 나빠짐 | 1번 문안으로 2줄로 정리하면 해소. 문안 유지 시 `nowrap` span 제거 | 중간 |
| 6 | 08쪽(L01) | `.callout` | "예시 몇 개를 함께 넣으면 답의 모양이 예시를 / 따라옵니다" — 둘째 줄에 "따라옵니다" 한 어절만 남음 | 문안을 "예시 몇 개를 함께 넣으면 <strong>답의 모양이 예시를 따릅니다</strong>"로 줄이거나 `<strong>` 안을 `<span class="nowrap">예시를 따라옵니다</span>`로 묶는다 | 중간 |
| 7 | 12쪽(R01) | `li.step.is-accent` > `.step-body` | "뜻이 가까우면 숫자도 / 가까움" — 카드 제목이 두 줄이 되고 둘째 줄에 "가까움"만 남음. ①·③ 카드는 한 줄이라 카드 본문 기준선이 어긋남 | `.step-body` 글자를 "가까운 뜻은 가까운 숫자로" 또는 "뜻이 비슷하면 숫자도 비슷"으로 줄여 한 줄에 맞춘다 | 중간 |
| 8 | 13쪽(S13)·20쪽(S20) | `.fallback`(막히면 상자) 위치 | 13·20쪽은 오른쪽 열(자리표시 아래), 9·17·25쪽은 왼쪽 열(완료 조건 아래). 같은 역할 상자가 장면마다 자리가 다름 | 13쪽: `.fallback`을 왼쪽 `.stack`의 `.done-box` 다음으로 이동. 20쪽도 동일하게 왼쪽 `.done-box` 다음으로 이동(오른쪽 자리표시는 `.image-frame` 없이 단독) | 중간 |
| 9 | 17쪽(S19) | 오른쪽 `.stack` > `p.dg-caption.is-left` | "Text-to-SQL이 아니라 정해진 쿼리이며 인자도 없습니다 · 아래는 실행 환경에서 막는 네 층"이 24px 회색 두 줄 — 강의실 뒤에서 읽히지 않는 크기의 긴 문장 | 앞 절은 `.explain` 끝에 합치고("…도구 호출 기록이 비는지 확인합니다. 이 도구는 정해진 쿼리라 인자가 없습니다."), 캡션에는 "실행 환경에서 막는 네 층"만 남겨 `.panel-title`(28px 볼드)로 바꾼다 | 중간 |
| 10 | 10쪽(S09)·14쪽(S14)·15쪽(S15)·19쪽(S29)·23쪽(S22) | `.compare-grid` / `.split-grid` 아래 여백 | 카드 하단(y≈745–760)부터 출처(y≈930)까지 170–185px이 빔. 카드에 항목이 1–3개뿐이라 카드가 낮고 아래가 크게 남음(6·7·18쪽의 130px보다 눈에 띔) | 카드 항목이 하나뿐인 10·15쪽은 `compare-list`에 항목 1개씩 보강(예: 15쪽 "SQL·Python" 카드에 "집계 기준은 코드에 고정"), 14·19·23쪽은 카드 내부 `padding`을 40→48px로 늘리거나 `.compare-grid`/`.split-grid`에 `.fill`을 줘 남은 높이를 채운다 | 중간 |
| 11 | 04쪽(S02) | `.split-grid` 아래 여백 | 카드 하단(y≈780)에서 푸터(y≈1010)까지 약 230px이 빔. 출처 줄이 없는 장면이라 더 크게 보임 | 오른쪽 `.placeholder`와 왼쪽 카드 묶음에 `.fill`을 주거나 세 도구 카드에 한 줄씩 설명을 더해(예: Part Finder "부품 번호 후보 검증 · 결과 없음 안내") 세로를 채운다 | 중간 |
| 12 | 04쪽(S02) | `.concept-badge` "개인 기록" | 큰 태그 줄 아래, 카드 위에 홀로 떠 있어 무엇의 라벨인지 모호함(다른 장면의 `개념 예시` 배지는 그림 틀 안 왼쪽 위) | 세 도구 카드 묶음을 `.split-panel`로 감싸고 배지를 그 안 왼쪽 위로 넣거나, 배지를 카드 줄과 같은 행 왼쪽에 `align-self:center`로 붙인다 | 낮음 |
| 13 | 02쪽(G01) | 오후 행 `.time-block` | 6번 카드 폭(≈375px)이 7·8번(≈215px)과 다르고 오전 행 등폭 카드와도 다름. 화살표 간격은 균등 | 6번 `.what`을 "Claude Code 설치·작동 / → 폴더 읽기 결과"로 줄이거나 오후 행에도 오전과 같은 고정 폭(`flex: 0 0 <px>`)을 준다 | 낮음 |
| 14 | 03쪽(G00) | `p.explain` | 둘째 줄에 "들어 있습니다."만 남음 | "…실습 완료 조건에도 설명과 확인이 들어 있습니다."를 "…실습 완료 조건에도 설명과 확인을 넣었습니다."처럼 첫 줄 끝 어절을 줄여 한 줄로 맞추거나 `<span class="nowrap">확인이 들어 있습니다.</span>` | 낮음 |
| 15 | 03쪽(G00) | `p.source` | "출처: YouTube · AgentOS 채널 · https://…" — 하우스 룰(출처 줄은 공식 문서·논문만)과 충돌 소지. 캡션에 "이 교육의 근거가 아님"을 이미 적어 둠 | 룰 해석을 확인하고, 유지한다면 라벨을 "참고 영상:"으로 바꿔 `출처:`와 구분한다 | 낮음 |
| 16 | 09쪽(S08) | `.time-blocks.is-steps.row.time-band` | 번호 블록 폭이 글자 길이대로(①≈160px … ⑤≈185px) 제각각이고 블록 사이 빈 간격이 큼. 13·17·25쪽은 등폭(`cols-3`, `row`) | `.time-band` 대신 13쪽과 같은 `row` 등폭 규칙을 쓰거나 각 블록에 `flex: 1`을 준다 | 낮음 |
| 17 | 12쪽(R01) | `p.source` | "LangChain Retrieval 가이드(Text splitters · Embeddings)" — 괄호 앞 띄어쓰기 없음(다른 장면은 "… 지침(SQL Agent …)" 같은 표기라 통일 필요) | 덱 전체 출처 줄의 괄호 표기를 하나로 정한다(권장: 낱말 뒤 바로 괄호, 현재 상태 유지) — 확인만 | 낮음 |
| 18 | 13쪽(S13) | `.chip-row` 넷째 칩 | "④ 없으면 없다"가 혼자 둘째 줄로 떨어지고, 칩 중 유일하게 문장형 종결 "…다" | 칩 글자를 "④ 없으면 없음"으로 바꾸고, 한 줄에 넣으려면 "① 찾아 읽기"로 줄이거나 `.chip-row` gap을 12→8px로 | 낮음 |
| 19 | 13쪽(S13)·17쪽(S19)·20쪽(S20) | 좌우 열 하단선 | 13쪽 좌 850/우 895, 17쪽 좌 865/우 795, 20쪽 좌 800/우 895 — 두 열의 마지막 상자 하단이 서로 다름 | 8번(막히면 상자 왼쪽 통일) 반영 후 오른쪽 `.placeholder`에 `flex:1`을 줘 왼쪽 열 높이에 맞춘다 | 낮음 |
| 20 | 17쪽(S19) | `.done-box` > `.done-text` | "정답을 아는 질문과 도구가 필요 / 없는 질문의 차이를 설명합니다" — "필요 없는"이 줄 사이에서 갈라짐 | `<span class="nowrap">필요 없는</span>` 또는 문안 "정답을 아는 질문과 도구가 필요 없는 질문을 구분해 설명합니다" | 낮음 |
| 21 | 19쪽(S29) | `.placeholder-desc` 둘째 줄 | "조건: 모델명·주소·키 가리기 · / 실행일 표기 · 1600×900 이상" — 줄 끝에 구분점 "·"만 남음 | 구분점 앞뒤를 `&nbsp;·&nbsp;`가 아닌 `<span class="nowrap">가리기 · 실행일 표기</span>`로 묶거나 문안 순서를 "1600×900 이상 · 실행일 표기 · 모델명·주소·키 가리기"로 | 낮음 |
| 22 | 25쪽(S26) | `.placeholder-desc` 첫 줄 | "넣을 자료: Claude Code가 오전 실습 폴더 구조를 설명하는 / 화면" — "화면"만 떨어짐 | 문안을 "넣을 자료: 오전 실습 폴더 구조를 설명한 Claude Code 화면"으로 | 낮음 |
| 23 | 25쪽(S26) | 오른쪽 `.dg-row` "막힌 단계에 손들기" + `.chip-row` | 다른 안내판(9·13·17·20)에 없는 요소가 오른쪽 아래에 추가돼 안내판 구조가 다름. 왼쪽 `.fallback`과 역할이 겹침 | 유지한다면 라벨을 `.fallback` 안 칩 줄(`.fallback > .chip-row`, 이미 CSS 있음)로 옮겨 "막히면 … 짝·강사 시연 화면으로" 아래에 붙이고 오른쪽은 자리표시만 둔다 | 낮음 |

수정 필요: 높음 2 · 중간 9 · 낮음 12
