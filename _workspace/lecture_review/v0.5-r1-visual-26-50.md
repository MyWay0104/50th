# v0.5 r1 시각 검토 · 26–50쪽

- 기준: `_workspace/roadmap1/review_r1/snaps/slide-26.png` ~ `slide-50.png`(1920×1080 실제 렌더)를 한 장씩 열어 정렬·치우침·글자·가독성·하우스 룰·일관성 6개 항목으로 봄. 쪽 ↔ 장면 ID는 `_workspace/roadmap1/scene_map_v0.5.md`.
- 범위: 26쪽(C01) ~ 50쪽(A10) 25장. 원인은 `topics/sk-hynix-ai-agent-guide-edu/index.html`에서 확인했고 원본은 고치지 않음.
- 결함으로 보지 않은 것: `[IMG-PLACEHOLDER]` 점선 자리표시(27·29·32쪽), 실습 안내판·자유 실습 단계의 번호 블록/칩/완료 조건/막히면 구조(34–38쪽), 40쪽 반전 배경, 부록 "부록 ·" 접두어, 코드 카드 고정폭 글꼴. 시간 표기·반말·"근거:"·B1–B7·사내 주소·모델명은 26–50쪽 화면에서 발견되지 않음.

## 장면별 판정

- 26쪽 · 통과 — (낮음 1건: 카드 제목 표기 불일치 "coding agent")
- 27쪽 · 통과
- 28쪽 · 통과 — (낮음 1건: 캡션 앞 녹색 점이 단일 강조색 규칙에서 벗어남)
- 29쪽 · 통과
- 30쪽 · 수정 필요 — 오른쪽 배지·패널 제목·표 셀이 좁아 낱말이 잘게 줄바꿈되고, 표 아래 캡션이 푸터 바로 위까지 내려옴
- 31쪽 · 통과
- 32쪽 · 통과 — (낮음 1건: 왼쪽 카드 하단 빈 공간)
- 33쪽 · 수정 필요 — 가운데 카드 "없으면 없다고"에서 "없다고" 한 낱말만 다음 줄로 떨어짐, "확인:" 라벨 중복
- 34쪽 · 통과
- 35쪽 · 통과 — (낮음 1건: 출처 표기 문구 형식 불일치)
- 36쪽 · 통과 — (낮음 1건: 오른쪽 열 하단 빈 공간)
- 37쪽 · 통과
- 38쪽 · 통과
- 39쪽 · 통과
- 40쪽 · 통과
- 41쪽 · 통과
- 42쪽 · 통과
- 43쪽 · 통과
- 44쪽 · 수정 필요 — MCP 카드 안 "앱·도구 연결 수" 캡션이 네 줄로 세로 분절, "개념 예시" 노트가 카드 밖으로 넘쳐 출처 줄에 붙음
- 45쪽 · 통과
- 46쪽 · 통과
- 47쪽 · 통과 — (낮음 1건: 상태 상자 제목의 가운뎃점 띄어쓰기 불일치)
- 48쪽 · 통과
- 49쪽 · 통과
- 50쪽 · 통과 — (낮음 1건: 오른쪽 승인 카드 모사 화면의 글자가 작음)

## 수정 요청

| 번호 | 쪽(장면 ID) | 요소 | 현재 상태 | 제안 | 심각도 |
|---:|---|---|---|---|---|
| 1 | 44쪽(A04) | `.compare-col`(MCP) 안 `.dg-row` > `.dg-caption` "앱·도구 연결 수" | 칩 `N×M → N+M` 옆 남은 폭이 60px 정도라 캡션이 "앱·/도구/연결/수" 네 줄로 세로 분절됨. 그 결과 카드 내용이 카드 높이(452–892)를 넘어 `.compare-note` "개념 예시"가 카드 테두리 아래(y≈900)로 밀려 출처 줄(y≈934)과 맞닿음 | `index.html` 2734행 `<span class="dg-caption">앱·도구 연결 수</span>`를 삭제하고, 2736행 `.compare-note`를 `개념 예시 · 앱·도구 연결 수`로 합침. 그래도 넘치면 `[data-scene-id="A04"] .compare-col .dg-row { margin-top: 12px; }`로 줄이기 | 높음 |
| 2 | 30쪽(C03) | 오른쪽 `.stack` > `.dg-row` 의 `.concept-badge` "공개 Beta 설계 기반 개념 예시" + `.panel-title` "요청: 제조사 A의 로봇 블레이드" | 한 줄에 로고·배지·제목이 함께 있어 배지가 2줄("…개념 / 예시"), 제목도 2줄("…로봇 / 블레이드")로 꺾임. 배지 안 낱말 하나만 둘째 줄 | 배지 문구를 `공개 Beta 개념 예시`로 줄이고(2221행), `.panel-title`을 `.dg-row` 밖 별도 줄로 빼서 한 줄로 두기. 또는 `[data-scene-id="C03"] .dg-row .concept-badge { white-space: nowrap; }` | 중간 |
| 3 | 30쪽(C03) | `.data-table` 셀 "PN-A-2001", "PN-B-1001", "종류 불일치", "제조사 불일치", "로봇 블레이드" | 4열이 700px 폭에 들어가면서 부품 번호가 "PN-A- / 2001"처럼 ID 중간에서 끊기고, 판정·종류 칸도 2줄 | `[data-scene-id="C03"] .data-table td:first-child, .data-table td:last-child { white-space: nowrap; }` 추가. 폭이 모자라면 `th`/`td` 좌우 padding을 24px→16px로 줄이거나 "Part Number" 헤더를 `Part No.`로 | 중간 |
| 4 | 30쪽(C03) | 표 아래 `.dg-caption.is-left` "완료 조건: 후보에 있는 번호만 · … · 표는 가상 데이터" | 2줄로 꺾여 y≈990까지 내려와 푸터(y≈1013)와 간격이 10px 남짓. 왼쪽 출처 줄(y≈934)보다 아래에 걸려 기준선이 어긋남 | 캡션을 `완료 조건: 후보 번호만 · 조건 모두 일치 · 없으면 '없음' · 가상 데이터`로 줄여 1줄로. 3번 항목으로 표 높이가 줄면 자연히 올라옴 | 중간 |
| 5 | 33쪽(S39) | 가운데 `.compare-col` > `.compare-list` li "확인: 있으면 근거 문장, 없으면 없다고" | "없다고" 세 글자만 다음 줄로 떨어짐. 같은 카드에 "확인:" 라벨이 두 번 있어 옆 카드(결과·확인·예외)와 구조가 다름 | 2326행을 `확인: 근거 문장, 없으면 없다고`로 줄여 1줄로. 아래 줄(2327행) "확인: 왜 골랐나는 사실·의견 구분"은 `예외:`나 `구분:`으로 라벨을 바꿔 세 카드 라벨 체계(결과·확인·예외)를 맞춤 | 중간 |
| 6 | 30쪽(C03) | 왼쪽 `.done-box` > `.done-text` "같은 파일이면 '차이 없음', 다른 / 행은 번호와 함께 표시합니다" | 상자 폭 910px 안에서 `text-wrap: balance`(1282행) 때문에 "다른 / 행은"으로 구 중간에서 끊김. 오른쪽에 빈 공간이 넓어 보임 | `[data-scene-id="C03"] .done-text { text-wrap: pretty; }` 또는 문구를 `같은 파일이면 '차이 없음', 다른 행은 번호와 함께`로 줄여 1줄에 맞춤 | 낮음 |
| 7 | 26쪽(C01) | 가운데 `.compare-heading` "coding agent" | 옆 카드 제목 "Agent 그 자체"·"지식 자산화"와 달리 소문자 영문. 같은 덱의 다른 장면은 "Coding Agent"/"Agent" 대문자 표기 | 2092행을 `Coding Agent`로 | 낮음 |
| 8 | 28쪽(S25) | 하단 `.dg-caption.is-left` 앞 `.status-dot.ok` | 녹색(#1aae39) 점이 단일 강조색(주황) 규칙에서 벗어남. 의미(통과/정상)도 캡션 내용과 무관 | 2166행의 `<span class="status-dot ok">` 삭제. 점이 필요하면 `.status-dot` 없이 `·` 기호로 | 낮음 |
| 9 | 32쪽(C04) | 왼쪽 `.split-image`(개인 기록 카드) | 오른쪽 열 높이에 맞춰 카드가 늘어나며 "폴더 이름은 예시 · 구조를 단순화" 아래 약 110px가 빔 | `[data-scene-id="C04"] .split-image { align-self: flex-start; }`로 카드를 내용 높이로 두거나, 카드 캡션을 카드 밖 `.dg-caption`으로 빼서 카드 자체를 짧게 | 낮음 |
| 10 | 35쪽·36쪽·37쪽·38쪽(S40–S43) | `.source` "출처: Claude Code Best practices 문서" | 27·28·29·30·34쪽은 "출처: Claude Code 공식 문서 Best practices" 형식이라 같은 문서의 표기가 두 가지 | 네 장면의 `.source`를 `출처: Claude Code 공식 문서 Best practices`로 통일 | 낮음 |
| 11 | 36쪽(S41) | 오른쪽 열 `.hint-box`("막히면") 아래 | 오른쪽 열이 y≈670에서 끝나 왼쪽 완료 조건 상자(y≈770)보다 100px 짧고, 그 아래 출처 줄까지 260px가 빔. 37쪽은 칩 줄이 있어 덜 비어 보임 | 36쪽 "막히면" 상자에 37·38쪽처럼 칩 줄 하나(예: `요청문` `범위` `완료 조건`)를 넣어 세 장의 오른쪽 열 높이를 맞춤 | 낮음 |
| 12 | 47쪽(A07) | `.dg-zone-label` "State · messages·category를 모든 노드가 공유" | "State · messages"는 띄운 가운뎃점, "messages·category"는 붙인 가운뎃점이 한 줄에 섞임 | 2816행을 `State · messages · category를 모든 노드가 공유` 또는 `State(messages·category)를 모든 노드가 공유`로 | 낮음 |
| 13 | 50쪽(A10) | 오른쪽 승인 카드 모사 화면(`쉘 명령 실행 승인이 필요합니다` 카드) | 모사 UI라 글자가 16–20px 수준이고 녹색 승인 버튼·연녹 배경이 팔레트 밖. 강의실 뒤에서는 명령 문자열이 읽히지 않음 | 모사 화면은 그대로 두되 카드 아래 캡션에 명령을 크게 한 번 더 적기(예: `실행 요청: python3 --version`), 또는 카드 안 글자 크기를 24px 이상으로. 녹색은 사내 배포 화면 재현이므로 유지 가능 | 낮음 |

수정 필요: 높음 1 · 중간 4 · 낮음 8
