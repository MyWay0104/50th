# v0.6 시각 검수 — 34~66쪽 (2026-09-18)

- 대상: `topics/sk-hynix-ai-agent-guide-edu` 66장 가운데 34~66쪽(33장). 스냅샷 `_workspace/roadmap2/review_v0.6/snaps/slide-34 … slide-66` 을 한 장씩 모두 열어 봤다. 비교용으로 `slide-02-G01`, `slide-06-E01` 도 봤다.
- 좌표는 1920×1080 스냅샷에서 눈으로 읽은 값이라 ±3px 오차가 있다. 덱 소스는 고치지 않았다. 고칠 방법의 선택자·줄 번호는 `index.html` 현재 상태 기준이다.
- 지시문에 있던 N03(31쪽)·LAB05(33쪽)는 담당 범위 밖이라 보지 않았다. 앞 구간 담당 보고서에서 확인해야 한다.

## 1. 지적 목록

| 번호 | 쪽 | 장면 ID | 등급 | 무엇이 어떻게 보이는가 | 고칠 방법 |
|---:|---:|---|---|---|---|
| 1 | 51 | M01 | 높음 | **지시 3 위반.** 번호 블록 4칸 가운데 4번("검증 · 다른 입력")만 글자가 두 줄이라, 세로 가운데 정렬 때문에 번호 원이 혼자 위로 올라갔다. 원 중심 y: 1·2·3번 ≈ 443, 4번 ≈ 425(18px 차이). 글자도 501 한 줄 / 483·520 두 줄로 갈린다. 강조(주황 채움) 블록이라 어긋남이 가장 먼저 눈에 띈다. | 문구로 해결(권장): 3362줄 `.what` "검증 · 다른 입력" → "검증". "다른 입력"은 위 관찰할 것 문장과 칩 ④ "예측 먼저"가 이미 말한다. 문구를 못 바꾸면 CSS: `.scene[data-scene-id="M01"] .time-blocks.is-steps.row .time-block { justify-content: flex-start; }` (네 원이 같은 높이에서 시작) |
| 2 | 66 | A10 | 높음 | **지시 3 위반.** 왼쪽 그림 첫 행 세 상자의 높이가 다르다. "도구 호출"·"멈춤" ≈ 120px(y 420–541), "사람 결정" ≈ 150px(y 406–556, 보조 글 "resume로 / 재개"가 두 줄). 제목 y 도 461·461·446 으로 갈린다. 아래 행(승인·거부)은 `stretch` 라 같은 높이인데 윗행만 제각각이다. | 3894줄 `<div class="dg-row">` → `<div class="dg-row stretch">` (높이 통일). 제목 높이까지 맞추려면 3899줄 보조 글 "resume로 재개" → "resume" 한 줄(상자 안쪽 폭 ≈ 134px, 28px 글자로 "resume로 재개" ≈ 165px 이라 폭 조정으로는 안 들어간다. 행 600px 을 세 상자·화살표 둘이 나눠 써서 상자를 넓힐 자리가 없다) |
| 3 | 35 | S21 | 중간 | 지시 3 경계. 왼쪽 패널 첫 행 "화면 / 처리 / 데이터"에서 "데이터"만 보조 글이 두 줄("기사 조각 · 사내 / 데이터")이라 제목이 15px 위로 올라갔다(제목 y 521·521·506). 상자 높이는 같고 상자마다 가운데 정렬은 맞다. | 2823줄 보조 글 "기사 조각 · 사내 데이터" → "기사 · 사내 데이터" + `.scene[data-scene-id="S21"] .split-panel .dg-row.stretch > .dg-box { padding-left: 16px; padding-right: 16px; }` (안쪽 폭 174 → 190px, 글 ≈ 173px 한 줄). 적용 뒤 스냅샷으로 한 줄인지 확인 |
| 4 | 39 | S23 | 중간 | 오른쪽 열 줄바꿈 두 곳. 셋째 불릿 "할 수 있는 일은 도구와 권한 범위 / 안입니다"(한 낱말만 다음 줄). 회색 상자 "다른 앱 화면 조작 · 사내 시스템은 별도 도구와 / 권한"(두 글자만 다음 줄). | 2961줄 → "도구와 권한 범위 안에서만 일합니다"(≈ 540px, 열 폭 ≈ 650px 한 줄). 2964줄 → "다른 앱 조작 · 사내 시스템은 별도 도구와 권한"(≈ 572px, 안쪽 폭 ≈ 600px) 또는 문구 유지하고 `.scene[data-scene-id="S23"] .dg-box.muted { text-wrap: balance; }` 로 두 줄을 고르게 |
| 5 | 40 | LAB06 | 중간 | 단계 1이 "… 확인하고, 없으면 / 넣기"로 끊겨 "넣기" 두 글자만 둘째 줄이다. 왼쪽 열은 칩 줄(y 624)에서 끝나고 완료 조건(y 787)까지 ≈ 160px 이 비어, 오른쪽 열(자리표시 + 칩, y 770까지)과 무게가 안 맞는다. | 2979줄 → "1 · 설정 파일의 주소와 채팅용 키 확인, 없으면 넣기"(≈ 670px, 열 폭 ≈ 850px 한 줄). 빈 공간은 한 줄이 되면 더 커지므로 `.scene[data-scene-id="LAB06"] .practice-grid > .stack:first-child { justify-content: center; }` 로 목록·칩 묶음을 오른쪽 열 높이의 가운데에 둔다 |
| 6 | 45 | S28 | 중간 | 자리표시 `[IMG-PLACEHOLDER · P-S28]` 가 점선 상자가 아니라 캡션 글 두 줄로, 네 카드 아래 끝(y 725) 바로 밑 17px(y 742)에 붙어 있다. 카드 설명처럼 읽히고, 다른 장면(LAB06·LAB07·C02·C04)의 점선 자리표시와 모양이 다르다. | 3183줄 `p.dg-caption.is-left` 에 장면 범위 CSS: `.scene[data-scene-id="S28"] .steps.row + .dg-caption { margin-top: 24px; padding: 16px 24px; border: 2px dashed var(--box-line); border-radius: var(--radius); }` (높이 ≈ 104px → y 749–853, 출처 줄 y 920 위). 자리표시 문구는 지우지 않는다 |
| 7 | 66 | A10 | 중간 | 승인 카드 그림 속 글자가 너무 작다. 머리말 "셸 명령 실행 승인이 필요합니다" ≈ 16px, "도구 · execute" ≈ 14px, 명령 줄 ≈ 15px, 승인·거부 버튼 ≈ 15px. 24px 기준에 크게 못 미쳐 뒷자리에서 안 읽힌다. 그림 폭은 이미 열 전체(1032px)라 CSS 로 키울 여지가 없다. | 자산 교체: `assets/img/appendix/cowork-hitl-approval-card.png` 를 창 폭을 좁히거나(≈ 640px) 브라우저 확대 150~175% 로 다시 캡처해 같은 파일명으로 덮는다. 교체 전에는 아래 캡션("실행할 명령과 승인·거부 버튼")이 내용을 대신하므로 장면은 유지 가능 |
| 8 | 56 | G02 | 중간 | 비유 축 띠는 G01과 같은 그림이다(아이콘 6개·순서·라벨·영문 보조·실습 칩 모두 일치, 상태만 전부 "지나옴" 채움 + 실선). 다른 점은 "준비 · 01" pill 이 없는 것 하나. **제자리(띠 왼쪽 위)에 되살릴 여유는 없다**: 주장 밑줄 아래 끝 y ≈ 265, 띠 위 끝 y ≈ 296(31px). pill 60 + 띠와의 간격 24 + 주장과의 간격 16 → 띠를 ≈ 69px 내려야 하는데, 빼 쓸 수 있는 곳이 카드→맺음 문장 간격(≈ 66px)과 맺음 문장→풋터(≈ 71px)뿐이라 여유가 20px 아래로 떨어진다. | 높이를 쓰지 않는 자리로 되살린다(제목 줄 오른쪽 끝, 제목은 x 705에서 끝나 비어 있음). 3530줄 앞에 `<li class="axis-prep is-done"><img class="axis-icon" src="assets/img/icons/key.svg" alt="준비 아이콘"><span data-editable="true">준비 · 01</span></li>` 추가 + CSS: `.scene[data-scene-id="G02"] .axis-prep { left: auto; right: 0; bottom: calc(100% + 112px); border: 1px solid var(--on-night-line-strong); background: var(--on-night-fill); color: var(--on-night); }` `.scene[data-scene-id="G02"] .axis-prep .axis-icon { filter: brightness(0) invert(1); }` (pill y ≈ 124–184. 흰 면 pill 을 그대로 쓰면 마무리 장에서 가장 밝은 요소가 되므로 "지나옴" 채움 값으로 낮춘다). G01과 자리가 달라지는 것이 싫으면 빼 둔 현 상태 유지 |
| 9 | 40·41·51·52·53·54 | LAB06·LAB07·M01·S40·LAB08·S43 | 중간 | 지시 2 관련(지각 기준). "막히면" 상자(`.fallback`)의 1px 점선 테두리가 다른 상자(1px 실선·2px 점선 자리표시)보다 눈에 띄게 흐리다. 색 토큰은 같지만 1px 점(dotted)이라 선의 절반이 비어 실제 보이는 진하기가 떨어진다. 배경과 면 색이 같아(투명) 테두리만이 경계다. | `.fallback { border-style: dashed; }` (1px 유지, 선 채움 비율만 올림). 그래도 약하면 `.fallback { border-width: 2px; padding: calc(<현재 값> - 1px); }` 로 자리표시와 같은 2px 점선. 상자 크기가 변하지 않게 안쪽 여백을 1px 줄인다 |
| 10 | 53 | LAB08 | 중간 | 좌우 열 아래 끝이 84px 어긋난다(왼쪽 2×2 블록 아래 끝 y ≈ 785, 오른쪽 "막히면" 상자 아래 끝 y ≈ 869). 왼쪽 블록 밑이 휑하다. 윗행 블록(≈ 117px)과 아랫행 블록(≈ 190px)의 높이도 다르다. | `.scene[data-scene-id="LAB08"] .practice-grid > div:first-child { display: flex; } .scene[data-scene-id="LAB08"] .practice-grid .time-blocks.is-steps { flex: 1; grid-auto-rows: 1fr; align-content: stretch; }` (2×2 블록이 오른쪽 열 높이를 채우고 두 행 높이가 같아진다). `.time-blocks` 가 grid 가 아니면 `align-items: stretch` + 각 블록 `flex: 1` |
| 11 | 53 | LAB08 | 낮음 | 3번 블록 보조 글이 "띠의 구현 · 첫 실행 · 기능 / 멈춤"으로 끊겨 "멈춤"만 둘째 줄. 진행 띠 칩 7개 가운데 4개(계획·구현·첫 실행·기능 멈춤)가 연달아 주황 강조라 "한 줄에 강조 1개" 규칙(패치 사양 A-5)과 어긋나고, 다음 쪽 S43(검증 1개만 강조)과 비교하면 무겁다. | `.scene[data-scene-id="LAB08"] .time-block .when { text-wrap: balance; }`. 강조 4개는 "이번 실습 범위"라는 뜻이면 유지하되, 줄이려면 앞 세 칩을 기본으로 돌리고 "기능 멈춤" 하나만 `accent` |
| 12 | 48 | C04 | 낮음 | 왼쪽 패널 아래 끝 y ≈ 829, 오른쪽 열(강조 문장 상자) 아래 끝 y ≈ 893. 64px 어긋남. | `.scene[data-scene-id="C04"] .split-grid > .split-panel { align-self: stretch; } .scene[data-scene-id="C04"] .split-panel > .dg { margin-block: auto; }` |
| 13 | 51 | M01 | 낮음 | 왼쪽 열 아래 끝(완료 조건 y ≈ 820)과 오른쪽 열 아래 끝("막히면" y ≈ 893)이 73px 어긋난다. | `.scene[data-scene-id="M01"] .practice-grid > .stack:first-child { justify-content: space-between; }` (번호 블록은 위, 완료 조건은 아래 끝에 맞춤) |
| 14 | 35 | S21 | 낮음 | "개념 예시"가 두 번 나온다(패널 안 배지 y 419, 패널 아래 캡션 y 877). 하네스 식의 "=" 기호가 칩 중심(y 721)보다 ≈ 9px 위(y 712)에 떠 있고, 식이 두 줄로 접혀 "+ Memory …" 칩만 둘째 줄이다. | 2848줄 `p.dg-caption.is-left`("개념 예시") 삭제. `.scene[data-scene-id="S21"] .chip-row > .dg-arrow { line-height: 1; align-self: center; }`. 식 접힘은 뜻 단위(Tools·Context / Memory)로 끊긴 것이라 그대로 둬도 된다 |
| 15 | 34 | S17 | 낮음 | 세 카드 가운데 "스크립트" 카드만 아래 칩 줄이 없어 카드 아래 ≈ 90px 이 빈다(다른 두 카드는 "오전에 만든 것" 칩). | 그대로 둬도 뜻은 맞다(오전에 스크립트는 안 만듦). 채우려면 2776줄 아래에 `<div class="chip-row"><span class="concept-badge">오전에는 없음</span></div>` — 문구는 내용 담당 확인 |
| 16 | 36 | G03 | 낮음 | 아래 1/4(y 750–990)이 빈다. "준비물" 칩 4개가 바로 위 세 상자와 내용이 겹친다(Claude Code·오전 실습 폴더·반복 업무 하나). | 칩 줄을 지우고 세 상자 묶음을 아래로 내려 가운데에 두거나(`.scene[data-scene-id="G03"] .dg-row.stretch { margin-top: 48px; }`), 칩 줄을 "채팅용 키" 하나만 남긴다 |
| 17 | 42 | C01 | 낮음 | 불릿 두 곳이 한 낱말만 다음 줄로 떨어진다: "예: 엑셀 비교 스크립트, 부품 검색 / 도구", "Obsidian 노트와 함께 나만의 / Agent". | `.scene[data-scene-id="C01"] .compare-list li { text-wrap: balance; }` |
| 18 | 44 | S25 | 낮음 | 지시 2 관련. 아래 설명 줄 앞의 초록 점(`.status-dot.ok`)은 캔버스 위 2.70:1 로 3:1 에 못 미치고, 상태 라벨 없이 글머리처럼 쓰였다(위 회색 상자의 갈색 점과 짝). | 점 옆 문장 앞에 라벨을 붙인다: "좋은 요청 · 오전에 만든 장비 조회 Agent에 …" 또는 점을 빼고 `.dg-caption` 만 둔다. 색은 `DESIGN.md` 고정값이라 바꾸지 않는다 |
| 19 | 60 | A04 | 낮음 | 네 카드 아래 "예: …" 줄의 높이가 Skill 카드만 12px 낮다(y 855, 나머지 843). Skill 카드 불릿 둘이 모두 두 줄로 접혀 카드 안쪽 아래 여백을 먹었다(아래 여백 ≈ 25px, 나머지 ≈ 37px). | 3680줄 "반복 절차를 SKILL.md에" → "절차를 SKILL.md에"(한 줄), 3681줄 "이름·설명만 보다가 고르면 본문 읽기" → "고를 때만 본문 읽기"(한 줄). 또는 본문 설명(3줄)을 2줄로 줄여 카드 높이를 확보 |
| 20 | 63·64 | A07·A08 | 낮음 | 지시 2 관련. 코드 강조 줄(`.code-body .hl`, 주황 글자 + 연주황 면)은 28px·400 이라 3.42:1 — 큰 글자 기준만 통과하고, 코드처럼 읽는 글로는 흐리다. 패치 사양 B-2 에 "선택 수정, 강사 승인 뒤"로 남겨 둔 항목 그대로다. | `.code-body .hl { color: var(--night); }` (9.1:1, 강조 느낌은 연주황 면이 유지). 강사 승인 필요 |
| 21 | 64 | A08 | 낮음 | "↓ no" 의 화살표(x ≈ 557)가 "관련성 평가" 상자 중심(x ≈ 576)보다 19px 왼쪽이다. 그림·코드 카드가 y ≈ 738 에서 끝나 출처 줄까지 ≈ 170px 이 빈다. | 화살표와 "no"를 한 묶음으로 가운데 정렬하지 말고 화살표만 상자 중심에: 해당 묶음에 `transform: translateX(19px)` 또는 "no"를 `position: absolute; left: calc(50% + 20px)` 로 분리. 빈 공간은 부록이라 허용 가능 |
| 22 | 66 | A10 | 낮음 | "사람 결정" 아래 ↓ 화살표(x ≈ 639)가 "거부" 상자 위에만 떨어져, 승인·거부 두 갈래가 아니라 거부로만 가는 것처럼 읽힌다. 승인 카드 그림의 흰 프레임에는 테두리가 없어 다른 패널(1px `--box-line`)과 다르다. | 아래 행을 `<div class="dg-zone">` 으로 감싸(라벨 "사람의 결정에 따라") 화살표가 묶음을 가리키게 하거나, 1272줄 `margin-right: 68px` 을 지우고 화살표를 행 가운데(`.dg-arrow.is-end` → `align-self: center`)에 둔다. 프레임: `.scene[data-scene-id="A10"] .split-image { border: 1px solid var(--box-line); }` |
| 23 | 50 | S39 | 낮음 | 윗줄 두 방식 상자에서 아이콘(y ≈ 420)이 제목(y ≈ 400)과 보조 글(y ≈ 441) 사이 높이에 떠서, 제목 옆 아이콘인지 묶음 전체의 아이콘인지 애매하다. | 아이콘을 제목 줄에 맞춘다: `.scene[data-scene-id="S39"] .dg-box > .inline-icon, .scene[data-scene-id="S39"] .dg-box > .dg-icon { align-self: flex-start; margin-top: 4px; }` 또는 아이콘을 제목 `span` 안으로 옮긴다(마크업 확인 뒤 적용) |

수정 필요: **높음 2 · 중간 8 · 낮음 13**

## 2. 사용자 지시 4건 판정

### 지시 1 — 텍스트 상자가 배경과 구분되는가: **통과**
- 예로 든 **C02(43쪽)**: 흰 상자 세 개는 1px 테두리가 또렷하고, 회색 "설명" 상자는 면이 캔버스보다 확실히 진하며 테두리도 보인다. 강조 상자(Claude Code)는 2px 주황.
- 회색 상자가 있는 나머지 쪽도 같은 수준으로 구분된다: S21(35, "오전 · 내가 조립"), S23(39, 위 세 층 띠와 권한 상자), S25(44, 위 나쁜 요청 띠), C04(48, ingest ⇄ query), A03(59, 공통 실행 기반 띠).
- 흰 패널 안에 놓인 흰 상자(S21·C04·S22)도 테두리로 구분된다. 상자가 6개 이상인 S25·S40·A06 이 무거워 보이지는 않는다.
- 예외적으로 약한 것은 `.fallback` 점선(지적 9)과 A10 그림 프레임(지적 22)뿐이다.

### 지시 2 — 대비: **조건부 통과** (위반이라 할 곳은 없고, 흐려 보이는 곳 3종)
- 흐려 보임: `.fallback` 1px 점선 — 40·41·51·52·53·54쪽(지적 9). S25(44쪽) 초록 점 2.70:1(지적 18). A07·A08(63·64쪽) 코드 강조 줄 3.42:1(지적 20).
- 반전 배경 세 장은 통과. **D06(37쪽)**: 지나온 타일 채움·테두리, 현재 타일 굵은 흰 테두리, 남은 타일("적용")의 1px 테두리와 60% 아이콘 모두 보인다. 머리말·보조 줄(연한 베이지)도 읽힌다. **D07(49쪽)**: 같다. **G02(56쪽)**: 타일·실습 칩·비교 카드 테두리(50% 흰색)와 밑줄이 또렷하고, 영문 보조 글(LLM·RAG …)과 맺음 문장도 읽힌다.
- 자리표시 2px 점선(LAB06·LAB07·C02·C04), 칩 테두리, 목록 점, 화살표, 아이콘은 모두 충분하다. M01 번호 원의 연주황 면(1.10:1)은 사양에서 장식으로 둔 것이고 숫자가 읽히므로 문제 삼지 않았다.

### 지시 3 — 상자 안 가운데 정렬: **위반 2쪽 + 경계 1쪽**
- 위반: **51쪽 M01**(번호 원 높이 18px 어긋남, 지적 1), **66쪽 A10**(같은 행 상자 높이·제목 높이 다름, 지적 2).
- 경계: **35쪽 S21**(같은 행 제목 15px 어긋남, 지적 3). 패치 사양 G절 위험 1번("줄 수가 다르면 시작 높이가 달라진다")이 실제로 나타난 세 곳이다.
- 나머지는 통과. 위쪽에 붙은 상자는 없다. S25·S40 6칸, C02·C03·C04·A06·A08·A09 상자, LAB08·S43 번호 블록(번호 숨김 + 오른쪽 아이콘) 모두 글자가 상자 가운데에 있고, S40 의 왼쪽 덧씌움 아이콘도 세로 가운데다. S28·A05 단계 카드의 번호 원과 아이콘은 네 카드에서 같은 높이다(의도대로 왼쪽 정렬).

### 지시 4 — 그림 네 모서리: **통과**
- **A01(57쪽)**: 논문 그림은 네 모서리가 둥근 흰 패널(1px 테두리) 안에 있고, 그림 바탕이 흰색이라 각진 모서리가 보이지 않는다.
- **A10(66쪽)**: 승인 카드 그림의 흰 프레임 네 모서리가 모두 둥글게 보인다(오른쪽 위·아래 포함). 다만 프레임 테두리가 없어 경계가 약하다(지적 22).
- 이 구간에 다른 사진·그림은 없다(나머지는 자리표시와 CSS 그림).

## 3. 그 밖의 확인 항목

- **C03(46쪽) 오른쪽 표**: 표 오른쪽 끝 x ≈ 1800 으로 오른쪽 여백 안에 들어왔다. 마지막 칸 "● 제조사 불일치"도 x ≈ 1777 에서 끝나 표 안이다. 좌우 열 위 끝(y ≈ 424)과 아래 끝(814 / 816)도 맞는다. **통과.**
- **구간 표지 D06·D07**: 띠 위 끝 y ≈ 720, 왼쪽 끝 x = 120 으로 두 장이 같다. 지나옴(채움 + 실선)·현재(굵은 테두리 + 굵은 라벨 + 밑줄)·남음(빈 타일 + 점선 + 흐린 라벨) 세 상태가 한눈에 구분된다. D06 머리말(Part 3 / Part 4 두 개)은 한 줄(x 120–882), 제목도 한 줄(x 1405 에서 끝남)이다. **통과.**
- **S23(39쪽) 오전 축소판 vs E01(6쪽)**: 층 순서(내 PC → 환경 파일 → 사내 LLM 게이트웨이)와 아이콘(Python 로고 · 열쇠 · 서버)이 같다. 이름은 줄인 형태다("내 PC · 실습 코드" → "내 PC", "환경 파일(.env)" → "환경 파일"). 방향만 세로 → 가로로 바뀌었다. **통과.**
- **안내판 글자 크기(LAB06·LAB07·LAB08·M01)**: 가장 작은 글자가 "막힌 단계에 손을 들어 알려 주세요"·출처 줄·자리표시 ID ≈ 24px 이다. 24px 미만으로 보이는 글자는 없다. LAB07 은 관찰할 것 3줄 + 단계 5줄 + 칩 두 줄로 빽빽하지만 잘림·겹침은 없고, 칩 줄 아래 끝(y 765)과 완료 조건(y 787) 사이 22px 이 가장 좁은 곳이다.
- **잘림·넘침·겹침**: 33장 모두 없음. 가장 빠듯한 곳은 A08 코드 줄 `grader = model.with_structured_output(`(x ≈ 1765, 카드 안쪽 끝 ≈ 1771).
- **오탈자·깨진 아이콘·이상한 기호**: 없음. 모든 장면에 시각 요소(아이콘·그림·개념도·자리표시)가 하나 이상 있다.
- 참고(시각 밖): G03(36쪽)에 11:40·13:20 이 크게 있다. 하우스 룰 "시간은 G01 한 장"의 예외(점심)로 합의된 것인지 내용 담당이 확인하면 된다. LAB08 의 "띠의 계획"·"띠의 검증 · 공유"는 위 진행 띠를 가리키는 말인데 수강생에게는 낯설 수 있다(내용 담당 몫).

## 4. 장면별 한 줄 판정

- 34쪽 S17 · 통과(낮음 1) / 35쪽 S21 · 수정 필요 — 같은 행 제목 높이, "개념 예시" 중복 / 36쪽 G03 · 통과(낮음 1) / 37쪽 D06 · 통과 / 38쪽 S22 · 통과
- 39쪽 S23 · 수정 필요 — 줄바꿈 두 곳 / 40쪽 LAB06 · 수정 필요 — "넣기" 줄바꿈, 왼쪽 열 빔 / 41쪽 LAB07 · 통과(공통 `.fallback`만) / 42쪽 C01 · 통과(낮음 1) / 43쪽 C02 · 통과
- 44쪽 S25 · 통과(낮음 1) / 45쪽 S28 · 수정 필요 — 자리표시가 카드에 붙음 / 46쪽 C03 · 통과 / 47쪽 S30 · 통과 / 48쪽 C04 · 통과(낮음 1)
- 49쪽 D07 · 통과 / 50쪽 S39 · 통과(낮음 1) / 51쪽 M01 · 수정 필요 — 번호 원 높이 / 52쪽 S40 · 통과(공통 `.fallback`만) / 53쪽 LAB08 · 수정 필요 — 좌우 열 아래 끝, 줄바꿈
- 54쪽 S43 · 통과(공통 `.fallback`만) / 55쪽 S44 · 통과 / 56쪽 G02 · 수정 검토 — "준비 · 01" / 57쪽 A01 · 통과 / 58쪽 A02 · 통과 / 59쪽 A03 · 통과
- 60쪽 A04 · 통과(낮음 1) / 61쪽 A05 · 통과 / 62쪽 A06 · 통과 / 63쪽 A07 · 통과(낮음 1) / 64쪽 A08 · 통과(낮음 2) / 65쪽 A09 · 통과 / 66쪽 A10 · 수정 필요 — 상자 높이, 그림 속 글자 크기

**문제없는 쪽**: 37 · 38 · 43 · 46 · 47 · 49 · 55 · 57 · 58 · 59 · 61 · 62 · 65 (13장). 공통 `.fallback` 한 건만 걸린 쪽: 41 · 52 · 54.

## 5. 총평

1. 전역 패치의 핵심(상자 테두리·회색 면·반전 배경 테두리·C03 표 넘침·표지 진행 띠)은 이 구간에서 모두 의도대로 보이고, 잘림·겹침·24px 미만 글자는 덱 본문에 한 곳도 없다.
2. 남은 문제는 거의 전부 "줄 수가 다른 상자가 한 행에 놓인 곳"과 "한 낱말만 다음 줄로 떨어진 곳"이다. M01·A10·S21 과 S23·LAB06·C01·LAB08 이 그것이고, 대부분 문구 몇 글자를 줄이면 CSS 를 건드리지 않고 풀린다.
3. CSS 로 손볼 것은 `.fallback` 점선(6장 공통), S28 자리표시 모양, LAB08·C04·M01 의 좌우 열 아래 끝 셋이고, A10 승인 카드 그림은 자산을 다시 캡처해야 뒷자리에서 읽힌다. G02 의 "준비 · 01"은 제자리에는 못 넣고 제목 줄 오른쪽이면 높이 부담 없이 되살릴 수 있다.

수정 필요: 높음 2 · 중간 8 · 낮음 13
