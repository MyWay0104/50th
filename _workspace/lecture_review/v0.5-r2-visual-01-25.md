# v0.5 r2 시각 검토 · 1–25쪽

- 기준: `_workspace/roadmap1/review_r2/snaps/slide-01.png`–`slide-25.png`(1920×1080 실제 렌더)를 한 장씩 열어 정렬·치우침·글자·가독성·하우스 룰·일관성을 봤다. 1회차 보고서(`v0.5-r1-visual-01-25.md`)와 반영 기록(`review_r1/fix_log.md`)을 먼저 읽고, 1회차 요청이 실제로 해결됐는지와 수정 때문에 생긴 회귀(겹침·넘침·빈 공간)를 우선 확인했다. 원인은 `topics/sk-hynix-ai-agent-guide-edu/index.html`에서 찾았고 원본은 고치지 않았다.
- 범위: 1–25쪽(장면 ID 대응은 `_workspace/roadmap1/scene_map_v0.5.md`). 결함으로 보지 않은 것: 오프화이트·주황 단색, 1쪽 짙은 주황 반전, 헤어라인 카드, 푸터, 쪽번호 없음, `[IMG-PLACEHOLDER]` 자리표시, 2·22쪽의 시각 표기, 실습 안내판 구조, 3쪽 YouTube 출처, 28쪽 녹색 상태 점.
- 심각도: 높음 = 잘림·겹침·깨짐·오탈자·요청 위반 / 중간 = 눈에 띄는 정렬 이상·큰 빈 공간·회귀 / 낮음 = 미세 조정.

## 1회차 요청 반영 확인

| 번호 | 쪽(장면 ID) | 1회차 요청 | 판정 | 비고 |
|---:|---|---|---|---|
| 1 | 16쪽(S16) | 코드 카드 하단 잘림 | 해결 | 설명문 2줄, 카드 하단 여백 복구 |
| 2 | 21쪽(S21) | 캡션이 패널 테두리와 겹침 | 해결 | 캡션이 격자 아래로 이동, 패널과 분리 |
| 3 | 21쪽(S21) | 하네스 칩 3개가 흩어지고 `+`가 허공 | 미해결 | 여전히 2줄. 첫 줄 끝 `+` 뒤에 `Context` 칩이 둘째 줄로 떨어짐(요청 표 #7) |
| 4 | 21쪽(S21) | 오른쪽 2블록 세로 중심 | 해결 | 왼쪽 패널 중앙(y≈487–738)에 정렬 |
| 5 | 16쪽(S16) | "말지'뿐입니다." 고아 | 해결 | |
| 6 | 08쪽(L01) | "따라옵니다" 고아 | 해결 | 강조 상자 한 줄 |
| 7 | 12쪽(R01) | "가까움" 고아 | 해결 | ② 카드 한 줄, 세 카드 기준선 일치 |
| 8 | 13·20쪽(S13·S20) | 막히면 상자 왼쪽 통일 | 해결 | 다만 17쪽(S19)은 원래부터 오른쪽이라 이제 17쪽만 다름(요청 표 #6) |
| 9 | 17쪽(S19) | 24px 회색 캡션 → 제목 | 해결 | `.panel-title` "실행 환경에서 막는 네 층" |
| 10 | 10·14·15·19·23쪽 | 카드 아래 큰 빈 공간 | 부분 해결 · 회귀 | 10·15쪽은 항목 보강으로 해결. 14·19·23쪽은 `.fill`로 카드가 바닥까지 늘어나 빈 공간이 카드 **안**으로 옮겨감(요청 표 #3·#4·#5) |
| 11 | 04쪽(S02) | 카드 아래 230px 빈 공간 | 해결 | 격자가 y≈950까지 채움. 카드 안 하단 여백은 낮음(요청 표 #17) |
| 12 | 04쪽(S02) | "개인 기록" 배지 위치 | 해결 | 카드 묶음 패널 안 왼쪽 위 |
| 13 | 02쪽(G01) | 오후 행 카드 폭 불일치 | 회귀 | 폭은 통일됐지만 6번 카드 글자가 2줄이 되어 6번만 높고 위치가 어긋남(요청 표 #2) |
| 14 | 03쪽(G00) | "들어 있습니다." 고아 | 미해결 | 문안은 바뀌었으나 "넣었습니다."가 그대로 둘째 줄 고아(요청 표 #10) |
| 15 | 03쪽(G00) | YouTube 출처 라벨 | 제외 | 사용자 결정 유지 |
| 16 | 09쪽(S08) | 번호 블록 등폭 | 해결 | 5개 등폭, 간격 균등 |
| 17 | 12쪽(R01) | 출처 괄호 표기 | 제외 | 확인만 |
| 18 | 13쪽(S13) | ④ 칩 둘째 줄·문장형 | 부분 해결 | 문구는 "④ 없으면 없음"으로 바뀌었으나 여전히 혼자 둘째 줄(요청 표 #11) |
| 19 | 13·17·20쪽 | 좌우 열 하단선 | 대부분 해결 | 13쪽 895/895, 20쪽 890/895 일치. 17쪽은 865/890으로 25px 차(요청 표 #14) |
| 20 | 17쪽(S19) | "필요 / 없는" 분리 | 미해결 | 문안이 바뀌어도 줄바꿈 위치가 같아 "필요 / 없는"이 그대로 갈라짐(요청 표 #8) |
| 21 | 19쪽(S29) | 줄 끝 "·" 고아 | 회귀 | 순서를 바꾸자 이번엔 "가리기"가 셋째 줄 고아(요청 표 #9) |
| 22 | 25쪽(S26) | "화면" 고아 | 해결 | 한 줄 |
| 23 | 25쪽(S26) | 손들기 칩 줄을 막히면 상자로 | 회귀 | 상자가 두 줄 문장 + 칩 줄로 커져 하단 테두리가 출처 줄과 겹침. 기록에 적힌 `.concept-badge`는 마크업에 없음(요청 표 #1·#13) |

## 장면별 판정

- 01쪽 · 통과
- 02쪽 · 수정 필요 — 6번 카드만 2줄이라 높이·위치가 7·8번과 어긋남(중간), 오후 행 오른쪽 2칸 빈 공간(낮음)
- 03쪽 · 수정 필요 — "넣었습니다."가 여전히 둘째 줄 고아(낮음)
- 04쪽 · 수정 필요 — 세 도구 카드 안 아래 약 110px 여백(낮음)
- 05쪽 · 통과
- 06쪽 · 통과
- 07쪽 · 통과
- 08쪽 · 통과
- 09쪽 · 통과
- 10쪽 · 수정 필요 — "근거 전달" 앞 녹색 점(28쪽 예외와 같은 규칙인지 확인, 낮음)
- 11쪽 · 통과
- 12쪽 · 통과
- 13쪽 · 수정 필요 — ④ 칩 혼자 둘째 줄(낮음)
- 14쪽 · 수정 필요 — `.fill`로 카드가 늘어나 두 카드 안 아래 약 170–200px 빈 공간(회귀, 중간), 표 안 녹색 점(낮음)
- 15쪽 · 통과
- 16쪽 · 통과
- 17쪽 · 수정 필요 — 막히면 상자가 오른쪽(9·13·20·25쪽은 왼쪽)(중간), 완료 조건 "필요 / 없는" 분리(낮음), 오른쪽 도식 위아래 여백과 좌우 하단선 25px 차(낮음)
- 18쪽 · 통과
- 19쪽 · 수정 필요 — 코드 카드 안 아래 약 190px 빈 공간(회귀, 중간), 자리표시 "가리기" 고아(낮음)
- 20쪽 · 수정 필요 — 막히면 "봅니다" 고아(낮음)
- 21쪽 · 수정 필요 — 하네스 칩 줄이 2줄이고 `+`가 첫 줄 끝에 남음(중간)
- 22쪽 · 통과
- 23쪽 · 수정 필요 — 두 카드 안 아래 약 190px 빈 공간(회귀, 중간)
- 24쪽 · 통과
- 25쪽 · 수정 필요 — 막히면 상자 하단 테두리가 출처 줄과 겹침(높음), "손들기" 고아·배지 없음(낮음)

## 수정 요청

| 번호 | 쪽(장면 ID) | 요소 | 현재 상태 | 제안 | 심각도 |
|---:|---|---|---|---|---|
| 1 | 25쪽(S26) | `.practice-grid` 왼쪽 `.stack` > `.fallback` ↔ `p.source` | 막히면 상자가 2줄 문장 + 칩 줄로 커져 왼쪽 열이 `.practice-grid{flex:1; min-height:0}` 높이를 넘치고, 상자 하단 점선(y≈935)이 출처 "출처: Claude Code 공식 문서…"(y≈933) 위에 그대로 겹침. 원인: r1 #23으로 칩 줄을 상자 안에 넣으며 상자가 약 120px 커졌는데 격자는 줄어들 수만 있어 넘친 부분이 아래 출처와 만남 | (1) `.fallback > p` 문안을 한 줄로: "<strong>막히면</strong>짝·강사 시연 화면으로 진행합니다(우회 접속 없음)" 로 줄이고, "막힌 단계에 손들기"는 칩 줄 맨 앞 `<span class="concept-badge">막힌 단계에 손들기</span>`로 옮긴다(9쪽 `.chip-row` 배지 방식과 같음). (2) 그래도 넘치면 `.scene[data-scene-id="S26"] .practice-grid { flex: 0 0 auto; }`로 격자를 내용 높이로 두고 `.scene[data-scene-id="S26"] .placeholder { min-height: 0 }` | 높음 |
| 2 | 02쪽(G01) | 오후 행 `.time-block`(6번) | `.what` "Claude Code 설치 확인·작동"이 카드 폭(≈315px) 안에서 2줄로 접혀 6번 카드가 7·8번보다 약 40px 높고(633–855 vs 655–835) 번호 원·아이콘 위치도 다름. 격자 기본 `align-items`가 stretch가 아니어서 카드 높이가 제각각 | `.scene[data-scene-id="G01"] .time-blocks.time-band { align-items: stretch; }`를 추가해 세 카드 높이를 맞추거나, 강사 문구 유지가 필수가 아니면 `.what`을 "Claude Code 설치·작동"으로 줄여 한 줄로 | 중간 |
| 3 | 14쪽(S14) | `.compare-grid.fill` > `.compare-col` | 카드가 y≈893까지 늘어났지만 왼쪽 내용은 y≈690, 오른쪽 표는 y≈720에서 끝나 카드 안 아래 170–200px이 빈 흰 면으로 남음. 1회차의 "카드 아래 빈 공간"이 "카드 안 빈 공간"으로 옮겨진 회귀 | `.fill` 클래스를 제거하고(1회차 상태로 복귀) 대신 카드 `padding`을 40→48px로 늘리거나 왼쪽 `compare-list`에 항목 1개(예: "날짜·수치는 문단 원문과 같은지")를 더해 카드 자체 높이를 키운다. `.fill`을 유지하려면 `.scene[data-scene-id="S14"] .compare-col { justify-content: center; }`로 내용을 세로 중앙에 | 중간 |
| 4 | 19쪽(S29) | `.split-grid.fill` > `.code-card` | 코드 카드가 y≈893까지 늘어났지만 코드 4줄은 y≈700에서 끝나 카드 안 아래 약 190px이 빔. 자리표시는 원래 채우는 요소라 문제없음 | `.fill`을 빼고 `.scene[data-scene-id="S29"] > .split-grid { align-items: start; }` 로 코드 카드는 내용 높이, 자리표시만 `min-height`로 두거나, `.scene[data-scene-id="S29"] .code-card { align-self: start; }`로 코드 카드만 늘어나지 않게 | 중간 |
| 5 | 23쪽(S22) | `.compare-grid.fill` > `.compare-col.stack` | 두 카드가 y≈893까지 늘어났지만 왼쪽 목록은 y≈700, 오른쪽 도식 캡션은 y≈705에서 끝나 카드 안 아래 약 190px이 빔(14쪽과 같은 회귀) | 14쪽과 같은 방법. 권장: `.fill` 제거 + 카드 `padding` 48px. 오른쪽 `.dg`에 `justify-content: center`를 주면 도식만 중앙으로 가서 왼쪽과 다시 어긋나므로 두 카드에 함께 적용 | 중간 |
| 6 | 17쪽(S19) | 오른쪽 `.stack` > `.fallback` (index.html 1849행) | 막히면 상자가 오른쪽 열 맨 아래(y≈780–890)에 있음. 1회차 #8 반영으로 13·20쪽이 왼쪽으로 옮겨져 이제 9·13·20·25쪽은 왼쪽, 17쪽만 오른쪽 | `.fallback`을 왼쪽 `.stack`의 `.done-box` 다음으로 이동. 오른쪽은 `.panel-title` + `.dg`만 남기고 `.scene[data-scene-id="S19"] .practice-grid > .stack > .dg { flex:1; justify-content:center }`는 유지(도식이 왼쪽 열 높이 안에서 중앙) | 중간 |
| 7 | 21쪽(S21) | `.split-panel` > `.chip-row` (1965–1973행) | "하네스 = [Tools · 조회 도구] + [Memory · 대화 이력] +" 까지 첫 줄, "[Context · 지침·근거 자료]"만 둘째 줄. 첫 줄 끝 `+`가 허공에 남아 수식이 끊겨 보임. 패널 내용폭(≈830px)에 배지+`=`+칩 3개+`+` 2개가 약 60px 초과 | (1) 셋째 칩을 "Context · 지침·자료"로 줄이거나 (2) `.scene[data-scene-id="S21"] .split-panel .chip-row { gap: 8px; }` + `.chip { padding-inline: 16px }` (3) 안 되면 `+`를 각 칩 **앞**으로 옮겨 "= [Tools] [+ Memory] [+ Context]" 순서로 두어 둘째 줄이 `+ Context`로 시작하게 | 중간 |
| 8 | 17쪽(S19) | `.done-box` > `.done-text` | "정답을 아는 질문과 도구가 필요 / 없는 질문을 구분해 설명합니다" — 1회차 문안으로 바꿨는데 줄바꿈 위치가 같아 "필요 없는"이 여전히 갈라짐 | `<span class="nowrap">필요 없는</span>`로 묶거나 문안을 "정답을 아는 질문과 도구 없이 답할 질문을 구분해 설명합니다"로 | 낮음 |
| 9 | 19쪽(S29) | `.placeholder-desc`(조건 줄) | "조건: 1600×900 이상 · 실행일 표기 · 모델명·주소·키 / 가리기" — 순서를 바꾸자 이번엔 "가리기"가 고아 | `<span class="nowrap">모델명·주소·키 가리기</span>`로 묶는다(순서 조정만으로는 폭이 아슬아슬해 반복됨) | 낮음 |
| 10 | 03쪽(G00) | `p.explain` | "…실습 완료 조건에도 설명과 확인을 / 넣었습니다." — 1회차 문안으로 바꿨으나 마지막 어절만 둘째 줄 | `<span class="nowrap">확인을 넣었습니다.</span>` 또는 문안 "…실습 완료 조건에도 설명과 확인이 있습니다."처럼 한두 글자 줄이기. `text-wrap: pretty`를 `.explain`에 주면 덱 전체 고아 어절이 줄어듦(8·17·19·20쪽 포함) | 낮음 |
| 11 | 13쪽(S13) | `.chip-row` 넷째 칩 | "④ 없으면 없음"이 혼자 둘째 줄. 네 칩 폭 합(≈840px)이 왼쪽 열 폭(≈855px)에 gap 포함 조금 넘침 | `.scene[data-scene-id="S13"] .practice-grid .chip-row { gap: 8px; }` 또는 첫 칩을 "① 찾아 읽기"로 | 낮음 |
| 12 | 20쪽(S20) | `.fallback` | "옆 사람 화면으로 함께 확인하고, 계속 막히면 강사 시연으로 / 봅니다" — "봅니다"만 둘째 줄 | `<span class="nowrap">강사 시연으로 봅니다</span>` 또는 문안 "…계속 막히면 강사 시연으로 진행합니다"(9·13쪽 어미와 통일) | 낮음 |
| 13 | 25쪽(S26) | `.fallback > p` | "…(우회 접속 없음) · 막힌 단계에 / 손들기" 고아. fix_log에는 라벨을 `.concept-badge`로 뒀다고 적혔으나 마크업엔 없고 문장 끝에 붙어 있음 | 1번 제안 (1)로 해결됨(배지로 분리) | 낮음 |
| 14 | 17쪽(S19) | 오른쪽 `.stack` (`.panel-title` + `.dg` + `.fallback`) | `.dg{flex:1; justify-content:center}` 때문에 제목(y≈395)과 2×2 도식(500–690) 사이, 도식과 막히면 상자(780) 사이에 90px씩 빈 띠. 좌우 하단선 865/890 | 6번(막히면 왼쪽 이동) 반영 후 `.dg`를 `justify-content: flex-start`로 두고 제목 바로 아래 붙이거나, 도식 `.dg-box` 높이를 키워 채운다 | 낮음 |
| 15 | 02쪽(G01) | 오후 행 `.time-blocks.time-band` | 5칸 격자에 카드 3개라 오른쪽 2칸(x≈1150–1800, 약 650px)이 빈 면. 오전 행과 폭을 맞춘 결과라 의도로 볼 수 있음 | 유지 가능. 채우고 싶다면 4·5칸 자리에 `.dg-box.muted` 한 칸("→ 종료 17:30 · 정리")을 두거나 오후 행만 `grid-column: span`으로 가운데 정렬 | 낮음 |
| 16 | 10쪽(S09)·14쪽(S14) | `.status-dot.ok` | 10쪽 "근거 전달" 제목 앞, 14쪽 표 "통과" 앞에 녹색 점. 28쪽 녹색 점은 사용자 결정으로 유지지만 10·14쪽도 같은 예외인지 미확인(주황 외 두 번째 색) | 확인만. 예외가 아니면 `.status-dot`를 `--accent`로 바꾸거나 10쪽은 점 없이 제목만 | 낮음 |
| 17 | 04쪽(S02) | `.split-panel > .dg-row.stretch > .dg-box` | 세 도구 카드가 y≈620–910으로 늘어났지만 글자는 y≈790에서 끝나 카드 안 아래 약 110px 여백. 격자를 채운 결과라 1회차의 230px 빈 면보다는 낫지만 카드 안 글자가 위로 쏠림 | `.scene[data-scene-id="S02"] .split-panel .dg-box { justify-content: center; }`로 세 카드 내용을 세로 중앙에 | 낮음 |

수정 필요: 높음 1 · 중간 6 · 낮음 10
