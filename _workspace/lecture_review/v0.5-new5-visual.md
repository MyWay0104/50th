# v0.5 신규 5장 정밀 시각 검토 (T01·V01·V02·T02·T03)

- 대상: `topics/sk-hynix-ai-agent-guide-edu/snapshots/frame-00~04` (1920×1080) + 현재 `index.html`의 실제 배치를 브라우저(Playwright, 1920×1080)에서 좌표로 재측정
- 주의: 스냅샷은 09:34:06 촬영, `index.html`은 09:34:44 저장(스냅샷 이후). 아래 좌표는 **현재 index.html 기준**이며, 스냅샷과 다른 곳은 따로 적었다. 원본은 고치지 않았다.
- 좌표는 장면 좌상단 기준 px, `x,y / w×h`.

## 장면별 판정

1. **10쪽 T01 단순 호출의 한계와 RAG — 통과.** 세 열 kicker(y438) · 제목(y479) · 밑줄 · 첫 항목(y575)이 3열 모두 ±1px로 맞고, 항목은 전부 한 줄. 칩 줄(y813–870)의 칩·화살표·문장 중심이 841~845로 정렬. 넘침·잘림·고아 없음.
2. **13쪽 V01 벡터 저장소가 필요한 이유 — 수정 필요(높음 1, 중간 1).** 오른쪽 열(도식+캡션 2줄+코드 위치) 내용 높이 476px가 `split-grid` 셀 443px를 33px 넘쳐 `코드 위치` 줄(y893–927)이 출처 줄 영역(y894–952)과 세로로 겹치고, 왼쪽 카드 하단(895)과 오른쪽 하단(927)이 32px 어긋난다. 스냅샷의 "조각: "설비 이상" → / 못 찾음" 고아 줄바꿈은 현재 index(3항목 "결과: 못 찾음")에서 해소됐으나 overview.html에는 미반영.
3. **14쪽 V02 FAISS·Chroma·Milvus — 통과.** 세 열 kicker(y492)·제목(y534)·첫 항목(y630) 정렬, kicker "라이브러리 · 서버 없음 · 오늘 실습"도 한 줄(455px 안). 카드(451–742)·공통 띠(766–834)·캡션(858–894)이 출처 줄 위까지 빈 공간 없이 채움.
4. **18쪽 T02 RAG의 한계와 도구 — 수정 필요(중간 1, 낮음 1).** 설명문 3줄째가 "합니다." 한 어절 고아(y≈390). 설명이 3줄이라 카드가 451까지 내려와 칩 줄 하단(894)과 출처 글자(918) 간격이 24px로 T01·T03(48px)보다 좁다. 카드·칩 정렬 자체는 T01과 동일하게 정상.
5. **23쪽 T03 도구의 한계와 화면 — 통과(낮음 1).** T01과 같은 배치·정렬. 3열 제목 "실습 4 · Streamlit 챗봇"(x1298–≈1700)과 Streamlit 로고(x1723–1763, y442–482) 사이 여유 약 23px로 겹치지 않음. 로고만 컬러라 다른 두 열의 단색 선 아이콘과 톤이 다름.

## 수정 요청

| 쪽 | 장면 ID | 요소 | 현재 상태(좌표) | 제안(마크업/CSS 수준) | 심각도 |
|---|---|---|---|---|---|
| 13 | V01 | 오른쪽 열 `.split-grid > .stack` (도식·캡션·`.guide-ref`) | 셀 992,451 / 808×443(OVF-Y). 내용: `.dg` 297 + gap 24 + 캡션 36 + 24 + 캡션 36 + 24 + `.guide-ref` 34 = 475 → 33px 넘침. `.guide-ref` 992,893 / 808×34가 `.source` 120,894 / 1680×58(글자 918–952)과 y 겹침. 왼쪽 카드 하단 895 vs 오른쪽 하단 927 | 원인: 설명문 3줄(139px)로 그리드가 451에서 시작. CSS(`/* V01·V02 */` 블록에 추가) `.scene[data-scene-id="V01"] .split-grid > .stack { gap: 12px; }` + `.scene[data-scene-id="V01"] .dg { gap: 16px; }`(합계 −44px → 431 ≤ 443). 대안(마크업): 캡션 두 줄을 한 줄 `윗줄 · 손으로 하는 저장 / 아랫줄 · 나중에 쓰는 방식`으로 합쳐 −60px | 높음 |
| 13 | V01 | 왼쪽 카드 `.compare-list`(overview 동기화) | index.html에는 `결과: 못 찾음 / 결과: 찾음` 3항목(각 1줄, y694·757·820)이나 overview.html(09:31 저장)에는 없음(grep 0건). 스냅샷 frame-01은 구버전이라 "→ / 못 찾음", "→ / 찾음" 고아 줄이 보임 | `python scripts\sync_overview.py topics\sk-hynix-ai-agent-guide-edu` 실행 후 `--check`, 스냅샷 재촬영 | 중간 |
| 18 | T02 | `.explain` 3줄째 | 120,280 / 1600×139(3줄). 마지막 줄 "합니다." 한 어절만 남음(스냅샷 y≈390) | CSS `.scene[data-scene-id="T02"] .explain { text-wrap: pretty; }`(고아만 해소, 3줄 유지) 또는 마크업 문구 축약으로 2줄화: "…다음 실습은 조회 함수를 도구로 붙여 모델이 부르고 코드가 실행합니다." 등 약 40자 감축 | 중간 |
| 18 | T02 | `.chip-row` ↔ `.source` 간격 | 칩 줄 120,837 / 1680×57(하단 894) 바로 아래 `.source` 894(글자 918) → 24px. T01·T03은 48px | 위 설명문 2줄화 시 자동 해소(카드가 405로 올라감). 3줄을 유지하면 `.scene[data-scene-id="T02"] .compare-col { padding: 28px 36px; }`로 카드 −8px | 낮음 |
| 10·18·23 | T01·T02·T03 | 칩 줄 `.dg-sub` 기준선 | `.dg-sub` 기본 `margin-top: 6px`가 `align-items: center` 안에서도 남아 문장 중심(844.5)이 칩 중심(841.5)·화살표(842)보다 3px 아래 | CSS `/* T01–T03 */` 블록의 `.chip-row > .dg-sub` 규칙에 `margin-top: 0;` 추가 | 낮음 |
| 23 | T03 | 3열 `.col-icon`(streamlit.svg) | 1723,442 / 40×40 컬러 로고. 제목 상자 1298,479 / 465×72와 y 3px 겹치나 글리프 겹침 없음, 가로 여유 ≈23px | 겹침은 없으므로 배치 수정 불필요. 톤 통일이 필요하면 다른 열처럼 단색 선 아이콘(예: `layout`·`app-window` 계열)으로 교체 | 낮음 |

**수정 필요: 높음 1 · 중간 2 · 낮음 3**
