# B1–B3 검토 (post-build)

작성: lecture_expert · 2026-09-14 · 대상: 오전 22장 (S01, G01, S02–S21) · 사용자 검토 전 내부 검토 1회차
입력: HyperFrames 렌더링 PNG 22장(1920×1080, `snap_am/frame-00`–`frame-21`, 전 장면을 이미지로 직접 확인), `lecture_review/B1-B3-pre.md`, `slide_copy/B1–B3.md`, `slide_ui/B1–B3.md`, `index.html`(CSS·발표자 노트 확인용, 읽기만)
표기: 요청 번호 `T` = 문구(slide_content_writer), `C` = 배치·CSS(slide_ui_designer / 빌더). 좌표는 1920×1080 화면 기준 눈대중 값이다.

## 판정 한눈에 보기

| 항목 | B1 | B2 | B3 |
|---|---|---|---|
| 1 목표 정렬 | 통과 | 통과 | 통과 |
| 2 시간 현실성 | 통과 | 통과 | 통과 |
| 3 인지 부하 | 통과 | 통과(조건부, 보류 P3) | 통과 |
| 4 오해 예방 | 통과 | 통과 | 통과 |
| 5 실습 전환 | 통과 | 통과 | **수정 필요** |
| 6 참여 질문 | 통과 | 통과 | 통과 |
| 7 오전·오후(차수 간) 연결 | 통과 | 통과 | 통과(B4 쪽 W11 이월) |
| 8 강사 사례 위치 | 통과 | 통과 | 통과 |
| 9 가독성(화면 확정) | **수정 필요** | **수정 필요** | **수정 필요** |

수정 필요: **B1 1건, B2 1건, B3 2건** (항목 기준). 요청은 문구 2건(T1–T2), 배치·CSS 2건(C1–C2)이다.

공통 결론: 사전 검토에서 지적한 내용 문제는 모두 화면과 노트에 반영됐다. 남은 문제는 대부분 **배치 결함**이다. 그중 가장 넓은 것은 `.stack`으로 감싼 5개 장면(S04·S10·S14·S16·S17)에서 제목(또는 부제)과 본문 카드 사이 간격이 사라진 것이다(C1). 원인은 CSS `.stack > * { margin-top: 0; }`가 안쪽 `.steps`·`.compare-grid`의 margin-top 56px를 지우는 것이다.

---

## B1 검토 · 09:00–09:40 · LLM 원리와 첫 호출 (S01, G01, S02–S08)

| 항목 | 판정 | 근거(화면·노트) | 수정 요청 |
|---|---|---|---|
| 1 목표 정렬 | 통과 | S02 부제가 화면에 "목표: 만들고, 설명하고, 조건 바꾸고, 검증하기"로 보인다(W2 반영). S01 노트는 B1 목표 문장("첫 40분은 LLM이 입력을 받아 답을 만드는 과정을 이해한 뒤 직접 호출해 temperature를 바꿔 비교합니다")으로 바뀌었다(W1, index.html 966행). S08 노트에는 "temperature가 앞에서 본 응답 생성 세 단계 중 어디에 영향을 주는지"를 되묻는 문장이 들어갔다(W6, 1243행). | — |
| 2 시간 현실성 | 통과 | S08 화면: board-meta "09:24–09:40 · 16분", 시간 블록 4 / 6(파랑) / 4 / 2분이 2×2로 모두 보인다. 합계 16분으로 사전 검토 계산과 같다. | — |
| 3 인지 부하 | 통과 | S04 캡션이 "실제 토큰 분할은 모델마다 다름 · 확률 수치는 표시하지 않음"으로 바뀌어 토크나이저가 화면에서 빠졌다(W4). S06 `실습 코드` 상자 보조가 "SDK로 요청 작성"으로 바뀌었다(W5). 코드 첫 줄 `from langchain_openai import ChatOpenAI`는 화면에 남아 있지만, 노트의 "langchain은 10시 구간에서 다루니"(1180행)가 이를 받친다. | — |
| 4 오해 예방 | 통과 | 화면으로 확인했다: S03 캡션 "포함 관계를 나타낸 그림 · 연표가 아님", S04 callout "학습은 매개변수를 바꾸고, 보통의 호출은 학습된 모델로 계산만 한다", S07 강조 열 "새 정보를 더하지 않음 / 낮아도 틀릴 수 있음 / 완전 동일도 보장 안 됨", S01 부제 "작은 AI 도구 만들기". | — |
| 5 실습 전환 | 통과 | S08 우측 하단 점선 상자에 "막히면 접속이 안 되면 준비된 출력으로 같은 비교를 계속합니다"가 1줄로 보인다. S08 3단계 관찰은 S09 노트 첫 문장 "실습 1 마지막에 최신 자료를 물었을 때…"(W7, 1276행)로 되짚는다. | — |
| 6 참여 질문 | 통과 | 사전 검토와 같다(S01 손들기, S04 재학습 질문, S07 예측, S08 발표). S08 확인 질문이 1개 늘었다. | — |
| 7 차수 간 연결 | 통과 | S08 bullets 3 "최신 자료 질문으로 한계 관찰"에서 S09 제목 "외부 자료가 필요한 질문"과 W7 노트로 이어진다. | — |
| 8 강사 사례 위치 | 통과 | S02 화면의 태그는 이름 4개("NAND Implant 공정 Engineer", "Part Finder", "Dokmo", "기술 블로그")뿐이다. 성과 수치는 없다. | — |
| 9 가독성 | **수정 필요** | 본문 32–40px, 제목 72px이다. 모든 장면이 정지 화면 한 장에 잘림 없이 들어간다(사전 검토에서 우려한 G01 callout·S06 코드 줄 모두 잘리지 않음). 강조는 장면당 1–2묶음으로 절제됐다. 결함 3개: ① **S04**: 제목 아랫변(y≈215)과 단계 카드 윗변(y≈225) 사이가 약 10px뿐이라 제목과 카드가 붙어 보인다. 같은 B1의 S03·S05·S06·S07은 약 65px다. ② **G01**: 타임라인 세로선이 시각 글자 "09:00·10:00·11:00"(x≈148), "13:20–16:00"(x≈1020)을 관통한다. 번호 원(x≈296, 1168)을 잇지 않는다. ③ **S06**: 영역 라벨 "수강생 Python 프로그램"이 2줄로 접히고, 둘째 줄 "프로그램"이 안쪽 `실습 코드` 상자의 윗 테두리와 겹친다. | C1, C2, T1 |

---

## B2 검토 · 10:00–10:40 · 외부 근거와 기사 RAG (S09–S14)

| 항목 | 판정 | 근거(화면·노트) | 수정 요청 |
|---|---|---|---|
| 1 목표 정렬 | 통과 | S09 부제 "같은 질문을 두 방식으로 답할 때"로 시작하고, S13 완료 조건 "답변의 한 문장을 뒷받침하는 기사 문장을 찾는다"와 S14 두 열 "근거에 있음 / 근거 밖"으로 닫힌다. 모두 화면에서 확인했다. | — |
| 2 시간 현실성 | 통과 | S13 화면: "10:14–10:36 · 22분", 블록 5 / 7(파랑) / 7 / 3분 = 22분. | — |
| 3 인지 부하 | 통과(조건부) | S10 callout "임베딩·Vector DB는 검색 방법 중 하나 · 모델을 다시 학습시키지 않음"이 화면에 그대로 있다. 실습에서 임베딩을 쓰지 않으면 연결이 끊긴다는 조건은 그대로 남는다 → 보류 P3. | — |
| 4 오해 예방 | 통과 | S09 권장 열 메모 "검색·자료 품질·검증은 여전히 필요", S10 callout "모델을 다시 학습시키지 않음", S12 열 메모 "코드 길이 차이는 성능 차이가 아님", S11 배지 "개인 기록"과 캡션 "개인 수집 경험을 단순화한 표"가 모두 보인다. | — |
| 5 실습 전환 | 통과 | S13 배치가 S08과 같다(좌: 단계 3개 + 완료 조건 / 우: 시간 블록 2×2 + 막히면). "막히면 기사 접속이 안 되면 준비된 저장 본문으로 같은 단계를 진행합니다"가 2줄로 보인다. | — |
| 6 참여 질문 | 통과 | S09(W7), S10, S14 노트로 3회다. | — |
| 7 차수 간 연결 | 통과 | 사전 검토와 같다. S14 노트의 SQL 전환 문장이 S15 부제 "질문 예: 최근 일주일 장비별 점검 건수는?"로 이어진다. | — |
| 8 강사 사례 위치 | 통과 | S11이 S10 1단계 "자료 준비와 분할" 직후에 나온다. 표의 "비어 있음" 칸만 강조했다. | — |
| 9 가독성 | **수정 필요** | 본문 32–40px, 코드 28px(JetBrains Mono)이다. 모든 내용이 한 화면에 보인다. S12 코드는 두 카드 모두 잘리지 않았다(좌 최장 줄 끝 x≈885 < 카드 오른쪽 938, 우 최장 줄 끝 x≈1663 < 1798). 결함: **S10·S14**에서 제목 아랫변(y≈215)과 카드 윗변(y≈225) 사이가 약 10px다. 같은 차수의 compare 장면 S09는 부제 아래 약 65px, split 장면 S11은 제목 아래 약 65px라서 **S09와 S14가 같은 compare 유형인데 배치가 다르게 보인다**. | C1 |

---

## B3 검토 · 11:00–11:40 · SQL Tool과 Streamlit (S15–S21)

| 항목 | 판정 | 근거(화면·노트) | 수정 요청 |
|---|---|---|---|
| 1 목표 정렬 | 통과 | 첫 장 S15 열 kicker "모델 / SQL·Python"이 실행 주체를 나눈다. 마지막 S21 노트에 "그런데 이 그림에서 SQL을 실제로 실행하는 것은 모델인가요, 프로그램인가요?"가 들어가(W10, 1646행) 목표를 되묻는다. | — |
| 2 시간 현실성 | 통과 | S19 화면 "11:14–11:30 · 16분", 블록 3 / 7(파랑) / 4 / 2분이다. S20 부제는 "11:30–11:38 · 8분 · 실행 3분 → 질문·결과 3분 → 코드 위치 2분"이다. | — |
| 3 인지 부하 | 통과 | S16 화면의 새 용어는 도구·인자·@tool이다. 칩 "인자 days=7"이 바로 예시로 붙는다. S18 노트의 TF-IDF가 "단어 빈도 기반 검색"으로 바뀌었다(W8, 1574행). | — |
| 4 오해 예방 | 통과 | S16 callout "모델은 함수를 직접 실행하지 않는다 · @tool은 설명과 입력 형식을 만든다", S17 강조 열 없이 callout "필요한 만큼만 규칙으로 충분하면 스크립트부터", S19 제목 "SQL Tool 연결", S15 메모 "가상 데이터 · EQ-01 12건 · EQ-02 7건 (최근 7일)", S18 캡션 "가상 데이터"가 모두 보인다. | — |
| 5 실습 전환 | **수정 필요** | S20의 대체 경로가 화면에 들어갔다(W9·U1). 완료 조건 상자 오른쪽 점선 상자에 "막히면 옆 사람 화면으로 함께 확인하고, 5분 넘게 막히면 강사 시연으로 봅니다"가 2줄로 보인다. 남은 빈 곳은 **실습 가이드 위치**다. S08·S13·S19는 모두 "… · 실습 가이드 해당 장"을 화면에 두는데, 오전에서 가장 늦어지기 쉬운 S20에만 없다. 부제는 시간 배분만 적는다. 수강생이 따라 할 절차를 어디서 찾는지 화면에 없다. | T2 |
| 6 참여 질문 | 통과 | S15, S17, S21(W10 실행 주체 질문 + "고쳐 보고 싶은 곳 고르기")로 3회 이상이다. | — |
| 7 오전·오후 연결 | 통과 | S21 해석 항목 3이 화면에 "오후: 이 앱을 Claude Code로 개선"으로 1줄에 보인다(W10). B3 끝 조건은 충족했다. B4 처음 조건(W11)은 오후 문구 작성으로 이월됐으므로 B4 사전 검토에서 판정한다. | (W11 이월) |
| 8 강사 사례 위치 | 통과 | S18 노트 첫 문장이 "앞에서 조회 쪽은 없는 식별자를 만들지 않아야 한다고 했는데…"로 S15를 되짚는다(W8). 화면의 배지 "공개 Beta 설계 기반 개념 예시"와 캡션 "가상 데이터 · 공개 Beta 구조를 단순화"로 범위를 한정한다. 수치는 없다. | — |
| 9 가독성 | **수정 필요** | 본문 32–40px다. S18 표 4열은 모두 1줄이고 결과 줄 "결과: 조건에 맞는 후보 없음"만 파랑이다. S21 해석 3줄은 모두 1줄이다. 결함: ① **S16** 제목 아랫변(y≈215)과 카드 윗변(y≈225) 사이가 약 10px다. ② **S17** 부제 아랫변(y≈280)과 카드 윗변(y≈289) 사이가 약 9px다. 같은 `cols-3` compare인 **S07**은 제목 아래 약 65px, 같은 차수의 compare **S15**는 부제 아래 약 65px라서 두 장면의 배치가 어긋난다. S20 이미지 자리는 아직 점선 자리표시다(약 1,400×388px, 보류 P4). | C1 |

---

## 사전 검토 요청 반영 확인

| 요청 | 확인 방법 | 결과 |
|---|---|---|
| W1 S01 노트 | index.html 966행 "하루 흐름은 다음 장에서…" | 반영 |
| W2 S02 부제 | 화면 frame-02 "목표: 만들고, 설명하고, 조건 바꾸고, 검증하기" | 반영 |
| W3 G01 노트 | 1023행 "아래에 적힌 네 번, 각 20분". 화면 callout "휴식 09:40 · 10:40 · 14:00 · 15:00 (각 20분)"과 일치 | 반영 |
| W4 S04 캡션 | 화면 frame-04 "실제 토큰 분할은 모델마다 다름 · …" | 반영 |
| W5 S06 보조·노트 | 화면 frame-06 상자 보조 "SDK로 요청 작성" + 1180행 | 반영 |
| W6 S08 노트 | 1243행 "세 단계 중 어디에" | 반영 |
| W7 S09 노트 | 1276행 "실습 1 마지막에 최신 자료…" | 반영 |
| W8 S18 노트 | 1574행 "없는 식별자를 만들지 않아야", "단어 빈도 기반 검색" | 반영 |
| W9 S20 fallback | 화면 frame-20 점선 상자 문구 + 1618행 | 반영 |
| W10 S21 li3·노트 | 화면 frame-21 li 3 "오후: 이 앱을 Claude Code로 개선" + 1646행 | 반영 |
| W11 S22 | 범위 밖(P4a 이월) | B4 사전 검토에서 확인 |
| U1 S20 배치 | 화면: 완료 조건 상자와 막히면 상자가 이미지 자리 아래 한 줄로 나란히 있고, 두 상자 윗변이 y≈743으로 맞다 | 반영 |
| UI 축소 S06·S12·S19 | S06 코드 5줄, 모델·vLLM 상자 이름만 / S12 `msgs` 6줄 / S19 스키마 칩 없음 | 반영 |

## 빌드 중 오케스트레이터 변경 사항의 교육적 판정

| 변경 | 판정 | 근거 |
|---|---|---|
| compare·split 카드를 내용 높이에 맞추고 남는 공간은 비움 | 문제없음 | S05·S07·S21 등은 카드 아래 약 200–250px가 비지만, 내용이 위쪽에 모여 시선이 한곳에 머문다. 빈 카드 안에 글이 떠 있는 것보다 낫다. 발표자가 말로 채우는 장면(S02)과도 어긋나지 않는다. 다만 이 변경과 별개로 `.stack` 장면의 위쪽 간격 결함(C1)은 고쳐야 같은 유형이 일관돼 보인다. |
| 코드 글꼴 JetBrains Mono 고정 | 문제없음 | S12 `choices[0]`의 0과 O, `llm`의 l과 1이 구분된다. 28px로 강의실 뒤에서도 읽힌다. 사전 검토에서 우려한 Consolas 부재로 인한 잘림은 없다. 참고(요청 아님): S06 한글 주석은 대체 글꼴로 그려져 단어 사이가 넓게 벌어진다("#  사용할  모델  이름"). 읽는 데 지장은 없다. |
| S12 LangChain 둘째 줄을 두 줄로 나눔 | 문제없음 | 화면에서 `base_url=BASE_URL)`이 `model=`과 같은 열에 매달려 정렬된다. 괄호 안 줄바꿈이라 유효한 Python이고, 수강생이 실습 코드에서 같은 형태를 봐도 혼동이 없다. 호출 줄 강조(`client.chat.completions.create(` / `llm.invoke(prompt)`)는 유지됐다. 두 코드가 6줄 대 5줄이 됐지만 열 메모 "코드 길이 차이는 성능 차이가 아님"이 그대로 성립한다. |

---

## 장면별 화면 메모 (문제가 있는 장면만)

- **G01 하루 시간표**: 오전·오후 두 열의 세로 연결선이 시각 칸(폭 120px)을 지난다. "09:00", "11:00", "13:20" 등 숫자 한가운데를 가는 회색 선이 보인다. CSS `.steps.timeline::before { left: 27px; }`는 기본 `.steps`(번호 원이 맨 왼쪽)용 위치다. 이 장면은 번호 원 앞에 `.step-time` 120px + 간격 28px가 있다. 시간표를 2분 동안 띄워 두는 장면이라 숫자가 흐려 보이면 안 된다 → C2.
- **S04 LLM의 응답 생성**: 제목과 단계 카드 3개가 약 10px 간격으로 붙는다. 아래쪽 callout(y≈803)과 출처(y≈933) 사이에는 약 110px가 남아 56px를 되돌려도 넘치지 않는다 → C1.
- **S06 모델·서버·API의 역할**: 좌측 개념도의 영역 라벨 "수강생 Python 프로그램"이 영역 폭(약 270px)을 넘어 2줄로 접힌다. 둘째 줄이 `실습 코드` 상자 테두리와 겹친다. 라벨은 absolute(top 14px, 22px 글자)라 1줄을 전제로 한다 → T1.
- **S10 RAG의 기본 구조**: S04와 같은 결함(제목–카드 약 10px). callout 아래 여유 약 220px → C1.
- **S14 답변과 검색 근거 확인**: 제목–카드 약 10px. 같은 차수의 compare S09(부제 아래 약 65px)와 달라 보인다. callout 아래 여유 약 170px → C1.
- **S16 Tool 호출의 실행 순서**: 제목–카드 약 10px. callout 아래 여유 약 100px. 사전 추정(840/872px)보다 실제 여유가 커서 56px를 되돌려도 callout 아랫변이 약 y≈866으로 출처 위에 머문다 → C1.
- **S17 스크립트·Workflow·Agent**: 부제 "같은 업무: 자료를 읽고 결과 만들기"와 카드가 약 9px로 붙는다. 같은 `cols-3`인 S07과 배치가 다르다. callout 아래 여유 약 115px → C1.
- **S20 실습 4 · Streamlit 화면 실행**: 대체 경로는 화면에 들어갔다. 실습 가이드 위치 표기만 다른 안내판 3장과 다르게 빠져 있다 → T2. 이미지 자리는 약 1,400×388px로 확보됐다. 교체 캡처는 보류 P4의 크롭 기준을 따른다.

참고(요청 아님, 취향 범위): S04 단계 2 설명과 S16 단계 1 설명의 마지막 낱말("계산", "전달")이 둘째 줄에 홀로 떨어진다. S21 `근거 데이터` 보조의 "DB"가 둘째 줄에 홀로 있다. 모두 읽는 데 지장이 없다.

---

## 수정 요청 요약

### 문구 (slide_content_writer) · 2건

| ID | 장면 · 요소 | 현재 상태(화면) | 바꿀 내용 | 이유(항목) |
|---|---|---|---|---|
| T1 | S06 · 개념도 좌측 영역 라벨(`.dg-zone-label`) | "수강생 Python 프로그램" → 2줄로 접히고 둘째 줄이 `실습 코드` 상자 테두리와 겹침 | "수강생 프로그램" (7자, 22px 굵게 약 175px로 1줄. 영역 안쪽 약 222px 이내). Python이라는 정보는 상자 보조 "SDK로 요청 작성"과 노트("SDK는 … Python 도구")에 남는다 | 글자 겹침 제거 (9) |
| T2 | S20 · `.scene-subtitle` | 11:30–11:38 · 8분 · 실행 3분 → 질문·결과 3분 → 코드 위치 2분 | 11:30–11:38 · 8분 · 실행 3분 → 질문·결과 3분 → 코드 위치 2분 · 실습 가이드 해당 장 (가이드 장 이름은 S08·S13·S19와 같이 최종 HTML 확인 후 교체). 빌드 후 1줄(약 1,250px 이하)인지 확인하고, 2줄이 되면 "실습 가이드 해당 장"만 남기고 "코드 위치 2분" 앞의 화살표 흐름은 노트로 옮긴다 | 실습 안내판 4장이 모두 가이드 위치를 화면에 두게 한다. 가장 늦어지기 쉬운 실습에서 수강생이 절차를 스스로 찾을 수 있게 한다 (5) |

### 배치·CSS (slide_ui_designer / 빌더) · 2건

| ID | 장면 | 현재 상태(화면) | 바꿀 내용 | 이유(항목) |
|---|---|---|---|---|
| C1 | S04, S10, S14, S16, S17 (장면 직속 `.stack`을 쓰는 5장) | 제목(또는 S17 부제)과 첫 카드 사이가 약 9–10px. 다른 장면은 약 65px. 원인은 `.stack > * { margin-top: 0; }`가 안쪽 `.steps`·`.compare-grid`의 margin-top 56px를 지우는 것 | `.scene > .stack { margin-top: 56px; }` 한 줄을 추가한다(G01·S06·S08·S13·S19처럼 그리드 안에 든 `.stack`에는 적용되지 않게 장면 직속만). 빌드 전에 5장 모두 `.stack`이 `section` 직속인지 확인한다. 적용 후 넘침 여유: 화면 기준 callout 아랫변이 S04 약 859, S10 약 745, S14 약 800, S16 약 866, S17 약 851로 모두 출처(약 y 915) 위에 머문다. overview.html의 복제 CSS에도 같은 규칙을 넣는다 | 같은 유형(compare: S07·S09·S15 대 S14·S17 / steps: S04·S10·S16)의 배치를 통일하고, 제목과 본문을 시각적으로 분리한다 (9) |
| C2 | G01 | 타임라인 세로선이 시각 글자(x≈148, x≈1020)를 관통하고 번호 원을 잇지 않음 | `.steps.timeline::before { left: 176px; }` (step-time 120 + 간격 28 + 원 반지름 28). 선이 원 중심(화면 x≈296, 1168)을 잇는지 스냅샷으로 확인한다. 맞추기 어렵다면 `.steps.timeline::before { display: none; }`로 선을 없앤다(시각·번호 원만으로 순서가 읽힌다). overview.html에도 같이 반영한다 | 하루 시간표의 시각 숫자가 선에 가려지지 않게 한다 (9) |

수정 후 확인 방법: `validate_topic.py`, `hyperframes lint`를 실행하고 S04·S06·S10·S14·S16·S17·S20·G01 스냅샷을 다시 찍어 위 좌표를 비교한다.

---

## 보류 (사용자 확인 필요)

| ID | 대상 | 확인할 내용 | 상태 |
|---|---|---|---|
| P1 | S08 → S09 | S08 3단계 "최신 자료 질문"을 B2 실습 기사와 같은 주제로 준비할 수 있는지. 어려우면 S09 노트 첫 문장을 "최신 자료를 물었을 때 근거가 붙어 있었나요?"로 조정 | 유효 |
| P2 | S17 노트 질문, S22 열 이름 | 기사 실습이 코드가 정한 순서로만 동작하는지, SQL 실습에서 모델이 도구 호출 여부를 고르는지. 강사가 S17 질문의 정답을 미리 알아야 하고, S22에서 오전 앱을 Agent라 부를지도 이 답으로 정한다(W11) | 유효 |
| P3 | S10 callout | 실습이 임베딩 검색을 쓰는지. 쓰지 않으면 화면의 "임베딩·Vector DB는 검색 방법 중 하나"를 "검색 방법은 여러 가지 · 모델을 다시 학습시키지 않음"으로 바꾼다. 빌드 화면에는 원문이 그대로 있다 | 유효 |
| P4 | S20 이미지 | Streamlit 캡처. 화면에서 이미지 자리는 약 1,400×388px로 확인됐다. 브라우저 창 전체가 아니라 입력 칸·처리 중 표시·결과와 근거 영역만 크롭한다. 교체 후 다시 검토한다 | 유효 |
| P5 | 수업 운영 | 문구 담당 보류 중 수업 진행을 막을 수 있는 B1-7(S08 준비 출력), B2-2(질문별 검색 단계), B2-3(저장 본문), B3-3(DB 권한·교육용 DB), B3-4(tool calling 지원), B3-8(오후 모델 경로)을 수업 전에 확인 | 유효 |
| N1 (새 항목) | 화면에 남은 자리표시 | 렌더링 화면에 자리표시 5종이 그대로 보인다: S01 "[강사명]" "[교육일]", S08·S13·S19 "실습 가이드 해당 장"(T2 반영 시 S20 포함), S20 "실제 화면 교체 예정" 점선 자리. PDF/PPTX export 전에 모두 실제 값으로 바꿔야 한다. 문구 보류 B1-1, B1-7, B2-1, B3-1, B3-7과 같은 항목이다 | export 전 필수 |
| N2 (새 항목) | G01 eyebrow | 화면에서 하루 시간표 장면 위에 "B1 LLM 원리와 첫 호출"이 붙어 있다. 하루 전체 안내 장면이 B1 소속으로 읽힌다. 매핑표대로 둘지, "오늘의 흐름" 같은 별도 라벨로 바꿀지(문구 보류 B1-9)는 사용자가 정한다. 교육적으로는 어느 쪽이든 진행에 지장이 없다 | 사용자 선택 |

---

## 반영 기록


작성: 오케스트레이터 · 2026-09-14

| 요청 | 결과 | 확인 |
|---|---|---|
| T1 S06 영역 라벨 | 반영: "수강생 Python 프로그램" → "수강생 프로그램" (index, slide_copy/B1, slide_ui/B1) | 스냅샷에서 1줄, 상자 테두리와 겹침 없음 |
| T2 S20 가이드 표기 | 반영: 부제 끝에 "· 실습 가이드 해당 장" (index, slide_copy/B3) | 스냅샷에서 부제 1줄 유지 |
| C1 스택 위 간격 | 반영: `.scene > .stack { margin-top: 56px; }` | S04·S10·S14·S16·S17 제목–첫 카드 간격이 다른 장면과 같아짐 |
| C2 G01 세로선 | 반영: `.steps.timeline::before { left: 175px; }` (시각 라벨 120 + 간격 28 + 번호 원 중심 28) | 선이 번호 원을 잇고 시각 숫자를 지나지 않음 |

반영 후 검증: sync_overview --renumber, validate 통과, lint 오류 0(경고 2 권고), Playwright 넘침 22장 0건, 스냅샷 22장 재촬영.
보류 P1–P5, N1(자리표시), N2(G01 eyebrow "B1")는 `_workspace/ppt_qa_report.md` 사용자 확인 목록으로 옮김.
