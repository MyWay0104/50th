# B4·B5 검토 (v0.4 pre-build)

작성: lecture_expert · 2026-09-16 · 대상: B4 S22–S27 + B5 S28–S33 (12장)
입력: `slide_copy/B4.md`·`B5.md` "# v0.4 보강 (2026-09-15)" 절과 v0.4 보류 목록, `slide_ui/B4.md`·`B5.md` "# v0.4 배치 (2026-09-15)" 절, `docs/slide-plan-v0.4-visual-narrative.md` 1·2·7-2·7-3·9(B4·B5)·11-2·11-4·12-1절, 조사정리 5절, `ppt_content_plan.md` 차수 목표, `slide_copy/B3.md` S21 v0.4, `slide_copy/B6.md` S34 v0.4, `lecture_review/B4-B7-pre.md`·`B4-B7-post.md`
표기: 요청 번호 `W4-nn` = slide_content_writer, `U4-nn` = slide_ui_designer. [필수]는 빌드 전에 반영할 것, [권장]은 반영하면 좋은 것. 9번 가독성은 배치 지시의 글자 크기·높이 계산으로 본 예비 판정이다.
다시 지적하지 않은 것(반영 중): S26 자리표시 조건 줄 27자, S25 설명 문단 45자 이내 1줄, S28 "산출물:" 표기 통일·캡션 삭제, S32 노트 "회색 카드" → "승인 요청 카드". 결정 사항(유지): 요청문 6칸 마지막 칸 "완료 조건", S30 제목 "프로젝트 지침과 작업 기록", 현업가이드 파일명·CLAUDE.md 발췌 미사용.

## 판정 한눈에 보기

| 항목 | B4 (S22–S27) | B5 (S28–S33) |
|---|---|---|
| 1 목표 정렬 | 통과 | 통과 |
| 2 시간 현실성 | 통과(주의, 보류 Q1) | 통과(주의: S31) |
| 3 인지 부하 | **수정 필요** (S24) | **수정 필요** (S29) |
| 4 오해 예방 | 통과 | **수정 필요** (S33) |
| 5 실습 전환 | **수정 필요** (S26) | **수정 필요** (S32) |
| 6 참여 질문 | 통과 | 통과 |
| 7 오전·오후 연결 | **수정 필요** (S22) | 통과 |
| 8 강사 사례 위치 | 통과 | 통과 |
| 9 가독성(예비) | 통과 · 권장 1건 (S25) | 통과 |
| a PDF만 읽어도 이해 | **수정 필요** (S22) | **수정 필요** (S29·S31) |
| b 기-승-전-결 | **수정 필요**, 경미 (S22 막 태그) | 통과 |
| c 새 시각 자료 | **수정 필요** (S22·S24·S25) | **수정 필요**, 경미 (S29 자리표시) |
| d 설명 문단·노트 정합 | **수정 필요** (S26) | **수정 필요** (S29·S31·S33) |
| e 사실 경계 16항목 | 통과(보류 P5) | 통과(보류 Q1·Q7) |

수정 요청: slide_content_writer **19건**(필수 12, 권장 7), slide_ui_designer **2건**(필수 1, 권장 1). 보류 **14건**.

공통 결론:

1. **v0.3 결정 두 개가 v0.4에서 문구를 줄이며 화면에서 빠졌다.** W11의 "오전 앱 = 이번 시간 작업 대상"(S22 부제)과 W21의 "실행 방법을 설명받고 대조"(S26 단계 3)다. 둘 다 노트에만 남았다. 글자 수를 늘리지 않고 설명 문단·관찰할 것에 되살린다(W4-02, W4-07).
2. **같은 대상을 장면마다 다른 말로 부른다.** 기능 범위(S24 화면·처리 / S25 "결과 표시 부분만" / S32 "화면·처리 부분"), 층 이름(S21 "모델·도구" / S24·S29 "처리"), 흐름 순서(S29 설명 "처리 → 조회" / 코드 "조회 → 처리"), 과제 조건(S32 "있으면"이 두 뜻). 초보 수강생은 이런 차이에서 멈춘다.
3. **노트 문장이 화면으로 올라오며 새로 생긴 오해가 있다.** S33 "예상과 다른 결과는 실패가 아니라"(회귀를 단서로 넘기는 읽기), S31 "버튼을 숨기는 것만으로 데이터 권한이…"(세 칸에 없는 넷째 메시지).
4. **S24 역할 띠(연결 설정·조립·도구·진입점)는 뺀다.** 3층 그림과 나누는 방식이 겹치고, 풀이 없는 새 용어이며, 실습과 이어지지 않는다.

사실 경계 16항목 중 이번 범위의 3·4·5·7·11·12·13·15번은 모두 막혀 있다. 11번만 사용자 확인(P5)이 남았다.

---

## B4 검토 · 13:20–14:00 · Claude Code와 개발 계획 (S22–S27)

차수 목표(콘티): 실행 환경을 확인하고 목표·범위·완료 조건을 작성한다.

| 항목 | 판정 | 근거 | 수정 요청 |
|---|---|---|---|
| 1 목표 정렬 | 통과 | 구간 표지가 없어 S22 노트 첫 문장이 목표를 말한다: "이번 40분의 목표는 Claude Code 실행 환경을 확인하고 오전 앱에서 고칠 기능 하나의 목표·범위·완료 조건을 적는 것입니다"(v0.3 B1 W1 방식 유지). 끝 S27 요지 "14:20의 준비물은 읽은 파일, 실행 방법, 완료 조건 한 줄이다"와 체크 카드 3개(파일 읽기 / 실행 방법 / 개선 목표 "고칠 기능과 완료 조건을 적었다")가 목표 두 부분(환경, 완료 조건)을 되묻는다. v0.4에서 요지가 생겨 마지막 장면의 목표 확인이 v0.3보다 화면에서 더 분명하다. | — |
| 2 시간 현실성 | 통과(주의, 보류 Q1) | 이론 S22–S25 각 4분 = 16분, S26 board-meta "13:36–13:56 · 20분" = 시간 블록 10 + 5 + 5, S27 4분. 합계 40분으로 콘티(16 / 20 / 4)와 같다. v0.4에서 노트가 늘었지만(S22 7문장, S23·S24·S25 6문장) 장당 4분 안이다. S25는 비교 설명 + 완료 조건 한 문장 쓰기 + "두 분 정도 읽어 보겠습니다"가 4분에 들어 있어 빠듯하다(v0.3과 같은 판단). S26 첫 10분은 사전 설치·로그인이 전제다(Q1). | — |
| 3 인지 부하 | 수정 필요 | 화면 새 용어: S22 Claude Code·Coding Agent(관찰·판단·행동은 일상어), S23 모델 경로·권한, S25 완료 조건, S26·S27 없음. 문제는 S24다. 한 장면에 나누는 방식이 두 개 있다: 3층 그림(화면 · 처리 · 데이터)과 그 아래 역할 띠 "코드를 역할별로 나눈 예: 연결 설정 · 조립 · 도구 · 진입점". "조립"·"진입점"은 비전공자에게 새 용어인데 화면·노트 어디에도 풀이가 없다. 세 층과 네 이름이 어떻게 맞물리는지(도구는 처리인가, 진입점은 화면인가)도 없다. 뒤 실습(S26·S32)에서 네 이름을 다시 쓰지 않고, 실습 코드에 이 구분이 있는지도 모른다(문구 보류 7). 원천이 실습 자료가 아닌 현업가이드의 파일 패턴이라는 점도 걸린다. 띠를 빼도 오른쪽 항목 "작은 앱은 함수 분리와 README로"와 노트가 같은 교훈을 전한다. | W4-04, U4-01 |
| 4 오해 예방 | 통과 | 3번: S22 오전 앱 열에 루프·Agent 호칭 없음, 요지 "모델에 도구를 붙인 점은 같고"(계획서 초안 "둘 다 모델+도구+반복"을 피함). 4번: S23 요지 "실행 위치와 모델이 처리하는 위치는 다르다", 설명 "요청과 읽은 파일 내용은 모델 경로로 전송된다". 5번: S23 회색 상자 "화면 조작·사내 시스템: 별도 도구·권한", 설명 "필요할 수 있다". 12번: S22 "항상 낫다는 뜻은 아니다", S24 회고에 수치 없음. 13번: S26 자리표시 조건 "모델명·계정·주소·키 가리기", S27 칩 "설치·접속"은 단계 이름만. 검증 과신: S26 완료 조건 "구조 설명을 실제 파일과 대조한다". 조사정리 5절의 13개 표현이 화면·노트에 남아 있지 않다. | — |
| 5 실습 전환 | 수정 필요 | S26 v0.4는 단계 목록을 빼고 시간 블록·관찰할 것·완료 조건만 남겼다. 그 결과 세 가지 문제가 생겼다. (1) v0.3 W21로 넣은 "실행 방법" 지시가 화면에서 사라졌다. 관찰할 것 "각 파일의 역할 설명을 받은 뒤…"와 완료 조건 "구조 설명을 실제 파일과 대조한다" 어디에도 실행 방법이 없는데, S27 체크 카드 2는 "실행 방법 · 앱을 실행하는 방법을 찾았다"를 묻는다(지시는 노트에만 있음). (2) v0.3 단계 1의 "사내 가이드대로"가 빠지고 막히면 문구가 "우회하지 말고 짝 실습·강사 시연으로"로 줄어, 무엇을 벗어나지 말라는 것인지가 화면에서 사라졌다(사실 경계 11번의 화면 근거). (3) 관찰할 것 둘째 문장이 "파일 읽기·Python 실행·화면 클릭 중 실제로 되는 것을 확인한다"로 화면 클릭 시도까지 지시한다. 노트는 "파일 읽기와 Python 실행"만 확인하게 한다(d와 겹침). 비전공자가 Claude Code에 다른 프로그램 클릭을 시키면 화면 조작용 패키지 설치를 제안받을 수 있어 우회 금지와 부딪친다. 대체 경로(짝·강사 시연)와 띠 "읽기·설명까지 · 수정은 14:20부터"는 좋다. | W4-07, W4-08, W4-09 |
| 6 참여 질문 | 통과 | S22 "공통점을 한 가지만 말해 주세요", S23 세 가지 예상, S24 "세 층 중 어디를 바꿔야 할까요? 한 분만", S25 완료 조건 한 문장 쓰기 + 두 분 읽기, S27 손들기 + "막힌 단계에 손들기" 칩 5칸. 5회. | — |
| 7 오전·오후 연결 | 수정 필요 | B3 끝은 충족한다: S21 v0.4 요지 "오전에 연결한 세 층 중 한 곳을 오후에 AI에게 고치게 한다", 오른쪽 상자 "13:20 · 이 앱이 오후 작업 대상". B4 처음이 v0.4에서 약해졌다. v0.3 S22 수정 기록(W11)은 부제 "오전 앱이 이번 시간 Claude Code의 작업 대상"을 "PDF만 봐도 오전·오후 연결이 보이게" 넣은 것이다. v0.4는 이 부제를 빼면서 "막 태그와 노트 첫 문장이 대신한다"고 적었다. 그러나 막 태그 "오전엔 내가 도구를 붙였고, 오후엔 도구에게 일을 맡긴다"는 무엇을 맡기는지 말하지 않는다. 화면의 대상 표시도 24px 캡션 "대상: 프로젝트 폴더"뿐이라 어느 폴더인지 알 수 없다. 노트 첫 문장만으로는 PDF 독자에게 연결이 보이지 않는다. 설명 문단 둘째 문장을 바꿔 v0.3 결정을 되살린다(글자 수·높이 변화 없음). W22(S25 노트 "점심 전에 골라 둔 곳")는 유지돼 있다. | W4-02 |
| 8 강사 사례 위치 | 통과 | S24 설명 첫 문장 "강사는 개발 후반에 작은 수정 하나가 여러 곳에 번져 고치기 어려웠던 경험이 있다"가 요지("경계와 입출력을 먼저 정하면…") 바로 뒤에서 개념을 뒷받침한다. 3인칭, 실패 경험, 수치·프로젝트 성과 없음, 출처 줄 "강사의 아키텍처 회고(사용자 제공)". 자랑으로 읽히지 않는다. | — |
| 9 가독성(예비) | 통과 · 권장 1건 | 제목 72, 요지 40, 설명 32, 막 태그 28px. 여유(UI 계산): S22 37, S23 28(설명 3줄 가정, 2줄이면 74), S24 101, S25 60(설명 1줄 반영 시), S26 격자 칸 안 30/44, S27 162px. 정지 화면 한 장에 모두 보인다. 걸리는 곳은 하나다. S25 여섯 칸의 예시(이 장면의 핵심 내용)가 `.dg-sub` 24px다. v0.3에서는 같은 예시가 32px 목록이었고, 계획서 7-3은 "그림 안 글자 32px 이상"이다. 여유 60px 안에서 28px로 올릴 수 있다. | U4-02 |
| a PDF만 읽어도 이해 | 수정 필요 | 설명 문단은 모두 3인칭이다(S24 "강사는"). 새 용어는 설명 안에서 풀린다(S22 "모델에 파일·명령 도구를 붙여 … 반복하는 Coding Agent"). 한 장면 한 메시지는 S24(3번)를 빼면 지켜진다. 고칠 곳: S22 설명 "…오후 실습에 쓰며, 항상 낫다는 뜻은 아니다"는 무엇보다 낫다는 것인지가 빠져 PDF만 읽으면 뜻이 흐리다(노트에는 "다른 도구보다"가 있다). W4-02에 합쳤다. 참고: S23은 같은 경로를 "승인된"(영역 라벨)·"설정된"(항목 2)·"허용된"(설명)으로 부른다. 뜻이 어긋나지는 않아 요청하지 않았다. | W4-02 |
| b 기-승-전-결 | 수정 필요(경미) | S22 막 태그 "전 · 뒤집다"와 역할 반전 문구는 계획서 2절·9절 지시대로 있다(11-4 "S22에 역할 반전 문구" 충족). S21(승)의 "이 앱이 오후 작업 대상"에서 S22로 들어오고, S27 띠 "14:20 적어 둔 기능을 계획하고, 고치고, 확인합니다"가 S28 막 태그로 넘긴다. 걸리는 점: 같은 화면에서 "도구"가 두 뜻이다. 막 태그 앞 절의 "도구"는 검색·SQL 같은 부품이고, 뒤 절 "도구에게 일을 맡긴다"의 "도구"는 Claude Code다. 바로 아래 요지 "모델에 도구를 붙인 점은 같고"는 다시 부품 뜻이다. 처음 온 수강생은 "파일·명령 도구에게 맡긴다"로 읽을 수 있다. 노트 둘째 문장("이미 도구가 붙은 Claude Code에게 일을 맡깁니다")처럼 받는 쪽 이름을 쓴다. | W4-01 |
| c 새 시각 자료 | 수정 필요 | **S22 루프**: Claude Code 열에만 있고(P2 전에는 오전 앱을 Agent로 그리지 않음), 배지 "개념 재구성"을 달았으며, 강조는 열 하나라 우열·과장 오해 위험은 낮다. 다만 가운데 상자 보조 "모델"은 관찰(파일·결과)·행동(도구 실행)과 달리 주체인지 대상인지 불분명하고, S17의 Agent 기준 "모델이 다음 행동 선택"과 이을 기회를 놓친다(W4-03). 되돌림 화살표가 없어 세 상자가 "정해진 순서"(S17의 Workflow)처럼 보일 수 있지만, 설명 "반복하는"과 캡션 "결과를 보고 다시 반복"이 막는다(빌드 후 캡션 확인). **S23 두 영역**: "모델" 상자가 "내 PC" 영역 밖에 있어 사실 경계 4번이 그림에 남는다. CSS B4-F가 없어도 뜻은 같다. **S24**: 띠 문제는 3번에서 봤다. 층 이름도 걸린다. 오전 S21 v0.4의 가운데 층은 "모델·도구"인데 S24·S29는 "처리"다. S29 주석 "# 처리: 규칙·모델"이 뒤에서 풀지만, S24 화면만 보면 같은 그림의 이름이 왜 바뀌었는지 알 수 없다(W4-05). **S25 6칸**: 틀은 좋다(성과 3 "입력·출력·제약·완료 조건"과 맞음). 그러나 범위 칸 "결과 표시 부분만 수정"이 바로 앞 S24 그림(화면·처리 두 곳을 파랑 영역으로 묶고 데이터는 "형태만 확인")과 S32 코드 위치 "화면·처리 부분"과 어긋난다. 30분 사이 세 장면이 같은 기능의 범위를 다르게 말한다. v0.3에도 있던 불일치지만 v0.4의 S24 강조 영역과 S32 코드 위치 줄 때문에 더 잘 보이게 됐다(W4-06). **S27 체크 카드**: 화살표·강조가 없어 점검 장면에 맞다. | W4-03, W4-05, W4-06 |
| d 설명 문단·노트 정합 | 수정 필요 | S26 관찰할 것(화면)은 "파일 읽기·Python 실행·화면 클릭"을 모두 확인하게 하고, 노트 마지막 문장은 "파일 읽기와 Python 실행"만 확인하게 한다(5번에서 판정). S24는 W4-04를 반영하면 노트 다섯째 문장 "그림 아래 네 이름은…"을 함께 고친다. 나머지(S22·S23·S25·S27)는 노트가 설명 문단을 풀어 말하는 관계이고 모순이 없다. S27 설명 1줄과 노트 넷째 문장(짝 진행)이 겹치지만 안내 문장이라 양쪽에 두는 편이 맞다. | W4-07, W4-09 |
| e 사실 경계 16항목 | 통과(보류 P5) | 3·4·5·12·13번은 4번 행 근거. 15번: S22 배지 "개념 재구성" + 출처 "교안 내용 재구성". 7번은 B4 대상이 아니다. 11번: 화면에는 단정 문장이 없고 S23 노트 "오후 Claude Code의 모델 경로는 오전 실습의 사내 추론 서버와 따로 준비·확인된 것입니다"만 있다(v0.3 결정 유지). S21 v0.4 화면은 같은 내용을 "별개라 따로 확인한다"로 낮췄다. P5가 강의 전까지 확인되지 않으면 강사는 S23에서도 그 표현으로 말한다. | — |

---

## B5 검토 · 14:20–15:00 · Claude Code로 앱 개선 (S28–S33)

차수 목표(콘티): 기존 코드를 읽고 작은 기능 하나를 개선·검증한다.

| 항목 | 판정 | 근거 | 수정 요청 |
|---|---|---|---|
| 1 목표 정렬 | 통과 | S28 노트 첫 문장 "이번 40분의 목표는 오전 앱의 기존 코드를 읽고 작은 기능 하나를 고친 뒤, 제대로 됐는지 확인하는 것입니다"와 요지 "바로 고치게 하지 말고, 읽고 계획하게 한 뒤 범위 안에서만 고친다". 끝 S33 3칸(예측 → 실행 "기존은 그대로? 새 입력은 안내?" → 설명 "바꾼 코드를 한 문장으로")이 개선·검증과 학습 순환의 "설명하기"를 되묻는다. S32 완료 조건 "기존 질문은 그대로, 새 기능은 동작"도 같은 목표다. | — |
| 2 시간 현실성 | 통과(주의: S31) | S28 4 + S29 4 + S30 5 + S31 3 = 16분, S32 "14:36–14:57 · 21분" = 4 + 4 + 8 + 5, S33 3분. 합계 40분으로 콘티(16 / 21 / 3)와 같다. 주의: S31은 3분인데 v0.4 노트가 6문장(자리표시 두 곳 설명, Git 조건문 포함)으로 늘었다. v0.3은 같은 3분에 맞춰 노트를 4문장으로 줄였었다. 질문에 답하는 시간까지 넣으면 넘칠 수 있어 W4-14(버튼 문장은 노트에만)·W4-15(자리표시 문장 줄임)로 줄인다. S33 3분 초과는 띠 "미확인"으로 처리된다. | W4-15 |
| 3 인지 부하 | 수정 필요 | 화면 새 용어: S28 없음(탐색·계획·구현·검증은 일상어), S30 CLAUDE.md·컨텍스트(띠 안에서 "현재 대화 + 읽은 파일 + 도구 출력"으로 바로 풀림), S31 "바뀐 줄 비교"(Git diff를 화면에서 빼 v0.3의 Q3 조건부 문제가 없어짐), S32·S33 없음. 문제는 S29 설명 둘째 문장 "React·별도 백엔드를 다 배우기 전에"다. 풀스택(설명 안에서 풀림)에 더해 React·백엔드 두 용어가 풀이 없이 나오고 뒤 실습과 이어지지 않는다("등장 직후 실습이나 예시로 연결"을 채우지 못함). 이 문장이 전하려는 것("다 배우기 전에 층부터")은 일반어로도 전할 수 있다. 노트의 "React나 별도 백엔드"는 경험자용 설명으로 그대로 둔다. | W4-11 |
| 4 오해 예방 | 수정 필요 | 7번: S30 설명 "CLAUDE.md는 모델이 읽는 지침일 뿐 권한을 막는 보안 장치가 아니다". "긴 대화 = 영구 기억": 요지 "긴 대화는 영구 기억이 아니다 — …파일에", 띠 "한도가 있고 길면 요약된다". 검증 과신: S32 관찰할 것 "완료했다는 말이 아니라 바뀐 줄과 실행 결과로 판단한다", S31 노트 "사람이 마지막으로 봐야 합니다". Top-down 오해: S33 노트 "먼저 예측하고 입력을 바꿔 보는 과정에서 필요한 기초가 채워집니다". 5번: S32 승인 카드. **새로 생긴 문제 1건:** S33 설명 "예상과 다른 결과는 실패가 아니라 … 단서다". v0.3에서는 노트("실패라기보다")에만 있던 말이 v0.4에서 화면 설명 문단으로 올라왔다. 이 장면의 입력에는 기존 정상 질문이 들어 있고, 기존 질문 결과가 달라진 것은 S32 완료 조건("기존 질문은 그대로")을 못 지킨 실패다. "실패가 아니라"를 그대로 두면 회귀를 "단서"로 넘기는 읽기가 가능해져 성과 4(검증)와 부딪친다. 뜻한 바는 "예측이 빗나가도 멈추지 말고 원인을 찾으라"다. 띠는 "미확인"만 다루고 "확인했더니 실패"를 적을 자리가 없다. | W4-18, W4-19 |
| 5 실습 전환 | 수정 필요 | S31 마지막 질문 "여러분이 고른 기능은 화면에서 무엇을 입력하고 무엇이 보이면 확인이 끝날까요?"에서 S32, S33으로 자연스럽게 이어진다. W24 짝 경로도 막히면 상자에 유지됐다. 단계 목록 3줄을 뺀 것은 받아들일 수 있다. 시간 블록 "탐색·계획·구현·검증"이 15분 전 S28 네 카드와 같은 이름·순서로 가로 배치되고, 단계별 할 일은 S28 카드와 노트 1–3문장에 있다. 문제는 기본 과제 줄 축소다. "기본 과제: 결과 없음 안내 · 있으면 근거 표시"는 "결과가 없으면 안내하고, 있으면 근거를 표시한다"는 한 기능으로 읽힌다. v0.3의 "이미 있으면"(기본 과제가 이미 구현돼 있으면 대체 과제로)이라는 뜻이 사라졌다. 21분 동안 띄워 두는 과제 이름이 두 뜻으로 읽히면 짝마다 다른 과제를 하게 된다. | W4-16 |
| 6 참여 질문 | 통과 | S28 "고치기 전에 정상 동작을 먼저 확인해 두어야 하는 이유", S29 손가락 1·2·3, S30 "네 줄 중 꼭 남겨야 할 내용 두 가지", S31 확인이 끝나는 기준, S33 실행 전 예측 + 두 분 설명. 5회 이상. | — |
| 7 차수 간 연결 | 통과 | B4→B5: S27 띠 "14:20 적어 둔 기능을 계획하고, 고치고, 확인합니다"와 S28 막 태그 "계획하고, 고치고, 확인한다"가 같은 말이다. S28 강조 카드 "계획 · 바꿀 곳·완료 조건"은 S25 완료 조건 칸으로, S29 질문 "앞 시간에 적은 개선 기능은 화면, 처리, 데이터 중 어디"는 B4 과제로 이어진다. B5→B6: S33 노트 끝 "15시 20분에는 이런 작업 경험과 자료를 다시 쓸 수 있게 정리하는 방법으로 넘어갑니다"를 S34 막 태그 "결 · 남기다 · 이 경험을 어떻게 내 것으로 쌓나?"가 받는다. S30(작업 기록 파일)이 B6의 기록 주제를 미리 연다. | — |
| 8 강사 사례 위치 | 통과 | Dokmo는 S31 요지 "바뀐 줄, 실행 결과, 실제 화면은 따로 확인한다" 바로 아래 설명 첫 문장에서 강조 칸 "목적에 맞게 쓰나"를 뒷받침한다. 실패 기록이고 수치가 없으며 조사정리 4-C 범위 안이다. 자랑으로 읽히지 않는다. 문구가 Dokmo를 "개인 기록"으로 불러 앱인지 기록인지 흐린 점은 a에서 다룬다. | — |
| 9 가독성(예비) | 통과 | 여유(UI 계산): S28 76(캡션 삭제 반영), S29 57(설명 2줄이면 103), S30 29, S31 34, S32 격자 칸 안 35/109, S33 125px. S30은 설명이 3줄로 늘면 넘치므로 이번 요청에서 S30 문구는 건드리지 않았다. S31은 캡처가 들어오면 여유가 모자라 설명을 1줄로 줄여야 한다고 UI가 적었는데, W4-14가 이 문제를 미리 푼다. S29 코드 28px, 시간 블록 숫자 72px라 멀리서 읽힌다. | — |
| a PDF만 읽어도 이해 | 수정 필요 | 3인칭은 지켜졌다. 고칠 곳은 세 가지다. (1) S31 설명 "개인 기록인 Dokmo에서는"은 Dokmo가 강사가 만든 웹앱이라는 것을 말하지 않고 기록 이름처럼 읽힌다(노트는 "제가 만든 개인 웹앱 Dokmo"). 둘째 문장 "버튼을 숨기는 것만으로 데이터 권한이 보장되지도 않는다"는 이 장면의 세 칸(바뀐 줄·실행 결과·실제 화면)에 없는 넷째 확인이라 한 장면 한 메시지를 깬다. "데이터 권한"이 풀이 없이 나오고 S32와도 이어지지 않는다. 노트 다섯째 문장에 같은 말이 있으니 화면에서만 뺀다. (2) S29 설명의 흐름 순서가 옆 코드와 다르다(d). (3) S29 React·백엔드(3번). | W4-11, W4-14 |
| b 기-승-전-결 | 통과 | S28 막 태그 "전 · 뒤집다 (2) · 계획하고, 고치고, 확인한다"는 B3 "승 · 연결하다 (2)"와 같은 형식이고 계획서 9절 B5 부제와 같다. 오전(승)과의 연결은 막 태그가 아니라 S27 띠와 S28 노트 "오전 앱의 기존 코드"가 맡는다. S27 띠와 막 태그가 같은 말이라 화면에서도 이어진다. B6(결)으로는 S33 노트 → S34 막 태그 "이 경험을"로 넘긴다. | — |
| c 새 시각 자료 | 수정 필요(경미) | S28 네 카드 + 산출물 줄: 가로 흐름이고 설명 "검증에서 문제가 보이면 탐색으로 돌아간다"가 되돌림을 말한다. S30 코드 카드 두 개: 강조·우열이 없고(v0.3 원칙 유지), 파일 모양이 요지 "파일에"를 보여 준다. S31 세 칸: 화살표가 없고(순서가 아닌 서로 다른 대상), 강조는 "목적에 맞게 쓰나" 하나다(v0.3과 같은 대상). S32 승인 카드: 제목에 "개념 예시(실제 화면과 다름)"가 있고 실제 UI를 복제하지 않았다. S33 3칸: 강조 "설명" 하나로, B5에서 새로 요구하는 행동과 맞다. 걸리는 곳은 하나다. S29 자리표시 P-S29 "넣을 자료: 오전 Streamlit 앱 화면 — 입력 칸·결과 영역에 화면·처리·데이터 표시". 앱 화면에 보이는 것은 화면 층뿐인데, 캡처 위에 처리·데이터를 표시하게 하면 "화면 영역 = 층"이라는 틀린 대응이 생긴다. 노트 셋째 문장("입력 칸과 결과 영역이 왼쪽 코드의 어느 줄에 해당하는지 짝지어 보면")이 뜻한 것은 코드 줄과의 짝짓기다. | W4-13 |
| d 설명 문단·노트 정합 | 수정 필요 | (1) S29: 설명 "입력을 받고, 처리하고, 데이터를 조회해 화면에 다시 보여 주는"과 노트 첫 문장 "입력을 받고, 규칙과 모델 호출로 처리하고, 데이터를 조회해"는 처리 → 조회 순서다. 왼쪽 코드는 `search(q)` "# 데이터: 조회" → `answer(q, rows)` "# 처리: 규칙·모델" 순서이고, 노트 둘째 문장도 "입력 칸, 조회, 모델 호출, 결과 표시가 차례로"다. 코드를 한 줄씩 읽고 설명하는 장면에서 문장과 코드의 순서가 다르면 초보 수강생은 둘 중 무엇이 맞는지 멈춘다. (2) S31: 설명 "개인 기록인 Dokmo" ↔ 노트 "제가 만든 개인 웹앱 Dokmo". (3) S33: 설명과 노트가 같은 "실패가 아니라 / 실패라기보다"를 되풀이한다(4번). 나머지(S28·S30·S32)는 노트가 그림을 가리키며 풀어 말하는 관계이고 모순이 없다. S32 노트 "회색 카드"는 반영 중이다. | W4-11, W4-12, W4-14, W4-19 |
| e 사실 경계 16항목 | 통과(보류 Q1·Q7) | 3·5번: S32 관찰할 것 "승인을 요청받으면" 조건문과 승인 카드. 7번: S30 설명. 11번: S29 노트(W23)의 오전 모델 경로는 B1 S06과 같고, 실제 구조는 Q7에 달려 있다. 12번: S31 Dokmo에 수치 없음. 13번: S29 가상 함수 이름과 자리표시 조건 "모델명·주소·키 가리기", S30 "## 실행 명령"은 소제목만, S32 승인 카드 대상은 역할 이름. 현업가이드 파일명·발췌 없음. | — |

---

## 수정 요청

| 번호 | 장면 | 요소 | 현재 문구/배치 | 요청 | 담당 | 이유 |
|---|---|---|---|---|---|---|
| W4-01 | S22 | 막 태그 본문 | "오전엔 내가 도구를 붙였고, 오후엔 도구에게 일을 맡긴다" | "오전엔 내가 모델에 도구를 붙였고, 오후엔 Coding Agent에게 앱 수정을 맡긴다" (약 1,170px < 1,680px → 1줄, 49px 유지) | slide_content_writer | [권장] b: 한 화면에서 "도구"가 부품과 Claude Code 두 뜻으로 쓰인다. 계획서 2절 막 질문 "내가 만든 앱을 AI에게 고치게 하면?"과도 맞는다 |
| W4-02 | S22 | 설명 문단 | "Claude Code는 모델에 파일·명령 도구를 붙여 관찰→판단→행동을 반복하는 Coding Agent다. 읽고 고치고 확인하는 흐름이 이어져 오후 실습에 쓰며, 항상 낫다는 뜻은 아니다." | "Claude Code는 모델에 파일·명령 도구를 붙여 관찰→판단→행동을 반복하는 Coding Agent다. 오후에는 이 흐름으로 오전 앱을 고친다. 다른 도구보다 항상 낫다는 뜻은 아니다." (105자로 현재와 같음 → 2줄 유지) | slide_content_writer | [필수] 7·a: v0.3 W11 결정(화면에서 보이는 "오전 앱 = 작업 대상")을 되살린다. "항상 낫다"에 비교 대상을 적는다 |
| W4-03 | S22 | 루프 가운데 상자 보조 | "판단" · 보조 "모델" | 보조를 "모델이 선택"으로 (5자, 약 120px < 상자 안쪽 142px → 1줄) | slide_content_writer | [권장] c·사실 경계 3번: S17의 Agent 기준 "모델이 다음 행동 선택"과 같은 말이 되고, 노트 넷째 문장 "모델이 다음 행동을 판단하고"와 맞는다 |
| W4-04 | S24 | 역할 띠 + 노트 다섯째 문장 | 띠: 소제목 "코드를 역할별로 나눈 예" + 칩 "연결 설정 · 조립 · 도구 · 진입점" / 노트: "그림 아래 네 이름은 코드를 역할별로 나눈 한 예일 뿐이어서 실습 코드에서는 이름과 파일 구성이 다를 수 있고, 작은 앱이라면 역할별 함수 몇 개와 짧은 README로 충분합니다." | 띠를 삭제한다. 노트 다섯째 문장은 "작은 앱이라면 파일을 많이 나눌 필요 없이 역할별 함수 몇 개와 짧은 README로 충분합니다."로 바꾼다 (노트 6문장 유지). 이 요청을 받으면 문구 보류 7(실습 코드에 네 역할 구분이 있는지)은 필요 없어진다 | slide_content_writer | [필수] 3·a·c: 한 장면에 나누는 방식이 두 개다. "조립"·"진입점"은 풀이 없는 새 용어이고, 실습과 이어지지 않으며, 원천이 실습 자료가 아니다 |
| U4-01 | S24 | 좌 `.stack` | 3층 줄 → 역할 띠 `.dg-row`(panel-title + chip-row) → 캡션 | 역할 띠 `.dg-row`를 삭제한다. 좌 = 3층 줄 207 + 24 + 캡션 34 = 265px, 여유 101 → 약 182px. 빈 높이는 채우지 않는다(요지와 같은 항목을 되살리지 않음) | slide_ui_designer | [필수] W4-04와 짝 |
| W4-05 | S24 | 캡션 | "개념 예시 · 이름·파일 구성은 실습 코드와 다를 수 있음" | "개념 예시 · 처리 = 업무 규칙과 모델 연결 · 이름은 실습 코드와 다를 수 있음" (약 780px < 913px → 1줄). 이 요청을 받지 않더라도 W4-04를 받으면 "·파일 구성"은 뺀다 | slide_content_writer | [권장] c: 오전 S21 가운데 층 "모델·도구"가 S24·S29에서 "처리"로 바뀐 것을 화면에서 잇는다(S29 주석 "# 처리: 규칙·모델"과 같은 뜻) |
| W4-06 | S25 | 범위 칸 보조 | "결과 표시 부분만 수정" | "화면·처리만 수정, 데이터 그대로" (15자, 약 350px < 497px → 1줄). 노트는 그대로 둔다. 보류 Q2로 과제가 "근거 표시"로 바뀌면 같은 원칙으로 다시 쓴다 | slide_content_writer | [필수] c: S24 강조 영역(화면·처리)과 S32 코드 위치("화면·처리 부분")에 범위를 맞춘다. 요청문의 범위가 앞 그림과 다르면 "범위를 적는다"는 이 장면의 교훈이 흔들린다 |
| W4-07 | S26 | 관찰할 것 | "각 파일의 역할 설명을 받은 뒤 실제 파일을 열어 대조한다. 앞서 예상한 파일 읽기·Python 실행·화면 클릭 중 실제로 되는 것을 확인한다." (80자) | "각 파일의 역할과 앱 실행 방법을 설명받은 뒤 실제 파일을 열어 대조한다. 앞서 예상한 것 중 파일 읽기와 Python 실행이 되는지 확인한다." (80자, 2줄 유지) | slide_content_writer | [필수] 5·d: W21(실행 방법 지시)을 화면에 되살려 S27 카드 2의 전제를 만든다. 화면 클릭 시도 지시를 빼 노트와 맞춘다 |
| W4-08 | S26 | 막히면 | "우회하지 말고 짝 실습·강사 시연으로" | "사내 가이드 밖 방법 말고 짝·강사 시연으로" (약 640px < 안쪽 793px → 1줄, 좌 칸 높이 변화 없음) | slide_content_writer | [필수] 5·e(11·13번): "사내 가이드대로"가 빠지면서 사라진 기준을 되살린다. 문구 담당 보류 11에 대한 답이다 |
| W4-09 | S26 | 노트 마지막 문장 | "아까 예상한 것 중 파일 읽기와 Python 실행이 실제로 되는지도 이때 확인해 보세요." | "아까 예상한 것 중 파일 읽기와 Python 실행이 실제로 되는지 이때 확인하고, 다른 프로그램 화면 클릭은 별도 도구가 연결돼 있지 않으면 되지 않으므로 직접 시켜 보거나 도구를 새로 설치하지 않습니다." (노트 7문장 유지) | slide_content_writer | [필수] 5·d: S23 노트 "예상이 맞는지는 잠시 뒤 실습 5에서 직접 확인합니다"의 셋째 항목(화면 클릭)을 닫고, 설치로 우회하지 않게 한다. 정답이 되는 조건은 Q1이다 |
| W4-10 | S27 | 막힌 단계 칩 5 | "실행법" | "실행 방법" (칩 약 158px, 띠 합 약 971px < 1,680px → 1줄) | slide_content_writer | [권장] a: 바로 위 카드 제목 "실행 방법"과 같은 이름으로 맞춘다 |
| W4-11 | S29 | 설명 문단 | "입력을 받고, 처리하고, 데이터를 조회해 화면에 다시 보여 주는 전체 흐름을 흔히 풀스택이라 부른다. React·별도 백엔드를 다 배우기 전에, 바꿀 부분이 어느 층인지부터 알아본다." | "입력을 받고, 데이터를 조회해 처리한 뒤 화면에 다시 보여 주는 전체 흐름을 흔히 풀스택이라 부른다. 화면·서버 기술을 다 배우기 전에, 바꿀 부분이 어느 층인지부터 알아본다." (약 101자, 2줄 예상 · UI의 3줄 보수 계산 안) | slide_content_writer | [필수] d·3: 옆 코드 순서(search → answer → write)에 문장 순서를 맞춘다. React·백엔드는 풀이 없이 나오고 실습과 이어지지 않는다(노트에는 유지) |
| W4-12 | S29 | 노트 첫 문장 | "화면에서 입력을 받고, 규칙과 모델 호출로 처리하고, 데이터를 조회해 다시 화면에 보여 주는 전체 흐름을 다루는 것을 흔히 풀스택이라고 부릅니다." | "화면에서 입력을 받고, 데이터를 조회하고, 규칙과 모델 호출로 처리해 다시 화면에 보여 주는 전체 흐름을 다루는 것을 흔히 풀스택이라고 부릅니다." | slide_content_writer | [필수] d: 노트 둘째 문장("입력 칸, 조회, 모델 호출, 결과 표시가 차례로")·코드와 같은 순서로 맞춘다 |
| W4-13 | S29 | 자리표시 "넣을 자료" 줄 | "넣을 자료: 오전 Streamlit 앱 화면 — 입력 칸·결과 영역에 화면·처리·데이터 표시" | "넣을 자료: 오전 Streamlit 앱 화면 — 입력 칸·결과 영역을 왼쪽 코드 줄과 짝지어 표시" (약 1,090px < 2 × 619px → 2줄 유지) | slide_content_writer | [권장] c: 앱 화면에 보이는 것은 화면 층뿐이다. 캡처 위에 처리·데이터를 표시하면 "화면 영역 = 층"이라는 틀린 대응이 생긴다. 노트 셋째 문장과 맞춘다 |
| W4-14 | S31 | 설명 문단 | "개인 기록인 Dokmo에서는 앱이 동작해도, 직접 써 본 점검에서 한글 이름 입력과 안내 부족이 드러났다. 버튼을 숨기는 것만으로 데이터 권한이 보장되지도 않는다." (91자, 2줄) | "강사의 웹앱 Dokmo는 동작했지만, 직접 써 본 점검에서 한글 이름 입력과 안내 부족이 드러났다." (약 50자, 약 1,350px → 1줄). 둘째 문장은 노트 다섯째 문장에 이미 있으므로 화면에서만 뺀다. 2줄을 유지해야 한다면 둘째 문장으로 "세 칸은 서로를 대신하지 않는다."를 쓴다 | slide_content_writer | [필수] a·d: Dokmo가 강사의 웹앱임을 화면에 밝히고(노트 "제가 만든 개인 웹앱 Dokmo"와 맞춤), 세 칸에 없는 넷째 확인("데이터 권한", 새 용어·실습과 무관)을 빼 한 장면 한 메시지로 만든다. 1줄이 되면 여유가 34 → 약 81px로 늘어 "캡처가 들어오면 설명 1줄 필요" 문제를 미리 푼다 |
| W4-15 | S31 | 노트 셋째 문장 | "왼쪽 아래 자리는 작은 기능 하나를 바꿨을 때 바뀐 줄이 어떻게 보이는지 보여 줄 화면이고, 실습 폴더가 Git 저장소라면 Git diff로 같은 내용을 볼 수 있습니다." | "실습 폴더가 Git 저장소라면 왼쪽 아래 같은 바뀐 줄 비교를 Git diff로 볼 수 있습니다." | slide_content_writer | [권장] 2: 3분 장면에 노트가 6문장이다. v0.3은 같은 3분에 4문장으로 맞췄다. 질문에 답하는 시간을 확보한다 |
| W4-16 | S32 | 소제목(`.panel-title`) | "기본 과제: 결과 없음 안내 · 있으면 근거 표시" | "기본 과제: 결과 없음 안내 · 이미 있으면 근거 표시" (29자, 약 610px < 851px → 1줄) | slide_content_writer | [필수] 5·a: 지금 문구는 "결과가 없으면 안내, 있으면 근거 표시"라는 한 기능으로 읽힌다. v0.3의 "이미"가 대체 과제의 조건이었다 |
| W4-17 | S32 | 노트 다섯째 문장 끝 | "…무엇을 하려는지 읽고 나서 승인하세요." | "…무엇을 하려는지 읽고 나서 승인하고, 무엇을 하려는지 모르겠으면 아니오를 고른 뒤 손을 들어 주세요." (반영 중인 "승인 요청 카드" 정정과 같은 문장이라 함께 반영) | slide_content_writer | [권장] 5·사실 경계 5번: 이해하지 못한 채 승인하는 것이 비전공자에게 가장 흔한 승인 실패다. 화면 카드의 "예 / 아니오" 중 아니오를 쓰는 경우를 알려 준다 |
| W4-18 | S33 | 설명 문단 둘째 문장 | "예상과 다른 결과는 실패가 아니라 바꾼 코드의 동작 조건을 알려 주는 단서다." | "예상과 다른 결과는 틀렸다고 끝내지 말고, 바꾼 코드가 그렇게 동작한 조건을 찾는 단서로 쓴다." (설명 약 92자, 약 2,470px → 2줄 유지, 여유 125px). 요지 "다르면 그것이 단서다"는 그대로 둔다 | slide_content_writer | [필수] 4·a: 이 장면의 입력에는 기존 정상 질문이 들어 있다. 기존 결과가 달라진 것은 S32 완료 조건을 못 지킨 실패다. "실패가 아니라"가 화면에 오르면 회귀를 단서로 넘기는 읽기가 된다 |
| W4-19 | S33 | 노트 셋째·다섯째 문장 | 셋째: "결과가 예상과 다르면 실패라기보다 바꾼 코드가 어떤 조건에서 동작하는지 알려 주는 단서이고, …" / 다섯째: "시간 안에 확인하지 못한 입력은 아래처럼 미확인으로 적어 두면 …" | 셋째: "결과가 예상과 다르면 틀렸다고 끝내지 말고 바꾼 코드가 어떤 조건에서 그렇게 동작하는지 찾는 단서로 쓰고, …"(뒤 "이렇게 먼저 예측하고 … 기초가 채워집니다"는 유지) / 다섯째: "시간 안에 확인하지 못한 입력은 아래처럼 미확인으로, 기존 질문 결과가 달라진 것은 고칠 일로 적어 두면 …" (노트 5문장 유지) | slide_content_writer | [필수] 4·d: 설명(W4-18)과 맞추고, 화면 띠가 다루지 않는 "확인했더니 실패"를 적을 자리를 말로 준다 |
| U4-02 | S25 | 요청문 6칸 `.dg-sub` | 24px(캡션층) | 이 틀에서만 28px로 올리는 변형 클래스(예: `.dg.is-form .dg-sub { font-size: 28px; }`). 최장 보조 "없는 키워드 → 안내, 기존 질문 그대로" 약 413px < 497px → 1줄. 두 줄 합 약 +10px, 여유 60 → 약 50px(설명 1줄 기준). S40(B6 공유 틀)에 같이 쓸지는 B6 배치 여유를 본 뒤 정한다 | slide_ui_designer | [권장] 9: 여섯 칸 예시가 이 장면의 핵심 내용인데 v0.3의 32px 목록에서 24px로 작아졌다. 계획서 7-3 "그림 안 글자 32px 이상"에 가깝게 한다 |

담당별 합계: slide_content_writer 19건(필수 W4-02·04·06·07·08·09·11·12·14·16·18·19, 권장 W4-01·03·05·10·13·15·17) / slide_ui_designer 2건(필수 U4-01, 권장 U4-02). 모든 문구 요청은 현재 배치의 줄 수 안에 들어가도록 길이를 맞췄다(늘어나는 곳 없음, S24·S31은 여유가 늘어남).

---

## 반영 중인 요청에 대한 교육적 기준 (다시 지적하는 것 아님)

- **S25 설명 1줄(45자 이내)**: UI 예시 둘째 안의 "'기존 결과 그대로'가 원래 기능을 지킨다"는 "지킨다" 때문에 조건이 기능을 보호한다는 뜻으로 읽힌다. 조건은 확인하게 할 뿐이다. 둘째 안 계열을 고르면 예: "칸을 채우면 확인 방법도 정해진다. '기존 질문 그대로'로 원래 기능을 확인한다."(44자, 약 1,120px). 첫째 안("여섯 칸을 채우면 작업 방향과 확인 방법이 함께 정해진다.", 30자)을 고르면 회귀 확인은 완료 조건 칸과 노트 셋째 문장이 맡으므로 그것도 받아들일 수 있다.
- **S28 "산출물:" 표기·캡션 삭제**: 문제없다. 절차 카드라 "개념 예시" 캡션이 없어도 측정값이나 실제 화면으로 오해될 여지가 없다. 노트 셋째 문장을 "각 카드의 산출물 줄은"으로 맞추는 것까지 함께 반영한다.
- **S26 조건 줄 27자**: 문제없다. 해상도 조건은 자산 대기 목록에 남는다.
- **S32 노트 "승인 요청 카드"**: 문제없다. W4-17을 같은 문장에 붙이면 한 번에 끝난다.

## 문구·배치 담당이 lecture_expert 판단을 요청한 항목

| 출처 | 질문 | 판정 |
|---|---|---|
| 문구 B4 v0.4 보류 11 | S26 단계 목록 3줄을 빼고 "사내 가이드대로"가 화면에서 빠진 것이 괜찮은가 | 단계 목록 삭제는 **수용**한다(시간 블록이 같은 순서·내용). "사내 가이드" 기준은 **되살린다**(W4-08). "실행 방법"은 관찰할 것에 되살린다(W4-07) |
| 문구 B4 v0.4 보류 1 | P2가 "모델이 도구 호출을 고른다"로 확인되면 오전 앱 열에도 루프를 넣을까 | **넣지 않기를 권한다.** 두 열에 같은 루프가 있으면 요지 "대상이 질문에서 프로젝트로 바뀐다"보다 "둘 다 Agent"가 먼저 읽혀 이 장면의 대비가 흐려진다. 노트에 한 문장만 더한다(v0.3 판정과 같음) |
| 문구 B4 v0.4 보류 9 | S25를 2열 카드 대신 "막연한 요청 줄 + 3×2 칸"으로 바꾼 구성 | **수용.** 위 → 아래가 "막연한 요청 → 채운 요청" 순서로 읽히고, 6칸이 S40과 같은 모양이라 B6에서 다시 알아볼 수 있다 |
| 문구 B5 v0.4 보류 6 | S30 두 열을 코드 카드 두 개로 | **수용.** 파일 모양이 요지 "파일에"를 보여 주고 사실 경계 7번 문장은 설명 문단에 있다 |
| 문구 B5 v0.4 보류 10 | S31 3열 카드 → 상자 3개 + 자리표시 두 칸 반씩 | **수용.** 확인 수단·질문이 상자 하나에 모여 뜻을 잃지 않는다 |
| 배치 S32 | 단계 목록 3줄 삭제, 승인 카드 추가 | **수용.** 5번 행 근거. 소제목만 W4-16으로 고친다 |
| 배치 S26 | 시간 블록 좌측, 자리표시 우측(오전 안내판·S32와 좌우가 다름) | **수용.** 자리표시를 화면 절반 이하로 두기 위한 배치다. 좌우 차이는 UI 메모대로 T8 전체 QA에서 대조한다 |
| 배치 S23 | CSS B4-F(`.dg-row.even`) 반영 여부 | 교육적 영향 없음. 없어도 "모델"이 "내 PC" 영역 밖에 있어 뜻이 같다 |

## 빌드 후 확인할 것

- S22: 막 태그 1줄(W4-01), 설명 2줄(W4-02), 여유 24px 이상. 캡션 "결과를 보고 다시 반복"이 루프 바로 아래에서 읽히는지(되돌림 표시가 이것뿐이다).
- S23: 설명 2줄인지 3줄인지와 여유.
- S24: 띠 삭제 후 좌우 높이(좌 약 265 / 우 140px). 오른쪽 항목이 너무 위에 붙어 보이면 세로 가운데 정렬을 검토한다.
- S25: 설명 1줄, `.dg-sub` 28px(U4-02) 1줄, 여유.
- S26: 관찰할 것 2줄, 막히면 1줄(W4-08), 좌 칸 여유 30px 유지.
- S29: 설명 줄 수. 1행 주석 어긋남(v0.3 사후 약 18px)은 참고.
- S30: 설명 2줄(3줄이면 넘친다).
- S31: 설명 1줄(W4-14), 여유.
- S32: 소제목 1줄(W4-16).

---

## 보류(사용자·강사 확인)

추정하지 않고 확인이 필요한 사실만 적는다. 기존 ID(P·Q·N)는 v0.3 검토와 같은 항목이다.

1. **P2** · S22: 오전 SQL 실습에서 모델이 도구 호출 여부를 고르는지. 루프를 Claude Code 열에만 둔 근거다.
2. **P5** · S23 노트, S26: 오후 Claude Code의 승인된 설치·계정·접속 방식과 모델 경로. S23 노트 "오전 실습의 사내 추론 서버와 따로 준비·확인된 것"의 전제다.
3. **Q1** · S23 질문의 정답, S26 관찰할 것(W4-07)·노트(W4-09), S32 관찰할 것·승인 카드: 수강생 PC의 Claude Code 사전 설치·로그인, Python 실행 허용 여부, 화면 조작 도구 연결 여부, 파일 수정·명령 실행 전 승인 설정.
4. **Q1 추가(신규)** · S32 노트: 교육 환경의 Claude Code 승인 요청에 "앞으로 묻지 않기"처럼 한 번에 계속 허용하는 선택지가 있는지. 있다면 노트에 "이번 실습에서는 매번 묻는 쪽만 고릅니다"를 한 구절 넣을지 강사가 정한다.
5. **Q2** · S24 영역 라벨, S25 6칸(W4-06 포함), S32 소제목(W4-16), S33 새 입력: "검색 결과 없음 안내"가 오전 앱에 이미 있는지, 대체 과제(근거 표시·결과 다운로드) 시작 코드와 S32 막히면의 "준비된 결과"가 있는지.
6. **Q3** · S28 산출물 "바뀐 줄", S31 첫 칸, P-S31: 실습 폴더가 Git 저장소이고 diff를 볼 수 있는지.
7. **Q7** · S29 코드 카드·노트(W23), S32 코드 위치 "화면·처리 부분", S24 층 이름(W4-05): 실제 실습 코드의 화면·조회·모델 호출 구조와, 기본 과제가 실제로 바꾸는 부분.
8. **N1** · S26·S32 board-meta, S32 코드 위치 줄: 실습 가이드 장 이름·앵커. 사내 HTML을 볼 수 있는 강사가 채운다.
9. **캡처** · P-S26·P-S29·P-S31·P-S31b 제공 시점(12-1 5번). P-S31b(Dokmo 안내 보완 전후 화면)를 공개할 수 있는지.
10. **S31 Dokmo 표현**: "동작했지만 직접 써 본 점검에서 한글 이름 입력과 안내 부족이 드러났다"가 강사 기록과 맞는지(문구 보류 B5-9). 화면에 "강사의 웹앱"으로 밝혀도 되는지.
11. **S22 출처**: "Kaggle Agents 백서(교안 내용 재구성)"의 저자·발행처·연도(문구 보류 B4-2).
12. **S28 노트**: 참고 강의 문구 "마지막 정상 지점과 첫 실패 지점을 증거로 말하기"가 원문과 맞는지(문구 보류 B5-2).
13. **S33**: 실습 데이터에서 결과가 실제로 0건인 키워드를 강사가 미리 준비하는지(문구 보류 B5-15).
14. **S24**: 강사 회고가 어느 프로젝트의 경험인지, 노트에서 프로젝트 이름을 말할지(문구 보류 B4-8). 강사가 고를 일이며 진행에는 지장이 없다.
