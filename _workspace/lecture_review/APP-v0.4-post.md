# APP 부록 A01–A10 검토 (v0.4 post-build)

- 검토 대상: `topics/sk-hynix-ai-agent-guide-edu/snapshots/app/frame-00…09` (1920×1080, 덱 48–57/57), contact-sheet-1
- 대조 자료: `_workspace/lecture_review/APP-v0.4-pre.md` 수정 요청 #2–#23, `_workspace/slide_copy/APP.md` v0.4 발표자 노트(최종 전문), `_workspace/slide_copy/B2.md` 수정 기록(청킹→분할, 생성→답변)
- 판정하지 않은 것: #1(eyebrow 본문 구간), #7("사내 실행 가이드" 명칭 통일). 둘 다 사용자 보류다. 10장 모두 eyebrow가 "질문 대응·후속 학습"인 것은 결함으로 보지 않았다.

## 사전 요청 반영 확인 (#2–#23, #7 제외)

| # | 장면 | 화면 확인 결과 | 판정 |
|---|---|---|---|
| 2 | A01 | 항목 3 보조 "헤드 = Q·K·V 계산 한 벌 · 헤드마다 다른 변환" 1줄 | 반영 |
| 3 | A02 | 요지 "RAG 품질은 재학습이 아니라 분할·검색·메타데이터로 다듬는다", 설명 1문장 "메타데이터는 답변에서 구분할 조건부터 설계한다." | 반영 |
| 4 | A02 | 설명 3문장 "검색 쪽은 재정렬(Reranking), 생성 쪽은 프롬프트 제약을 본다." 설명 2줄 유지 | 반영 (용어는 아래 요청 3) |
| 5 | A03 | 설명 "추상화 수준은 기능이 미리 묶인 정도다. 단순 호출 하나면 SDK로 충분하다. LangChain Agent와 Deep Agents는 LangGraph 위에서 동작한다." | 반영 |
| 6 | A04 | MCP 항목 1 "앱 N개·도구 M개를 한 규약으로", 칩 1줄 안 | 반영 |
| 8 | A05 | "예: 콘솔 인코딩 오류로 서버가 안 뜸" / "예: 임베딩 모델 바꾸면 벡터 새로 생성" | 반영 (줄바꿈은 아래 요청 5) |
| 9 | A05 | 출처 줄 끝 "근거: 강사 강의안" | 반영 |
| 10 | A07 | "상태(State)·단계(Node)·연결(Edge)로 흐름을 정의한다. … 내부는 그래프다." 2줄 | 반영 (그림 용어와 어긋남, 아래 요청 8) |
| 11 | A07 | 코드 제목 "StateGraph 빌드 · 교육용 축약(일부 줄 생략)" | 반영 |
| 12 | A07 | 캡션 "모두 END로 · 개념 예시, 오전 앱 구조 아님 · 교안 내용 재구성" 1줄 | 반영 |
| 13 | A08 | 설명 1문장 "본문 B2의 '검색이 틀렸나' 확인을 그래프 안에 넣은 구조다." | 반영 (필수 해소) |
| 14 | A08 | 칩 줄 삭제, 노트 "같은 계열인 Corrective RAG는…" | 반영 |
| 15 | A08 | 노트 "상한에 닿으면 억지로 답하지 않고 근거를 찾지 못했다고 알리게 합니다" | 반영 |
| 16 | A09 | 설명 "…계획·파일·위임 등을 미리 묶은 하네스다. Claude Code도 구현은 다르지만 같은 역할의 실행 틀이다." 노트에 "LangGraph나 Deep Agents로 만든 것은 아니고" 추가 | 반영 (필수 해소) |
| 17 | A09 | 칩 "긴 출력 보관" | 반영 |
| 18 | A09 | Memory 보조 "상태·저장소" | 반영 |
| 19 | A09 | 코드 `interrupt_on={...},` 강조 없음 | 반영 |
| 20 | A09 | 노트 "현업 활용 가이드의 사내 배포 패키지 예시" | 반영 |
| 21 | A10 | 노트 "…승인 카드 부분만 잘라 보여 드립니다." 대조표에서 "모델명 두 곳 … 가린 복사본" 문구 삭제 확인 | 반영 (필수 해소, 노트 문장 다듬기는 아래 요청 14) |
| 22 | A10 | 캡션 "지정한 도구만 멈춤 · 결과는 다시 모델로 · 개념 예시 · 교안 내용 재구성" 1줄 | 반영 |
| 23 | A10 | 오른쪽 캡션 "사내 배포 패키지 예시 화면의 승인 카드 · 실행할 명령과 승인·거부 버튼 · 수강생 실습 화면 아님" | 반영 (2줄 줄바꿈 위치는 아래 요청 13) |

사전 요청 21건(#2–#23 중 #7 제외) 모두 화면·노트에 반영됐다. 필수 3건(#13·#16·#21)도 해소됐다.

## 장면별 판정

| 장면 | 1 가독성 | 2 시각 결함 | 3 사실 경계 | 4 화면·노트 | 5 본문 용어 | 근거 |
|---|---|---|---|---|---|---|
| A01 (48) | 통과 | 통과 | 통과 | 통과 | 통과 | 왼쪽 항목 3개 36px·보조 28px, 설명 2줄. 논문 그림 속 라벨(약 14px)은 뒤에서 안 읽히지만 왼쪽 항목 2·3이 "그림 왼쪽/오른쪽"으로 풀어 준다. 그림 카드 하단 865 → 출처 줄 약 55px 간격. 노트의 "그림 중 왼쪽이 헤드 하나의 계산"과 화면이 맞다 |
| A02 (49) | 통과 | 통과 | 통과 | 통과 | **수정 필요** | 띠·카드·출처 간격 약 54px로 정상. 같은 단계를 띠는 "분할", 카드 1 제목은 "청킹·임베딩"으로 적어 한 화면에 두 이름이 나온다. 본문 B2는 v0.4에서 청킹→분할(필수)·생성→답변으로 바꿨는데 띠 끝이 "생성"이다(요청 2·3·4) |
| A03 (50) | 통과 | **수정 필요** | 통과 | **수정 필요** | 통과 | 아래 띠 "공통 실행 기반 · LangGraph 런타임"이 x 120–1413까지만 있고 Deep Agents 카드(x 1262–1800)는 약 150px만 걸친다. 오른쪽 끝에 캡션이 들어가서 띠가 짧아졌다. 설명("LangChain Agent와 Deep Agents는 LangGraph 위에서 동작한다")과 노트("화면 아래 띠처럼 … create_agent와 Deep Agents는 모두 LangGraph 실행 기반 위")는 둘 다 올라탄다고 하는데, 그림은 Deep Agents가 기반 밖에 있는 것처럼 보인다(요청 1) |
| A04 (51) | 통과 | 통과 | 통과 | 통과 | 통과 | 세 카드 높이·정렬 같음. N×M → N+M 칩과 "앱·도구 연결 수" 1줄. Hook "명령 실행형·모델 판단형"은 노트와 맞다. "사내 가이드" 명칭은 H1 보류 |
| A05 (52) | 통과(보조 24px 경계) | **수정 필요** | 통과 | **수정 필요** | 통과 | 카드 네 장 모두 한두 낱말만 다음 줄로 떨어진다: "…실습 가이드와 / 비교", "…지원 여부 / 확인", "서버가 / 안 뜸", "옵션 / 누락", "벡터 / 새로 생성", "파일 못 / 찾음"(요청 5). 노트는 아직 "문서를 다시 인덱싱합니다"라고 해서 #8에서 뺀 말이 강사 입으로 다시 나온다(요청 6). 사내 주소·키·모델명 없음 |
| A06 (53) | **수정 필요** | **수정 필요** | 통과 | 통과 | 통과 | 띠와 캡션이 y 447–650에서 끝나고 출처 줄(약 920)까지 약 270px가 비어 화면 위쪽으로 쏠린다. 상자 보조 줄은 24px 회색이라 뒤에서 읽기 어렵고 "문서화", "상황"이 한 낱말씩 다음 줄로 떨어진다(요청 7). 노트 "화면 띠의 다섯 가지"와 상자 5개가 맞다 |
| A07 (54) | 통과 | 권장 1건 | 통과 | 권장 1건 | 권장 1건 | 캡션~출처 간격 약 60px, 왼쪽 두 캡션 모두 1줄이라 줄바꿈 문제 없음. "조건부 Edge: 질문 종류로 다음 노드 선택 ↓"의 화살표가 x 약 575에 있어 "SQL 조회" 상자 하나만 가리키는 것처럼 보인다(요청 9). 설명은 "단계(Node)"인데 그림·노트는 "노드"라서 한 화면에 Node·단계·노드 세 표기가 섞인다(요청 8). StateGraph·add_node·add_edge·add_conditional_edges·compile(checkpointer=…)·invoke(…, config)는 공식 이름 그대로다 |
| A08 (55) | 통과 | 권장 1건 | 통과 | 통과 | 통과 | 칩 줄이 빠져서 코드 카드 하단(780)과 출처 줄 사이가 약 140px로 넉넉하다. "관련성 평가 → 답변 생성" 화살표가 y 482로 첫 화살표(y 510)보다 높고 "yes"는 그 아래 540에 있어 두 화살표 높이가 다르다(요청 11). 코드 제목만 "import 생략"으로 남아 A07 "일부 줄 생략"과 기준이 다르다. 코드에 `model`·`prompt` 정의가 없어 import만 뺀 것도 아니다(요청 10). with_structured_output·Literal 스키마 과장 없음 |
| A09 (56) | 통과 | 권장 1건 | 통과 | 통과 | 통과 | 상자 보조 줄 3개("외부 기능", "상태·저장소", "지침·규칙") 모두 1줄로 칸 안에 들어간다. "모델" 상자만 보조 줄 없이 글자가 위쪽에 붙어 파란 상자 아래 절반이 빈다(요청 12). 캡션~출처 간격 약 67px. create_deep_agent 인자 model·tools·subagents·skills·backend·interrupt_on은 공식 이름이고 값은 생략했다. 요지 "도구·기억·맥락" 순서가 Tools·Memory·Context 상자와 짝지어져 본문 "도구(Tool)"와 이어진다 |
| A10 (57) | 통과 | 권장 2건 | 통과 | 권장 1건 | 통과 | 잘라 낸 승인 카드 그림에 모델명·주소 없음(명령 예시 "python3 --version", 도구 "execute"만). 오른쪽 캡션 2줄째가 "화면 아님" 두 낱말만 남는다(요청 13). ↓ 화살표가 "멈춤" 아래(x 약 524)에 있어 승인·거부가 "사람 결정"을 건너뛰는 것처럼 보인다(요청 15). 노트 "…받는 장면이며, 수강생 실습 화면은 아니며, …" 처럼 '-며'가 두 번 이어진다(요청 14). interrupt()·Command(resume)·체크포인터는 공식 개념과 맞다 |

## 수정 요청

| 번호 | 장면 | 요소 | 현재 | 요청 | 담당 | 구분 | 이유 |
|---|---|---|---|---|---|---|---|
| 1 | A03 | 아래 띠 · 캡션 배치 | 띠 "공통 실행 기반 · LangGraph 런타임"이 x 120–1413까지만 있고, 오른쪽에 캡션 "개념 예시 · 기반 관계, 배우는 순서 아님" | 띠를 x 120–1800으로 늘려 세 카드 아래를 모두 받친다. 캡션은 띠 안 오른쪽 끝에 작은 글씨로 넣는다(띠 가운데 라벨 약 480px, 캡션 약 370px라 한 줄에 들어간다). 띠 밑에 따로 한 줄을 두면 출처 줄 간격이 약 20px로 줄어드니 그 방식은 쓰지 않는다 | slide_ui_designer | **필수** | 설명과 노트는 두 프레임워크 모두 LangGraph 위라고 하는데, 그림은 Deep Agents가 공통 기반 밖에 있는 것처럼 보인다. PDF만 읽는 수강생에게 기반 관계를 거꾸로 전한다(항목 3·4) |
| 2 | A02 | 카드 1 제목 · 노트 | 카드 1 제목 "청킹·임베딩" / 노트 "청킹은 긴 문서를 찾기 좋은 단위로 나누는 일인데," | 카드 1 제목 "분할·임베딩" / 노트 "분할은 흔히 청킹이라고도 부르는데, 긴 문서를 찾기 좋은 단위로 나누는 일이고," | slide_content_writer | **필수** | 바로 위 띠와 카드 1 항목은 "분할"인데 제목만 "청킹"이라 비전공자는 두 단계로 읽는다. 본문 B2가 v0.4 R2(필수)로 청킹→분할을 확정했다. 노트에 원어를 한 번 남겨 두면 교안·외부 문서로 넘어갈 때도 이어진다 |
| 3 | A02 | 띠 5칸 · 설명 3문장 | 띠 "생성" / 설명 "…생성 쪽은 프롬프트 제약을 본다." | 띠 "답변" / 설명 "…답변 쪽은 프롬프트 제약을 본다." | slide_content_writer | 권장 | 본문 B2가 R3로 생성→답변을 쓴다. 설명 2문장의 "찾은 근거로 잘못 답했는지"와도 낱말이 이어진다. 글자 수 차이 없음 |
| 4 | A02 | 띠 3칸 | "저장" | "벡터 저장소" | slide_content_writer(slide_ui_designer 칸 폭 확인, 칸 약 225px) | 권장 | 본문 B2가 "벡터 저장소"를 반복해 쓴다. 부록에서 "저장"만 보면 본문의 어느 부품인지 짝짓기 어렵다. 칸에 1줄로 들어가지 않으면 적용하지 않는다 |
| 5 | A05 | 카드 설명 줄 · 예시 줄 줄바꿈 | "설치 판본을 실습 가이드와 / 비교", "도구 호출·임베딩 지원 여부 / 확인", "예: 콘솔 인코딩 오류로 서버가 / 안 뜸", "예: 호환 서버용 임베딩 옵션 / 누락", "예: 임베딩 모델 바꾸면 벡터 / 새로 생성", "예: 실행 위치가 달라 파일 못 / 찾음" | 카드 설명 줄과 예시 줄에 `word-break: keep-all; text-wrap: balance`를 적용해 두 줄 길이를 고르게 한다. 글자 크기는 줄이지 않는다 | slide_ui_designer | 권장 | 카드 네 장 모두 한두 낱말이 다음 줄로 떨어져 끝말이 따로 읽힌다. "안 뜸", "누락"처럼 결론에 해당하는 말이 떨어져 나간다 |
| 6 | A05 | 노트 | "…임베딩 모델을 바꿨다면 기존 벡터와 차원이 맞지 않을 수 있으니 문서를 다시 인덱싱합니다." | "…임베딩 모델을 바꿨다면 기존 벡터와 차원이 맞지 않을 수 있으니 문서 벡터를 새로 만들어야 합니다." | slide_content_writer | 권장 | 사전 #8에서 화면의 "재인덱싱"을 "벡터 새로 생성"으로 바꿨다. 강사가 노트대로 "인덱싱"이라고 말하면 같은 낯선 말이 다시 나오고, 화면 예시 줄과도 다르다 |
| 7 | A06 | 띠 배치 · 상자 보조 줄 | 띠·캡션이 y 447–650에 있고 출처 줄까지 약 270px가 빈다. 보조 줄은 24px 회색이고 "문서화", "상황"이 한 낱말씩 다음 줄로 떨어진다 | 상자 높이를 늘리고 보조 줄을 28px로 올린다(A05 카드 설명 줄과 같은 크기). 띠+캡션 묶음은 요지·설명 아래 남는 공간의 세로 가운데에 둔다. 보조 줄에도 `text-wrap: balance`를 적용한다 | slide_ui_designer | 권장 | 화면 위쪽으로 쏠려 아래 4분의 1이 비고, 이 장의 핵심인 다섯 확인 항목의 풀이가 화면에서 가장 작은 글씨다. 여백이 충분하니 글자를 키워도 넘치지 않는다 |
| 8 | A07 | 설명 1문장 | "LangGraph는 상태(State)·단계(Node)·연결(Edge)로 흐름을 정의한다." | "LangGraph는 상태(State)·노드(Node, 처리 단계)·연결(Edge)로 흐름을 정의한다." | slide_content_writer(slide_ui_designer 2줄 유지 확인) | 권장 | 그림("classify 노드", "다음 노드 선택", "모든 노드가 공유")·코드(`add_node`)·노트("모든 노드가 같은 State")는 "노드"를 쓴다. 설명만 "단계"라 한 화면에 세 표기가 섞인다. +6자라 1줄째 끝 "내부는"이 2줄로 넘어가도 2줄째가 약 1060px에서 끝나 2줄을 유지한다 |
| 9 | A07 | 조건부 Edge 캡션의 ↓ | "조건부 Edge: 질문 종류로 다음 노드 선택 ↓"(화살표가 x 약 575, "SQL 조회" 상자 위) | 화살표를 캡션 앞으로 옮겨 "↓ 조건부 Edge: 질문 종류로 다음 노드 선택"으로 쓰고, 캡션 줄을 세 상자 묶음(x 147–900) 가운데에 맞춘다 | slide_ui_designer | 권장 | 지금 위치에서는 분기가 "SQL 조회" 하나로만 가는 것처럼 보인다. 세 갈래 중 하나를 고른다는 뜻이 그림으로 드러나야 한다 |
| 10 | A08 | 코드 제목 | "관련성 판정 스키마 · 교육용 축약(import 생략)" | "관련성 판정 스키마 · 교육용 축약(일부 줄 생략)" | slide_content_writer | 권장 | 코드에 `model`·`prompt` 정의도 없어 import만 뺀 것이 아니다. A07 코드 제목(사전 #11)과 기준을 맞춘다. 글자 수도 짧아진다 |
| 11 | A08 | "관련성 평가 → 답변 생성" 화살표 | 화살표 y 약 482, "yes" 라벨 y 약 540(첫 화살표는 상자 가운데 y 약 510) | 화살표를 첫 화살표와 같은 상자 가운데 높이에 두고 "yes"는 화살표 바로 위에 작게 둔다 | slide_ui_designer | 권장 | 같은 줄의 두 화살표 높이가 달라 정렬이 어긋나 보이고, "yes"가 화살표와 떨어져 무엇에 붙은 라벨인지 흐려진다 |
| 12 | A09 | "모델" 상자 | 보조 줄 없이 "모델"만 상자 위쪽(y 약 538)에 있어 상자 아래 절반이 빔 | "모델" 글자를 상자 세로 가운데에 둔다(다른 세 상자의 제목+보조 두 줄 묶음과 같은 가운데선) | slide_ui_designer | 권장 | 강조된 파란 상자가 비어 보여 렌더 결함처럼 읽힌다. 문구를 추가하지 않고 정렬만 고친다 |
| 13 | A10 | 오른쪽 캡션 줄바꿈 | 1줄 "사내 배포 패키지 예시 화면의 승인 카드 · 실행할 명령과 승인·거부 버튼 · 수강생 실습" / 2줄 "화면 아님" | 2줄째가 "수강생 실습 화면 아님"으로 시작하도록 끊는다(해당 구절을 `white-space: nowrap` 묶음으로 두거나 앞에서 줄을 바꾼다). 문구는 그대로 둔다 | slide_ui_designer | 권장 | "수강생 실습 / 화면 아님"으로 끊겨 경계 문구가 반쪽씩 읽힌다. 이 캡션에서 가장 중요한 말이 "수강생 실습 화면 아님"이다 |
| 14 | A10 | 노트 | "…승인과 거부를 받는 장면이며, 수강생 실습 화면은 아니며, 승인 카드 부분만 잘라 보여 드립니다." | "…승인과 거부를 받는 장면입니다. 수강생 실습 화면은 아니고, 승인 카드 부분만 잘라 보여 드립니다." | slide_content_writer | 권장 | '-며'가 두 번 이어져 소리 내어 읽을 때 문장이 끊기지 않는다. 뜻은 사전 #21 그대로다 |
| 15 | A10 | 개념도 ↓ 화살표 | ↓가 "멈춤" 상자 아래(x 약 524) | ↓를 "사람 결정" 상자 아래(x 약 816)로 옮긴다. 승인·거부 두 상자는 지금 폭 그대로 둔다 | slide_ui_designer | 권장 | 승인·거부는 사람 결정의 결과인데, 지금은 멈춤에서 바로 갈라져 사람 결정을 건너뛰는 흐름으로 보인다. 노트 "사람이 승인하면 … 거부하면"과도 맞춘다 |

### 담당별 요약

- slide_content_writer: 2(필수)·3·4·6·8·10·14. 4·8은 slide_ui_designer가 폭·줄 수를 확인한다.
- slide_ui_designer: 1(필수)·5·7·9·11·12·13·15.
- index.html만 고치고 `python scripts\sync_overview.py topics\sk-hynix-ai-agent-guide-edu --renumber`로 overview를 다시 만든 뒤, A02·A03·A05·A06·A07·A10은 스냅샷을 다시 찍어 확인한다.

## 보류(사용자·강사 확인)

- H1(사전에서 이어짐). A04 "사내 가이드", A05 설명 "사내 실행 가이드", A05 카드 2 "실습 가이드"가 같은 문서인지. 답을 받은 뒤 사전 #7을 적용한다.
- H2(사전에서 이어짐). eyebrow 둘째 칸에 본문 구간을 표시할지(사전 #1, V17).
- H3(사전에서 이어짐). 수강생 배포 PDF에 발표자 노트가 들어가는지. 들어가지 않으면 요청 2의 노트 부분, 6, 14는 강사 발화에만 영향이 있다.
- H4(사전에서 이어짐). A09 "Claude Code도 … 같은 역할의 실행 틀이다"의 출처. 지금 출처 줄은 LangChain Concepts와 교안뿐이다.
- H5(신규). A07 출처 줄 "뿌리강의 REMASTERED"를 수강생 PDF에 그대로 둘지. 수강생이 무엇인지 알 수 없는 자료 이름이다. V11(교안 호칭)과 같이 정한다.

수정 필요 잔여: 15건 (필수 2 · 권장 13)
