# v0.5 r2 시각 검토 · 26–50쪽

- 기준: `_workspace/roadmap1/review_r2/snaps/slide-26.png` ~ `slide-50.png`(1920×1080 실제 렌더)를 한 장씩 열어 정렬·치우침·글자·가독성·하우스 룰·일관성 6개 항목으로 봄. 쪽 ↔ 장면 ID는 `_workspace/roadmap1/scene_map_v0.5.md`.
- 범위: 26쪽(C01) ~ 50쪽(A10) 25장. 1회차 보고서 `v0.5-r1-visual-26-50.md`와 반영 기록 `review_r1/fix_log.md`의 13개 요청이 렌더에서 실제로 해결됐는지, 수정 때문에 생긴 회귀(겹침·넘침·빈 공간)가 있는지 확인. 원인은 `topics/sk-hynix-ai-agent-guide-edu/index.html`에서 봤고 원본은 고치지 않음.
- 결함으로 보지 않은 것: `[IMG-PLACEHOLDER]` 점선 자리표시(27·29·32쪽, 29쪽 한 줄형 포함), 안내판·자유 실습 단계의 번호 블록/칩/완료 조건/막히면 구조(34–38쪽), 40쪽 반전 배경, 부록 "부록 ·" 접두어, 28쪽 녹색 상태 점, 50쪽 승인 카드 모사 화면(녹색·작은 글자), 코드 카드 고정폭 글꼴. 시간 표기·반말·"근거:"·B1–B7·사내 주소·모델명·키는 26–50쪽 화면에서 발견되지 않음.

## 1회차 요청 반영 확인

| 번호 | 쪽(장면) | 1회차 요청 요지 | 판정 | 비고 |
|---:|---|---|---|---|
| 1 | 44쪽(A04) | MCP 카드 캡션 세로 분절·노트 넘침 | 해결 | "개념 예시 · 앱·도구 연결 수"가 카드 안(y≈843)에 들어가고 카드 하단(892)·출처(934)와 분리됨. 네 카드 높이 동일 |
| 2 | 30쪽(C03) | 배지·패널 제목 2줄 꺾임 | 해결 | 배지 "공개 Beta 개념 예시" 1줄, 제목 "요청: 제조사 A의 로봇 블레이드" 별도 줄 1줄 |
| 3 | 30쪽(C03) | 표 셀 낱말 분절(부품 번호·판정·종류) | 부분 해결 | 부품 번호·판정 칸은 1줄. **종류 칸 "로봇 블레이드"는 여전히 "로봇 / 블레이드" 2줄**(nowrap 규칙이 `td:first-child`·`td:last-child`에만 적용, 1324–1325행). 아래 수정 요청 1 |
| 4 | 30쪽(C03) | 표 아래 캡션 2줄, 푸터 근접 | 해결 | 캡션 1줄(y≈907), 출처(934)·푸터(1013)와 간격 확보 |
| 5 | 33쪽(S39) | "없다고" 고아 어절, "확인:" 중복 | 해결 | 세 카드 모두 결과·확인·예외 라벨 1줄씩 |
| 6 | 30쪽(C03) | 완료 조건 상자 "다른 / 행은" 구 중간 끊김 | 해결 | "…다른 행은 번호와 / 함께 표시합니다"로 구 단위 줄바꿈 |
| 7 | 26쪽(C01) | "coding agent" 표기 | 해결 | "Coding Agent" |
| 8 | 28쪽(S25) | 녹색 상태 점 | 제외(사용자 결정) | 유지 상태 확인 |
| 9 | 32쪽(C04) | 왼쪽 카드 하단 빈 공간 | 해결 | 카드가 내용 높이(y 358–828)로 끝남. 오른쪽 열(~900)과 70px 차이는 자연스러움 |
| 10 | 35–38쪽(S40–S43) | 출처 표기 형식 불일치 | 해결 | 네 장 모두 "출처: Claude Code 공식 문서 Best practices" |
| 11 | 36쪽(S41) | 오른쪽 열 하단 빈 공간 | 해결 | 막히면 상자에 칩 줄(요청문·범위·완료 조건) 추가, 오른쪽 열 끝(745)이 왼쪽 완료 조건 상자(770)와 맞음 |
| 12 | 47쪽(A07) | 가운뎃점 띄어쓰기 불일치 | 해결 | "State · messages · category를 모든 노드가 공유" |
| 13 | 50쪽(A10) | 승인 카드 글자 크기·녹색 | 제외(사용자 결정) | 유지 상태 확인 |

회귀: 없음. 1회차 수정으로 새로 생긴 겹침·넘침·빈 공간은 25장 어디에도 없음.

## 장면별 판정

- 26쪽(C01) · 통과
- 27쪽(C02) · 통과 — (낮음 1건: 자리표시 안내 "…스크립트 실행· / 결과 표)" 고아 어절, 1회차부터 있던 상태)
- 28쪽(S25) · 통과
- 29쪽(S28) · 통과
- 30쪽(C03) · 수정 필요 — 표 종류 칸 "로봇 / 블레이드" 2줄 잔존(1회차 3번 부분 해결). 왼쪽 열이 오른쪽보다 약 110px 짧으나 출처 줄이 받쳐 줘 허용
- 31쪽(S30) · 통과
- 32쪽(C04) · 통과 — (낮음 1건: 출처 줄 없음, 26·27·31쪽과 불일치)
- 33쪽(S39) · 통과 — (낮음 1건: 출처 줄 없음)
- 34쪽(M01) · 통과
- 35쪽(S40) · 통과
- 36쪽(S41) · 통과
- 37쪽(S42) · 통과
- 38쪽(S43) · 통과
- 39쪽(S44) · 통과
- 40쪽(G02) · 통과
- 41쪽(A01) · 통과
- 42쪽(A02) · 통과
- 43쪽(A03) · 통과
- 44쪽(A04) · 통과 — (낮음 1건: MCP 카드 항목 "…M개를 한 / 규약으로" 고아 어절)
- 45쪽(A05) · 통과 — (낮음 1건: 본문 "주소·계정· / 키는" 가운뎃점 뒤에서 줄이 바뀜)
- 46쪽(A06) · 통과
- 47쪽(A07) · 통과
- 48쪽(A08) · 통과
- 49쪽(A09) · 통과
- 50쪽(A10) · 통과

## 수정 요청

| 번호 | 쪽(장면 ID) | 요소 | 현재 상태 | 제안(마크업 수준) | 심각도 |
|---:|---|---|---|---|---|
| 1 | 30쪽(C03) | `.data-table` 둘째 행 종류 칸 `<td>로봇 블레이드</td>`(2274행) | "로봇 / 블레이드"로 2줄이 되어 둘째 행만 높이가 커짐(첫 행 68px, 둘째 행 118px). 1회차 3번의 nowrap CSS(1324–1325행)가 첫·마지막 칸만 대상이라 종류 칸은 제외됨 | 1324행 선택자에 `td:nth-child(2)`를 추가해 `.scene[data-scene-id="C03"] .data-table td:first-child, .scene[data-scene-id="C03"] .data-table td:nth-child(2), .scene[data-scene-id="C03"] .data-table td:last-child { white-space: nowrap; }`로. 표 폭(730px)에 4열이 들어가므로 넘치지 않음. 혹은 2274행 문구를 `블레이드`로 줄이고 2267행 제목은 그대로 두기 | 중간 |
| 2 | 44쪽(A04) | MCP `.compare-col` > `.compare-list` li "앱 N개·도구 M개를 한 규약으로"(2781행) | 카드 폭(400px)에서 "…M개를 한 / 규약으로"로 낱말 하나만 둘째 줄. 옆 카드(Subagent·Hook) 항목은 모두 1줄이라 첫 줄 높이가 어긋나 보임 | 2781행을 `앱 N개·도구 M개를 한 규약으로` → `앱 N개·도구 M개를 규약 하나로`(같은 뜻, 1줄에 들어감) 또는 `<span class="nowrap">한 규약으로</span>`로 묶어 "…M개를 / 한 규약으로" 구 단위 줄바꿈 | 낮음 |
| 3 | 27쪽(C02) | `.placeholder-desc` "넣을 자료: CSV 집계 시연 화면 (질문·스크립트 실행·결과 표)"(2182행) | 괄호 안이 "…스크립트 실행· / 결과 표)"로 갈라져 "결과 표)"만 둘째 줄. 자리표시라 캡처가 들어오면 사라지는 문구이나, 자리표시 상태로 배포될 수 있음 | 2182행을 `넣을 자료: CSV 집계 시연 화면(질문·실행·결과 표)`로 줄여 1줄에 맞춤 | 낮음 |
| 4 | 45쪽(A05) | `.explain` 본문 "…주소·계정·키는 사내 실행 가이드에서 확인합니다."(2810행대) | 첫 줄이 "주소·계정·"에서 끝나고 "키는"이 둘째 줄 첫머리에 옴. 가운뎃점 나열 중간에서 줄이 바뀌어 "키"가 따로 읽힘 | `<span class="nowrap">주소·계정·키는</span>`으로 묶기(덱 공통 `.nowrap` 클래스 존재). 또는 문장을 `사내 실행 가이드에서 주소·계정·키를 확인합니다.`로 어순 변경 | 낮음 |
| 5 | 32쪽(C04)·33쪽(S39) | `.source` 줄 없음 | 26·27·28·29·30·31·34–38쪽은 출처 줄이 있고 32·33쪽만 없어 하단 기준선(y≈934)이 비어 보임. 32쪽은 Obsidian·Claude Code Memory 개념을 다루므로 출처 근거가 있음 | 32쪽 2347행 `</div>` 뒤에 `<p class="source" data-editable="true">출처: Claude Code 공식 문서 Memory</p>` 추가. 33쪽은 실습 안내라 출처가 불필요하면 그대로 두되, 두 장의 일관성만 확인 | 낮음 |

수정 필요: 높음 0 · 중간 1 · 낮음 4
