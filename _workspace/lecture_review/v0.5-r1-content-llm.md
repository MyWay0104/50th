# llm-professional 검토 · v0.5 덱(50장) 최종 내용 검토

검토 대상: `_workspace/roadmap1/review_r1/deck_text.md` · 기준: `ROADMAP1.md` 0절·0-1절 결정, 직전 검토(`review_llm_professional-post.md`), `user_lab_flow.md`, 교안 요약(`pdf_summaries.md`, `pdf_4day_page_index.md`)
판정 기준: 개념 오류·결정 위반(높음) / 짝 불일치·과장(중간) / 표현(낮음). 원본은 고치지 않았다.

## 총평

- 직전 보강안 6건(S15 인자 문구, S21 Memory "오전엔 비워 둔 자리", S17 "사실상 순서가 정해진 셈", S22 "한 바퀴", R01 "②저장까지" 경계·점수 언급 삭제, S14 "검색 줄은 해당 없음")은 모두 정확히 들어갔다. 결정 기본값 3개(실습 2 제목 유지, S17 Workflow, S08 칩 "⑤ 환각")도 그대로다.
- 원론 문장 50장 전부를 다시 읽은 결과 **개념 오류(높음)는 없다.** 토큰·컨텍스트 윈도우·temperature·메시지 역할·임베딩·청킹·tool_calls·루프 종료 조건·환각·CLAUDE.md 비보안·Skill progressive disclosure·MCP N+M·HITL interrupt/Command·LangGraph State/Node/Edge·Agentic RAG grade→rewrite 모두 교안·공식 문서와 맞는다.
- 남은 문제는 짝·일관성 4건: (1) S20 "장비 ID가 조회 함수로 들어간다"가 결정 7(인자 없음)과 겹쳐 읽힐 수 있음, (2) 하네스 "Context"가 S21(근거 자료)·C04(CLAUDE.md)·A09(지침·규칙)에서 다르게 정의됨, (3) 실습 2-④ "없으면 없다"는 프롬프트 제약이 있어야 관찰되는데 그 조건이 안내에 없음, (4) S30 "긍정봇과 같은 이유"는 Claude Code가 한 세션 안에서는 이력을 유지하므로 과장.
- 개념 칩과 직전 개념의 짝, 실습 직전 새 용어 3개 제한, 관찰 포인트(system 한 줄·temperature 3회·조각 수·tool_calls 빔·연결 짚기·파일 대조)는 모두 충족한다.
- 오전→오후 연결(S21→S22→C04)과 부록 참조 8곳(R01→A03, S17→A07, S19·M01→A10, C04→A04, S44→A06, A02·A08→S14, A04→C04)은 현재 순서와 맞는다.

## 수정 요청

| 번호 | 장면 ID | 화면/노트 | 현재(정확한 문구) | 요청(정확한 새 문구) | 심각도 | 이유 |
|---|---|---|---|---|---|---|
| 1 | S20 | 화면 "관찰할 것" + 노트 | "입력한 장비 ID가 어떤 조회 함수로 들어가고, 결과가 어느 코드에서 화면에 그려지는지 짚습니다." (노트: "입력 칸의 값이 어떤 조회 함수로 들어가고") | "입력한 장비 ID가 어떤 경로로 조회 함수와 모델에 전달되고, 결과가 어느 코드에서 화면에 그려지는지 짚습니다." (노트: "입력 칸의 값이 어떤 경로로 조회 함수와 모델에 전달되고") | 중간 | 결정 7·S16·S19는 "도구는 인자가 없다"고 세 번 못박았는데, S20은 ID가 함수 인자로 들어간다고 읽힌다. 인자 없는 도구라면 ID는 user 메시지로 모델에 가고 모델이 표에서 고른다. 앱 구조를 모르는 상태에서는 경로를 특정하지 않는 문구가 안전하다(확인 질문 1) |
| 2 | S21 | 화면 칩 + 노트 | 화면 "Context · 근거 자료" / 노트 "모델에 도구, 대화 이력, 근거 자료를 묶어 주는 이 틀을 하네스라고 부르며" | 화면 "Context · 지침(system 한 줄)과 근거 자료" / 노트 "모델에 도구, 대화 이력, 지침과 근거 자료를 묶어 주는 이 틀을 하네스라고 부르며" | 중간 | 같은 낱말 Context가 S21=근거 자료, C04=CLAUDE.md, A09=지침·규칙으로 세 번 다르게 정의된다. 4일 교안 p32 하네스 3대 구성요소(Tools·Memory·Context)에서 Context는 모델에 넣어 주는 지침·맥락 전체다. 오전에 손으로 만진 system 한 줄(L01)을 포함시키면 C04 "CLAUDE.md = Context"가 오전 경험의 연장으로 읽힌다 |
| 3 | S13 | 노트 | "마지막으로 기사에 없는 것을 물어 없다고 답하는지 확인합니다." | "마지막으로 기사에 없는 것을 물어 없다고 답하는지 확인합니다. 없다고 답하지 않고 아는 척 답하면, 프롬프트에 '본문에 없으면 없다고 답한다'는 제약이 있는지 먼저 봅니다. 다음 장의 답변 쪽 점검이 바로 이것입니다." | 중간 | 본문을 통째로 넣어도 프롬프트 제약이 없으면 모델은 자기 지식으로 답할 수 있다(5일 교안 3대 제약→RAG 해결표 p74–75의 "근거 없으면 모른다고" 조건). 관찰 결과가 둘 중 어느 쪽이든 S14 2×2 표의 "프롬프트 제약" 칸과 짝이 맞게 된다 |
| 4 | S30 | 노트 | "오전 긍정봇이 두 번째 말을 기억하지 못했던 것과 같은 이유로, 대화는 저절로 남지 않고 다음 작업에 필요한 사실은 파일에 남겨야 합니다." | "오전 긍정봇이 두 번째 말을 기억하지 못했던 것과 같은 원리, 곧 모델은 이번에 보낸 목록만 본다는 원리 때문에, 새 대화에서는 목록이 비어 있고 긴 대화는 요약되어 세부가 빠질 수 있으므로 다음 작업에 필요한 사실은 파일에 남겨야 합니다." | 중간 | 긍정봇은 assistant 메시지를 다시 넣지 않아서 기억 못 했고(L01), Claude Code는 한 세션 안에서는 이력을 유지한다(공식 문서 Memory·Context window). "같은 이유"는 과장이고 "같은 원리(목록에 있는 것만 본다)"가 정확하다 |
| 5 | S10 | 화면 | "인덱싱은 문서를 나누고 임베딩해 저장해 두는 과정이고, 벡터 검색은 여러 검색 방법 중 하나입니다." | "인덱싱은 문서를 나누고 숫자로 바꿔 저장해 두는 과정이고, 검색 방법은 여러 가지가 있습니다." | 낮음 | 임베딩·벡터 검색은 다음 장 R01에서 이름을 붙이는 용어인데 S10 화면에서 정의 없이 먼저 쓰인다. S10 화면 새 용어를 인덱싱 하나로 줄이면 S10+R01 합쳐 새 용어가 4개→3개(인덱싱·청킹·임베딩)가 되고, 벡터 검색은 R01 ③에서 처음 나온다. 노트의 "임베딩과 벡터 저장소는 검색 방법 중 하나이고…" 문장은 그대로 둔다 |
| 6 | S07 | 화면 | "응답 생성 3단계에서 분포를 얼마나 뾰족하게 볼지 정합니다." | "2단계에서 계산한 분포를 3단계에서 고를 때 얼마나 뾰족하게 볼지 정합니다." | 낮음 | temperature는 2단계의 분포 자체를 늘리거나 좁힌 뒤 3단계에서 고른다(5일 교안 temperature 절 p56·58–59). S04 화면 "3단계의 선택에 영향"과 문장을 맞추면서 분포가 2단계 것임을 남긴다 |
| 7 | S08 | 노트 | "system 한 줄만 바꿔 답의 말투와 형식이 어떻게 달라지는지 보고" | "system 한 줄만 바꿔 답의 말투와 형식, 때로는 내용까지 어떻게 달라지는지 보고" | 낮음 | L01 노트가 "내용이 달라질지, 말투와 형식이 달라질지 예측해 두시고"라고 열어 두었는데 S08 노트가 답을 "말투와 형식"으로 미리 닫는다. system은 관점·언어·범위 지시로 내용도 바꾼다(5일 교안 system→context→role 누적 실험 p66–68) |
| 8 | S15 | 화면 하단 캡션 | "흐름도는 개념 예시" | "흐름도는 개념 예시 · 오늘 실습에서 모델이 정하는 것은 부를지 말지뿐" | 낮음 | 화면 흐름도 "모델·조건 해석 → SQL·Python·집계"는 여전히 모델의 해석이 SQL로 흘러가는 그림이다. 노트에는 결정 7 경계가 있으나 화면에는 없어, 화면만 보는 수강생이 S16 "인자가 없어"와 충돌을 느낄 수 있다 |
| 9 | A04 | 노트 | "세 기능은 상하위 단계가 아니라 서로 다른 역할이라 필요하면 함께 조합하고" | "네 기능은 상하위 단계가 아니라 서로 다른 역할이라 필요하면 함께 조합하고" | 낮음 | 화면은 "넷은 상하위 단계가 아니라"인데 노트는 "세 기능"이다. Skill을 포함한 넷이 맞다 |
| 10 | A09 | 노트 | "Context에는 모델 호출 앞뒤에서 로그를 남기거나 가드레일을 거는 미들웨어가 들어갑니다." | "하네스에는 이 셋 외에도 모델 호출 앞뒤에서 로그를 남기거나 가드레일을 거는 미들웨어가 들어갑니다." | 낮음 | 4일 교안 p56–58·p83은 미들웨어를 "하네스 미들웨어 스택"(에이전트 동작 전후의 훅)으로 두지 Context 구성요소로 두지 않는다. Context에 넣으면 요청 2의 정의(지침·근거 자료)와도 어긋난다 |

## 직전 보강안 반영 대조

| 보강안 | 반영 위치 | 확인 |
|---|---|---|
| S15 "'최근 일주일'을 조회 인자로 바꿈" 삭제 | 화면 "'최근 일주일'에 어떤 기준이 필요한지 읽어 냄" · 노트 "다만 오늘 실습의 조회는 인자 없이 코드에 고정돼 있어…" | 반영됨 |
| S21 Memory 빈 자리 | 화면 칩 "Memory · 대화 이력(오전엔 비워 둔 자리)" · 노트 "오전 긍정봇이 기억 못 했던 그 빈 자리이고 오후 프로젝트 지침 장면에서 파일로 채웁니다" · 화면 첫 줄 "오전에 모델 둘레에 붙인 것들이 곧 하네스" | 반영됨 |
| S17 Workflow 근거 | 노트 "부를지 말지는 모델이 정했지만 도구가 하나이고 인자도 없어 사실상 순서가 정해진 셈이라 Workflow에 가깝습니다. 도구가 여럿이 되고…" | 반영됨 |
| S22 한 바퀴 | 노트 "도구 호출→결과→답변 한 바퀴가 … 여러 바퀴 도는 것뿐입니다" | 반영됨 |
| R01 ③ 경계 | 노트 "오늘 실습은 ②저장까지 손으로 하고, ③은 저장해 둔 조각을 나중에 어떻게 쓰는지로만 봅니다" · 점수(거리/유사도) 언급 삭제 | 반영됨 |
| S14 검색 줄 경계 | 노트 "오늘은 본문을 통째로 넣었으니 검색 줄은 해당 없고…" | 반영됨 |

## 개념-실습 짝 · 용어 수 · 관찰 가능성 (전 장 통과 요약)

- 실습 1(S08) 칩 ①~⑤ ↔ E01·L01·S07·L01·S09: 짝 맞음. 관찰: content+토큰 사용량, system 한 줄, temperature 2값×3회, 긍정봇 3단 비교, 두 번째 말 기억 못 함, 최신 자료 답.
- 실습 2(S13) 칩 ①~④ ↔ S13 노트(찾아서 읽기)·R01 ①②·S09·S09: 짝 맞음. 관찰: 조각 수, chunk_size 변경, 요약 문장↔본문 대조, 없는 것 묻기(요청 3으로 조건 보강).
- 실습 3(S19) 칩 ①~④ ↔ S15·S15·S16·S16: 짝 맞음. 관찰: 테이블 값, tool_calls 기록, 도구 없는 질문에서 빔.
- 실습 4(S20) 칩 ①~③ ↔ S29 세 층: 짝 맞음. 관찰: 입력→함수→화면 연결 줄 짚기(요청 1로 문구 조정).
- 실습 5(S26) 칩 ①~③ ↔ S23·S22·G00 검증 습관: 짝 맞음. 관찰: 구조 설명 ↔ 실제 파일 대조.
- 선택 과제·자유 실습(M01·S40~S43) ↔ S25·S28·S30: 짝 맞음. 관찰: 바뀐 줄·실행 결과·다른 입력.
- 실습 직전 새 용어: S04 3개(토큰·다음 토큰 분포·컨텍스트 윈도우), S07 1개, L01 3개(system·user·assistant; few-shot은 노트만), S10+R01 4개→요청 5로 3개, S16 3개(tool_calls·docstring·@tool), S29 1개(풀스택), S22 1개(Coding Agent), S30 2개(CLAUDE.md·작업 기록), C04 1개(Skill, 나머지 부록). 모두 한도 안.
- 오후 연결 문장: S21 "오전엔 내가 도구를 붙였고, 오후엔 도구가 이미 붙은 Agent에게 맡깁니다" → S22 "모델에 도구를 붙인 점은 같고, 대상이 프로젝트로 넓어지며" → C04 "Obsidian 파일 = Memory · CLAUDE.md = Context · Skill = 절차 파일". 하네스·Skill·MCP·HITL은 본문에 한 줄씩, 상세는 A04·A09·A10. 필요한 만큼만 있다.

## 확인 질문(사용자)

1. **S20 Streamlit 앱 구조**: 입력한 장비 ID가 (a) 별도 조회·필터 함수의 인자로 들어가는가, (b) user 메시지로 모델에 가고 모델이 인자 없는 도구를 불러 표에서 고르는가? (a)면 S20 현재 문구를 두되 S16·S19의 "인자 없음"은 실습 3 도구에 한정된다는 한 줄을 S20 노트에 추가, (b)면 요청 1대로 바꾼다.
2. **실습 2 요약 프롬프트**: "본문에 없으면 없다고 답한다"는 제약이 준비된 코드에 들어 있는가? 있으면 요청 3의 문장을 "…제약이 프롬프트 어디에 있는지 찾아봅니다"로, 없으면 요청 3대로(관찰 결과가 "아는 척 답함"이 되고 S14 프롬프트 제약으로 이어짐).
3. **Context 용어**: 요청 2처럼 S21에서 Context를 "지침과 근거 자료"로 넓혀 C04·A09와 맞출지, 아니면 S21은 그대로 두고 A09 화면 "Context 지침·규칙"을 "Context 지침·근거 자료"로 바꿀지.
4. (기존 보류) 실습 3 코드가 create_agent류(모델이 반복 판단)인지 bind_tools 뒤 한 번 실행인지. 전자면 S17 답을 "가장 작은 Agent"로 바꾼다.

수정 필요: 높음 0 · 중간 4 · 낮음 6
