# 검수 1회차 수정 반영 지시 (2026-09-17)

대상: `topics/sk-hynix-ai-agent-guide-edu/index.html` (50장). overview.html은 손대지 않는다(나중에 sync로 재생성).
검토 보고서 4건(`_workspace/lecture_review/v0.5-r1-*.md`)의 수정 요청을 아래 판단에 따라 반영한다. 보고서의 "제안/요청" 열에 정확한 새 문구·마크업이 있으니 그대로 쓴다.

## 반영 원칙

- **높음·중간은 전부 반영**, 낮음은 아래 "제외" 목록을 빼고 반영한다.
- 문구 변경은 보고서의 "요청(정확한 새 문구)"를 그대로 쓴다. 마크업 안에 `<span class="nowrap">` 등이 끼어 있으면 그 구조를 유지하며 글자만 바꾼다.
- CSS는 `<style id="scene-styles">` 맨 끝에 `/* r1 fix */` 주석 아래 장면 범위 선택자(`.scene[data-scene-id="S16"] .code-card { … }`)로만 추가한다. 인라인 style 금지. hex 색 새로 만들지 않는다(기존 변수·색만).
- 하우스 룰: 존댓말, 시간 표기는 G01·G03에만, 출처는 공식 문서·논문만, 사내 정보 금지, `data-editable` 유지.
- 같은 요소를 두 보고서가 다르게 요청한 경우는 아래 "충돌 판정"을 따른다.

## 충돌 판정 (오케스트레이터 결정)

1. **S21(21쪽) 하네스 칩** — 시각 #2·#3(칩 길이 줄이기), 강사 #6(괄호 문구), LLM #2(Context 정의)를 합쳐: 칩은 `Tools · 조회 도구` / `Memory · 대화 이력` / `Context · 지침·근거 자료` 세 개로 짧게. 괄호 설명은 화면에서 빼고 노트에만: 강사 #7 문장을 쓰되 "그중 대화 이력은 오전 긍정봇에선 비워 둔 자리이고, 오후에는 Claude Code 쪽에서 이 자리를 지침 파일과 작업 기록으로 채우는 방식을 봅니다."로. 노트의 "모델에 도구, 대화 이력, 근거 자료를 묶어 주는" → "모델에 도구, 대화 이력, 지침과 근거 자료를 묶어 주는". `p.dg-caption`은 시각 #2대로 `.split-panel` 밖 `.split-grid` 다음 형제로 이동. 오른쪽 `.dg` 세로 중앙(시각 #4)은 CSS `align-self:center`. 강사 #8(본문 첫 문장 교체)도 반영.
2. **S39(33쪽) 지식 정리 카드** — 강사 #1 문구("결과: 출처 연결 노트 / 확인: 근거 문장 · 사실·의견 구분 / 예외: 자료에 없으면 '없음'")를 쓰면 시각 #5도 해결. 강사 #2 카드 순서 ①②③ 재배치 + 노트 순서 문장 수정.
3. **S13(13쪽) 넷째 칩** — "④ 없으면 없음". 자리표시 "대체" 문구는 강사 #21.
4. **S08(9쪽) 노트 환각 문장** — 강사 #10대로 "이것을 다음 장에서 환각이라고 부르겠습니다."로 줄인다(정의는 S09).
5. **S16(16쪽)** — 시각 #1 문안(explain 2줄)으로 교체하고 `nowrap` span 제거, CSS `flex-shrink:0` 보험 추가.
6. **A04(44쪽)** — 시각 #1대로 `.dg-caption "앱·도구 연결 수"` 삭제하고 `.compare-note`를 "개념 예시 · 앱·도구 연결 수"로. 노트 "세 기능" → "네 기능"(강사 #18 = LLM #9).

## 제외 (반영하지 않음, 이유)

- 시각 1–25 #15 G00 YouTube 출처: 사용자가 URL과 함께 유지 요청한 항목(final-touch 기록). 라벨만 "참고 영상:"으로 바꾸는 것도 하지 않는다.
- 시각 1–25 #17 출처 괄호 표기: 현재 상태 유지(확인만).
- 시각 26–50 #13 A10 승인 카드 녹색·글자 크기: 사내 배포 화면 재현이라 유지. 캡션에 명령 한 줄 추가는 하지 않는다(화면 캡처 아님을 이미 표기).
- 시각 26–50 #8 S25 녹색 점: 상태 점 예외 규칙(DESIGN.md "정상/주의 상태 점 하나에 초록·주황 허용")에 해당하므로 유지.
- 보류·확인 질문 항목(S20 대화 이력, S17 Agent 여부, C04 1개월차 주체, 소속 표기 등)은 손대지 않는다.

## 특히 주의할 반영

- 시각 1–25 #8·#19: S13·S20의 `.fallback`을 왼쪽 `.stack`의 `.done-box` 다음으로 이동해 S08·S19·S26과 구조를 맞춘다. 오른쪽 열은 자리표시만.
- 시각 1–25 #9: S19 캡션 문장을 explain 끝으로 합치고 캡션은 `.panel-title`로 "실행 환경에서 막는 네 층".
- 시각 1–25 #10·#11: 카드 항목 보강은 문구 예시대로(S15 "집계 기준은 코드에 고정", S09는 한 항목씩), 나머지는 CSS(`.fill` 또는 padding)로 세로를 채운다. 글자 크기를 키우는 방식은 쓰지 않는다.
- 시각 1–25 #23: S26 "막힌 단계에 손들기" 칩 줄을 `.fallback > .chip-row`로 옮기고 오른쪽은 자리표시만.
- 시각 26–50 #2·#3·#4: C03 배지 문구 "공개 Beta 개념 예시", 제목을 별도 줄, 표 셀 nowrap CSS, 캡션 1줄 문구.
- 강사 #16: A05 화면 예시와 노트 문장 교체.
- LLM #1: S20 관찰 문구·노트를 "어떤 경로로 조회 함수와 모델에 전달되고"로.
- LLM #3·#4·#5·#6·#7·#8·#10: 문구 그대로.
- 강사 #3·#4·#5: S40·S41 "선택 과제나 CSV 과제" 통일.
- 강사 #9·#11·#12·#13·#14·#15·#17·#19·#20: 문구 그대로.
- 시각 26–50 #7 "Coding Agent", #10 출처 통일, #11 S41 칩 줄, #12 A07 가운뎃점, #9 C04 CSS, #6 C03 done-text.
- 시각 1–25 #6·#7·#12·#13·#14·#16·#18·#20·#21·#22: 문구·CSS 제안대로.

## 반영 후 검사 (반드시)

```powershell
python scripts\sync_overview.py topics\sk-hynix-ai-agent-guide-edu --renumber
python scripts\validate_topic.py topics\sk-hynix-ai-agent-guide-edu
python scripts\deck\qa_rules.py topics\sk-hynix-ai-agent-guide-edu --rules topics\sk-hynix-ai-agent-guide-edu\deck-rules.json
python scripts\sync_overview.py topics\sk-hynix-ai-agent-guide-edu --check
```

결과를 `_workspace/roadmap1/review_r1/fix_log.md`에 항목별 "반영 / 제외(이유) / 변형(이유)"로 적는다.
