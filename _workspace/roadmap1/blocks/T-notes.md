# T 블록 메모 (T01·T02·T03 · 2026-09-17)

- check_fragment 결과: 통과 (장면 3개: T01 T02 T03).
- 열 제목 "① 지금 되는 것 / ② 아직 안 되는 것 / ③ 그래서 다음"은 `.compare-kicker`에 두고, `.compare-heading`에는 실습 이름(실습 1 · 첫 호출 등)을 넣었다.
- 띠는 spec의 한 줄 문장을 `.dg-box muted → .dg-box accent` 두 상자와 `.dg-caption`(뒷문장)으로 나눴다(`.is-band`가 caption을 오른쪽 끝에 고정하는 구조라서).
- T01 ②열의 "(실습 1 ⑤에서 본 그럴듯한 답 = 환각)" 괄호 설명은 카드 넘침 방지를 위해 화면에서 빼고 노트에만 담았다. `.compare-note`는 쓰지 않았다.
