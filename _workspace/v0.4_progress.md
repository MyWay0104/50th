# v0.4 개정 진행 기록 (중단 후 재개용)

대상: `topics/sk-hynix-ai-agent-guide-edu` 52장 → 57장 · 브랜치 `feat/sk-hynix-deck-v0.4` · 결정 원본: `docs/slide-plan-v0.4-visual-narrative.md` 12-1절
최종 갱신: 2026-09-16 (부록 조립으로 57장, B2+B3 빌드 진행 중)

## 문제 분석

에이전트가 여러 개 동시에 돌다가 사용 한도에 걸리면 한꺼번에 멈춘다. 멈춘 뒤에는 어느 단계·어느 장면까지 끝났는지 파일로 알 수 있어야 이어서 진행할 수 있다.

## 설계

단계는 차수 묶음(B1 / B2+B3 / B4+B5 / B6+B7 / 부록)마다 같은 순서로 돈다.

```text
문구(slide_copy) → UI 배치(slide_ui) → 강사 사전 검토(lecture_review/<묶음>-v0.4-pre.md)
  → 빌드(빌더가 BLOCK 조각 작성 → 검사 → index.html에 조립) → sync_overview.py --renumber
  → validate·lint·--check·Playwright 넘침 검사 → 강사 사후 검토(<묶음>-v0.4-post.md)
→ T8 전체 QA → T9 문서 동기화 → 마지막 검토(Playwright, 시각·내용)
```

- 에이전트는 장면 하나를 끝낼 때마다 파일에 바로 저장한다. 진행 위치는 파일의 `## <장면ID> · <제목> (v0.4)` 제목으로 확인한다.
- 단계가 끝날 때마다 커밋해 세이브 포인트를 남긴다.

## 구현 — 현재 상태

| 묶음 | 문구 (slide_copy) | UI 배치 (slide_ui) | 강사 사전 | 빌드 | 강사 사후 |
|---|---|---|---|---|---|
| B1 (S01·G01·G00·S02–S08) | 완료 | 완료 | 완료·반영 (14건, W7 보류) | 완료 (`683e400`) | 진행 중 |
| B2+B3 (S09–S21) | 완료 | 완료 | 완료·반영 (21건, `0f6ae1b`) | 진행 중 (Sonnet 빌더) | — |
| B4+B5 (S22–S33) | 완료 | 완료 | 완료·반영 (21건) | 완료 (`d09de37`) | 완료·반영 (`3fb6cb2`, R-04 보류) |
| B6+B7 (S34–S44·G02) | 완료 | 완료 | 완료·반영 (18건) | 완료 (`03755c4`) | 완료·반영 (`e4c1f33`, P12 유지) |
| 부록 (A01–A10) | 완료 | 완료 | 완료·반영 (21건, #1·#7 보류) | 완료 (`6056cb0`, 57장) | 진행 중 |

shrimp 작업: T1·T2 완료, T3(문구·UI·사전 검토)는 5개 묶음 모두 끝남, T4–T7(빌드·사후 검토)는 B2+B3만 남음, T8·T9 대기.

세이브 포인트 커밋: `708e886`(T1 CSS) · `84ef240`(T2 이미지) · `d09de37`(B4·B5 빌드) · `03755c4`(B6·B7 빌드) · `683e400`(B1 빌드, 53장) · `6056cb0`(부록 빌드, 57장) · `e4c1f33`(B6·B7 사후 반영)

계획 이탈 기록(계획서 8절과 다르게 확정한 곳):
- S16: 카드 아래 작은 줄을 메시지 이름(도구 스키마·ToolMessage·AIMessage)에서 쉬운 말(이름·설명·형식 / 조회 결과 / 답변·다음 요청)로 바꾸고 메시지 이름은 노트로 옮김 (B2-B3 사전 R4, 새 용어 부담).
- S17: 5패턴 이름 띠를 화면에서 빼고 노트로 옮김, 세 열 아래 캡션 한 줄 (B2-B3 사전 R5).
- S19: 오른쪽 칸을 한 줄 칩 띠 대신 2×2 타일로 (B2-B3 사전 R20, 강의실 가독성).

운영 원칙(2026-09-16, 두 번째 한도 중단 후):
- 에이전트 동시 실행은 3–4개로 제한한다(같은 계정의 다른 Claude 세션도 한도를 함께 쓴다).
- 작은 문구 수정은 멈춘 에이전트를 깨우지 않고 오케스트레이터가 직접 고친다(재개하면 긴 대화 기록을 다시 읽어 한도를 크게 쓴다).
- 검토 에이전트에는 필요한 절만 읽도록 범위를 지정한다.

## 코드 — 재개 명령

```bash
# 문구 진행 위치
grep -n '^## .*(v0.4' _workspace/slide_copy/*.md
# 배치 진행 위치
grep -n '^## .*(v0.4 배치' _workspace/slide_ui/*.md
# 조립된 BLOCK 확인
grep -n 'BLOCK:' topics/sk-hynix-ai-agent-guide-edu/index.html
# shrimp 상태: list_tasks(status=all)
```

- 빌드 보조 도구(세션 scratchpad, 저장소 밖): `splice_blocks.py`(BLOCK 조각을 index.html에 끼움, `--extract`로 현재 BLOCK 추출), `check_fragment.py`(조각 규칙 검사: 장면 ID 순서, data-skill, 필수 요소, 인라인 style·br·hex·외부 이미지·현업가이드 파일명·CSS에 없는 클래스·data-editable 누락). 새 세션에서는 이 설명대로 다시 만든다.
- 빌더 공통 지침(세션 scratchpad `builder_brief.md`): 빌더는 `index.html`에 직접 쓰지 않고 `scratchpad/blocks/<NAME>.html` 조각을 장면마다 덧붙여 저장한다. 문구는 slide_copy v0.4 절, 구조는 slide_ui v0.4 배치 절을 따른다. `check_fragment.py` 통과 후 오케스트레이터가 `splice_blocks.py`로 조립한다.
- 부록 확인 결과(2026-09-16): A07–A10 API 이름은 Context7 공식 문서와 일치. A10은 승인 카드만 자른 `assets/img/appendix/cowork-hitl-approval-card.png`를 쓴다.
- Playwright 넘침 검사: 로컬 서버(`python -m http.server 8765`, topic 폴더)에서 `index.html`을 열고 장면마다 여백 밖 요소·잘림·가로 삐짐·출처 줄 위 여유(px)를 잰다. `.speaker-note`·`.source`·`.deck-footer`·`.page-num`은 측정에서 뺀다. 57장 기준 문제 0건(2026-09-16). 실습 안내판처럼 격자가 칸을 채우는 장면은 출처 위 여유 0px이 정상이다.

## 테스트 방법

- 문구·배치 단계: 위 grep으로 장면 수가 묶음의 장면 수와 같은지 확인한다.
- 빌드 단계: `check_fragment.py` 통과 → 조립 → `python scripts/validate_topic.py`, `npx hyperframes lint`, `python scripts/sync_overview.py <topic> --check` → Playwright 넘침 검사 0건.

## 향후 개선사항

- 에이전트 동시 실행 수를 줄이면 한도에 걸릴 위험은 줄지만 전체 시간이 늘어난다. 지금은 장면 단위 저장으로 중단 손실을 줄인다.
- 본문이 화면 위쪽에 몰려 아래가 비는 장면(예: S24·S27·S29)은 마지막 시각 검토에서 세로 균형을 다시 본다.
