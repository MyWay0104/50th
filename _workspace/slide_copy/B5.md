# B5 화면 문구 · 14:20–15:00 · Claude Code로 앱 개선

작성: slide_content_writer · 2026-09-14 · 대상 장면: S28–S33 (6장)
기준: `_workspace/ppt_content_plan.md` v0.2 매핑표 + v0.1 콘티(B5), `docs/slide-plan-2026-09-13.md` 2-6·2-7절, `docs/research_claude.md` 1·2절, 조사정리 4-C·5·6-5·6-6·8절, 출처목록 2·9절, `_workspace/slide_ui/00_component_catalog.md` 2·4·5절

## 공통 표기

- eyebrow: `B5` 칩 + `Claude Code로 앱 개선`. 구간 첫 장면 S28만 시각 칩 `14:20–15:00`을 붙인다.
- 풋터: `AI Agent Guide · SK hynix 사내 교육`
- 글자 수는 공백 포함. 제목 20자, title-bullets 항목 25자, split 해석 항목 18자 안팎, compare 2열 항목 22자·3열 항목 14자, steps `.row` 4칸 설명 13자 이내(B3 S16 폭 기준).
- 시간 배분(콘티 1절): 이론 S28 4 + S29 4 + S30 5 + S31 3 = 16분(14:20–14:36), 안내 실습 S32 21분(14:36–14:57), 확인 S33 3분(14:57–15:00). 합계 40분.
- Claude Code 기능 설명은 `research_claude.md`의 공식 문서 범위 안에서만 썼다. 명령 UI(`/context`, `/compact` 등)는 넣지 않았다(보류 참고).
- 사실 경계 번호는 계획서 2-6절 13항목 번호다.

---

## S28 · 탐색·계획·구현·검증

- data-skill: `steps` (`.steps.row` 가로 4단계)
- eyebrow: `B5` · `Claude Code로 앱 개선` · 시각 칩 `14:20–15:00`
- 제목: 탐색·계획·구현·검증
- 단계:
  1. 탐색 — 구조와 정상 동작 확인
  2. 계획 — 바꿀 곳과 완료 조건 (`is-accent`)
  3. 구현 — 계획한 범위만 수정
  4. 검증 — 새 기능·기존 기능 확인
- 예시 칩 (`.chip-row`, 개념 예시, 단계 2 아래): `변경: 결과 표시 함수` · `확인: 없는 키워드` · `확인: 기존 질문`
- 보조 문장(callout): **계획 먼저** 여러 파일을 바꾸거나 방법이 불확실할 때
- 출처: Claude Code 공식 문서 Best practices
- 발표자 노트: 이번 40분의 목표는 오전 앱의 기존 코드를 읽고 작은 기능 하나를 고친 뒤, 제대로 됐는지 확인하는 것입니다. 먼저 Claude Code에게 바로 고치게 하지 않고, 현재 구조를 읽고 지금의 정상 동작이 무엇인지부터 확인하게 합니다. 그다음 바꿀 곳과 완료 조건을 계획으로 정리하게 하고, 그 계획을 우리가 읽어 본 뒤 계획한 범위만 수정하게 합니다. 공식 모범사례도 여러 파일을 바꾸거나 방법이 불확실할 때 계획을 먼저 세우라고 권하며, 한두 줄짜리 작은 수정까지 긴 계획을 세울 필요는 없습니다. 고치기 전에 정상 동작을 먼저 확인해 두어야 하는 이유는 무엇일까요? 한 분만 짧게 답해 주세요.
- 제작 메모: 콘티 화면 문구 3개(현재 구조와 동작 확인 / 변경할 부분과 완료 기준 정리 / 작은 수정 후 결과 확인)를 화면 구성 지시대로 네 단계로 나눴다. 예시 칩은 실제 파일 이름 대신 역할 이름을 쓴다(최종 ZIP 확인 전).
- 체크리스트 대조: research_claude 2절 "아키텍처를 처음부터 견고하게" 계열 오해를 막으려고 callout에 계획이 필요한 조건을 적고 노트에 "작은 수정까지 긴 계획 불필요"를 넣음(공식 모범사례 범위). 강조 칸을 "계획"에 둔 이유: B4 S25 완료 조건과 이어지는 단계. 13번: 파일명·명령 없음. 명령 UI(계획 모드 등)는 교육 환경 확인 전이라 이름을 쓰지 않음.
- 자산 대기: 없음

## S29 · 웹앱의 입력부터 데이터까지

- data-skill: `split` (`.wide-left` 권장)
- eyebrow: `B5` · `Claude Code로 앱 개선`
- 제목: 웹앱의 입력부터 데이터까지
- 시각 쪽 (코드 카드 `.code-card`, 5줄)
  - 성격 배지: 개념 예시
  - 코드 제목: 한 파일 안의 화면·처리·데이터
  - 코드 (한 줄 42자 이하):
    ```python
    q = st.text_input("질문")  # 화면: 입력
    if q:
        rows = search(q)       # 데이터: 조회
        ans = answer(q, rows)  # 처리: 규칙·모델
        st.write(ans)          # 화면: 결과
    ```
  - 캡션: 함수 이름은 가상 · 실제 실습 코드와 다를 수 있음
- 해석 항목:
  1. 화면: 입력을 받고 결과 표시
  2. 처리: 업무 규칙·모델 연결
  3. 데이터: 기사 본문·DB 조회
- 출처: Streamlit Architecture 문서 · Streamlit API 문서(st.text_input, st.write) · 근거: 사용자 제공 강의안
- 발표자 노트: 화면에서 입력을 받고, 규칙과 모델 호출로 처리하고, 데이터를 조회해 다시 화면에 보여 주는 전체 흐름을 다루는 것을 흔히 풀스택이라고 부릅니다. Streamlit에서는 이 흐름을 Python 코드 몇 줄로 이을 수 있어서, 왼쪽 예시처럼 입력 칸, 조회, 모델 호출, 결과 표시가 한 파일 안에 차례로 나옵니다. Streamlit은 Python 서버가 코드를 실행하고 브라우저는 화면을 보여 주는 구조라서, 오전 앱은 화면과 처리 사이에 별도 백엔드 서버를 두지 않았고 모델만 오전에 본 추론 서버에 API로 요청합니다. React나 별도 백엔드를 처음부터 모두 배우기보다, 지금 바꿀 부분이 화면인지, 처리인지, 데이터인지 알아보는 것부터 시작하면 AI가 만든 코드도 더 잘 판단할 수 있습니다. 앞 시간에 적은 개선 기능은 화면, 처리, 데이터 중 어디를 바꾸는 일인가요? 손가락으로 1, 2, 3을 표시해 주세요.
- 제작 메모: 코드는 실제 실습 코드가 아닌 표준 예시다. `st.text_input` 반환값을 `if`로 확인한 뒤 `st.write`로 표시하는 형태는 Context7 Streamlit 공식 문서 예시와 같다. `search`, `answer`는 가상 함수 이름이다. 주석은 27열에 맞췄고 한 줄 최대 38자(4줄)다. 해석 항목 2의 "업무 규칙·모델 연결"은 S24 표의 두 영역 이름을 그대로 써서 두 장면의 용어를 맞췄다.
- 체크리스트 대조: 13번 해당. 모델 이름·주소·키 없이 가상 함수 이름만 사용. 콘티 화면 구성 "별도 API 서버가 없는 구조에 임의로 서버를 추가하지 않는다"를 지키고, 노트에서 Streamlit 서버–브라우저 구조를 공식 문서 범위로 설명. research_claude 2절 "기술스택 공부는 필수이고 누구나 풀스택 개발 가능"을 "바꿀 부분의 역할부터 알면 AI가 만든 결과를 더 잘 판단"으로 바꿈. 콘티 항목 3 "데이터: 조회와 저장"은 오전 앱이 저장 기능을 가진다는 근거가 없어 "기사 본문·DB 조회"로 수정.
- 수정 기록(2026-09-14): W23 반영 — 노트 3문장 "오전 앱에는 별도 API 서버를 따로 두지 않았습니다"를 "오전 앱은 화면과 처리 사이에 별도 백엔드 서버를 두지 않았고 모델만 오전에 본 추론 서버에 API로 요청합니다"로 교체(B1 S06 모델 처리 위치와 충돌 제거, 사실 경계 4·11번). 노트 6문장 유지. 실제 실습 구조가 다르면 보류 3(강사 검토 Q7)에 따라 다시 맞춘다.
- 자산 대기: 없음 (코드 카드로 완성. 실제 실습 코드 캡처로 바꿀지는 보류 목록 참고)

## S30 · 프로젝트 지침과 작업 기록

- data-skill: `compare` (2열)
- eyebrow: `B5` · `Claude Code로 앱 개선`
- 제목: 프로젝트 지침과 작업 기록
- 열:
  - 열 1 — 작은 라벨: 매번 필요한 안내 / 열 제목: CLAUDE.md
    - 실행·테스트 방법
    - 주요 파일의 역할
    - 규칙 예: 숫자는 코드로 계산
    - 열 메모: 모델이 읽는 지침 · 권한 설정은 아님
  - 열 2 — 작은 라벨: 이번 작업의 상태 / 열 제목: 작업 기록 파일
    - 결정한 내용과 이유
    - 확인한 결과와 검증 방법
    - 남은 문제와 다음 행동
    - 열 메모: 긴 대화는 요약되며 세부가 빠질 수 있음
- 강조 열: 두지 않음. 두 파일은 우열이 아니라 쓰임이 다르므로 아래 callout으로 공통 원칙을 강조한다.
- 보조 문장(callout): **파일로 남기기** 다음 작업에 필요한 사실은 대화가 아닌 파일에
- 출처: Claude Code 공식 문서 Memory · Context window
- 발표자 노트: 새 대화를 시작할 때마다 프로젝트 구조와 실행 방법을 다시 설명하는 대신, CLAUDE.md에 반복해서 필요한 안내를 적어 두면 Claude Code가 작업을 시작할 때 읽습니다. 다만 CLAUDE.md는 모델이 읽는 지침일 뿐 권한을 막는 보안 장치가 아니므로, 접근을 제한해야 하는 일은 권한 설정과 실행 환경에서 막아야 합니다. 오른쪽 작업 기록에는 이번에 결정한 내용, 확인한 결과, 남은 문제를 적어 둡니다. 대화에는 주고받은 말뿐 아니라 읽은 파일과 도구 출력도 쌓이고, 길어지면 요약되면서 세부가 빠질 수 있어서 긴 대화가 곧 영구 기억은 아니기 때문입니다. 개인 전역 설정을 통째로 복사하기보다 이 프로젝트에 필요한 몇 줄만 적는 것이 좋습니다. 지금 작업을 내일 이어서 한다면 꼭 남겨야 할 내용 두 가지는 무엇일까요?
- 체크리스트 대조: 7번 해당(핵심 장면). 열 메모와 노트에 "모델이 읽는 지침 · 권한 설정은 아님"을 명시(S19 노트 "프롬프트≠접근 제한"과 같은 원칙). 콘티 제목 "프로젝트 지침과 작업 기억"을 "프로젝트 지침과 작업 기록"으로 바꿈: "기억"이 Claude Code가 스스로 영구 기억한다는 뜻으로 읽힐 수 있어서다(조사정리 5절 "긴 대화=영구 기억" 오해). research_claude 1절 6·7번 범위만 사용하고 auto memory·명령 UI는 설명하지 않음. 규칙 예시 "숫자는 코드로 계산"은 research_claude 1절 6번 예시와 B3 S15 원칙을 따름. 콘티 제작 메모대로 전역 개인 설정을 복사하지 않음.
- 자산 대기: 없음

## S31 · 변경 내용과 실제 사용 확인

- data-skill: `compare` (`.cols-3`)
- eyebrow: `B5` · `Claude Code로 앱 개선`
- 제목: 변경 내용과 실제 사용 확인
- 열:
  - 열 1 — 작은 라벨: Git diff / 열 제목: 무엇이 바뀌었나
    - 바뀐 파일과 줄 확인
    - 범위 밖 변경은 없나
  - 열 2 — 작은 라벨: 실행 결과 / 열 제목: 조건을 만족하나
    - 완료 조건 입력 실행
    - 기존 질문도 다시 실행
  - 열 3 (**핵심 열, `is-accent`**) — 작은 라벨: 사용 화면 / 열 제목: 목적에 맞게 쓰나
    - 직접 입력해 보기
    - 안내 문구가 이해되나
- 보조 문장(callout, 성격 배지 "개인 기록"): **Dokmo** 한글 입력·안내 부족은 직접 써 본 점검에서 드러남
- 출처: 근거: 강사 개인 기록(Dokmo 수동 점검 기록) · Claude Code 공식 문서 Best practices
- 발표자 노트: 제가 만든 개인 웹앱 Dokmo에서는 앱이 동작하는 상태에서도, 직접 써 보는 수동 점검에서 한글 이름 입력 문제와 다음에 무엇을 해야 할지 알려 주는 안내가 부족한 문제가 드러났습니다. 그래서 변경 확인은 세 가지를 따로 봅니다: 무엇이 바뀌었는지는 바뀐 줄을 보여 주는 Git diff로, 조건을 만족하는지는 실행 결과로, 목적에 맞게 쓸 수 있는지는 실제 화면에 직접 입력해 봅니다. Claude Code에게 확인 방법을 알려 주고 실행 결과를 받을 수는 있지만, 그 결과가 맞는지와 사용자에게 편한지는 사람이 마지막으로 봐야 합니다. 여러분이 고른 기능은 화면에서 무엇을 입력하고 무엇이 보이면 확인이 끝날까요?
- 체크리스트 대조: 12번 해당. Dokmo 사례에 이용자 수·수정 건수·기간을 넣지 않고 callout 배지 "개인 기록"과 출처 줄 "근거: 강사 개인 기록"으로 한정. 조사정리 4-C 범위("현재 사이트를 재검증한 결과 아님")를 넘지 않게 점검 항목 이름 두 개만 씀. research_claude 2절 "Claude Code가 결과를 검증하므로 믿어도 된다" → "검증 방법을 주고 실제 확인 결과를 받되, 중요한 판단은 사람이"로 노트에 반영. 콘티 화면 문구 3개를 열 제목(질문)과 작은 라벨(확인 수단)로 나눠 3열에 배치. 3분 장면이라 노트는 4문장으로 둠.
- 자산 대기: 없음 (텍스트로 완성. Dokmo 실제 화면을 넣을지는 보류 목록 참고)

## S32 · 실습 6 · 오전 앱의 작은 개선

- data-skill: `title-bullets` (실습 안내판)
- eyebrow: `B5` · `Claude Code로 앱 개선`
- 제목: 실습 6 · 오전 앱의 작은 개선
- 안내 줄(board-meta): 14:36–14:57 · 21분 · 실습 가이드 해당 장
- 보조 문장(scene-subtitle): 기본 과제: 검색 결과 없음 안내 · 이미 있으면 근거 표시
- 실습 단계:
  1. 구조를 읽고 계획·완료 조건 정리
  2. 계획한 범위만 구현
  3. 새 입력·기존 입력 둘 다 실행
- 시간 블록 (`.time-blocks.row`, 합계 21분):
  - 4분 · 탐색
  - 4분 · 계획
  - 8분 · 구현
  - 5분 · 검증 (`is-accent`)
- 완료 조건: 기존 질문 결과가 그대로이고 새 기능이 동작한다
- 막히면: Claude Code가 안 되면 짝과 한 화면으로, 앱의 모델 호출이 막히면 준비된 결과로 확인합니다
- 실습 가이드 장: 실습 가이드 해당 장 (최종 HTML 장 이름 확인 후 교체)
- 출처: Claude Code 공식 문서 Best practices · 근거: 사용자 제공 강의안·시간표
- 발표자 노트: 처음 4분은 Claude Code에게 오전 앱의 구조와 수정할 위치를 설명하게 하고, 다음 4분은 바꿀 곳과 완료 조건을 담은 계획을 받아 우리가 읽고 확인합니다. 이어서 8분 동안 계획한 범위만 구현하게 하는데, 전체 앱을 다시 짜거나 기능을 여러 개 붙이는 것은 이번 범위에 넣지 않습니다. 마지막 5분은 없는 키워드 같은 새 기능용 입력과 오전에 정상 동작하던 질문을 둘 다 실행하고, 바뀐 내용도 확인합니다. 기본 과제인 검색 결과 없음 안내가 이미 되어 있다면 근거 표시(또는 결과 다운로드)로 바꿔 진행하세요. Claude Code가 파일 수정이나 명령 실행 승인을 요청하면 무엇을 하려는지 읽고 나서 승인하세요. 앱의 모델 호출이 막히면 준비된 결과로 화면과 규칙 부분만 확인하고, Claude Code 환경이 준비되지 않은 분은 앞 시간에 정한 짝과 같은 화면으로 진행합니다.
- 체크리스트 대조: 5번 관련. 노트에 "승인 요청을 읽고 승인"을 넣어 허용된 도구·권한 범위 안에서 일한다는 점을 실습 행동으로 바꿈(요청이 올 때의 행동으로만 적고 교육 환경 권한 설정은 보류). 초안 완료 조건 "정상 입력 유지와 새 기능 확인"을 기존 질문을 먼저 두는 문장으로 바꿔 회귀 확인을 앞세움. 13번: 명령·주소 없음. 콘티 제작 메모 "API 장애 시 저장된 도구 결과로 화면·업무 규칙 검증"을 fallback 문구로 옮김.
- 수정 기록(2026-09-14): W24 반영 — 화면 막히면 문구를 "Claude Code가 안 되면 짝과 한 화면으로, 앱의 모델 호출이 막히면 준비된 결과로 확인합니다"로 교체해 가장 흔한 실패(Claude Code 환경 미비)의 짝 경로를 안내판에 표시(오전 S20 W9와 같은 기준). 노트의 짝 경로 문장은 그대로 둠(노트 6문장 변화 없음).
- 제작 메모: 강조 칸은 B5 목표(개선·검증)의 핵심인 "검증"에 둔다. 기본 과제 줄은 콘티 화면 구성 지시("검색 결과 없음 안내를 기본 과제로 표시")에 따라 scene-subtitle로 넣었다. 안내판에 넘친다고 UI 담당이 알려 오면 "기본 과제: 검색 결과 없음 안내"만 남기고 대체 과제는 노트로 옮긴다.
- 자산 대기: 안내판에는 이미지 자리가 없다. 계획서 2-7의 Streamlit 개선 전후 화면은 실습 중 실행 화면으로 대신한다(슬라이드 삽입 없음).

## S33 · 다른 입력으로 재확인

- data-skill: `title-bullets`
- eyebrow: `B5` · `Claude Code로 앱 개선`
- 제목: 다른 입력으로 재확인
- 보조 문장(scene-subtitle): 실행 전에 예상 결과를 먼저 말하기
- 항목 (`.bullets`, 3개):
  1. **기존 정상 입력** 이전과 같은 결과인가
  2. **새 기능 입력** 예상한 안내가 나오나
  3. **설명** 바꾼 코드가 하는 일을 한 문장으로
- 보조 문장(callout): 확인하지 못한 입력은 **미확인**으로 적어 둔다
- 출처: 근거: 강사 Python 교육 진행 규칙(예측·실행·설명·입력 변형)
- 발표자 노트: 실습에서 한 번 성공한 입력 말고, 옆 사람이 골라 준 다른 입력을 하나 받아서 실행 전에 결과를 먼저 예상해 말해 보겠습니다. 기존 정상 질문이 이전과 같은 결과를 내는지, 새 기능에 해당하는 입력에서 예상한 안내나 표시가 나오는지 차례로 확인합니다. 결과가 예상과 다르면 실패라기보다, 바꾼 코드가 어떤 조건에서 동작하는지 알려 주는 단서입니다. 두 분 정도 바꾼 코드가 무엇을 하는지 한 문장으로 설명해 주세요. 시간 안에 확인하지 못한 입력은 미확인으로 적어 두면 자유 실습에서 이어 갈 수 있고, 15시 20분에는 이런 작업 경험과 자료를 다시 쓸 수 있게 정리하는 방법으로 넘어갑니다.
- 체크리스트 대조: 12번 통과(수치 없음). 콘티 화면 구성 "검증하지 못한 항목은 미확인으로 남긴다"를 callout으로 올림. 콘티 항목 3개에 판단 기준(같은 결과인가·예상한 안내가 나오나)을 붙여 확인 질문 형태로 바꿈. 조사정리 5절 "Top-down이면 기초를 배울 필요가 없음" 오해를 막는 예측·설명 활동을 노트에 넣어 B5 목표(개선·검증)를 되묻게 함.
- 자산 대기: 없음

---

## 보류(사용자 확인 필요)

1. S30: 매핑표(`ppt_content_plan.md` 상단)의 제목 "프로젝트 지침과 작업 기억"을 이 문서의 "프로젝트 지침과 작업 기록"으로 바꿀지. 반영하면 매핑표·visual plan 제목도 동기화해야 한다(오케스트레이터).
2. S28·S32: 사내 교육 환경에서 계획 모드 같은 명령 UI를 쓸지. 쓴다면 이름과 동작을 교육 환경에서 재확인한 뒤 실습 가이드에 넣고, 슬라이드 노트에는 한 문장만 더한다.
3. S29: 실제 실습 코드의 화면·조회·모델 호출 구조가 코드 카드 예시(`st.text_input` → 조회 → 모델 호출 → `st.write`)와 같은 흐름인지. 다르면 실습 코드 기준으로 교체한다. 계획서 2-7의 S29 Streamlit 캡처를 코드 카드 대신 쓸지도 함께 정한다. 노트 3문장(W23, "화면과 처리 사이에 별도 백엔드 서버를 두지 않았고 모델만 추론 서버에 API로 요청")도 이 확인에 달려 있다. 별도 백엔드(FastAPI 등)가 있는 판본이면 코드 카드와 이 문장을 그 구조로 바꾼다(강사 검토 Q7).
4. S30: `/context`·`/compact` 등 맥락 관리 명령과 auto memory를 소개할지. 현재는 교육 환경 재확인 전이라 뺐다. 소개한다면 명령 동작을 교육 환경에서 확인한 뒤 노트에 추가한다.
5. S30: 실습 폴더에 CLAUDE.md 예시 파일을 미리 넣어 둘지(B6 S36·S37과 연결).
6. S31: Dokmo 사례 표현 확인 — "앱이 동작하는 상태에서도 수동 점검에서 한글 이름 입력·안내 부족이 드러남"이 강사 기록과 맞는지. 공개 가능한 Dokmo 화면(개선 전후)이 있으면 넣을지(계획서 2-7 S31 대기).
7. S31·S32: 수강생 실습 폴더가 Git 저장소이고 Git diff를 볼 수 있는 환경인지. 아니라면 S31 열 1의 작은 라벨과 노트 문장을 바꿔야 한다.
8. S32: 실습 가이드(HTML)의 해당 장 이름과 앵커. 기본 과제(검색 결과 없음 안내)가 오전 앱에 이미 있는지(B4 보류 8과 같은 항목), 대체 과제(근거 표시·결과 다운로드) 시작 코드와 fallback용 "준비된 결과"가 있는지.
9. S32: 교육 환경의 Claude Code 권한 설정. 노트의 "승인을 요청하면 읽고 승인" 문장은 수정·명령 실행 전에 승인을 묻는 설정을 전제로 한다.
10. S33: 새 기능 확인용 입력(실습 데이터에서 실제로 결과가 0건인 키워드)을 강사가 미리 준비할지.

---

# v0.4 보강 (2026-09-15)

작성: slide_content_writer · 기준: `docs/slide-plan-v0.4-visual-narrative.md` 0·2·4·5·7절, 9절 B5, 11-2(16항목), 12-1 · `_workspace/slide_ui/00_component_catalog.md` 7절 · 조사정리 4-C·5절 · `ppt_qa_report.md` Q1–Q7 · `docs/research_claude.md` 1·2절

## 공통 표기 (v0.4)

- 글자 수는 공백 포함. 요지 40자 이내, 설명 문단 120자 이내(1줄 약 50자).
- 높이는 카탈로그 7-2절 예산으로 추정했다. 본문 가용: 요지 + 설명 3줄 401px, 2줄 447px, 구간 첫 장면(S28)은 각 −49px. 실습 안내판(S32)은 요지 대신 board-meta가 들어가 설명 2줄 기준 약 477px.
- 사실 경계 번호는 계획서 11-2절 16항목 번호다.
- 현업가이드(`skh_llm_guide`)의 파일명·함수명·장 이름·CLAUDE.md 발췌는 화면·노트·출처 줄 어디에도 쓰지 않았다. 계획서 S30의 "현업가이드 CLAUDE.md 발췌 6줄"과 S32의 "edit app.py? [y/n]"은 이 규칙에 따라 일반 표현으로 바꿨다.
- 화면에 기획 ID를 쓰지 않았다. 계획서 S32의 "S29 ①·③"은 "화면·처리 부분"으로 적었다.
- Q3(실습 폴더가 Git 저장소인지)이 미확인이라 화면에는 "Git diff" 대신 "바뀐 줄 비교"를 쓰고, Git 이야기는 노트에서 조건문으로만 한다.
- eyebrow·풋터는 v0.3과 같다. 시각 칩은 S28에만 둔다.

## S28 · 탐색·계획·구현·검증 (v0.4)

- data-skill: `steps` `.steps.row` (v0.3과 같음)
- 막 태그: `<strong>전 · 뒤집다 (2)</strong>` + "계획하고, 고치고, 확인한다" (강조어 제외 14자, 전체 약 26자)
- 화면 문구:
  - eyebrow: `B5` · `Claude Code로 앱 개선` · 시각 칩 `14:20–15:00`
  - 제목: 탐색·계획·구현·검증
  - 요지 (37자): 바로 고치게 하지 말고, 읽고 계획하게 한 뒤 범위 안에서만 고친다
  - 설명 문단 (81자·3문장, 2줄): 여러 파일을 바꾸거나 방법이 불확실할 때는 계획을 먼저 세운다. 한두 줄 수정에 긴 계획은 필요 없다. 검증에서 문제가 보이면 탐색으로 돌아간다.
  - 본문 (`.steps.row` 카드 4개, 카드 사이 `→`는 CSS가 그림). 각 카드 = 번호 + 단계 이름 + 설명 2줄(`.step-desc` 두 줄: 할 일 / 산출물):
    1. 탐색 — 구조·정상 동작 / 산출물: 구조 설명
    2. 계획 (`is-accent`) — 바꿀 곳·완료 조건 / 산출물: 계획 문서
    3. 구현 — 계획한 범위만 / 산출물: 바뀐 줄
    4. 검증 — 새 기능·기존 기능 / 산출물: 실행 결과
  - 출처: Claude Code 공식 문서 Best practices · 뿌리강의 REMASTERED(습관 문구)
- v0.3 대비 변경:
  - 뺌: 예시 칩 줄(`변경: 결과 표시 함수` · `확인: 없는 키워드` · `확인: 기존 질문`)과 callout "계획 먼저 · 여러 파일을 바꾸거나 방법이 불확실할 때". 요지·설명과 막 태그가 들어오면 v0.3 구성(약 519px)이 예산(약 398px)을 넘는다. callout 문장은 설명 1문장으로 옮겼고, 예시 칩은 산출물 줄이 대신한다.
  - 줄임: 카드 설명을 10자 이하 1줄로("구조와 정상 동작 확인" → "구조·정상 동작", "바꿀 곳과 완료 조건" → "바꿀 곳·완료 조건", "계획한 범위만 수정" → "계획한 범위만", "새 기능·기존 기능 확인" → "새 기능·기존 기능").
  - 새로 넣음: 막 태그, 요지, 설명 문단, 카드마다 산출물 한 줄(구조 설명 / 계획 문서 / 바뀐 줄 / 실행 결과).
  - 계획서 산출물 "diff"는 Q3 미확인이라 "바뀐 줄"로 바꿈. 계획서의 "검증 → 탐색으로 되돌아가는 점선"은 카탈로그에 되돌림 화살표가 없어 설명 3문장으로 대신함.
  - 계획서 설명 초안의 첫 문장(네 단계 정의)은 카드가 보여 주므로 뺐고, 넷째 문장(뿌리강의 습관 문구)은 노트로 옮겼다.
  - 산출물 줄 표기와 캡션 (v0.4 UI 요청 반영, B5-v4-1): 카드 둘째 줄 "→ 구조 설명" / "→ 계획 문서" / "→ 바뀐 줄" / "→ 실행 결과"를 "산출물: 구조 설명" / "산출물: 계획 문서" / "산출물: 바뀐 줄" / "산출물: 실행 결과"로 바꿈(각 1줄, G01 시간표 표기와 같음). 캡션 "개념 예시 · → 뒤는 단계마다 남는 산출물"은 삭제(캡션이 있으면 출처 줄 위 여유가 19px로 기준 24px 미만, 빼면 76px). "산출물:" 머리가 캡션의 설명을 대신한다. 노트 셋째 문장 "각 카드의 화살표 뒤는"도 "각 카드의 산출물 줄은"으로 맞춤.
- 시각 자료: 인포그래픽(`b5/explore-plan-code-verify` 논리 이름, 기존 `.steps.row` 카드에 산출물 줄 추가). 카드 4개, 강조는 "계획" 한 곳(B4 완료 조건과 이어지는 단계). 카드 설명 폭 약 300px(28px 글자 10자). 높이 추정: 카드 약 311 + 캡션 58 → 약 369px(예산 398px).
- 발표자 노트 (최종 전문): 이번 40분의 목표는 오전 앱의 기존 코드를 읽고 작은 기능 하나를 고친 뒤, 제대로 됐는지 확인하는 것입니다. 네 카드 순서대로, 먼저 Claude Code에게 현재 구조와 지금의 정상 동작을 설명하게 하고, 바꿀 곳과 완료 조건을 계획으로 받아 우리가 읽어 본 뒤, 계획한 범위만 수정하게 하고, 새 기능과 기존 기능을 둘 다 실행해 확인합니다. 각 카드의 산출물 줄은 그 단계가 끝났을 때 손에 남는 것, 곧 구조 설명·계획 문서·바뀐 줄·실행 결과이고, 검증에서 문제가 보이면 탐색으로 되돌아갑니다. 공식 모범사례도 여러 파일을 바꾸거나 방법이 불확실할 때 계획을 먼저 세우라고 권하며, 한두 줄짜리 작은 수정까지 긴 계획을 세울 필요는 없습니다. 참고 강의에서 말하는 "마지막 정상 지점과 첫 실패 지점을 증거로 말하기"도 같은 습관이어서, 단계마다 산출물이 남아 있어야 어디까지 정상이었는지 말할 수 있습니다. 고치기 전에 정상 동작을 먼저 확인해 두어야 하는 이유는 무엇일까요? 한 분만 짧게 답해 주세요.
- 체크리스트 대조:
  - 13번 통과: 파일명·명령 없음. 계획 모드 같은 명령 UI 이름은 교육 환경 확인 전이라 쓰지 않음(v0.3 보류 2 유지).
  - 12번 통과: 수치 없음.
  - research_claude 1절 4번·2절: "작은 수정까지 거대한 계획 불필요"를 설명 2문장으로 화면에 올림(공식 모범사례 범위).
  - 산출물 "바뀐 줄"로 Q3(Git 여부)에 기대지 않음.
  - 나머지 해당 없음.
- 자산 대기: 없음

## S29 · 웹앱의 입력부터 데이터까지 (v0.4)

- data-skill: `split` `.wide-left` (v0.3과 같음)
- 막 태그: 해당 없음
- 화면 문구:
  - eyebrow: `B5` · `Claude Code로 앱 개선`
  - 제목: 웹앱의 입력부터 데이터까지
  - 요지 (30자): Streamlit 앱도 화면·처리·데이터로 나눠 읽는다
  - 설명 문단 (102자·2문장, 2–3줄): 입력을 받고, 데이터를 조회해 처리한 뒤 화면에 다시 보여 주는 전체 흐름을 흔히 풀스택이라 부른다. 화면·서버 기술을 다 배우기 전에, 바꿀 부분이 어느 층인지부터 알아본다.
  - 본문 왼쪽 (코드 카드 `.code-card`, 5줄 — 역할이 붙은 줄은 4줄)
    - 코드 제목: 개념 예시 · 함수 이름은 가상 · 실제 실습 코드와 다를 수 있음
    - 코드 (v0.3과 같음, 한 줄 38자 이하):
      ```python
      q = st.text_input("질문")  # 화면: 입력
      if q:
          rows = search(q)       # 데이터: 조회
          ans = answer(q, rows)  # 처리: 규칙·모델
          st.write(ans)          # 화면: 결과
      ```
  - 본문 오른쪽 (플레이스홀더 v2)
    - [IMG-PLACEHOLDER · P-S29]
    - 넣을 자료: 오전 Streamlit 앱 화면 — 입력 칸·결과 영역을 왼쪽 코드 줄과 짝지어 표시
    - 조건: 모델명·주소·키 가리기 · 실행일 표기 · 1600×900 이상
    - 대체: 확보 전에는 왼쪽 코드 예시로 설명
  - 출처: Streamlit Architecture 문서 · Streamlit API 문서(st.text_input, st.write) · 근거: 사용자 제공 강의안
- v0.3 대비 변경:
  - 뺌: 해석 항목 3줄(화면 / 처리 / 데이터). 코드 주석이 같은 세 역할을 줄마다 보여 주고, 오른쪽 열은 플레이스홀더가 차지한다. 별도 성격 배지와 코드 아래 캡션도 코드 제목 한 줄로 합쳐 높이 약 96px를 줄였다.
  - 바꿈: 코드 제목 "한 파일 안의 화면·처리·데이터" → "개념 예시 · 함수 이름은 가상 · 실제 실습 코드와 다를 수 있음". 실제 실습 코드가 한 파일인지 모르므로(Q7) "한 파일 안의"를 화면에서 뺐다.
  - 새로 넣음: 요지, 설명 문단, 플레이스홀더 P-S29.
  - 계획서 요지 초안("풀스택은 … Streamlit은 그걸 Python 한 파일로 잇는다", 54자)은 길고 "한 파일"이 Q7에 기대어 바꿨다. 설명 초안 둘째 문장("오전 앱은 별도 백엔드 서버 없이 모델만 추론 서버에 API로 요청했다")도 Q7 미확인이라 화면에서 빼고 노트에만 v0.3 표현으로 남겼다.
  - 계획서의 "주석 라벨을 잇는 얇은 선"은 카탈로그에 선 클래스가 없어 코드 주석으로 대신하고, 원 번호(①–④)는 JetBrains Mono에 글리프가 없을 수 있어 넣지 않았다.
- 시각 자료: 코드 카드(5줄) + 플레이스홀더 v2 `P-S29`(오른쪽 열 약 703px, 화면 절반 이하). 강사 캡처가 들어오면 P-S20 캡처(`b3/streamlit-app.png`)를 같이 쓸 수 있다. 높이 추정: 코드 카드 약 330px, 플레이스홀더 약 339px(예산 401px).
- 발표자 노트 (최종 전문): 화면에서 입력을 받고, 데이터를 조회하고, 규칙과 모델 호출로 처리해 다시 화면에 보여 주는 전체 흐름을 다루는 것을 흔히 풀스택이라고 부릅니다. Streamlit에서는 이 흐름을 Python 코드 몇 줄로 이을 수 있어서, 왼쪽 예시처럼 입력 칸, 조회, 모델 호출, 결과 표시가 차례로 나오고, 줄 끝 주석이 각 줄의 역할입니다. 오른쪽 자리에는 오전 앱의 실제 화면이 들어가는데, 입력 칸과 결과 영역이 왼쪽 코드의 어느 줄에 해당하는지 짝지어 보면 됩니다. Streamlit 공식 문서 설명처럼 Python 서버가 코드를 실행하고 브라우저는 화면을 보여 주는 구조라서, 오전 앱은 화면과 처리 사이에 별도 백엔드 서버를 두지 않았고 모델만 오전에 본 추론 서버에 API로 요청합니다. React나 별도 백엔드를 처음부터 모두 배우기보다, 지금 바꿀 부분이 화면인지, 처리인지, 데이터인지 알아보는 것부터 시작하면 AI가 만든 코드도 더 잘 판단할 수 있습니다. 앞 시간에 적은 개선 기능은 화면, 처리, 데이터 중 어디를 바꾸는 일인가요? 손가락으로 1, 2, 3을 표시해 주세요.
- 체크리스트 대조:
  - 13번 해당: 모델 이름·주소·키 없이 가상 함수 이름만. 플레이스홀더 조건에 모델명·주소·키 가리기를 명시.
  - 11번·4번 관련: 노트 넷째 문장(W23)은 오전 모델 경로를 B1 S06과 같게 설명한다. 오전 실습 코드 구조(Q7)가 확인되지 않아 화면에는 올리지 않았다.
  - research_claude 2절 "기술스택 공부는 필수이고 누구나 풀스택 개발 가능" → "바꿀 부분이 어느 층인지부터"로 유지. "누구나 완성"을 보장하는 표현 없음.
  - 현업가이드 인용 금지: 코드는 Streamlit 공식 문서 형태의 표준 예시이고 현업가이드 파일명·함수명을 쓰지 않음.
  - 나머지 해당 없음.
- 자산 대기: P-S29 (오전 Streamlit 앱 실제 화면 · P-S20 캡처 재사용 가능)
- 수정 기록(2026-09-16): (v0.4 강사 사전 검토 반영) lecture_review/B4-B5-v0.4-pre.md W4-11·12·13


## S30 · 프로젝트 지침과 작업 기록 (v0.4)

- data-skill: `compare` (v0.3과 같음)
- 막 태그: 해당 없음
- 화면 문구:
  - eyebrow: `B5` · `Claude Code로 앱 개선`
  - 제목: 프로젝트 지침과 작업 기록 (v0.3 제목 유지. 계획서의 "작업 기억"은 v0.3 보류 1에 따라 쓰지 않음)
  - 요지 (38자): 긴 대화는 영구 기억이 아니다 — 다음 작업에 필요한 사실은 파일에
  - 설명 문단 (88자·2문장, 2줄): CLAUDE.md는 모델이 읽는 지침일 뿐 권한을 막는 보안 장치가 아니다. 대화에는 읽은 파일과 도구 출력도 쌓여, 길어지면 요약되며 세부가 빠질 수 있다.
  - 본문 (`.stack`)
    - `.compare-grid` 2칸, 각 칸에 `.code-card` (카드 제목 + 4줄):
      - 왼쪽 코드 제목: CLAUDE.md · 프로젝트 지침 예시
        ```text
        ## 저장소 성격
        ## 실행 명령
        ## 주의점
        - 숫자는 코드로 계산
        ```
      - 오른쪽 코드 제목: 작업 기록 · 예시 양식
        ```text
        결정: 무엇을 하기로 했나
        근거: 왜 그렇게 정했나
        검증: 무엇으로 확인했나
        다음: 남은 일과 다음 행동
        ```
    - 컨텍스트 띠(`.callout`): `<strong>컨텍스트</strong>` 현재 대화 + 읽은 파일 + 도구 출력 · 한도가 있고 길면 요약된다
  - 출처: Claude Code 공식 문서 Memory · Context window
- v0.3 대비 변경:
  - 뺌: 두 열의 kicker·열 제목·목록(CLAUDE.md 3줄, 작업 기록 3줄)과 열 메모 2개, callout "파일로 남기기 · 다음 작업에 필요한 사실은 대화가 아닌 파일에"(요지로 올림). `compare-col` 카드 두 개(열 제목 + 4줄 목록이면 한 열 약 416px)는 요지·설명 뒤 예산(447px)에 컨텍스트 띠까지 넣지 못한다.
  - 바꿈: 열 메모 "모델이 읽는 지침 · 권한 설정은 아님"과 "긴 대화는 요약되며 세부가 빠질 수 있음"을 설명 문단 두 문장으로 옮김. 목록을 파일처럼 보이는 코드 카드로 바꿈. v0.3 CLAUDE.md 목록 "규칙 예: 숫자는 코드로 계산"은 왼쪽 카드 넷째 줄로 유지.
  - 새로 넣음: 요지, 설명 문단, 프로젝트 지침 예시(소제목 3개 — 저장소 성격·실행 명령·주의점), 작업 기록 양식 4줄(결정·근거·검증·다음), 컨텍스트 띠.
  - 계획서의 "현업가이드 저장소 CLAUDE.md 발췌 6줄"과 설명 초안 다섯째 문장("현업가이드 저장소의 CLAUDE.md·AGENTS.md가 한 예다"), 출처 줄의 "현업가이드 CLAUDE.md(발췌)"는 인용 금지 규칙에 따라 뺐다. 왼쪽 카드는 소제목 이름만 쓴 일반 예시다.
- 시각 자료: 코드 카드 2개(파일 모양 예시) + 컨텍스트 띠. 카드 폭 약 820px, 한 줄 한글 16자 이하(고정폭 글꼴에서 한글은 약 2칸이라 42칸 규칙 안). 높이 추정: 카드 약 287 + 띠 104 + 간격 → 약 415px(예산 447px). UI 담당이 `compare-col` 안에 코드 카드를 넣기로 하면 열 제목을 빼야 예산 안에 든다.
- 발표자 노트 (최종 전문): 새 대화를 시작할 때마다 프로젝트 구조와 실행 방법을 다시 설명하는 대신, 왼쪽 카드처럼 CLAUDE.md에 저장소 성격, 실행 명령, 주의점 같은 반복해서 필요한 안내를 적어 두면 Claude Code가 작업을 시작할 때 읽습니다. 다만 CLAUDE.md는 모델이 읽는 지침일 뿐 권한을 막는 보안 장치가 아니므로, 접근을 제한해야 하는 일은 권한 설정과 실행 환경에서 막아야 합니다. 오른쪽 작업 기록에는 결정, 근거, 검증, 다음 행동 네 줄을 적어 둡니다. 아래 띠처럼 대화에는 주고받은 말뿐 아니라 읽은 파일과 도구 출력도 쌓이고, 공식 문서 Context window 설명대로 한도가 있어 길어지면 요약되면서 세부가 빠질 수 있어서, 긴 대화가 곧 영구 기억은 아닙니다. 개인 전역 설정을 통째로 복사하기보다 이 프로젝트에 필요한 몇 줄만 적는 것이 좋습니다. 지금 작업을 내일 이어서 한다면 네 줄 중 꼭 남겨야 할 내용 두 가지는 무엇일까요?
- 체크리스트 대조:
  - 7번(CLAUDE.md = 권한 강제) 해당·핵심: 설명 1문장 "모델이 읽는 지침일 뿐 권한을 막는 보안 장치가 아니다"를 화면에 올리고 노트에서 "권한 설정과 실행 환경에서 막는다"로 보충.
  - 조사정리 5절 "긴 대화 = 영구 기억" 오해: 요지와 컨텍스트 띠로 반박. "항상 같은 정도로 성능 저하" 같은 일반화는 쓰지 않음.
  - 13번 해당: "실행 명령"은 소제목 이름만 쓰고 실제 명령을 적지 않음.
  - 현업가이드 인용 금지(카탈로그 7-7): 발췌·파일명·저장소 이름 없음. 소제목 세 개는 사용자가 지정한 일반 이름이다.
  - 나머지 해당 없음.
- 자산 대기: 없음

## S31 · 변경 내용과 실제 사용 확인 (v0.4)

- data-skill: `compare` (v0.3과 같음, v0.3은 `.cols-3`)
- 막 태그: 해당 없음
- 화면 문구:
  - eyebrow: `B5` · `Claude Code로 앱 개선`
  - 제목: 변경 내용과 실제 사용 확인
  - 요지 (27자): 바뀐 줄, 실행 결과, 실제 화면은 따로 확인한다
  - 설명 문단 (91자·2문장, 2줄): 강사의 웹앱 Dokmo는 동작했지만, 직접 써 본 점검에서 한글 이름 입력과 안내 부족이 드러났다.
  - 본문 (`.stack`)
    - 확인 3칸 (`.dg-row` 상자 3개, 화살표 없음), 상자 = 질문 + 보조(확인 수단):
      - `무엇이 바뀌었나` · 바뀐 줄 비교 · 범위 밖 변경 확인
      - `조건을 만족하나` · 완료 조건 입력·기존 질문 실행
      - `목적에 맞게 쓰나` (`.accent`) · 직접 입력 · 안내가 이해되나
    - 플레이스홀더 2칸 (`.compare-grid` 2열, 각 칸 폭 약 50%):
      - 왼쪽:
        - [IMG-PLACEHOLDER · P-S31]
        - 넣을 자료: 작은 기능의 바뀐 줄 비교 화면
        - 조건: 파일 경로·계정 가리기 · 실행일 표기
        - 대체: 확보 전에는 설명 텍스트로 진행
      - 오른쪽:
        - [IMG-PLACEHOLDER · P-S31b]
        - 넣을 자료: Dokmo(개인 기록) 안내 보완 전후 화면
        - 조건: 이름·이메일 등 개인정보 가리기
        - 대체: 확보 전에는 설명 텍스트로 진행
  - 출처: 근거: 강사 개인 기록(Dokmo 수동 점검 기록) · Claude Code 공식 문서 Best practices
- v0.3 대비 변경:
  - 뺌: 3열 카드(`compare-col` × 3, 약 324px)와 callout "개인 기록 · Dokmo 한글 입력·안내 부족은 직접 써 본 점검에서 드러남". 3열 카드 아래 플레이스홀더 두 개(약 261px)를 함께 두면 약 588px로 예산(447px)을 넘는다. 3열 카드를 한 줄 상자 3개(122px)로 바꿨고, callout 내용은 설명 1문장으로 옮겼다.
  - 바꿈: 열마다 "작은 라벨(확인 수단) + 열 제목(질문) + 목록 2줄"이던 것을 "상자 = 질문 + 보조 한 줄"로 합침. 열 1 작은 라벨 "Git diff"를 "바뀐 줄 비교"로 바꿈(Q3 미확인). Git 이야기는 노트에서 조건문으로만 함.
  - 새로 넣음: 요지, 설명 문단, 플레이스홀더 P-S31·P-S31b.
  - 계획서 요지 초안("테스트 통과·앱 켜짐·사용자가 편함·허용 데이터만 접근은 서로 다른 확인이다", 42자)은 40자를 넘어, 이 장면의 세 칸과 같은 말로 줄였다. 넷째 항목(허용 데이터만 접근)은 설명 2문장으로 남겼다.
  - 계획서의 "플레이스홀더 각 폭 30%"는 확인 3칸 아래 두 칸이 반씩 나누는 편이 글자가 덜 접혀 50%로 바꿨다(두 칸 합쳐도 화면 절반을 넘는 한 칸은 없음).
- 시각 자료: 인포그래픽(확인 3칸, 강조는 "목적에 맞게 쓰나" 한 곳, v0.3 강조 열과 같음) + 플레이스홀더 v2 `P-S31`, `P-S31b`. 상자 폭 약 546px, 보조 20자 이하. 높이 추정: 3칸 122 + 간격 24 + 플레이스홀더 약 261 → 약 407px(예산 447px, 설명 2줄 기준. 설명이 3줄로 늘면 넘치므로 설명 문단을 늘리지 않는다).
- 발표자 노트 (최종 전문): 제가 만든 개인 웹앱 Dokmo에서는 앱이 동작하는 상태에서도, 직접 써 보는 수동 점검에서 한글 이름 입력 문제와 다음에 무엇을 해야 할지 알려 주는 안내가 부족한 문제가 드러났고, 오른쪽 아래 자리에는 그 안내를 보완하기 전후 화면이 들어갑니다. 그래서 변경 확인은 위 세 칸처럼 따로 봅니다: 무엇이 바뀌었는지는 바뀐 줄 비교로, 조건을 만족하는지는 실행 결과로, 목적에 맞게 쓸 수 있는지는 실제 화면에 직접 입력해 봅니다. 실습 폴더가 Git 저장소라면 왼쪽 아래 같은 바뀐 줄 비교를 Git diff로 볼 수 있습니다. 공식 모범사례도 Claude Code에게 확인 방법을 알려 주고 실행 결과를 받으라고 권하지만, 그 결과가 맞는지와 사용자에게 편한지는 사람이 마지막으로 봐야 합니다. 버튼을 화면에서 숨겼다고 해서 그 데이터에 접근할 권한까지 막힌 것은 아니라는 점도 기억해 두세요. 여러분이 고른 기능은 화면에서 무엇을 입력하고 무엇이 보이면 확인이 끝날까요?
- 체크리스트 대조:
  - 12번(성능 수치·이용자 수) 해당: Dokmo 사례에 이용자 수·수정 건수·기간을 넣지 않음. 설명 문단 첫머리 "개인 기록인"과 플레이스홀더 "Dokmo(개인 기록)", 출처 줄 "강사 개인 기록"으로 성격을 표시. 조사정리 4-C 범위("현재 사이트를 재검증한 결과 아님")를 넘지 않게 점검 항목 두 가지(한글 이름 입력·안내 부족)만 씀.
  - research_claude 2절 "Claude Code가 결과를 검증하므로 믿어도 된다" → 노트 넷째 문장 "확인 방법을 주고 실행 결과를 받되, 마지막 판단은 사람이"로 유지.
  - 조사정리 4-C 강의 메시지("화면의 버튼을 숨기는 것만으로 데이터 권한이 보장되지 않는다")를 설명 2문장으로 화면에 올림.
  - Q3: "Git diff"를 화면에서 빼고 "바뀐 줄 비교"로 둠.
  - 13번 통과: 플레이스홀더 조건에 경로·계정·개인정보 가리기를 명시.
  - 나머지 해당 없음.
- 자산 대기: P-S31 (작은 기능 변경의 바뀐 줄 비교 화면 · `assets/img/b5/git-diff-example.png`), P-S31b (Dokmo 안내 보완 전후 화면 · `assets/img/b5/dokmo-before-after.png`)
- 수정 기록(2026-09-16): (v0.4 강사 사전 검토 반영) lecture_review/B4-B5-v0.4-pre.md W4-14·15


## S32 · 실습 6 · 오전 앱의 작은 개선 (v0.4)

- data-skill: `title-bullets` 실습 안내판 (v0.3과 같음)
- 막 태그: 해당 없음
- 화면 문구:
  - eyebrow: `B5` · `Claude Code로 앱 개선`
  - 제목: 실습 6 · 오전 앱의 작은 개선
  - 안내 줄(board-meta): 14:36–14:57 · 21분 · 실습 가이드 해당 장
  - 관찰할 것 (74자·2문장, 강조어 포함 2줄): 파일 수정·명령 실행 승인을 요청받으면 무엇을 하려는지 읽고 나서 승인한다. 완료했다는 말이 아니라 바뀐 줄과 실행 결과로 판단한다.
  - 본문 왼쪽 (`.stack`)
    - 소제목(`.panel-title`): 기본 과제: 결과 없음 안내 · 이미 있으면 근거 표시
    - 승인 요청 개념 카드 (`.code-card`, 2줄):
      - 코드 제목: 승인 요청 · 개념 예시(실제 화면과 다름)
      - 본문:
        ```text
        수정 요청 · 대상: 화면·처리 부분
        허용할까요?  예 / 아니오
        ```
    - 완료 조건: 기존 질문은 그대로, 새 기능은 동작
  - 본문 오른쪽
    - 시간 블록 (`.time-blocks.row`, 합계 21분): `4분` 탐색 / `4분` 계획 / `8분` 구현 / `5분` 검증 (`is-accent`)
    - 막히면: Claude Code가 안 되면 짝과 한 화면으로, 앱의 모델 호출이 막히면 준비된 결과로 확인합니다
    - 코드 위치(`.guide-ref`): 코드 위치: 화면·처리 부분 · 실습 가이드 해당 장
  - 출처: Claude Code 공식 문서 Best practices · Permissions · 근거: 사용자 제공 강의안·시간표
- v0.3 대비 변경:
  - 뺌: 실습 단계 목록 3줄("구조를 읽고 계획·완료 조건 정리" / "계획한 범위만 구현" / "새 입력·기존 입력 둘 다 실행"). 시간 블록 네 칸(탐색·계획·구현·검증)과 같은 순서라 중복이고, 승인 카드를 넣을 높이가 없다. 단계별 할 일은 노트 1–3문장에 유지.
  - 줄임: 기본 과제 줄 "기본 과제: 검색 결과 없음 안내 · 이미 있으면 근거 표시"(30자) → "기본 과제: 결과 없음 안내 · 있으면 근거 표시"(27자, 1줄). 대체 과제는 화면에 남겼다(Q2 미확인이라 수강생이 화면만 보고도 바꿀 수 있어야 한다). 완료 조건 "기존 질문 결과가 그대로이고 새 기능이 동작한다"(26자, 2줄) → "기존 질문은 그대로, 새 기능은 동작"(19자, 1줄). 기존 기능을 앞에 두는 순서는 유지.
  - 새로 넣음: 관찰할 것 2문장, 승인 요청 개념 카드, 코드 위치 줄.
  - 계획서 관찰할 것 초안의 "(오늘의 HITL)"은 이 시점에 처음 나오는 용어라 화면에서 빼고 노트에서 부록과 연결했다. 초안의 "diff"는 Q3 미확인이라 "바뀐 줄"로 바꿨다.
  - 계획서 승인 카드 예시 "edit app.py? [y/n]"은 파일명을 쓰므로(실습 코드 파일명 미확인, 현업가이드 파일명 인용 금지) 역할 이름 "결과 표시 부분"으로 바꿨다. 계획서 코드 위치 라벨 "화면·처리 함수(S29 ①·③)"는 기획 ID를 빼고 "화면·처리 부분"으로 적었다.
  - 시간 블록 숫자(4/4/8/5분)와 강조 칸(검증), 막히면 문구(W24)는 유지.
  - 노트 표현 정정 (v0.4 UI 요청 반영, B5-v4-2): 다섯째 문장 "왼쪽 회색 카드는 승인 요청의 모양을…"을 "왼쪽 승인 요청 카드는 요청 화면의 모양을…"으로 바꿈. 화면의 승인 카드는 흰 코드 카드라 "회색"과 맞지 않는다. 화면 문구는 바꾸지 않았다.
- 시각 자료: 승인 요청 개념 카드(`.code-card`, 실제 UI를 복제하지 않은 일반 표현, 카드 제목에 "개념 예시(실제 화면과 다름)" 표기) + 시간 블록 + 코드 위치 줄. 높이 추정: 왼쪽 소제목 40 + 카드 약 201 + 완료 조건 149 + 간격 → 약 438px, 오른쪽 시간 블록 166 + 막히면 134 + 코드 위치 54 → 약 354px(예산 약 477px).
- 발표자 노트 (최종 전문): 처음 4분은 Claude Code에게 오전 앱의 구조와 수정할 위치를 설명하게 하고, 다음 4분은 바꿀 곳과 완료 조건을 담은 계획을 받아 우리가 읽고 확인합니다. 이어서 8분 동안 계획한 범위만 구현하게 하는데, 전체 앱을 다시 짜거나 기능을 여러 개 붙이는 것은 이번 범위에 넣지 않습니다. 마지막 5분은 없는 키워드 같은 새 기능용 입력과 오전에 정상 동작하던 질문을 둘 다 실행하고, 바뀐 내용도 확인합니다. 기본 과제인 검색 결과 없음 안내가 이미 되어 있다면 근거 표시(또는 결과 다운로드)로 바꿔 진행하세요. 왼쪽 승인 요청 카드는 요청 화면의 모양을 일반화한 개념 예시로 실제 화면과 다를 수 있는데, Claude Code가 이런 식으로 파일 수정이나 명령 실행 승인을 요청하면 무엇을 하려는지 읽고 나서 승인하고, 무엇을 하려는지 모르겠으면 아니오를 고른 뒤 손을 들어 주세요. 부록의 HITL, 곧 사람이 작업 흐름 중간에서 승인하는 방식을 오늘 직접 겪는 장면이고, 공식 문서 Permissions도 이런 승인 범위를 설정으로 정하는 방법을 설명합니다. 앱의 모델 호출이 막히면 준비된 결과로 화면과 규칙 부분만 확인하고, Claude Code 환경이 준비되지 않은 분은 앞 시간에 정한 짝과 같은 화면으로 진행합니다.
- 체크리스트 대조:
  - 5번(로컬 접근 = 모든 일) 관련: 관찰할 것 1문장과 승인 카드로 "허용된 도구·권한 범위 안에서, 승인을 읽고 일한다"를 실습 행동으로 바꿈. "승인을 요청받으면" 조건문으로 써서 교육 환경 승인 설정(Q1 미확인)을 단정하지 않음.
  - 13번 해당: 명령·주소·파일명 없음. 승인 카드 대상도 역할 이름.
  - Q2: 기본 과제가 이미 구현됐을 경우의 대체 과제를 화면과 노트에 둠. Q3: "바뀐 줄"로 Git 여부에 기대지 않음.
  - research_claude 2절 "Claude Code가 결과를 검증하므로 믿어도 된다" 오해 방지: 관찰할 것 2문장 "완료했다는 말이 아니라 바뀐 줄과 실행 결과로 판단".
  - 나머지 해당 없음.
- 자산 대기: 없음 (계획서 6절에 이 장면 자산 없음. Streamlit 개선 전후 화면은 실습 중 실행 화면으로 대신)
- 수정 기록(2026-09-16): (v0.4 강사 사전 검토 반영) lecture_review/B4-B5-v0.4-pre.md W4-16·17
- 수정 기록(2026-09-16): (v0.4 강사 사후 검토 반영) R-03 승인 카드 대상을 "화면·처리 부분"으로(코드 위치와 같은 범위). 문구 1곳.


## S33 · 다른 입력으로 재확인 (v0.4)

- data-skill: `title-bullets` (v0.3과 같음)
- 막 태그: 해당 없음
- 화면 문구:
  - eyebrow: `B5` · `Claude Code로 앱 개선`
  - 제목: 다른 입력으로 재확인
  - 요지 (28자): 실행 전에 결과를 먼저 말하고, 다르면 그것이 단서다
  - 설명 문단 (80자·2문장, 2줄): 옆 사람이 골라 준 입력 하나로 실행 전에 결과를 예상해 말한다. 예상과 다른 결과는 틀렸다고 끝내지 말고, 바꾼 코드가 그렇게 동작한 조건을 찾는 단서로 쓴다.
  - 본문 (`.stack`)
    - 3칸 카드 (`.dg-row` 상자 3개, 사이 화살표 `→` 2개):
      - `예측` · 보조: 실행 전에 결과를 말한다
      - `실행` · 보조: 기존은 그대로? 새 입력은 안내?
      - `설명` (`.accent`) · 보조: 바꾼 코드를 한 문장으로
    - 캡션: 개념 예시 · 예측·실행·설명 순서
    - 띠(`.callout`): 확인하지 못한 입력은 + 회색 칩(`.chip`) `미확인` + 으로 적어 두고 자유 실습에서 잇는다
  - 출처: 근거: 강사 Python 교육 진행 규칙(예측·실행·설명·입력 변형)
- v0.3 대비 변경:
  - 뺌: 부제 "실행 전에 예상 결과를 먼저 말하기"(요지·설명 1문장이 대신함), 확인 목록 3줄(`.bullets`). 요지·설명 뒤 3칸 카드와 목록을 함께 두면 약 450px로 예산(447px)을 넘는다.
  - 바꿈: 목록 3줄을 3칸 카드로. "기존 정상 입력 · 이전과 같은 결과인가"와 "새 기능 입력 · 예상한 안내가 나오나"는 "실행" 칸 보조 한 줄로 합치고, "설명 · 바꾼 코드가 하는 일을 한 문장으로"는 "설명" 칸으로 옮김. callout "확인하지 못한 입력은 미확인으로 적어 둔다"에 회색 칩과 "자유 실습에서 잇는다"를 더함.
  - 새로 넣음: 요지, 설명 문단, 3칸 카드, "미확인" 칩.
  - 계획서 설명 초안 넷째 문장("시간 안에 못 본 입력은 '미확인'으로 적어 자유 실습에서 잇는다")은 띠와 같은 말이라 설명에서 빼고 띠로 옮겼다(설명 3줄 → 2줄).
- 시각 자료: 인포그래픽(3칸 카드, 강조는 "설명" 한 곳 — B5에서 새로 요구하는 행동) + 회색 "미확인" 칩 예시. 상자 폭 약 506px, 보조 16자 이하. 높이 추정: 카드 122 + 캡션 58 + 띠 약 112 + 간격 → 약 316px(예산 447px).
- 발표자 노트 (최종 전문): 실습에서 한 번 성공한 입력 말고, 옆 사람이 골라 준 다른 입력을 하나 받아서 실행 전에 결과를 먼저 예상해 말해 보겠습니다. 세 칸 순서대로, 예측한 다음 기존 정상 질문이 이전과 같은 결과를 내는지, 새 기능에 해당하는 입력에서 예상한 안내나 표시가 나오는지 차례로 실행해 확인합니다. 결과가 예상과 다르면 틀렸다고 끝내지 말고 바꾼 코드가 어떤 조건에서 그렇게 동작하는지 찾는 단서로 쓰고, 이렇게 먼저 예측하고 입력을 바꿔 보는 과정에서 필요한 기초가 채워집니다. 두 분 정도 마지막 칸처럼 바꾼 코드가 무엇을 하는지 한 문장으로 설명해 주세요. 시간 안에 확인하지 못한 입력은 아래처럼 미확인으로, 기존 질문 결과가 달라진 것은 고칠 일로 적어 두면 자유 실습에서 이어 갈 수 있고, 15시 20분에는 이런 작업 경험과 자료를 다시 쓸 수 있게 정리하는 방법으로 넘어갑니다.
- 체크리스트 대조:
  - 12번 통과: 수치 없음.
  - 조사정리 5절 "Top-down이면 기초를 배울 필요가 없음" 오해: 노트 셋째 문장 "먼저 예측하고 입력을 바꿔 보는 과정에서 필요한 기초가 채워진다"로 대응(개인 Python 교육 규칙 근거).
  - 서사(11-4): 마지막 노트 문장 "15시 20분에는 …"으로 다음 구간(B6) 연결 유지.
  - 나머지 해당 없음.
- 자산 대기: 없음
- 수정 기록(2026-09-16): (v0.4 강사 사전 검토 반영) lecture_review/B4-B5-v0.4-pre.md W4-18·19


## v0.4 보류 목록

강사·사용자 확인이 필요한 항목이다. 추정해 채우지 않았다.

1. S28·S32: 사내 교육 환경에서 계획 모드 같은 명령 UI를 쓸지(v0.3 보류 2 유지). 쓴다면 이름과 동작을 교육 환경에서 확인한 뒤 노트에 한 문장만 더한다.
2. S28: 노트의 참고 강의 문구 "마지막 정상 지점과 첫 실패 지점을 증거로 말하기"는 계획서 9절(뿌리강의 5장·11장)에서 옮긴 것이다. 원문 표현은 이번에 확인하지 못했다. 원문과 다르면 노트 문장을 원문에 맞추거나 따옴표를 뺀다.
3. S29 · Q7: 실제 실습 코드의 화면·조회·모델 호출 구조. 코드 카드 예시와 노트 넷째 문장("화면과 처리 사이에 별도 백엔드 서버를 두지 않았고 모델만 추론 서버에 API로 요청")이 이 확인에 달려 있다. 별도 백엔드가 있는 판본이면 두 곳을 그 구조로 바꾼다(v0.3 보류 3 유지).
4. S29 · P-S29: 오전 Streamlit 앱 캡처의 제공 시점. P-S20 캡처를 그대로 쓸지, 화면·처리·데이터 표시를 따로 넣은 캡처를 받을지 정한다.
5. S30: 제목 "프로젝트 지침과 작업 기록"(v0.3)과 계획서 "프로젝트 지침과 작업 기억" 중 무엇으로 확정할지(v0.3 보류 1 유지). 이 문서는 "기록"을 썼다. "기억"이 Claude Code가 스스로 영구 기억한다는 뜻으로 읽힐 수 있어서다.
6. S30: 두 열을 `compare-col` 카드 대신 코드 카드 두 개로 둔 구성. UI 담당이 `compare-col`을 유지하려면 열 제목을 빼야 예산 안에 든다. 실습 폴더에 CLAUDE.md 예시 파일을 미리 넣어 둘지도 함께 정한다(v0.3 보류 5).
7. S30: `/context`·`/compact` 등 맥락 관리 명령과 auto memory를 소개할지(v0.3 보류 4 유지). 지금은 넣지 않았다.
8. S31 · Q3: 실습 폴더가 Git 저장소이고 diff를 볼 수 있는지. 지금은 화면에 "바뀐 줄 비교"를 쓰고 Git diff는 노트에서 조건문으로만 말한다. Git 저장소로 확인되면 첫 칸 보조를 "Git diff · 범위 밖 변경 확인"으로 바꿀 수 있다. S28 산출물 "바뀐 줄", S32 관찰할 것도 같은 기준이다.
9. S31: Dokmo 표현("앱이 동작해도 직접 써 본 점검에서 한글 이름 입력과 안내 부족이 드러남")이 강사 기록과 맞는지, P-S31b(개선 전후 화면)를 공개할 수 있는지(v0.3 보류 6 유지). P-S31(바뀐 줄 비교 화면)의 제공 시점도 정한다.
10. S31: 3열 카드를 한 줄 상자 3개로 바꾸고 플레이스홀더 두 칸을 반씩 둔 구성. 계획서는 플레이스홀더 각 폭 30%였다. UI 담당이 확인한다.
11. S32 · Q1: 교육 환경의 Claude Code 승인 설정. 관찰할 것 "승인을 요청받으면 … 읽고 나서 승인한다"는 수정·명령 실행 전에 승인을 묻는 설정을 전제로 한다(v0.3 보류 9 유지). 승인 없이 바로 수정되는 설정이면 관찰할 것 1문장과 승인 카드를 바꿔야 한다.
12. S32: 승인 요청 개념 카드 문구가 사내 버전 Claude Code의 실제 승인 화면과 너무 다르면 수강생이 헷갈릴 수 있다. 강사가 실제 화면을 보고 카드 문구를 가깝게 고칠지 정한다(실제 UI 복제는 하지 않는다).
13. S32 · Q2: 기본 과제(검색 결과 없음 안내)가 오전 앱에 이미 있는지, 대체 과제(근거 표시·결과 다운로드) 시작 코드와 fallback용 "준비된 결과"가 있는지(v0.3 보류 8 유지). 실습 가이드 장 이름·앵커는 강사가 채운다(N1).
14. S32: 코드 위치 줄 "화면·처리 부분"은 코드 예시의 역할 이름이다. 실습 코드에서 기본 과제가 실제로 어느 부분을 바꾸는지(Q7)는 강사가 확인한다.
15. S33: 새 기능 확인용 입력(실습 데이터에서 실제로 결과가 0건인 키워드)을 강사가 미리 준비할지(v0.3 보류 10 유지).
