# B4 배치 지시 · 13:20–14:00 · Claude Code와 개발 계획

작성: slide_ui_designer · 2026-09-14 · 대상: S22–S27 (6장)
입력: `_workspace/slide_copy/B4.md`, `00_component_catalog.md`(2026-09-14 개정본), `index.html` `<style id="scene-styles">` 실측값, topic `DESIGN.md` 최소 적용 규칙, `slide_ui/B1–B3.md`, `lecture_review/B1-B3-post.md`

## 공통 배치 규칙 (요약: 원문은 `slide_ui/B1.md` 공통 절)

- 장면 내부 872×1680px. eyebrow+제목 1줄 145, 부제 64, 출처 58, 본문 margin-top 56. 본문 가용 높이는 제목+출처일 때 **613px**, 부제가 있으면 **549px**. callout 1줄은 104px.
- 글자 폭 추정: 한글 1.0em, 라틴·숫자 0.55em, 공백·기호 0.3em. 코드는 JetBrains Mono 0.6em(28px → 16.8px/자), 코드 안 한글은 대체 글꼴 28px/자.
- `split-grid`·`compare-grid`는 **가장 긴 칸 높이에 맞춘다**(기본). 이번 차수에는 `.fill`을 쓰지 않는다(점선 자리표시 없음).
- `.stack`: 세로 쌓기(gap 24px). 장면 직속 `.stack`은 위 간격 56px, `.stack` 직계 자식의 위 여백은 0이다.
- compare 열 내부 순서: `.compare-kicker` → `.compare-heading` → `ul.compare-list` → `p.compare-note`(있을 때). 상태 점은 `<div class="compare-heading"><span class="status-dot ok" aria-hidden="true"></span><span data-editable="true">열 제목</span></div>`.
- 스플릿 패널 머리줄: 배지와 패널 제목이 둘 다 있으면 `.dg-row` 한 줄.
- 실습 안내판(S26): 오전 S08·S13·S19와 같다. `board-meta` → `.practice-grid` [좌 `.stack`(bullets → done-box) / 우 일반 `div`(time-blocks → fallback)]. 가이드 장 이름은 `board-meta`에만 둔다(`.guide-ref` 없음).
- 구간 첫 장면 S22만 eyebrow에 `.time-chip` "13:20–14:00".
- 남색 `night` 사용 없음. 풋터: `AI Agent Guide · SK hynix 사내 교육`.
- 이 차수에는 `title-image`가 없으므로 `.done-row`(S20 전용)를 쓰지 않는다.

---

## S22 · 오전 앱과 Coding Agent

- data-skill: `compare` · section 클래스: `scene clip`
- 배치 순서: eyebrow(`.block-chip` "B4" + span "Claude Code와 개발 계획" + `.time-chip` "13:20–14:00") → `h2.scene-title` → `p.scene-subtitle` → `.compare-grid`(2열) [열1 `.compare-col` / 열2 `.compare-col.is-accent`] → `.source`
- 요소 ↔ 문구
  - 제목: 오전 앱과 Coding Agent
  - `.scene-subtitle`: 오전 앱이 이번 시간 Claude Code의 작업 대상
  - 열1: kicker "모델 + 우리가 연결한 도구" / heading "오전 앱" / li: 입력: 업무 질문 · 도구: 기사 검색·SQL 조회 · 결과: 근거를 붙인 답변 화면 / note: 쓸 수 있는 도구는 우리가 코드로 정해 줌
  - 열2(`is-accent`): kicker "모델 + 파일·명령 도구 + 실행 환경" / heading "Claude Code" / li: 입력: 작업 요청과 프로젝트 폴더 · 도구: 파일 읽기·수정·명령 실행 · 결과: 바뀐 파일과 실행 결과 / note: 읽고 → 고치고 → 실행해 확인하기를 반복
  - `.source`: 출처: Claude Code 공식 문서 How Claude Code works · Anthropic, Building effective agents
- 강조: 열2 `.is-accent` 1개. 상태 점 없음. 목록의 "입력:·도구:·결과:"는 굵게 하지 않는다(DESIGN: 굵은 본문 금지. `compare-list strong`은 스타일이 없음).
- 글자 크기: 72 / 40 / 34 / 32 (4종, kicker·note 24는 캡션)
- 넘침 근거
  - 폭: 목록 가용 820 − 84 − 30 = 706px(약 22자). 최장 "입력: 작업 요청과 프로젝트 폴더" ≈ 한글 13×32 + 기호·공백 4×10 = 456px → 1줄. kicker 최장 "모델 + 파일·명령 도구 + 실행 환경" ≈ 한글 11×24 + 기호 50 = 314px < 736px → 1줄. note 최장 ≈ 434px → 1줄. 부제 ≈ 한글 13×34 + 라틴 10×19 + 공백 60 = 690px < 1,500px → 1줄
  - 높이: 열2 = 84 + kicker·heading 114 + 목록(24 + 3×45 + 2×18 = 195) + note 54 = **447px ≤ 549**. 전체 145 + 64 + 56 + 447 + 58 = 770px
- 플레이스홀더: 없음 (Claude Code 화면은 S26 시연으로 대신)
- 빌드 메모: 두 열 모두 kicker·heading·항목이 1줄이라 "입력/도구/결과" 세 줄이 좌우로 같은 높이에 맞는다. 비교 기준이 가로로 읽히도록 항목 순서를 바꾸지 않는다. 열 이름에 "Agent"를 붙이지 않는다(W11).

## S23 · 로컬 실행과 접근 범위

- data-skill: `split` · section 클래스: `scene clip`
- 변형: `.split-grid.wide-left` (좌 913 / 우 703px)
- 배치 순서: eyebrow → 제목 → `.split-grid.wide-left` [좌 `.split-panel` / 우 `ul.bullets`] → `.source`
  - 좌 패널: `.concept-badge` "개념 예시"(단독 줄) → `.dg` [ 1행 `.dg-row` / 2행 `.dg-row` ] → `.dg-caption`
  - 1행 `.dg-row`:
    ```html
    <div class="dg-row">
      <div class="dg-zone">
        <div class="dg-zone-label" data-editable="true">내 PC · 실행 환경</div>
        <div class="dg-box" data-editable="true">Claude Code<span class="dg-sub">파일·명령 도구</span></div>
      </div>
      <div class="stack">
        <span class="dg-arrow" aria-hidden="true">⇄</span>
        <span class="dg-sub" data-editable="true">요청과 필요한 맥락</span>
      </div>
      <div class="dg-box accent" data-editable="true">모델<span class="dg-sub">승인된 모델 경로</span></div>
    </div>
    ```
  - 2행 `.dg-row`: `.dg-box.muted` "화면 조작 · 사내 시스템" + `.dg-sub` "별도 도구·연결·권한 필요" (행 전체 폭)
- 문구의 "영역 `승인된 모델 경로`(accent 영역) > 강조 상자 `모델`"은 **영역 대신 강조 상자의 보조 줄**로 둔다. 이유(scene-styles 확인): `.dg-row` 안의 `.dg-zone`은 flex 기본값(0 1 auto)이라 내용 폭만 차지한다. "모델" 상자만 든 영역은 약 168px인데, 영역 라벨 "승인된 모델 경로"(22px 굵게, 약 177px)는 absolute라 영역 폭을 넓히지 못한다. 그래서 라벨이 2줄로 접히고 상자 테두리와 겹친다(오전 S06 T1과 같은 결함). `.dg-box`는 `flex:1`이라 남은 폭(약 297px)을 모두 받는다. 영역을 살리려면 카탈로그 추가 요청 F(선택)가 필요하다.
- 요소 ↔ 문구
  - 제목: 로컬 실행과 접근 범위
  - `.dg-caption`: 주소·공급사 대신 역할 이름으로 표시
  - `li` 1: 파일·명령은 내 PC에서 실행
  - `li` 2: 모델 처리는 설정된 경로에서 + `span.sub` "요청과 읽은 내용이 함께 전달됨"
  - `li` 3: 할 수 있는 일은 도구·권한 범위
  - `.source`: 출처: Claude Code 공식 문서 How Claude Code works · Permissions · Data usage
- 강조: `.dg-box.accent`(모델) 1개. bullets strong 없음. 2행 muted 상자는 "기본으로 되지 않는 일"을 중립색으로 구분한다.
- 글자 크기: 72 / 40 / 32 / 28 (4종, dg-sub·zone-label·badge 22–24는 라벨)
- 넘침 근거
  - 폭(1행): 패널 안쪽 913 − 82 = 831px. 영역1 = 상자 max-content("Claude Code" 약 186px + 패딩 48 + 테두리 2 = 236) + 영역 패딩 48 + 테두리 4 = **288px**. 영역 라벨 "내 PC · 실행 환경" ≈ 170px < 288 − 24 = 264px → 1줄. 화살표 스택 = dg-sub "요청과 필요한 맥락" 약 206px. 강조 상자 = 831 − 288 − 206 − 40 = **297px**, 안쪽 245px ≥ "승인된 모델 경로" 약 182px → 1줄
  - 폭(우): bullets 703 − 52 = 651px. 최장 li 3 ≈ 한글 12×40 + 기호·공백 5×12 = 540px → 1줄. sub ≈ 396px → 1줄
  - 높이(좌): 1행 = 영역1(56 + 24 + 4 + 상자 123) = 207px. 2행 상자 123px. `.dg` = 207 + 24 + 123 = 354. 패널 82 + 배지 41 + 24 + 354 + 24 + 캡션 34 = **559px ≤ 613** (여유 54px)
  - 높이(우): 3×56 + sub 45 + 2×28 = 269px
- 플레이스홀더: 없음 (CSS 개념도)
- 빌드 메모: 화살표 라벨은 오전 S06과 같이 `.stack`[`.dg-arrow` + `.dg-sub`]로 둔다. 1행은 기본 `.dg-row`(세로 가운데 정렬)라 강조 상자(126px)가 영역(207px) 높이의 가운데에 온다. 공급사 로고·주소를 넣지 않는다.

## S24 · 변경하기 쉬운 프로그램 구조

- data-skill: `split` · section 클래스: `scene clip`
- 변형: `.split-grid.wide-left`. 오전 S21과 같은 좌우 비율이다. S21 구조도 → S24 네 영역 표 → B5 S29 코드 순으로 "왼쪽 시각 자료 / 오른쪽 해석" 배치를 유지한다.
- 배치 순서: eyebrow → 제목 → `.split-grid.wide-left` [좌 `.split-panel` / 우 `ul.bullets`] → `.source`
  - 좌 패널: 머리줄 `.dg-row`[`.concept-badge` "개념 예시" + `.panel-title` "바꿀 기능: 검색 결과 없음 안내"] → `table.data-table` → `.dg-caption`
- 표
  - `th`: 영역 | 맡는 일 | 이번 변경
  - 행1: 화면 | 입력·결과 표시 | `td.is-match` "안내 문구 추가"
  - 행2: 업무 규칙 | 조건 확인·판정 | `td.is-match` "0건이면 안내로 분기"
  - 행3: 데이터 접근 | 기사·DB 조회 | 빈 결과 형태만 확인
  - 행4(`tr.is-dim`): 모델 연결 | 요청·응답 처리 | 변경 없음
- 요소 ↔ 문구
  - 제목: 변경하기 쉬운 프로그램 구조
  - `.dg-caption`: 오전 앱을 네 영역으로 나눈 예시 · 실제 파일 구성과 다를 수 있음
  - `li`: 영역마다 입력·출력을 정해 둠 / 바꿀 곳과 확인할 곳이 좁아짐 / 작은 앱은 함수 분리와 README로
  - `.source`: 근거: 강사의 아키텍처 회고(사용자 제공) · Claude Code 공식 문서 Best practices
- 강조: `td.is-match` 2칸(같은 뜻 "바꿀 곳", 한 묶음). 행4는 흐림으로 "변경 없음"을 구분. bullets strong 없음.
- 글자 크기: 72 / 40 / 30(표) / 28 (4종, th·캡션 24, 배지 22)
- 넘침 근거
  - 폭(표): 패널 안쪽 831px. 열별 최장 내용(30px): "데이터 접근" 약 159 / "조건 확인·판정" 약 198 / "0건이면 안내로 분기"(굵게) 약 290px. 합 647 + 셀 패딩 6×24 = **791px < 831** → 모든 셀 1줄 (여유 40px, 주의)
  - 폭(머리줄): 배지 약 120 + 20 + 패널 제목 약 376 = 516px < 831 → 1줄
  - 폭(우): 651px에 최장 li 3 "작은 앱은 함수 분리와 README로" ≈ 한글 10×40 + 라틴 6×22 + 공백 4×12 = 580px → 1줄
  - 높이(좌): 82 + 머리줄 41 + 24 + 표(헤더 67 + 75×4 + 테두리 2 = 369) + 24 + 캡션 34 = **574px ≤ 613** (여유 39px). 행2 셀이 2줄로 접히면 +42 → 616px로 3px 넘치므로 셀 문구를 늘리지 않는다
- 플레이스홀더: 없음 (개념 표). Streamlit 캡처를 더하기로 하면(문구 보류 7) 표와 캡처가 한 패널에 들어가지 않는다(캡처 최소 240 + 24 = 264px 추가 → 838px). 그때는 장면 분할이나 표 대체를 다시 정한다
- 빌드 메모: 오전 S18 표와 같은 마크업(`thead`/`tbody`, 셀마다 `data-editable`). 캡션은 S18과 같이 가운데 정렬 `.dg-caption`. 별도 서버 같은 영역 행을 추가하지 않는다.

## S25 · 작업 요청과 완료 조건

- data-skill: `compare` · section 클래스: `scene clip`
- 배치 순서: eyebrow → 제목 → `.compare-grid`(2열) [열1 `.compare-col` / 열2 `.compare-col.is-accent`] → `.source` (부제·callout 없음)
- 열2 성격 배지는 heading 줄 끝에 인라인으로 둔다. 두 열의 heading 높이가 같아 머리선이 좌우로 맞는다.
  ```html
  <div class="compare-heading"><span class="status-dot ok" aria-hidden="true"></span><span data-editable="true">요청문 예시</span> <span class="concept-badge" data-editable="true">개념 예시</span></div>
  ```
- 요소 ↔ 문구
  - 제목: 작업 요청과 완료 조건
  - 열1: kicker "막연한 요청" / heading `status-dot.warn` + "“앱 개선해줘”" / li: 무엇을 바꿀지 불분명 · 어디까지 바꿀지 모름 · 끝났는지 판단 기준 없음 / note: 결과를 받아도 맞는지 확인하기 어려움
  - 열2(`is-accent`): kicker "완료 조건이 있는 요청" / heading `status-dot.ok` + "요청문 예시" + 배지 "개념 예시" / li 5개: 목표: 결과 0건일 때 안내 표시 · 범위: 결과 표시 부분만 수정 · 제약: 새 패키지 설치 없이 · 완료: 없는 키워드 → 안내 문구 · 완료: 기존 질문 결과는 그대로 / note: 입력 예와 기대 결과가 곧 완료 조건
  - `.source`: 출처: Claude Code 공식 문서 Best practices · 근거: 사용자 제공 강의안
- 강조: 열2 `.is-accent` 1개. 상태 점 초록·주황은 DESIGN 예외 범위(점만).
- 글자 크기: 72 / 40 / 32 (3종, kicker·note 24, 배지 22)
- 넘침 근거
  - 폭: 목록 706px. 최장 "완료: 기존 질문 결과는 그대로" ≈ 한글 11×32 + 기호·공백 4×10 = 392px → 1줄. 열2 heading = 점 28 + "요청문 예시" 200 + 공백 10 + 배지 약 124 = 362px < 736px → 1줄. 배지(41px)는 heading 줄 높이 50px 안에 들어가 줄 높이가 거의 늘지 않는다(약 1px)
  - 높이: 열2 = 84 + 114 + 목록(24 + 5×45 + 4×18 = 321) + note 54 = **573px ≤ 613** (여유 40px). 열1은 grid로 같은 높이가 되고 note가 바닥에 붙는다
- 플레이스홀더: 없음
- 빌드 메모: 요청문은 `.code-card`가 아니라 `compare-list` 항목이다(문구 제작 메모). 열1은 항목이 3개라 목록 아래가 빈다. 의도한 것이다(열1 note는 `margin-top:auto`로 바닥에 붙음).

## S26 · 실습 5 · Claude Code 실행

- data-skill: `title-bullets` · section 클래스: `scene clip`
- 배치 순서(안내판 공통, S08·S13·S19와 동일): eyebrow → 제목 → `p.board-meta` → `.practice-grid` [좌 `.stack`(`ul.bullets` → `.done-box`) / 우 `div`(`.time-blocks.cols-3` → `.fallback`)] → `.source`
- 변형: `.time-blocks.cols-3`(3칸 한 줄). 블록이 3개라 기본 2열로 두면 한 칸이 비고, `.row`(4열)로 두면 한 칸이 남는다.
- 요소 ↔ 문구
  - 제목: 실습 5 · Claude Code 실행
  - `.board-meta`: 13:36–13:56 · 20분 · 실습 가이드 해당 장 (가이드 장 이름은 여기에만)
  - `.bullets li`: 사내 가이드대로 실행·접속 확인 / 실습 폴더를 열고 파일 읽기 확인 / 파일별 역할 설명을 받고 대조
  - `.time-block` 3개(왼→오): "10`<small>분</small>`" + "실행·접속 확인" / "5분" + "폴더·파일 읽기" / "5분" + "역할 설명·대조"(`.is-accent`)
  - `.done-box`: `.done-label` "완료 조건" · `.done-text` "프로젝트 구조 설명을 받고 실제 파일과 맞는지 확인한다"
  - `.fallback`: `<strong>막히면</strong>` + "다른 접속 방식으로 우회하지 말고 짝 실습이나 강사 시연으로 참여합니다"
  - `.source`: 근거: 사용자 제공 강의안·시간표 · Claude Code 공식 문서 How Claude Code works
- 강조: `.time-block.is-accent`(5분 역할 설명·대조) 1개 + done-box 파랑 테두리(컴포넌트 고정).
- 글자 크기: 72(제목·분 숫자) / 40 / 36 / 28 (4종, S08과 같음)
- 넘침 근거
  - 가용 높이: 872 − 145 − board-meta 51 − 48 − 58 = **570px**
  - 폭(좌): 850px, bullets 798px. 최장 li 2 ≈ 한글 13×40 + 공백 4×12 = 568px → 1줄. done-text ≈ 한글 23×36 + 공백 7×11 = 905px > 안쪽 774px → **2줄**(허용)
  - 폭(우): 773px. 칸 폭 (773 − 40)/3 = 244, 안쪽 186px(강조 칸 184px). `.what` "실행·접속 확인"·"폴더·파일 읽기"·"역할 설명·대조"는 모두 ≈ 한글 6×28 + 기호·공백 17 = **185px로 경계**다. 강조 칸은 2줄("역할 / 설명·대조")로 접힐 가능성이 크다 → 문구 축소 요청 1. fallback ≈ 1,008px > 안쪽 715px → 2줄
  - 높이(좌): 224 + 24 + done-box 202 = **450px**
  - 높이(우): 칸 168(라벨이 2줄이면 206, grid로 세 칸이 같은 높이) + fallback(20 + 36 + 78 + 2 = 136) → **304px (최대 342px) ≤ 570**
- 플레이스홀더: 없음. Claude Code 실행 화면은 실습 중 강사 시연으로 대신한다. 슬라이드에 넣기로 하면 안내판 구조를 바꿔야 하므로 문구 보류 10이 정해진 뒤 다시 배치한다
- 빌드 메모: 설치·로그인 명령과 주소를 넣지 않는다. 오전 안내판 3장과 같이 `.fallback`은 우측 시간 블록 아래, 좌측은 `.stack`.

## S27 · 환경 확인과 다음 작업

- data-skill: `title-bullets` · section 클래스: `scene clip`
- 배치 순서: eyebrow → 제목 → `.stack` [ `ul.bullets`(3개) → `.callout` ] → `.source`
- 요소 ↔ 문구
  - 제목: 환경 확인과 다음 작업
  - `li` 1: `<strong>파일 읽기</strong>` 실습 폴더의 파일을 읽었다
  - `li` 2: `<strong>실행 방법</strong>` 앱을 실행하는 방법을 찾았다
  - `li` 3: `<strong>개선 목표</strong>` 고칠 기능과 완료 조건을 적었다
  - `.callout`: `<strong>14:20</strong>` + " 적어 둔 기능을 계획하고, 고치고, 확인합니다" (strong 뒤 공백 1칸 유지)
  - `.source`: 근거: 사용자 제공 강의안·시간표
- 강조: bullets strong 3개(같은 역할의 확인 항목 이름, 한 묶음) + callout strong "14:20" = 2묶음.
- 글자 크기: 72 / 44 / 34 (3종)
- 넘침 근거
  - 폭: bullets 1,560 − 52 = 1,508px. 최장 li 3 ≈ 한글 17×44 + 공백 5×13 = 813px → 1줄. callout ≈ "14:20" 90 + 한글 18×34 + 공백·쉼표 70 + 패딩 72 = 850px < 1,680px → 1줄
  - 높이: bullets 3×62 + 2×28 = 242, + 24 + 104 = 370. 145 + 56 + 370 + 58 = **629px ≤ 872**. 아래 약 240px는 빈 여백이다(손들기 점검 장면, 의도)
- 플레이스홀더: 없음
- 빌드 메모: 장면 직속 `.stack`이라 제목 아래 56px 간격이 CSS로 들어간다(오전 C1 반영). B5 S33과 같은 구조(확인 bullets → callout)로 두 구간의 마무리 장면을 맞춘다. 파일명·실행 명령을 넣지 않는다.

---

## 카탈로그 추가 요청

| ID | 요청 | 이유 | 본문 대체안 |
|---|---|---|---|
| F (선택) | `.dg-row.even > .dg-zone { flex: 1; min-width: 0; }` (새 변형 클래스 `.even`) | S23 1행을 문구 원안대로 "영역 ⇄ 영역"으로 그리려면 두 영역이 줄 폭을 나눠 가져야 한다. 지금 `.dg-zone`은 내용 폭만 차지하므로 "모델" 상자 하나만 든 영역(약 168px)에서 라벨 "승인된 모델 경로"(약 177px)가 2줄로 접히고 상자와 겹친다. 기본 `.dg-row`에 바로 적용하면 오전 S06 폭이 바뀌므로 변형 클래스로 한정한다 | 요청 없이도 본문은 완성된다. 오른쪽을 영역 대신 `.dg-box.accent` "모델" + `.dg-sub` "승인된 모델 경로"로 둔다(`dg-box`의 `flex:1`로 남은 폭 297px 확보) |

## 문구 축소 요청(slide_content_writer)

| # | 장면 · 요소 | 현재 문구 | 권장 문구 | 이유 |
|---|---|---|---|---|
| 1 | S26 · `.time-block .what` 3칸 | 실행·접속 확인 / 폴더·파일 읽기 / 역할 설명·대조 | 실행·접속 / 파일 읽기 / 설명·대조 | `.cols-3` 칸 안쪽 186px(강조 칸 184px)에 6자 라벨이 약 185px라 경계다. 강조 칸이 "역할 / 설명·대조" 2줄로 접히면 오전 안내판 3장(모두 1줄)과 달라 보인다. 넘침은 아니다(2줄이어도 우측 342px ≤ 570px). 거절하면 원문 그대로 빌드한다 |

---

# v0.4 배치 (2026-09-15)

작성: slide_ui_designer · 입력: `slide_copy/B4.md` "# v0.4 보강 (2026-09-15)" 절·v0.4 보류 목록, `00_component_catalog.md` 5·7절, `index.html` `<style id="scene-styles">`(v0.4 텍스트 층 포함) 실측값, topic `DESIGN.md` 최소 적용 규칙, 계획서 7-3절. index.html은 수정하지 않았다.
작업 순서: S22 → S23 → S24 → S26 → S27 → (B5 S28–S33) → S25. S25는 B6 공통 6칸 틀을 확인한 뒤 맨 마지막에 썼으므로 이 절에서 S27 뒤에 있다.

## v0.4 공통 배치 규칙 (B4·B5)

- 배치 순서: `eyebrow` → `.act-tag`(S22만) → `h2.scene-title` → `p.thesis` → `p.explain` → 본문 컴포넌트 1개 → `p.source` → `aside.speaker-note` → 풋터·쪽 번호. 실습 안내판(S26)은 `.thesis` 없이 `p.board-meta` → `p.explain`(`<strong>관찰할 것</strong>` + 2문장) → `.practice-grid`.
- 높이 값(scene-styles 실측): eyebrow+제목 1줄 145 · 막 태그 49 · 요지 81 · 설명 1줄 62 / 2줄 109 / 3줄 155 · 본문 위 간격 32(`.scene > .thesis + *`, `.scene > .explain + *` 규칙이 `.split-grid`·`.compare-grid`·`.stack`·`.practice-grid`의 56·48px을 이긴다) · 출처 1줄 58 · board-meta 51.
- 부품 높이: `.dg-box`(주 글자 + `.dg-sub` 1줄) 123, 강조 테두리 2px이면 125. 이 값은 보수적으로 잡은 것이다. `.dg-sub`는 `.dg-box`의 line-height 1.3을 물려받으므로 CSS 계산값은 121(강조 123)이며, `slide_ui/B6.md` v0.4 측정 기준과 같다. 이 문서의 상자 높이는 2px씩 크게 잡혀 있어 실제 여유는 표보다 2–4px 크다 · `.dg-box` 한 줄 84 · `.dg-zone` 안 상자 1개 207 · `.dg-caption` 1줄 34 · `.callout` 1줄 104 · `.chip`/`.chip-row` 한 줄 57 · `.panel-title` 39 · `.code-card` = 제목 67 + 본문 48 + 줄 수×43.4 + 테두리 2 · `.placeholder.is-spec` = 84 + id 34 + 간격 30 + 설명 줄 수×39.2.
- 여유 = 872 − (위 요소 합). 출처 줄 위 여유는 24px 이상이어야 한다. v0.3 추정값과 실측값의 차이는 ±6px였다(S22 102→105, S23 54→60, S28 130→129). 이 문서의 추정은 조금 보수적이다.
- 글자 폭: 한글 1.0em, 라틴·숫자 0.55em, 공백·기호 0.3em. `→`는 대체 글꼴에서 1.0em로 셌다. 코드: JetBrains Mono 28px에서 16.8px/자, 코드 안 한글은 28px/자.
- 글자 크기 종류는 캡션층(24px 이하: dg-sub·캡션·배지·라벨)을 빼고 센다(v0.3과 같은 방식). v0.4에서 모든 장면에 요지 40·설명 32가 들어오므로 callout(34)이 있는 장면은 5종이 된다. 34와 32는 같은 문장층으로 보고 따로 표시했다.
- compare 열 안에 `.dg`를 넣을 때(S22)는 열을 `class="compare-col stack"`으로 둔다. `.compare-col`에는 자식 사이 간격이 없어서 `.dg`가 열 제목 밑줄에 붙기 때문이다. `.stack`이 gap 24px을 주고 `.stack > *` 규칙이 `.compare-heading`의 위 여백 8px과 `.compare-list`의 위 여백 24px을 0으로 만든다. 그러면 제목 밑줄에서 목록까지 24px로 v0.3과 같은 간격이 된다. 두 열 모두에 붙여 제목 밑줄 높이를 맞춘다.
- `data-editable="true"`는 텍스트 leaf마다 붙인다. `.dg-box` 안의 `.dg-sub`는 기존 마크업처럼 상자에 `data-editable`을 한 번만 붙인다. 화살표·상태 점·체크 칸은 `aria-hidden="true"`.
- 강조는 장면당 파랑 한 곳(열·상자·영역·시간 블록 중 하나)이다. 안내판의 done-box 파랑 테두리는 컴포넌트에 고정된 것이다. 요지 밑줄은 v0.4 공통 층이라 세지 않는다. 남색 반전·그림자·공급사 로고·주소는 없다.

## S22 · 오전 앱과 Coding Agent (v0.4 배치)

- section: `class="scene clip"` · `data-skill="compare"` · `data-scene-id="S22"` (구간 첫 장면: time-chip + act-tag)
- 배치 순서: eyebrow → `.act-tag` → 제목 → `.thesis` → `.explain` → `.compare-grid`(2열) [열1 `.compare-col.stack` 목록 / 열2 `.compare-col.stack.is-accent` 루프 그림] → `.source`
- v0.3 대비: `.scene-subtitle`, 두 열의 kicker와 note, 열2 목록을 뺐다. 열2는 목록 대신 `.dg` 루프를 넣는다.

```html
<section id="s-23" class="scene clip" data-skill="compare" data-scene-id="S22" data-start="110" data-duration="5" data-track-index="0">
  <div class="eyebrow"><span class="block-chip" data-editable="true">B4</span><span data-editable="true">Claude Code와 개발 계획</span><span class="time-chip" data-editable="true">13:20–14:00</span></div>
  <p class="act-tag" data-editable="true"><strong>전 · 뒤집다</strong>오전엔 내가 도구를 붙였고, 오후엔 도구에게 일을 맡긴다</p>
  <h2 class="scene-title" data-editable="true">오전 앱과 Coding Agent</h2>
  <p class="thesis" data-editable="true">모델에 도구를 붙인 점은 같고, 대상이 질문에서 프로젝트로 바뀐다</p>
  <p class="explain" data-editable="true">Claude Code는 모델에 파일·명령 도구를 붙여 관찰→판단→행동을 반복하는 Coding Agent다. 읽고 고치고 확인하는 흐름이 이어져 오후 실습에 쓰며, 항상 낫다는 뜻은 아니다.</p>
  <div class="compare-grid">
    <div class="compare-col stack">
      <div class="compare-heading" data-editable="true">오전 앱</div>
      <ul class="compare-list">
        <li data-editable="true">대상: 업무 질문</li>
        <li data-editable="true">도구: 우리가 연결한 검색·SQL</li>
        <li data-editable="true">결과: 근거를 붙인 답변</li>
      </ul>
    </div>
    <div class="compare-col stack is-accent">
      <div class="compare-heading"><span data-editable="true">Claude Code</span> <span class="concept-badge" data-editable="true">개념 재구성</span></div>
      <div class="dg">
        <div class="dg-row stretch">
          <div class="dg-box" data-editable="true">관찰<span class="dg-sub">파일·결과</span></div>
          <span class="dg-arrow" aria-hidden="true">→</span>
          <div class="dg-box" data-editable="true">판단<span class="dg-sub">모델</span></div>
          <span class="dg-arrow" aria-hidden="true">→</span>
          <div class="dg-box" data-editable="true">행동<span class="dg-sub">도구 실행</span></div>
        </div>
        <p class="dg-caption is-left" data-editable="true">대상: 프로젝트 폴더 · 결과를 보고 다시 반복</p>
      </div>
    </div>
  </div>
  <p class="source" data-editable="true">출처: Claude Code How Claude Code works · Building effective agents · Kaggle Agents 백서(교안 내용 재구성)</p>
  <aside class="speaker-note">(문구 파일 S22 v0.4 발표자 노트 전문)</aside>
  <div class="deck-footer" data-editable="true">AI Agent Guide · SK hynix 사내 교육</div>
  <div class="page-num">23 / 57</div>
</section>
```

| 요소 | 높이(px) | 근거 |
|---|---|---|
| eyebrow + 제목 1줄 | 145 | 제목 약 789px < 1,600 → 1줄 |
| `.act-tag` 1줄 | 49 | 강조어 약 149 + 본문 약 711 = 860px < 1,680 |
| `.thesis` 1줄 | 81 | 한글 27×40 + 공백·쉼표 9×12 = 1,188px < 1,680 |
| `.explain` 2줄 | 109 | 라틴 21자 370 + 한글 57×32 = 1,824 + 공백·기호 약 259 + `→` 2개 보정 45 = 약 2,498px ÷ 1,600 = 1.56줄 |
| 본문 위 간격 | 32 | |
| `.compare-grid` | 361 | 열2 = 패딩 80 + 테두리 4 + 제목 72 + 24 + `.dg`(상자 123 + 24 + 캡션 34 = 181). 열1 = 82 + 72 + 24 + 목록 170 = 348 → 행 높이는 361 |
| `.source` 1줄 | 58 | 약 1,240px |
| **합계** | **835** | **여유 37px** (≥ 24) |

- 가로: 열 폭 (1,680 − 40)/2 = 820, 열 안쪽 740(강조 열 736). 열2 루프는 상자 3개 + 화살표 2개(각 40) + 간격 4×20이다. 상자 폭은 (736 − 80 − 80)/3 = **192px**, 상자 안쪽은 142px. 주 글자 2자 64px, `.dg-sub` 최장 "파일·결과"·"도구 실행"은 약 103px → 모두 1줄. 캡션 약 440px < 736 → 1줄. 열2 제목은 "Claude Code" 232px + 배지 약 147px = 약 400px < 736 → 1줄.
- 열1 목록: 안쪽 706px. 최장 "도구: 우리가 연결한 검색·SQL" 약 421px → 1줄. 세 항목이 모두 1줄이어서 열1 목록 높이(170)가 열2 루프 높이(181)와 거의 같다. 두 열 카드 속이 고르게 찬다.
- 강조: `.compare-col.is-accent`(열2) 한 곳. 루프 상자 3개는 기본 흰 상자다(`.dg-box.accent` 없음). 배지 "개념 재구성"은 회색 테두리 배지다.
- 글자 크기: 72 / 40 / 32(설명·목록·상자) / 28(막 태그). 캡션층 24·22 제외 4종.
- 주의점
  - 관찰→판단→행동 루프는 열2(Claude Code)에만 둔다. 오전 앱이 Agent인지 확인되지 않았다(보류 P2). 열1에 화살표나 루프를 추가하지 않는다.
  - 되돌아가는 화살표는 카탈로그 문자(`→`, `↓`, `⇄`)로 그릴 수 없다. 그래서 캡션 "결과를 보고 다시 반복"이 대신한다. 곡선이나 SVG를 새로 만들지 않는다.
  - `.dg-row.stretch`를 쓰면 `.dg-sub`가 한 상자에서만 2줄로 접혀도 세 상자 높이가 같게 유지된다.
  - 카탈로그 7-3의 "교안 내용 재구성" 표기는 배지 "개념 재구성"과 출처 줄로 채웠다. QA가 캡션에도 있어야 한다고 하면 캡션 끝에 " · 교안 내용 재구성"을 붙여도 약 630px로 1줄에 들어간다.
  - 위험: 설명 문단이 3줄로 늘면 +46px이 되어 여유가 −9px로 넘친다. 설명 문단에 글자를 더하지 않는다.
- 플레이스홀더: 없음 (S22 끝)

## S23 · 로컬 실행과 접근 범위 (v0.4 배치)

- section: `class="scene clip"` · `data-skill="split"` · `data-scene-id="S23"`
- 배치 순서: eyebrow → 제목 → `.thesis` → `.explain` → `.split-grid.wide-left` [좌 `.dg` / 우 `ul.bullets`] → `.source`
- v0.3 대비: `.split-panel` 감싸개, 단독 배지, li 2의 `.sub`를 뺐다. 오른쪽 "모델"은 점선 영역 `승인된 모델 경로` 안에 넣어 두 영역 구도로 둔다(CSS 요청 F 필요, 아래 참고).

```html
<div class="split-grid wide-left">
  <div class="dg">
    <div class="dg-row even">
      <div class="dg-zone">
        <div class="dg-zone-label" data-editable="true">내 PC · 실행 환경</div>
        <div class="dg-box" data-editable="true">Claude Code<span class="dg-sub">파일·명령 실행</span></div>
      </div>
      <div class="stack">
        <span class="dg-arrow" aria-hidden="true">⇄</span>
        <span class="dg-sub" data-editable="true">요청·맥락</span>
      </div>
      <div class="dg-zone">
        <div class="dg-zone-label" data-editable="true">승인된 모델 경로</div>
        <div class="dg-box accent" data-editable="true">모델<span class="dg-sub">응답 생성</span></div>
      </div>
    </div>
    <div class="dg-row">
      <div class="dg-box muted" data-editable="true">화면 조작·사내 시스템: 별도 도구·권한</div>
    </div>
    <p class="dg-caption" data-editable="true">개념 예시 · 주소·공급사 대신 역할 이름으로 표시</p>
  </div>
  <ul class="bullets">
    <li data-editable="true">파일·명령은 내 PC에서 실행</li>
    <li data-editable="true">모델 처리는 설정된 경로에서</li>
    <li data-editable="true">할 수 있는 일은 도구·권한 범위</li>
  </ul>
</div>
```

- 요지: 실행 위치와 모델이 처리하는 위치는 다르다
- 설명: 파일 읽기·수정과 명령은 내 PC에서 실행되지만, 요청과 읽은 파일 내용은 모델 경로로 전송된다. 그래서 허용된 경로와 보낼 자료 범위를 먼저 확인한다. 화면 조작·사내 시스템은 별도 도구·권한이 필요할 수 있다.
- 출처: Claude Code 공식 문서 How Claude Code works · Permissions · Data usage

| 요소 | 높이(px) | 근거 |
|---|---|---|
| eyebrow + 제목 | 145 | |
| `.thesis` 1줄 | 81 | 한글 19×40 + 공백 4×12 = 808px |
| `.explain` 3줄(보수) | 155 | 한글 약 83×32 + 라틴 2 + 공백·기호 33 = 약 3,008px ÷ 1,600 = 1.88줄. keep-all 줄바꿈 손실로 3줄이 될 수 있어 3줄로 잡음 |
| 본문 위 간격 | 32 | |
| `.split-grid` | 373 | 좌 `.dg` = 1행 207 + 24 + 2행 84 + 24 + 캡션 34. 우 bullets = 3×56 + 2×28 = 224 |
| `.source` | 58 | |
| **합계** | **844** | **여유 28px** (설명이 2줄로 끝나면 74px) |

- 가로: 좌 913px / 우 703px(간격 64). `.dg-row.even`에서 두 영역이 남은 폭을 나눠 가진다. 화살표 묶음(`.stack`, "요청·맥락" 약 103px) + 간격 40을 빼면 영역 폭은 각각 (913 − 103 − 40)/2 = **385px**이다. 영역 라벨 "내 PC · 실행 환경" 약 170px과 "승인된 모델 경로" 약 169px은 385 − 24 = 361px 안에서 1줄로 들어간다. 상자 안쪽 약 283px에는 "Claude Code" 186px과 "파일·명령 실행" 134px가 들어간다. 2행 muted 상자는 약 496px < 865 → 1줄. 캡션 약 416px → 1줄.
- 우 bullets: 안쪽 651px. 최장 "할 수 있는 일은 도구·권한 범위" 약 540px → 1줄.
- 1행은 기본 `.dg-row`(세로 가운데 정렬)로 둔다. 문구 파일은 `.stretch`를 적었지만 두 영역의 높이가 같다(각 207). 그리고 `.stretch`로 두면 화살표 묶음 `.stack`이 행 높이로 늘어나 화살표가 위에 붙는다(`.dg-row.stretch > .dg-arrow` 가운데 정렬 규칙은 직계 화살표에만 적용된다).
- 강조: `.dg-box.accent`(모델) 한 곳. 두 영역은 모두 회색 점선이다(`.dg-zone.accent` 없음. 강조가 두 곳이 되지 않게 한다). 2행 muted 상자는 "기본으로 되지 않는 일"을 중립색으로 구분한다.
- 글자 크기: 72 / 40 / 32(설명·상자) / 40(bullets, 요지와 같음). 캡션층 제외 3종.
- 주의점
  - CSS 요청 F(`.dg-row.even`)가 반영되지 않으면 영역이 내용 폭만 차지한다. 그러면 1행이 약 638px로 왼쪽에 몰리고 오른쪽에 약 275px이 빈다. 넘치지는 않는다(영역2 약 207px, 라벨 169px < 183px). F가 없을 때는 `even`을 빼고 빌드한다. 모양 문제만 남는다.
  - 공급사 로고·주소·모델명을 넣지 않는다. 역할 이름만 쓴다.
  - 계획서의 "배경 톤으로 영역 구분"은 카탈로그 7-3에 따라 점선 영역(`.dg-zone`)으로 대신한다.
- 플레이스홀더: 없음 (S23 끝)

## S24 · 변경하기 쉬운 프로그램 구조 (v0.4 배치)

- section: `class="scene clip"` · `data-skill="split"` · `data-scene-id="S24"`
- 배치 순서: eyebrow → 제목 → `.thesis` → `.explain` → `.split-grid.wide-left` [좌 `.stack`(3층 그림 → 역할 띠 → 캡션) / 우 `ul.bullets` 2개] → `.source`
- v0.3 대비: 4행 표와 패널 머리줄, li "바꿀 곳과 확인할 곳이 좁아짐"을 뺐다. 표 대신 화면·처리·데이터 3층 그림을 넣는다. 오전 S21 3층 그림, B5 S29와 층 이름이 같다.

```html
<div class="split-grid wide-left">
  <div class="stack">
    <div class="dg-row">
      <div class="dg-zone accent">
        <div class="dg-zone-label" data-editable="true">이번 변경 · 검색 결과 없음 안내</div>
        <div class="dg-row">
          <div class="dg-box" data-editable="true">화면<span class="dg-sub">안내 문구 추가</span></div>
          <span class="dg-arrow" aria-hidden="true">⇄</span>
          <div class="dg-box" data-editable="true">처리<span class="dg-sub">0건이면 안내</span></div>
        </div>
      </div>
      <span class="dg-arrow" aria-hidden="true">⇄</span>
      <div class="dg-box" data-editable="true">데이터<span class="dg-sub">형태만 확인</span></div>
    </div>
    <div class="dg-row">
      <div class="panel-title" data-editable="true">코드를 역할별로 나눈 예</div>
      <div class="chip-row">
        <span class="chip" data-editable="true">연결 설정</span>
        <span class="chip" data-editable="true">조립</span>
        <span class="chip" data-editable="true">도구</span>
        <span class="chip" data-editable="true">진입점</span>
      </div>
    </div>
    <p class="dg-caption is-left" data-editable="true">개념 예시 · 이름·파일 구성은 실습 코드와 다를 수 있음</p>
  </div>
  <ul class="bullets">
    <li data-editable="true">영역마다 입력·출력을 정해 둠</li>
    <li data-editable="true">작은 앱은 함수 분리와 README로</li>
  </ul>
</div>
```

- 요지: 경계와 입출력을 먼저 정하면 바꿀 곳과 확인할 곳이 좁아진다
- 설명: 강사는 개발 후반에 작은 수정 하나가 여러 곳에 번져 고치기 어려웠던 경험이 있다. 처음부터 완벽한 구조보다, 바뀌기 쉬운 부분의 경계와 주고받는 값을 먼저 정한다.
- 출처: 근거: 강사의 아키텍처 회고(사용자 제공) · Claude Code 공식 문서 Best practices

| 요소 | 높이(px) | 근거 |
|---|---|---|
| eyebrow + 제목 | 145 | 제목 약 1,000px → 1줄 |
| `.thesis` 1줄 | 81 | 한글 26×40 + 공백 7×12 = 1,124px |
| `.explain` 2줄 | 109 | 한글 약 68×32 + 공백·기호 24×9.6 = 약 2,406px ÷ 1,600 = 1.5줄 |
| 본문 위 간격 | 32 | |
| `.split-grid` | 346 | 좌 = 3층 줄 207 + 24 + 역할 띠 57 + 24 + 캡션 34. 우 = 2×56 + 28 = 140 |
| `.source` | 58 | |
| **합계** | **771** | **여유 101px** |

- 가로(3층 줄, 좌 913px): 강조 영역은 내용 폭을 차지한다. 안쪽 줄은 상자 2개 + 화살표 40 + 간격 40이다. "안내 문구 추가"가 약 158px이라 상자는 각 약 199px, 영역은 약 530px이 된다. 영역 라벨 "이번 변경 · 검색 결과 없음 안내"는 약 291px < 506px → 1줄. 바깥 화살표 40 + 간격 40을 빼면 "데이터" 상자는 약 303px이다(`flex:1`로 남은 폭을 받음). 상자 폭이 199 / 199 / 303으로 조금 다르지만, "데이터"가 강조 영역 밖에 있는 것이 폭 차이로도 보이므로 그대로 둔다. 이 줄에는 `.even`을 쓰지 않는다. 쓰면 영역 안 상자가 142px로 좁아져 "안내 문구 추가"가 2줄로 접힌다.
- 세로 정렬: 3층 줄은 기본 `.dg-row`(가운데 정렬)로 둔다. "데이터" 상자(123)는 영역(207) 높이의 가운데에 온다. `.stretch`로 두면 "데이터" 상자가 207px로 늘어나 글자가 위에 붙고 아래가 빈다.
- 역할 띠: 소제목 약 305px + 간격 20 + 칩 줄(연결 설정 158 · 조립 94 · 도구 94 · 진입점 122 + 간격 36 = 504px) = 약 829px < 913 → 한 줄(57px). 오전·v0.3과 같은 "스플릿 머리줄 `.dg-row`" 방식이다. 문구 파일 추정(띠 108px, 두 줄)보다 51px 줄었다.
- 우 bullets: 안쪽 651px. "작은 앱은 함수 분리와 README로" 약 580px → 1줄.
- 강조: `.dg-zone.accent`(이번 변경 두 곳을 묶은 영역) 한 곳. 상자·칩은 모두 기본색이다. 계획서의 "파랑 테두리 2곳"은 영역 하나로 묶었다.
- 글자 크기: 72 / 40(요지·bullets) / 32(설명·상자) / 28(소제목·칩). 캡션층 제외 4종.
- 주의점
  - 칩 이름 4개는 역할 이름이다. 현업가이드 파일명이나 실습 코드 파일명으로 바꾸지 않는다(카탈로그 7-7). 실습 코드에 해당 구분이 없으면 띠를 통째로 뺀다(보류 7). 빼도 여유만 81px 늘어난다.
  - 기본 과제가 이미 구현돼 있다는 답이 오면(보류 6·Q2) 영역 라벨과 상자 보조 두 개만 바꾼다. 글자 수가 늘면 영역 폭이 넓어지고 "데이터" 상자가 좁아진다. "데이터" 상자는 약 180px까지 줄어도 1줄이다(보조 5자 127px 기준).
- 플레이스홀더: 없음 (S24 끝)

## S26 · 실습 5 · Claude Code 실행 (v0.4 배치)

- section: `class="scene clip"` · `data-skill="title-bullets"` · `data-scene-id="S26"` (실습 안내판)
- 배치 순서: eyebrow → 제목 → `.board-meta` → `.explain`(관찰할 것) → `.practice-grid` [좌 `.stack`(시간 블록 → done-box → fallback) / 우 `.stack`(플레이스홀더 v2 → callout)] → `.source`
- v0.3 대비: 단계 bullets 3줄을 뺐다. 시간 블록을 좌측 맨 위로, fallback을 좌측 맨 아래로 옮기고, 우측에는 플레이스홀더와 띠를 둔다. 좌측 폭 851px이라 시간 블록 칸이 v0.3(244px)보다 넓어진다(270px).

```html
<p class="board-meta" data-editable="true">13:36–13:56 · 20분 · 실습 가이드 해당 장</p>
<p class="explain" data-editable="true"><strong>관찰할 것</strong>각 파일의 역할 설명을 받은 뒤 실제 파일을 열어 대조한다. 앞서 예상한 파일 읽기·Python 실행·화면 클릭 중 실제로 되는 것을 확인한다.</p>
<div class="practice-grid">
  <div class="stack">
    <div class="time-blocks cols-3">
      <div class="time-block"><div class="min" data-editable="true">10<small>분</small></div><div class="what" data-editable="true">실행·접속</div></div>
      <div class="time-block"><div class="min" data-editable="true">5<small>분</small></div><div class="what" data-editable="true">파일 읽기</div></div>
      <div class="time-block is-accent"><div class="min" data-editable="true">5<small>분</small></div><div class="what" data-editable="true">설명·대조</div></div>
    </div>
    <div class="done-box"><div class="done-label" data-editable="true">완료 조건</div><div class="done-text" data-editable="true">구조 설명을 실제 파일과 대조한다</div></div>
    <div class="fallback" data-editable="true"><strong>막히면</strong>우회하지 말고 짝 실습·강사 시연으로</div>
  </div>
  <div class="stack">
    <div class="placeholder is-spec">
      <div class="placeholder-id" data-editable="true">[IMG-PLACEHOLDER · P-S26]</div>
      <div class="placeholder-desc" data-editable="true">넣을 자료: Claude Code가 실습 폴더 구조를 설명하는 화면</div>
      <div class="placeholder-desc" data-editable="true">조건: 모델명·계정·주소·키 가리기 · 실행일 표기</div>
      <div class="placeholder-desc" data-editable="true">대체: 확보 전에는 강사 시연 화면으로 대신</div>
    </div>
    <div class="callout" data-editable="true"><strong>읽기·설명까지</strong> 수정은 14:20부터</div>
  </div>
</div>
<p class="source" data-editable="true">근거: 사용자 제공 강의안·시간표 · Claude Code 공식 문서 How Claude Code works · Best practices</p>
```

(조건 줄은 문구 축소 요청 B4-v4-1을 반영한 문구다. 원문은 끝에 " · 1600×900 이상"이 붙는다.)

| 요소 | 높이(px) | 근거 |
|---|---|---|
| eyebrow + 제목 | 145 | |
| `.board-meta` | 51 | |
| `.explain` 2줄 | 109 | 강조어 150 + 한글 50×32 + Python 106 + 공백·기호 211 = 약 2,067px ÷ 1,600 = 1.3줄 |
| 본문 위 간격 | 32 | `.practice-grid`의 48px 대신 32px |
| `.practice-grid` | 477 | `flex:1`이라 남은 높이를 모두 채운다 |
| `.source` | 58 | |
| **합계** | **872** | **출처 줄 위 여유 0px (설계상. v0.3 S26·S32와 같음)** |

격자 칸 안 여유(이 장면에서 판정할 값):

| 칸 | 내용 | 합 | 칸 안 여유 |
|---|---|---|---|
| 좌 851px | 시간 블록 170 + 24 + done-box 152 + 24 + fallback 77 | 447 | **30px** |
| 우 773px | 플레이스홀더(84 + id 34 + 간격 30 + 설명 4줄 157 = 305) + 24 + callout 104 | 433 | **44px** (원문 조건 줄이면 5줄 344 → 5px) |

- 가로
  - 시간 블록 칸: (851 − 40)/3 = 270, 안쪽 212px. "설명·대조" 약 120px → 1줄. v0.3의 경계 문제(185/186px)가 없어진다.
  - done-text "구조 설명을 실제 파일과 대조한다" 약 512px < 775 → 1줄(v0.3은 2줄). fallback 약 530px < 793 → 1줄(v0.3은 2줄).
  - 플레이스홀더 폭 773px = 화면 폭의 40%(≤ 50%). 안쪽 689px. "넣을 자료…" 약 734px → 2줄. 원문 조건 줄 "…· 1600×900 이상"은 약 786px → 2줄이다. 축소한 조건 줄은 약 552px → 1줄. "대체…" 약 442px → 1줄.
  - callout 약 460 + 패딩 72 = 532px < 773 → 1줄.
- 플레이스홀더 높이 동작: `.placeholder`는 `height:100%; min-height:240px`이다. 우 `.stack`이 격자 칸 높이(477)로 늘어나므로 플레이스홀더는 flex 축소로 477 − 24 − 104 = **349px**가 되고 내용은 가운데 정렬된다. 내용 305px < 349 → 넘치지 않는다. 원문 조건 줄이면 내용이 344px이라 여유가 5px뿐이다(글꼴 차이로 잘릴 수 있음). 그래서 문구 축소 요청 B4-v4-1을 올린다.
- 강조: `.time-block.is-accent`(5분 설명·대조) 한 곳 + done-box 파랑 테두리(컴포넌트 고정). callout은 파랑 틴트 띠라 strong "읽기·설명까지"가 파랑 글자다. 강조 묶음은 "설명·대조" 1곳 + "읽기·설명까지" 1곳으로 둘 다 "이번 실습은 읽기·설명"이라는 한 뜻을 가리킨다.
- 글자 크기: 72(제목·분 숫자) / 36(done-text) / 34(callout) / 32(설명) / 28(board-meta·what·fallback·자리 설명). 5종이다(34·32는 같은 문장층). callout을 빼면 4종이 되지만, 이 띠가 "수정은 14:20부터"를 알리는 유일한 화면 문구라서 유지한다.
- 주의점
  - 가이드 장 이름은 `board-meta`에만 둔다(`.guide-ref` 없음). 설치·로그인 명령과 주소는 넣지 않는다.
  - 오전 안내판(S08·S13·S19)은 좌측이 bullets인 배치다. S26은 단계 목록이 없어 좌우 역할이 다르다. 같은 v0.4 구성(시간 블록이 좌측 위, 플레이스홀더가 우측)은 B5 S32와 맞추지 않았다(S32는 플레이스홀더 없음). 실습 안내판 v0.4 공통 모양은 B1–B3 배치 결과와 T8 전체 QA에서 대조한다.
  - 캡처가 들어오면 `.placeholder` 자리를 `<div class="split-image h-md"><img …></div>`(360px)로 바꾼다. 우측 = 360 + 24 + 104 = 488 > 477이므로 **h-sm(280px)을 쓴다**(우측 408px, 칸 안 여유 69px).
- 플레이스홀더: P-S26 (우측, 773×349px 자리) (S26 끝)

## S27 · 환경 확인과 다음 작업 (v0.4 배치)

- section: `class="scene clip"` · `data-skill="title-bullets"` · `data-scene-id="S27"`
- 배치 순서: eyebrow → 제목 → `.thesis` → `.explain`(1줄) → `.stack` [체크 카드 `.dg-row.stretch` → 막힌 단계 띠 `.dg-row` → `.callout`] → `.source`
- v0.3 대비: bullets 3줄을 체크 카드 3개로 바꿨다. 막힌 단계 5칸 띠를 새로 넣었다. callout은 그대로다. B5 S33과 같은 틀(상자 3개 → 캡션이나 띠 → callout)이다.
- 체크 칸: 특수 글자 ☐를 쓰지 않는다. CSS로 그린 빈 사각형 `.dg-check`(CSS 요청 G)를 카드 제목 앞에 인라인으로 둔다.

```html
<div class="stack">
  <div class="dg-row stretch">
    <div class="dg-box"><span class="dg-check" aria-hidden="true"></span><span data-editable="true">파일 읽기</span><span class="dg-sub" data-editable="true">실습 폴더의 파일을 읽었다</span></div>
    <div class="dg-box"><span class="dg-check" aria-hidden="true"></span><span data-editable="true">실행 방법</span><span class="dg-sub" data-editable="true">앱을 실행하는 방법을 찾았다</span></div>
    <div class="dg-box"><span class="dg-check" aria-hidden="true"></span><span data-editable="true">개선 목표</span><span class="dg-sub" data-editable="true">고칠 기능과 완료 조건을 적었다</span></div>
  </div>
  <div class="dg-row">
    <div class="panel-title" data-editable="true">막힌 단계에 손들기</div>
    <div class="chip-row">
      <span class="chip" data-editable="true">설치</span>
      <span class="chip" data-editable="true">접속</span>
      <span class="chip" data-editable="true">폴더 열기</span>
      <span class="chip" data-editable="true">파일 읽기</span>
      <span class="chip" data-editable="true">실행법</span>
    </div>
  </div>
  <div class="callout" data-editable="true"><strong>14:20</strong> 적어 둔 기능을 계획하고, 고치고, 확인합니다</div>
</div>
```

- 요지: 14:20의 준비물은 읽은 파일, 실행 방법, 완료 조건 한 줄이다
- 설명(1줄): 환경이 안 된 사람은 휴식 뒤 짝과 같은 화면으로 진행한다.
- 출처: 근거: 사용자 제공 강의안·시간표
- 카드 안 편집 단위: 이 카드는 체크 칸 span이 있어서, 상자 전체에 `data-editable`을 붙이면 체크 칸까지 편집 영역에 들어간다. 그래서 제목 span과 `.dg-sub` span에 각각 `data-editable`을 붙인다. `.dg-sub`는 `display:block`이라 제목 아래 줄로 내려간다.

| 요소 | 높이(px) | 근거 |
|---|---|---|
| eyebrow + 제목 | 145 | |
| `.thesis` 1줄 | 81 | 한글 27×40 + "14:20" 110 + 공백·기호 9×12 = 약 1,298px |
| `.explain` 1줄 | 62 | 약 850px < 1,600 |
| 본문 위 간격 | 32 | |
| `.stack` | 332 | 카드 123 + 24 + 띠 57 + 24 + callout 104 |
| `.source` | 58 | |
| **합계** | **710** | **여유 162px** (v0.3 실측 246px. 손들기 점검 장면이라 아래 여백은 의도한 것) |

- 가로: 카드 폭 (1,680 − 40)/3 = 547, 안쪽 약 496px. 제목 줄 = 체크 칸 28 + 12 + "파일 읽기" 138 = 178px. 보조 최장 "고칠 기능과 완료 조건을 적었다" 약 340px → 1줄. 띠 = 소제목 약 241 + 20 + 칩 5개(94·94·158·158·122 + 간격 48 = 674) = 약 935px < 1,680 → 한 줄. callout 약 850px → 1줄.
- 강조: 없음(점검 장면). callout strong "14:20"만 파랑이다. 체크 칸은 회색 테두리(`--ink-3`)다. 칩은 모두 기본색이다(막힌 단계 중 하나를 강조하지 않는다).
- 글자 크기: 72 / 40 / 34(callout) / 32(설명·카드 제목) / 28(소제목·칩). 5종이다(34·32는 같은 문장층).
- 주의점
  - CSS 요청 G가 반영되기 전에는 `.dg-check` span을 빼고 빌드한다(카드는 그대로 읽힌다). ☐·□ 같은 글자로 대신하지 않는다.
  - 칩 "설치"·"접속"은 단계 이름만 쓴다. 설치 명령·주소를 넣지 않는다.
  - 카드는 가운데 정렬(`.dg-box` 기본)이다. 체크 칸은 제목 앞에 붙어 제목과 함께 가운데에 온다. 계획서의 "왼쪽 위 체크 칸"을 절대 위치로 두려면 CSS가 하나 더 필요해서 인라인으로 단순화했다.
- 플레이스홀더: 없음 (S27 끝)

## S25 · 작업 요청과 완료 조건 (v0.4 배치)

- section: `class="scene clip"` · `data-skill="compare"`(값 유지. 구조는 `.compare-grid`가 아니라 위아래 비교. validate는 허용값만 검사한다. B6 S36과 같은 판단)
- 작업 시점 확인: 이 장면을 쓸 때 `slide_ui/B6.md`에 "## 공통 컴포넌트 (v0.4)" 절이 없었다(S37까지 작성된 상태). 그래서 아래 "공통 컴포넌트 제안 — 요청문 6칸 틀"을 이 문서에서 정의한다. 오케스트레이터가 B6 S40과 맞춘다.
- 구성 판단: 작가안(막연한 요청 한 줄 + 3×2 칸)을 **채택**한다. `.compare-grid` 2열에 6칸을 넣으면 한 열 폭 820px에 칸 이름·예시 6줄 목록과 열 제목·kicker가 들어가야 한다. 그러면 약 540px로 가용 447px을 넘는다. 3×2 칸은 칸마다 이름과 예시가 한 상자에 들어가 S40 빈칸 양식과 같은 모양이 된다. 비교 구도는 위 주황 점(막연한 요청)과 아래 초록 점(6칸 캡션)으로 남긴다.
- 배치 순서: eyebrow → 제목 → `.thesis` → `.explain`(1줄, 문구 축소 요청 B4-v4-2) → `.stack` [막연한 요청 `.dg-row` → 요청문 6칸 틀 `.dg`] → `.source`

```html
<section id="s-26" class="scene clip" data-skill="compare" data-scene-id="S25" data-start="125" data-duration="5" data-track-index="0">
  <div class="eyebrow"><span class="block-chip" data-editable="true">B4</span><span data-editable="true">Claude Code와 개발 계획</span></div>
  <h2 class="scene-title" data-editable="true">작업 요청과 완료 조건</h2>
  <p class="thesis" data-editable="true">입력 예와 기대 결과가 곧 완료 조건이다</p>
  <p class="explain" data-editable="true">(B4-v4-2 반영 1줄 문장. 예: 여섯 칸을 채우면 작업 방향과 확인 방법이 함께 정해진다.)</p>
  <div class="stack">
    <div class="dg-row">
      <div class="dg-box muted"><span class="status-dot warn" aria-hidden="true"></span><span data-editable="true">“앱 개선해줘” — 무엇을·어디까지·언제 끝인지 없음</span></div>
    </div>
    <div class="dg">
      <div class="dg-row stretch">
        <div class="dg-box" data-editable="true">목표<span class="dg-sub">결과 0건일 때 안내 표시</span></div>
        <div class="dg-box" data-editable="true">입력<span class="dg-sub">없는 키워드로 검색</span></div>
        <div class="dg-box" data-editable="true">출력<span class="dg-sub">“관련 기사 없음” 안내 문구</span></div>
      </div>
      <div class="dg-row stretch">
        <div class="dg-box" data-editable="true">범위<span class="dg-sub">결과 표시 부분만 수정</span></div>
        <div class="dg-box" data-editable="true">제약<span class="dg-sub">새 패키지 설치 없이</span></div>
        <div class="dg-box accent" data-editable="true">완료 조건<span class="dg-sub">없는 키워드 → 안내, 기존 질문 그대로</span></div>
      </div>
      <p class="dg-caption is-left"><span class="status-dot ok" aria-hidden="true"></span><span data-editable="true">완료 조건이 있는 요청문 6칸 · 개념 예시</span></p>
    </div>
  </div>
  <p class="source" data-editable="true">출처: Claude Code 공식 문서 Best practices · 근거: 사용자 제공 강의안(요청문 템플릿)</p>
  <aside class="speaker-note">(문구 파일 S25 v0.4 발표자 노트 전문)</aside>
  <div class="deck-footer" data-editable="true">AI Agent Guide · SK hynix 사내 교육</div>
  <div class="page-num">26 / 57</div>
</section>
```

| 요소 | 높이(px) | 근거 |
|---|---|---|
| eyebrow + 제목 | 145 | |
| `.thesis` 1줄 | 81 | 한글 17×40 + 공백 4×12 = 728px |
| `.explain` **1줄**(요청 반영) | 62 | 45자 이내 → 1,600px 이내 |
| 본문 위 간격 | 32 | |
| `.stack` | 434 | 막연한 요청 84 + 24 + 6칸 틀(121 + 24 + 123 + 24 + 캡션 34 = 326). 상자 높이는 CSS 계산값(121/123) |
| `.source` | 58 | |
| **합계** | **812** | **여유 60px** |
| (요청 거절 · 설명 원문 2줄) | +47 | 여유 **13px**. 기준 미달. 아래 대안 참고 |

- 가로: 6칸 = 3칸 × (1,680 − 40)/3 = **547px**, 글자 칸 약 497px(강조 495). 칸 이름 최장 "완료 조건" 138px. 보조 최장 "없는 키워드 → 안내, 기존 질문 그대로" 약 354px → 모두 1줄. 막연한 요청 상자는 전폭(1,680px)이고, 점 28 + 문장 약 740 = 약 768px → 1줄, 가운데 정렬. 캡션 약 440px → 1줄.
- 강조: `.dg-box.accent`("완료 조건") 한 곳. 상태 점 주황(막연한 요청)·초록(6칸)은 DESIGN 예외 범위(점만)다. 막연한 요청은 `.muted`(오프화이트 면)로 중립 처리한다.
- 글자 크기: 72 / 40 / 32(설명·상자). 캡션층 제외 3종.
- 요청이 거절될 때의 대안(설명 원문 2줄 유지): 6칸 틀의 캡션 줄을 빼고(−58px, 여유 71px), "개념 예시" 표시를 출처 줄 끝 " · 6칸 예시는 개념 예시"로 옮긴다. 이때 초록 점은 없어진다. 공통 틀에서 캡션이 빠진 판이 되므로 S40과 모양이 한 줄 달라진다. 그래서 1순위는 설명 1줄 요청이다.
- 주의점
  - 설명 원문의 첫 문장("'앱 개선해줘'에는 무엇을·어디까지·언제 끝인지가 없다.")은 막연한 요청 상자 문장("무엇을·어디까지·언제 끝인지 없음")과 같은 말이다. 줄여도 화면 정보가 줄지 않는다.
  - 칸 이름은 "완료 조건"이다(오케스트레이터 결정). "완료 기준"으로 쓰지 않는다. 칸 순서(목표 · 입력 · 출력 / 범위 · 제약 · 완료 조건)는 S40과 한 글자까지 같아야 한다.
  - 제약 칸에 패키지 이름·설치 명령을 넣지 않는다. 기본 과제가 이미 구현돼 있다는 답이 오면(보류 6) 보조 줄 6개만 "근거 표시" 기준으로 바꾼다. 보조 줄은 20자 이하로 유지한다(497px ÷ 24px).
- 플레이스홀더: 없음 (S25 끝)

## 공통 컴포넌트 제안 (v0.4) — 요청문 6칸 틀 (S25 · S40 공유)

B6 담당 문서에 공통 절이 생기면 오케스트레이터가 이 제안과 맞춘다. 새 CSS는 필요 없다.

- 구조: `.dg` > `.dg-row.stretch`(목표 · 입력 · 출력) + `.dg-row.stretch`(범위 · 제약 · 완료 조건) + `p.dg-caption.is-left`. 화살표 없음(순서가 아니라 채울 칸).
- 칸: `.dg-box` = 칸 이름(32px, 텍스트 노드) + `.dg-sub` 1줄(24px). S25는 예시, S40은 채울 내용 안내("무엇을 만들까 · 결과 하나" 등). 상자 전체에 `data-editable`.
- 강조: "완료 조건" 칸만 `.dg-box.accent`. 다른 칸·캡션에 파랑 없음.
- 크기: 전폭 1,680px 기준 칸 547px, 글자 칸 약 497px, **보조 20자 이하**(1줄). 높이 = 121 + 24 + 123 + 24 + 캡션 34 = **326px**(캡션을 빼면 268px).
- 캡션: 틀 아래 왼쪽. S25 "완료 조건이 있는 요청문 6칸 · 개념 예시"(앞에 `.status-dot.ok`), S40 "13:20 구간(B4) 작업 요청과 같은 여섯 칸 · 빈칸 양식"(점 없음, 실습 안내판이라 상태 비교가 없다).
- 칸 이름·순서 고정: 목표 · 입력 · 출력 · 범위 · 제약 · 완료 조건.
- 폭이 좁은 칸(예: `.practice-grid` 좌 851px)에 넣으면 칸이 약 270px(글자 칸 약 220px, 보조 9자)가 되어 S25와 모양이 달라진다. S40도 전폭에 두는 것을 권한다.

```html
<div class="dg">
  <div class="dg-row stretch">
    <div class="dg-box" data-editable="true">목표<span class="dg-sub">…</span></div>
    <div class="dg-box" data-editable="true">입력<span class="dg-sub">…</span></div>
    <div class="dg-box" data-editable="true">출력<span class="dg-sub">…</span></div>
  </div>
  <div class="dg-row stretch">
    <div class="dg-box" data-editable="true">범위<span class="dg-sub">…</span></div>
    <div class="dg-box" data-editable="true">제약<span class="dg-sub">…</span></div>
    <div class="dg-box accent" data-editable="true">완료 조건<span class="dg-sub">…</span></div>
  </div>
  <p class="dg-caption is-left" data-editable="true">…</p>
</div>
```

(공통 컴포넌트 제안 끝)

## CSS 요청

ID 앞에 `B4-`를 붙여 B6 문서의 요청(A·B…)과 겹치지 않게 했다.

| ID | 클래스 | 속성 | 용도 | 반영 전 빌드 |
|---|---|---|---|---|
| B4-F (v0.3 F 재요청) | `.dg-row.even > .dg-zone` | `flex: 1; min-width: 0;` | S23 1행: 두 점선 영역(내 PC / 승인된 모델 경로)이 줄 폭을 반씩 나눠 갖게 한다. 기본 `.dg-row`에 바로 적용하면 오전 S06의 폭이 바뀌므로 변형 클래스로 한정한다 | `even` 없이 빌드한다. 넘침은 없고, 1행이 약 638px로 왼쪽에 몰린다 |
| B4-G | `.dg-check` | `display: inline-block; width: 28px; height: 28px; margin-right: 12px; border: 2px solid var(--ink-3); border-radius: 4px; vertical-align: -0.1em;` | S27 체크 카드의 빈 체크 칸(☐ 글자 대신). 모서리 4px은 28px 작은 사각형이라 12px이면 원처럼 보여서다(`.code-body .hl` 4px과 같은 예외) | span을 빼고 빌드한다. ☐·□ 글자로 대신하지 않는다 |

## 문구 축소 요청 (slide_content_writer)

| # | 장면 · 요소 | 현재 문구(글자 수) | 요청(목표 글자 수) | 이유 |
|---|---|---|---|---|
| B4-v4-1 | S26 · 플레이스홀더 "조건" 줄 | "조건: 모델명·계정·주소·키 가리기 · 실행일 표기 · 1600×900 이상"(37자, 773px 칸에서 2줄) | "조건: 모델명·계정·주소·키 가리기 · 실행일 표기"(27자, 1줄) | 원문이면 우측 칸 내용이 344px로 플레이스홀더 높이 349px과 5px 차이라 글꼴 차이로 잘릴 수 있다. 줄이면 여유가 44px이다. 문구 파일이 넘칠 때의 대안으로 이미 적어 둔 것이다. 해상도 조건은 자산 대기 목록에 남긴다 |
| B4-v4-2 | S25 · `.explain` | "'앱 개선해줘'에는 무엇을·어디까지·언제 끝인지가 없다. 여섯 칸을 채우면 작업 방향과 확인 방법이 함께 정해지고, 특히 '기존 결과 그대로'가 원래 기능을 확인하게 한다."(96자, 2줄) | **45자 이내 1줄**(1,600px 이내). 예: "여섯 칸을 채우면 작업 방향과 확인 방법이 함께 정해진다."(30자) 또는 "칸을 채우면 방향과 확인 방법이 정해진다. '기존 결과 그대로'가 원래 기능을 지킨다."(45자, 2문장) | 2줄이면 출처 줄 위 여유가 13px로 기준 24px에 못 미친다. 1줄이면 60px이다. 첫 문장은 막연한 요청 상자와 같은 말이고, 나머지 뜻은 "완료 조건" 칸과 노트에 있다. 거절하면 S25 대안(6칸 캡션 삭제)으로 빌드한다 |
