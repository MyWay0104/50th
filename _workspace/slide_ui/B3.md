# B3 배치 지시 · 11:00–11:40 · SQL Tool과 Streamlit

작성: slide_ui_designer · 2026-09-14 · 대상: S15–S21 (7장)
입력: `_workspace/slide_copy/B3.md`, `00_component_catalog.md`, `index.html` scene-styles 실측값, topic `DESIGN.md`, `ppt_visual_plan.md` v0.2

## 공통 배치 규칙 (요약 — 원문은 `slide_ui/B1.md` 공통 절)

- 장면 내부 872×1680px. eyebrow+제목 145, 부제 64, 출처 58, 본문 margin-top 56. 본문 가용 높이: 제목+출처 **613px**, 부제 추가 시 **549px**. callout 1줄 104px.
- 글자 폭 추정: 한글 1.0em, 라틴·숫자 0.55em, 공백·기호 0.3em.
- **`.stack`**: 여러 블록을 세로로 쌓을 때 `<div class="stack">`(flex column, gap 24px)를 래퍼로 쓴다. (카탈로그 5절)
- 상태 점·편집 span, 스플릿 패널 머리줄(`.dg-row` 한 줄), compare 열 내부 순서, 실습 안내판 구조는 B1·B2와 동일.
- 가상 데이터 장면(S15·S16·S18·S19)은 "가상 데이터" 표기가 화면에 반드시 보이게 한다.

---

## S15 · 정확한 조회와 계산

- data-skill: `compare` · section 클래스: `scene clip`
- 배치 순서: eyebrow(+`.time-chip` "11:00–11:40") → 제목 → `p.scene-subtitle` → `.compare-grid`(2열) [열1 / 열2 `.is-accent`] → `.source`
- 요소 ↔ 문구
  - `.scene-subtitle`: 질문 예: 최근 일주일 장비별 점검 건수는?
  - 열1: kicker "모델" / heading "질문 해석" / li: '최근'의 날짜 기준을 확인 · 표현을 조회 조건으로 바꿈 · 결과를 문장으로 설명
  - 열2(is-accent): kicker "SQL·Python" / heading "조회·집계" / li: 실제 데이터에서 건수 계산 · 날짜·단위 조건을 코드로 고정 · 없는 식별자는 만들지 않음 / note: 가상 데이터 · EQ-01 12건 · EQ-02 7건 (최근 7일)
  - `.source`: LangChain SQL Agent 가이드 · Part Number Finder Beta 공개 README
- 강조: 열2 `.is-accent` 1개. 상태 점 없음.
- 글자 크기: 72 / 40 / 34 / 32 (4종)
- 넘침 근거
  - 폭: 목록 708px, 최장 "날짜·단위 조건을 코드로 고정" ≈ 380px → 1줄. note ≈ 500px < 열 안쪽 738px → 1줄
  - 높이: 열 = 80 + 114 + 195 + 54 = 443px ≤ **549**
- 플레이스홀더: 없음
- 빌드 메모: 열1은 note가 없어 목록 아래가 비는 것이 정상. 가상 건수는 note에만 두고 heading·목록에 숫자를 넣지 않는다.

## S16 · Tool 호출의 실행 순서

- data-skill: `steps` · section 클래스: `scene clip`
- 배치 순서: eyebrow → 제목 → `.stack` [ `ol.steps.row`(4단계) → `.chip-row`(예시) → `.callout` ] → `.source`
- 카드 내부 순서(B2 S10과 동일): `.panel-title`(영역 라벨) → `.step-num` → `.step-body`(+`.step-desc`)
- 요소 ↔ 문구
  - 카드1: 라벨 "프로그램" / 도구 알려주기 / 도구 이름·설명·입력 형식 전달
  - 카드2: 라벨 "모델" / 호출 요청 / 도구 이름과 인자를 반환
  - 카드3(`.step.is-accent`): 라벨 "프로그램" / 실행과 반환 / 실제 함수·SQL을 실행
  - 카드4: 라벨 "모델" / 결과로 응답 / 답하거나 다음 도구를 요청
  - `.chip-row`: `.concept-badge` "개념 예시" → `.chip` "요청 count_by_equipment" → `.chip` "인자 days=7" → `.chip` "결과 가상 데이터 2행"
  - `.callout`: 모델은 함수를 직접 실행하지 않는다 · @tool은 설명과 입력 형식을 만든다
  - `.source`: LangChain Tools 문서 · OpenAI Function calling 가이드
- 강조: `.step.is-accent`(카드3) 1개 + callout 틴트. 칩은 모두 기본색.
- 글자 크기: 72 / 38 / 34 / 28 (4종)
- 넘침 근거
  - 폭: 카드 (1680 − 3×64)/4 = 372, 안쪽 298px(약 10자/줄, 28px). body 최장 "도구 알려주기" ≈ 240px → 1줄. desc 최장 "도구 이름·설명·입력 형식 전달" ≈ 380px → 2줄. 칩 줄 합계 ≈ 1,000px → 1줄. callout ≈ 1,110px → 1줄
  - 높이: 카드 40 + 39 + 24 + 72 + 24 + 51 + 6 + 78 + 40 = 374px. 145 + 56 + 374 + 24 + 55 + 24 + 104 + 58 = **840px ≤ 872** (여유 32px, 주의 — desc 하나가 3줄이 되면 879px로 넘침. desc는 13자 이내 유지)
- 플레이스홀더: 없음
- 빌드 메모: 예시 칩은 "단계 2·3 아래"에 개별 배치할 수 없으므로 카드 줄 아래 한 줄로 둔다(요청 → 인자 → 결과 순서가 단계 2→3과 대응). `.steps.row`의 카드 사이 "→"는 CSS가 자동으로 그린다(마크업에 넣지 않음).

## S17 · 스크립트·Workflow·Agent

- data-skill: `compare` · section 클래스: `scene clip`
- 배치 순서: eyebrow → 제목 → `p.scene-subtitle` → `.stack` [ `.compare-grid.cols-3` → `.callout` ] → `.source`
- 요소 ↔ 문구
  - `.scene-subtitle`: 같은 업무: 자료를 읽고 결과 만들기
  - 열1: kicker "규칙이 명확" / heading "스크립트" / li: 정해진 규칙대로 처리 · 단순하고 예측 쉬움 / note: 예: 파일 형식 변환
  - 열2: kicker "순서가 고정" / heading "Workflow" / li: 코드가 순서를 정함 · 단계에 모델 호출 포함 / note: 예: 수집 → 검색 → 답변
  - 열3: kicker "상황에 따라" / heading "Agent" / li: 모델이 다음 행동 선택 · 호출·시간·사용량 증가 / note: 예: 필요할 때 검색·SQL 선택
  - `.callout`: `<strong>필요한 만큼만</strong>` + " 규칙으로 충분하면 스크립트부터"
  - `.source`: Anthropic, Building effective agents
- 강조: callout `strong` 1개. **`.is-accent` 열 없음**(우열·진화 단계로 읽히지 않게). 열 사이에 화살표를 넣지 않는다.
- 글자 크기: 72 / 40 / 34 / 32 (4종)
- 넘침 근거
  - 폭: 3열 목록 426px(약 13자). 최장 "호출·시간·사용량 증가" ≈ 한글 9×32 + 기호 2×10 + 공백 10 = 318px → 1줄. note 최장 ≈ 330px < 456px
  - 높이: 열 = 80 + 114 + 목록(24 + 2×45 + 18 = 132) + note 54 = 380px. 145 + 64 + 56 + 380 + 24 + 104 + 58 = **831px ≤ 872**
- 플레이스홀더: 없음
- 빌드 메모: B1 S07과 같은 `cols-3` 구조지만 강조 열이 없다는 점만 다르다. 세 열 kicker 색은 모두 `--ink-3`(기본).

## S18 · Part Finder의 후보 검증

- data-skill: `split` · section 클래스: `scene clip`
- 배치 순서: eyebrow → 제목 → `.split-grid.wide-left` [좌 `.split-panel` / 우 `ul.bullets`] → `.source`
  - 좌 패널: 머리줄 `.dg-row`[`.concept-badge` "공개 Beta 설계 기반 개념 예시" + `.panel-title` "요청: 제조사 A의 로봇 블레이드"] → `table.data-table` → `.dg-caption`
- 표
  - `th`: Part Number | 파트 종류 | 제조사 | 판정
  - `tr.is-dim` ×3: PN-A-2001 | O-ring | A | 종류 불일치 / PN-B-1001 | 로봇 블레이드 | B | 제조사 불일치 / PN-A-3001 | 밸브 | A | 종류 불일치
  - 마지막 행(결과 줄): `<tr><td colspan="4" class="is-match" data-editable="true">결과: 조건에 맞는 후보 없음</td></tr>`
- 요소 ↔ 문구
  - `.dg-caption`: 가상 데이터 · 공개 Beta 구조를 단순화
  - `li`: 데이터에 있는 후보만 사용 / 파트 종류와 조건을 함께 확인 / 맞는 후보가 없으면 '결과 없음'
  - `.source`: Part Number Finder Beta 공개 README (GitHub MyWay0104)
- 강조: 결과 줄 `td.is-match` 1개.
- 글자 크기: 72 / 40 / 30(표) / 28 (4종)
- 넘침 근거
  - 폭: `.wide-left` 좌 913px, 패널 안쪽 831px. 4열 내용 합 ≈ 150 + 188 + 30 + 180 + 셀 패딩 4×48 = 740px → 1줄씩. 1fr 1fr(안쪽 726px)이면 셀이 접히므로 `.wide-left` 필수. 우 703 − 52 = 651px에 최장 li 3 ≈ 540px → 1줄
  - 높이(좌): 82 + 머리줄 41 + 24 + 표(66 + 74×4 + 2 = 364) + 24 + 34 = **569px ≤ 613**
- 플레이스홀더: 없음
- 빌드 메모: 결과 줄을 표 밖 callout으로 빼면 +104px로 넘친다(686px). 반드시 표의 마지막 행으로 둔다. 정확도·이용자 수를 넣지 않는다.

## S19 · 실습 3 · SQL Tool 연결

- data-skill: `title-bullets` · section 클래스: `scene clip`
- 배치: B1 S08과 동일(안내판 공통). 기본 2열 `.time-blocks`.
- 요소 ↔ 문구
  - `.board-meta`: 11:14–11:30 · 16분 · 실습 가이드 해당 장 (별도 `.guide-ref` 없음)
  - `.bullets li`: 테이블과 날짜·단위 확인 / 읽기 도구 연결 후 호출 / 정답 아는 질문·빈 결과 비교
  - `.time-block`: "3분" 스키마 확인 / "7분" 연결·조회(`.is-accent`) / "4분" 다른 입력 / "2분" 결과 설명
  - `.done-box`: 완료 조건 · 정답을 아는 질문과 결과 없는 질문의 차이를 설명한다
  - `.fallback`: `<strong>막히면</strong>` DB 접속이 안 되면 같은 구조의 교육용 DB로 진행합니다
  - 보조 칩(교육용 스키마 예 `날짜` `장비` `건수` · 가상 데이터): **넣지 않는다**(문구상 "선택" 항목, 빌드 메모 참고)
  - `.source`: LangChain SQL Agent 가이드 · 근거: 사용자 제공 강의안·시간표
- 강조: `.time-block.is-accent`(7분) 1개 + done-box 테두리.
- 글자 크기: 72 / 40 / 36 / 28 (4종)
- 넘침 근거
  - 가용 높이 570px. done-text ≈ 한글 22×36 → 2줄
  - 높이(좌): 224 + 24 + 202 = **450px**. 칩 줄(패널 제목 39 + 24 + 칩 55 + 24)을 넣으면 592px > 570px로 넘침
  - 높이(우): 356 + fallback 2줄 136 = **492px ≤ 570**
- 플레이스홀더: 없음
- 빌드 메모: 칩을 빼는 이유는 높이 초과와 안내판 3장(S08·S13·S19) 구조 통일. 실제 스키마가 확정되면(보류 2번) 강사가 실습 중 화면으로 보여 준다.

## S20 · 실습 4 · Streamlit 화면 실행

- data-skill: `title-image` · section 클래스: `scene clip title-image`
- 배치 순서(가운데 정렬): eyebrow(좌측 고정) → 제목 → `p.scene-subtitle` → `.image-frame` > `.placeholder` → **`.done-row` [ 좌 `.done-box` | 우 `.fallback` ]** → `.source`
  ```html
  <div class="done-row">
    <div class="done-box"><div class="done-label" data-editable="true">완료 조건</div><div class="done-text" data-editable="true">입력 → 함수 → 화면 결과의 연결을 설명한다</div></div>
    <div class="fallback" data-editable="true"><strong>막히면</strong>옆 사람 화면으로 함께 확인하고, 5분 넘게 막히면 강사 시연으로 봅니다</div>
  </div>
  ```
- 가로 한 줄 컨테이너 선택 근거(scene-styles 확인)
  - `.compare-grid`·`.practice-grid`·`.split-grid`: 모두 `flex:1`이라 `.image-frame`(flex:1)과 남는 높이를 나눠 가진다 → 이미지 자리가 절반 가까이 줄어 불가
  - `.dg-row`: flex 행이라 두 상자가 내용 폭 합(756 + 20 + 약 1,040 = 1,816px > 1,680px)에서 비례 수축 → done-box 약 698px, done-text 2줄(+49px) → 이미지 자리 339px로 불가
  - **`.time-blocks`**: `display:grid; repeat(2, 1fr); gap:20px`, margin·flex 없음 → 두 칸 (1680 − 20)/2 = **830px 고정**. 채택. `.time-block` 자식 없이 2열 그리드로만 쓴다
- 요소 ↔ 문구
  - 제목: 실습 4 · Streamlit 화면 실행
  - `.scene-subtitle`: 11:30–11:38 · 8분 · 실행 3분 → 질문·결과 3분 → 코드 위치 2분
  - `.placeholder-label`: 실제 화면 교체 예정
  - `.placeholder-desc`: 준비된 Streamlit 실습 앱 — 입력 칸 · 처리 중 표시 · 결과와 근거 영역
  - `.done-box`: `.done-label` "완료 조건" · `.done-text` "입력 → 함수 → 화면 결과의 연결을 설명한다" (문구의 "완료:" 접두어는 done-label로 대신)
  - `.fallback`: `<strong>막히면</strong>` + "옆 사람 화면으로 함께 확인하고, 5분 넘게 막히면 강사 시연으로 봅니다" (강사 검토 W9 제안 문구. `slide_copy/B3.md` 확정본이 다르면 확정본을 따른다)
  - `.source`: Streamlit Architecture 문서 · 근거: 사용자 제공 강의안·시간표
- 강조: done-box 파랑 테두리 1개. `.fallback`은 점선 중립색. 부제의 시간 숫자는 강조하지 않는다.
- 글자 크기: 72 / 36 / 34 / 28 (4종)
- 넘침 근거
  - 폭: 부제 ≈ 960px < 1,500px → 1줄. done-box 안쪽 830 − 76 = 754px ≥ done-text 약 680px → 1줄. fallback 안쪽 830 − 58 = 772px, 문구 ≈ strong 96 + 한글 28×28 + 숫자·기호·공백 약 100 ≈ 980px → 2줄
  - 높이: 행 = max(done-box 위 간격 24 + 153, fallback 위 간격 20 + 116) = **177px**. 기존 done-box 단독(24 + 153)과 같다. image-frame = 872 − 145 − 64 − 40 − 177 − 58 = **약 388px 유지**. fallback이 3줄이 되어도 20 + 155 = 175 ≤ 177px → 영향 없음
- 플레이스홀더 규격: `.image-frame`(최대 1400×388px, flex:1) 안 `.placeholder` 가득 채움. 점선 2px `--line-strong`, 흰 배경. 라벨·설명은 위 문구. 가짜 앱 화면을 CSS로 그리지 않는다.
- 빌드 메모: `.scene.title-image .done-box { margin-top:24px }` 규칙은 그리드 안에서도 적용되고 `.fallback`은 margin-top 20px이라 두 상자 윗변이 4px 어긋난다(아랫변은 grid stretch로 맞음). 행 폭 1,680px가 이미지 자리 최대 폭 1,400px보다 넓다. 둘 다 넘침은 아니다 → 카탈로그 추가 요청 E(선택). 두 상자 글자는 장면의 `text-align:center`를 물려받는다. done-box는 유지한다(강사 검토 답변). 실제 캡처는 보류 P4대로 크롭해 388px 높이에서 읽히게 한다.
- 수정 기록(2026-09-14): U1 반영 — 이미지 자리 아래를 done-box 단독에서 `.time-blocks` 2열 [done-box | fallback(W9 문구)]로 변경. `.dg-row`는 수축으로 done-text가 2줄이 되어 제외. 행 높이 177px 유지로 image-frame 약 388px 유지. 카탈로그 추가 요청 E(선택) 추가.
- 수정 기록(2026-09-14, 오케스트레이터): 요청 E가 CSS에 반영되어 S20의 두 상자 틀을 `.time-blocks`에서 `.done-row`로 교체. 윗변 정렬과 1400px 폭 문제 해소.

## S21 · 오전 실습의 전체 구조

- data-skill: `split` · section 클래스: `scene clip`
- 배치 순서: eyebrow → 제목 → `.split-grid.wide-left` [좌 `.split-panel` / 우 `ul.bullets`] → `.source`
  - 좌 패널: `.concept-badge` "개념 예시" → `.dg` > `.dg-row.stretch`[ `.dg-box.accent` "사용자 화면" + `.dg-sub` · `.dg-arrow` "⇄" · `.dg-box` "모델과 도구" + `.dg-sub` · `.dg-arrow` "⇄" · `.dg-box` "근거 데이터" + `.dg-sub` ] → `.dg-caption`
- 요소 ↔ 문구
  - `.dg-sub`: Streamlit · 입력과 결과 표시 / LLM 호출 · 검색 · SQL Tool / 기사 본문 · 교육용 DB
  - `.dg-caption`: 파랑 표시: 오후에 고쳐 볼 작은 기능 후보 위치
  - `li`: 화면·모델·데이터를 나눠 연결 / 한 곳을 고치면 다른 곳도 확인 / 오후: Claude Code로 작은 개선
  - `.source`: 근거: 사용자 제공 강의안 · Streamlit Architecture 문서
- 강조: `.dg-box.accent`(사용자 화면) 1개. 캡션이 이 파랑을 설명한다.
- 글자 크기: 72 / 40 / 32 / 28 (4종, dg-sub 24는 라벨)
- 넘침 근거
  - 폭: 패널 안쪽 831px, 상자 (831 − 2×40 − 4×20)/3 ≈ 224, 안쪽 174px. 상자 이름 최장 5자 ≈ 165px → 1줄. dg-sub ≈ 320px → 2줄. bullets 651px에 최장 li 1 ≈ 540px → 1줄
  - 높이(좌): 82 + 41 + 24 + 상자(40 + 42 + 6 + 68 + 2 = 158) + 24 + 34 = **363px ≤ 613**
- 플레이스홀더: 없음
- 빌드 메모: `.dg-row.stretch`로 세 상자 높이를 맞춘다. 전체를 "Agent"로 부르는 라벨을 넣지 않는다. B1 S06과 달리 영역(`.dg-zone`)은 쓰지 않는다.

---

## 카탈로그 추가 요청

| ID | 요청 | 이유 | 본문 대체안 |
|---|---|---|---|
| A (B1과 동일) · 반영됨 | `.stack { display:flex; flex-direction:column; gap:24px; min-width:0; }` | S16·S17 callout, S19 좌측 칸 세로 간격 | — |
| D · 반영됨 | `.scene.title-image .done-box { margin-top:24px; }` | S20 완료 조건 상자가 이미지 자리 바로 아래 0px로 붙는다. image-frame의 `flex:1`을 살려야 해서 `.stack`로 감쌀 수 없다(감싸면 자리 높이가 240px로 줄어듦) | — |
| E (선택) | `.done-row { display:grid; grid-template-columns:1fr 1fr; gap:24px; width:100%; max-width:1400px; margin-top:24px; }` `.done-row > .done-box, .done-row > .fallback { margin-top:0; }` | S20 U1 배치를 `.time-blocks`로 대신하면 두 상자 윗변이 4px 어긋나고(24/20px), 행 폭 1,680px가 이미지 자리 1,400px보다 넓으며, 시간 블록 클래스를 의미 없이 재사용하게 된다. 넘침은 아님 | 반영됨(2026-09-14): S20은 `.done-row`를 쓴다 |

## 문구 축소 요청(slide_content_writer)

| # | 장면 · 요소 | 현재 문구 | 권장 문구 | 이유 |
|---|---|---|---|---|
| 1 | S19 · 보조 칩(선택) | 교육용 스키마 예: `날짜` `장비` `건수` · 가상 데이터 | 화면에서 삭제(필요하면 발표자 노트로) | 넣으면 좌측 칸 592px > 570px. 안내판 3장 구조 통일 |

---

# v0.4 배치 (2026-09-15)

작성: slide_ui_designer · 2026-09-16 · 대상: S15–S21 (7장)
입력: `slide_copy/B3.md` "# v0.4 보강 (2026-09-15)" 절, v0.4 보류 목록, 맨 끝 오케스트레이터 결정(2026-09-16, S19 스키마 표 제외), `00_component_catalog.md` 5·7절, `index.html` scene-styles 전체와 현재 S15–S21 마크업, topic `DESIGN.md`, `docs/slide-plan-v0.4-visual-narrative.md` 7-1~7-3절·8절 B3.
index.html은 고치지 않았다. `id`·`data-start`·`page-num`은 현재값을 두고 `sync_overview.py --renumber`에 맡긴다. `.speaker-note`에는 문구 파일의 해당 장면 "발표자 노트 (최종 전문)"을 넣는다.

## v0.4 공통 배치 규칙

`slide_ui/B2.md`의 "# v0.4 배치 → v0.4 공통 배치 규칙"과 같다. 측정값 표, cascade 확인 사항, 글자 크기 세기, 실습 안내판 공통 틀(S13·S19·S20)을 그대로 쓴다. B3에서 더하는 약속은 두 가지다.

- 책임 영역 톤(계획서 7-3): **모델이 하는 단계 = 오프화이트(`.dg-box.muted`, S16 카드는 `.step.is-muted`)**, 코드·프로그램이 하는 단계 = 흰색, 그중 핵심 1곳만 파랑 테두리(`accent`/`is-accent`). S15·S16·S18에 같은 뜻으로 쓴다.
- 파랑 구조 강조는 장면당 1곳이다. 작가 문구에 파랑이 두 곳 지정된 S15·S18은 아래에서 하나로 줄였다.

---

## S15 · 정확한 조회와 계산 (v0.4 배치)

- section 클래스 `scene clip` · data-skill `compare`
- 배치 순서: eyebrow(+`.time-chip`) → `.act-tag` → 제목 → `.thesis` → `.explain` → `.stack` [ `.dg-row.stretch`(상자 4 + `→` 3) → `.compare-grid`(2열, kicker 없음) ] → `.source`
- 요소 ↔ 클래스 ↔ 문구
  - 흐름 상자: 질문(흰) → 조건 해석(`muted`) → 집계(`accent`) → 설명(`muted`). 톤 지정은 작가와 같다.
  - 2열 비교: 두 열 모두 `.compare-col`(기본). **작가가 지정한 열 2의 `is-accent`는 뺀다.** 노트가 "파란 상자인 집계"를 가리키므로 파랑은 흐름의 "집계" 상자 1곳에만 둔다. 열 2를 되살리면 파랑이 2곳이 된다(높이 영향은 +2px뿐).

```html
<section id="s-16" class="scene clip" data-skill="compare" data-scene-id="S15" data-start="75" data-duration="5" data-track-index="0">
  <div class="eyebrow"><span class="block-chip" data-editable="true">B3</span><span data-editable="true">SQL Tool과 Streamlit</span><span class="time-chip" data-editable="true">11:00–11:40</span></div>
  <p class="act-tag" data-editable="true"><strong>승 · 연결하다 (2)</strong>정확한 조회는 누가 책임지나?</p>
  <h2 class="scene-title" data-editable="true">정확한 조회와 계산</h2>
  <p class="thesis" data-editable="true">숫자와 식별자는 코드가 맡고, 모델은 해석하고 설명한다.</p>
  <p class="explain" data-editable="true">'최근 일주일 장비별 건수'는 날짜 기준·단위·중복 처리를 정해야 답이 하나로 나온다. 기준은 코드에 고정하고, 정답을 아는 작은 데이터로 먼저 확인한다.</p>
  <div class="stack">
    <div class="dg-row stretch">
      <div class="dg-box" data-editable="true">질문<span class="dg-sub">최근 7일 장비별 건수?</span></div>
      <span class="dg-arrow" aria-hidden="true">→</span>
      <div class="dg-box muted" data-editable="true">조건 해석<span class="dg-sub">모델</span></div>
      <span class="dg-arrow" aria-hidden="true">→</span>
      <div class="dg-box accent" data-editable="true">집계<span class="dg-sub">SQL·Python</span></div>
      <span class="dg-arrow" aria-hidden="true">→</span>
      <div class="dg-box muted" data-editable="true">설명<span class="dg-sub">모델</span></div>
    </div>
    <div class="compare-grid">
      <div class="compare-col">
        <div class="compare-heading" data-editable="true">모델 · 해석과 설명</div>
        <ul class="compare-list">
          <li data-editable="true">'최근'을 날짜 조건으로 바꿈</li>
        </ul>
      </div>
      <div class="compare-col">
        <div class="compare-heading" data-editable="true">SQL·Python · 조회와 집계</div>
        <ul class="compare-list">
          <li data-editable="true">없는 식별자는 만들지 않음</li>
        </ul>
      </div>
    </div>
  </div>
  <p class="source" data-editable="true">출처: LangChain SQL Agent 가이드 · Part Number Finder Beta 공개 README · 조사정리 4-B(개인 기록) · 흐름도는 개념 예시</p>
  <aside class="speaker-note">(slide_copy/B3.md S15 v0.4 발표자 노트 최종 전문)</aside>
  <div class="deck-footer" data-editable="true">AI Agent Guide · SK hynix 사내 교육</div>
  <div class="page-num">16 / 52</div>
</section>
```

- 문구 축소 요청 1(아래 목록)이 반영되면 흐름 줄만 이렇게 바꾼다(보조 줄 없이 상자 한 줄).

```html
    <div class="dg-row stretch">
      <div class="dg-box" data-editable="true">질문</div>
      <span class="dg-arrow" aria-hidden="true">→</span>
      <div class="dg-box muted" data-editable="true">모델 · 조건 해석</div>
      <span class="dg-arrow" aria-hidden="true">→</span>
      <div class="dg-box accent" data-editable="true">SQL·Python · 집계</div>
      <span class="dg-arrow" aria-hidden="true">→</span>
      <div class="dg-box muted" data-editable="true">모델 · 설명</div>
    </div>
```

- 높이 예산

| 요소 | 현재 문구 | 축소 요청 반영 |
|---|---|---|
| eyebrow + 제목 | 145 | 145 |
| 막 태그 | 49 | 49 |
| 요지 1줄 | 81 | 81 |
| 설명 2줄 | 109 | 109 |
| 본문 위 간격 | 32 | 32 |
| 흐름 줄 | 123 (상자 + 보조 줄) | 86 (상자 한 줄) |
| gap | 24 | 24 |
| 2열 비교 (82 + 80 + 24 + 45) | 231 | 231 |
| 출처 1줄 | 58 | 58 |
| **합계** | **852** | **815** |
| **출처 줄 위 여유** | **20 — 기준 24 미달**(보정해도 약 23) | **57** |

- 가로 배치와 줄바꿈
  - 흐름 상자: (1680 − 화살표 3×40 − gap 6×20)/4 = 360px, 안쪽 310px(accent 306). 현재 보조 줄 최장 "최근 7일 장비별 건수?" ≈ 216px, 축소안 최장 "SQL·Python · 집계" ≈ 261px → 모두 1줄.
  - 비교 열 820px. 열 제목 최장 "SQL·Python · 조회와 집계" ≈ 446px/738px → 1줄. 목록 최장 ≈ 370px → 1줄.
  - 요지 ≈ 1,120px → 1줄. 설명 ≈ 2,170px → 2줄.
- 글자 크기: 72 / 40(요지·열 제목) / 32(설명·상자·목록) — 3종(dg-sub 24는 라벨)
- 주의점
  - 현재 문구로 빌드해도 넘치지는 않는다. 다만 출처 줄 위 여유가 20px로 기준 24px에 못 미친다. 작가 계산(380/398px)과 차이가 나는 이유는 compare 열 테두리·heading 여백과 흐름 상자 높이(보조 줄 포함 123px)다.
  - 글자 크기는 줄이지 않는다. 문구 축소 요청 1(보조 줄을 상자 이름에 합침)을 반영한다. 거절되면 대안으로 두 열의 목록 줄을 빼는 방법(−69px)이 있지만, "없는 식별자는 만들지 않음"은 S18 노트가 되짚는 문장이라 권하지 않는다.
  - "개념 예시"는 작가 결정대로 출처 줄 끝에 둔다(캡션 줄을 둘 높이가 없다).
- 빌드 메모(S15): v0.3의 `scene-subtitle`, 두 열의 kicker·목록 4개·`compare-note`(가상 건수)는 뺀다.

## S16 · Tool 호출의 실행 순서 (v0.4 배치)

- section 클래스 `scene clip` · data-skill `steps`
- 배치 순서: eyebrow → 제목 → `.thesis` → `.explain` → `.stack` [ `ol.steps.row.is-compact`(카드 4장, 번호 원 없음) → `.code-card`(3줄, `code-title` 없음) ] → `.source`
- 작가 제안 확인
  - 번호 원(`.step-num`) 빼기와 번호를 카드 위 라벨("① 프로그램")에 합치기: **채택**. 번호 원을 두면 카드가 +96px라 들어가지 않는다.
  - 설명 문단이 3줄일 때의 대안(작은 줄을 라벨에 합쳐 "③ 프로그램 · ToolMessage"): **불가**. 28px 굵은 글자로 약 343px라 카드 안쪽 296px에서 2줄로 접히므로 줄어드는 높이가 거의 없다.
  - 작가 추정 441px는 카드 테두리·라벨 줄 높이를 작게 잡은 값이다. 실제 카드는 244px, 본문 합계는 448px로 여유가 −1px이다. 그래서 **CSS 요청 V4-2(카드 위아래 패딩 40→24)**가 필요하다. 글자 크기는 줄이지 않는다.
- 요소 ↔ 클래스 ↔ 문구
  - 카드 `li.step` > `.panel-title`(누가 하는가) + `.step-body`(단계 이름) > `.step-desc`(메시지 종류)
  - 카드 1 흰색 "① 프로그램 / 도구 알려주기 / 도구 스키마"
  - 카드 2 `is-muted` "② 모델 / 호출 요청 / tool_calls"
  - 카드 3 `is-accent` "③ 프로그램 / 실행과 반환 / ToolMessage"
  - 카드 4 `is-muted` "④ 모델 / 결과로 응답 / AIMessage"
  - `is-muted`(CSS 요청 V4-3)는 계획서 7-3의 "모델 영역 = 오프화이트 톤"을 카드에 옮긴 것이다. 작가가 "톤 변형이 필요하면 UI가 요청"이라고 남긴 항목이다. 카드 사이 "→"는 CSS가 그린다.
  - 코드: 1줄 끝 주석만 `span.cm`

```html
<section id="s-17" class="scene clip" data-skill="steps" data-scene-id="S16" data-start="80" data-duration="5" data-track-index="0">
  <div class="eyebrow"><span class="block-chip" data-editable="true">B3</span><span data-editable="true">SQL Tool과 Streamlit</span></div>
  <h2 class="scene-title" data-editable="true">Tool 호출의 실행 순서</h2>
  <p class="thesis" data-editable="true">모델은 도구 호출을 요청하고, 실행은 프로그램이 한다.</p>
  <p class="explain" data-editable="true">프로그램이 도구의 이름·설명·입력 형식을 알려 주면 모델은 쓸 도구와 인자를 돌려준다. @tool은 docstring을 도구 설명으로 쓰므로, docstring이 모호하면 도구를 잘못 고른다.</p>
  <div class="stack">
    <ol class="steps row is-compact">
      <li class="step">
        <div class="panel-title" data-editable="true">① 프로그램</div>
        <div class="step-body" data-editable="true">도구 알려주기<span class="step-desc">도구 스키마</span></div>
      </li>
      <li class="step is-muted">
        <div class="panel-title" data-editable="true">② 모델</div>
        <div class="step-body" data-editable="true">호출 요청<span class="step-desc">tool_calls</span></div>
      </li>
      <li class="step is-accent">
        <div class="panel-title" data-editable="true">③ 프로그램</div>
        <div class="step-body" data-editable="true">실행과 반환<span class="step-desc">ToolMessage</span></div>
      </li>
      <li class="step is-muted">
        <div class="panel-title" data-editable="true">④ 모델</div>
        <div class="step-body" data-editable="true">결과로 응답<span class="step-desc">AIMessage</span></div>
      </li>
    </ol>
    <div class="code-card">
<pre class="code-body" data-editable="true">@tool  <span class="cm"># 교육용 가상 함수</span>
def count_by_equipment(days: int) -> list:
    """최근 days일의 장비별 점검 건수"""</pre>
    </div>
  </div>
  <p class="source" data-editable="true">출처: LangChain Tools · OpenAI Function calling 가이드 · 교안-4일 Tool Calling 흐름(교안 내용 재구성) · 뿌리강의 9장 · 코드는 개념 예시</p>
  <aside class="speaker-note">(slide_copy/B3.md S16 v0.4 발표자 노트 최종 전문)</aside>
  <div class="deck-footer" data-editable="true">AI Agent Guide · SK hynix 사내 교육</div>
  <div class="page-num">17 / 52</div>
</section>
```

- 높이 예산

| 요소 | V4-2 반영 | V4-2 없음 |
|---|---|---|
| eyebrow + 제목 | 145 | 145 |
| 요지 1줄 | 81 | 81 |
| 설명 2줄 | 109 | 109 |
| 본문 위 간격 | 32 | 32 |
| 카드 (강조 카드 기준) | 214 = 24+24 + 4 + 라벨 39 + 24 + 이름 54 + 작은 줄 45 | 244 = 40+40 + 4 + 39 + 24 + 51 + 45 |
| gap | 24 | 24 |
| 코드 카드 3줄 (2 + 48 + 3×43.4) | 180 | 180 |
| 출처 1줄 | 58 | 58 |
| **합계** | **843** | **873** |
| **출처 줄 위 여유** | **29** | **−1 (넘침)** |

- 가로 배치와 줄바꿈
  - 카드 (1680 − 64×3)/4 = 372px, 안쪽 298px(강조 296). 단계 이름 40px 최장 "도구 알려주기" ≈ 252px → 1줄. 라벨 "① 프로그램" ≈ 150px, 작은 줄 "ToolMessage" ≈ 169px → 1줄.
  - 코드 카드 폭 1680px, 최장 2번째 줄 42자 × 16.8 = 706px → 여유 충분.
  - 요지 ≈ 1,060px → 1줄. 설명 ≈ 2,520px(영문 23자 포함) → 2줄. 작가 확인 요청에 대한 답이다.
  - 출처 ≈ 1,510px → 1줄(여유 170px). 출처 문구를 늘리면 2줄이 되어 −34px다.
- 글자 크기: 72 / 40(요지·단계 이름, V4-2가 38→40으로 맞춤) / 32(설명) / 28(라벨·작은 줄·코드) — 4종. V4-2가 없으면 38이 따로 남아 5종이 된다.
- 주의점
  - V4-2는 이 장면 전용 변형(`is-compact`)이다. 다른 `.steps.row` 장면에는 영향이 없다. 단계 이름을 40px로 올리는 것은 요지(40)와 2px 차이로 어긋나 보이는 것을 막는 확대이고, 넘침 해결을 위해 글자를 줄이는 것이 아니다.
  - 설명 문단이 3줄로 늘면 V4-2를 반영해도 −17px로 넘친다. 문구를 늘리지 않는다.
  - V4-2가 반영되지 않으면 이 장면을 빌드하지 않는다. 대안은 작은 줄(메시지 종류 4개)을 노트로 옮기는 문구 축소(−45px, 여유 44px)다. 아래 문구 축소 요청 2(조건부).
  - V4-3(`is-muted`)이 없으면 모델 카드도 흰색이다. 넘침과 무관하고, 라벨 "② 모델"로 구분은 남는다.
  - 강조는 카드 3 `is-accent` 1곳이다. 코드 3줄은 폭 1680px에 비해 짧아 오른쪽이 빈다. 코드 카드는 좁히는 클래스가 없어 그대로 둔다.
- 빌드 메모(S16): v0.3의 `.step-num` 4개, 카드 설명 문장, `.chip-row`(예시 칩), `.callout`은 뺀다.

## S17 · 스크립트·Workflow·Agent (v0.4 배치)

- section 클래스 `scene clip` · data-skill `compare`
- 배치 순서: eyebrow → 제목 → `.thesis` → `.explain` → `.stack` [ `.compare-grid.cols-3` → `.chip-row`(패턴 칩 5개 + 캡션) ] → `.source`
- 틀 결정
  - 작가 배치(칩 줄 아래 캡션 줄 따로)는 본문 442px, 여유 13px로 기준에 못 미친다. 작가 대안(kicker 3개 삭제, −34px)은 "규칙이 명확 / 순서가 고정 / 상황에 따라"라는 선택 기준을 지우므로 쓰지 않는다.
  - 대신 **캡션을 칩 줄 끝에 같은 줄로** 넣는다. 칩 5개 ≈ 878px + 캡션 ≈ 752px + gap 12 = 1,642px ≤ 1,680px라 한 줄에 들어간다. 줄 하나(−58px)를 줄여 여유 71px가 된다. 캡션 문구는 그대로다.
  - 열 안에서 heading과 상자 사이 간격을 두려고 `[heading + dg-box]`를 `.stack`으로 감싼다. heading의 margin-top 8은 스택 규칙으로 0이 된다. kicker와 heading의 글자 사이는 줄 높이 여백으로 약 10px 남는다.
- 요소 ↔ 클래스 ↔ 문구
  - 열 1: kicker "규칙이 명확" / heading "스크립트" / `.dg-box` "규칙 → 결과"
  - 열 2: kicker "순서가 고정" / heading "Workflow" / `.dg-box` "검색 → LLM → 답변"
  - 열 3: kicker "상황에 따라" / heading "Agent" / `.dg-box` "LLM ⇄ 도구 반복"
  - 칩 5개 + `.dg-caption.is-left`(칩 줄 안 마지막 항목)
  - 강조 열·강조 상자 없음(작가 지정, 우열 금지). 열 사이 화살표 없음.

```html
<section id="s-18" class="scene clip" data-skill="compare" data-scene-id="S17" data-start="85" data-duration="5" data-track-index="0">
  <div class="eyebrow"><span class="block-chip" data-editable="true">B3</span><span data-editable="true">SQL Tool과 Streamlit</span></div>
  <h2 class="scene-title" data-editable="true">스크립트·Workflow·Agent</h2>
  <p class="thesis" data-editable="true">문제에 필요한 만큼의 자율성만 준다.</p>
  <p class="explain" data-editable="true">Workflow는 코드가 순서를 정해 모델·도구를 부르고, Agent는 모델이 다음 행동을 고른다. Agent는 호출이 늘어 시간·사용량이 커지고 결과 예측이 어려워진다.</p>
  <div class="stack">
    <div class="compare-grid cols-3">
      <div class="compare-col">
        <div class="compare-kicker" data-editable="true">규칙이 명확</div>
        <div class="stack">
          <div class="compare-heading" data-editable="true">스크립트</div>
          <div class="dg-box" data-editable="true">규칙 → 결과</div>
        </div>
      </div>
      <div class="compare-col">
        <div class="compare-kicker" data-editable="true">순서가 고정</div>
        <div class="stack">
          <div class="compare-heading" data-editable="true">Workflow</div>
          <div class="dg-box" data-editable="true">검색 → LLM → 답변</div>
        </div>
      </div>
      <div class="compare-col">
        <div class="compare-kicker" data-editable="true">상황에 따라</div>
        <div class="stack">
          <div class="compare-heading" data-editable="true">Agent</div>
          <div class="dg-box" data-editable="true">LLM ⇄ 도구 반복</div>
        </div>
      </div>
    </div>
    <div class="chip-row">
      <span class="chip" data-editable="true">순차 연결</span>
      <span class="chip" data-editable="true">라우팅</span>
      <span class="chip" data-editable="true">병렬</span>
      <span class="chip" data-editable="true">오케스트레이터-워커</span>
      <span class="chip" data-editable="true">평가-개선</span>
      <p class="dg-caption is-left" data-editable="true">Anthropic이 정리한 Workflow 패턴 이름 · 우열 아님 · 교안 내용 재구성</p>
    </div>
  </div>
  <p class="source" data-editable="true">출처: Anthropic, Building effective agents · 교안-4일 5대 워크플로 패턴(교안 내용 재구성)</p>
  <aside class="speaker-note">(slide_copy/B3.md S17 v0.4 발표자 노트 최종 전문)</aside>
  <div class="deck-footer" data-editable="true">AI Agent Guide · SK hynix 사내 교육</div>
  <div class="page-num">18 / 52</div>
</section>
```

- 높이 예산

| 요소 | px |
|---|---|
| eyebrow + 제목 | 145 |
| 요지 1줄 | 81 |
| 설명 2줄 | 109 |
| 본문 위 간격 | 32 |
| 3열 (82 + kicker 34 + heading 72 + 24 + 상자 84) | 295 |
| gap | 24 |
| 칩 줄(캡션 포함 한 줄) | 57 |
| 출처 1줄 | 58 |
| **합계** | **801** |
| **출처 줄 위 여유** | **71** (캡션이 다음 줄로 넘어가도 +46px → 여유 25) |

- 가로 배치와 줄바꿈
  - 3열 (1680 − 32×2)/3 = 539px, 안쪽 457px. 상자 안쪽 407px. 최장 "검색 → LLM → 답변" ≈ 283px → 1줄. 열 제목 "Workflow" 40px ≈ 176px.
  - 칩 폭: 순차 연결 158, 라우팅 122, 병렬 94, 오케스트레이터-워커 298, 평가-개선 158(패딩 포함). 칩 합계 830 + gap 48 = 878px. 캡션(24px) ≈ 752px. 한 줄 합계 1,642px로 여유 38px.
  - 요지 ≈ 800px → 1줄. 설명 ≈ 2,270px → 2줄.
- 글자 크기: 72 / 40(요지·열 제목) / 32(설명·상자) / 28(칩) — 4종
- 주의점
  - 칩 줄의 기본 정렬(stretch) 때문에 캡션 글자가 칩 글자보다 약 12px 위에 붙는다. CSS 요청 V4-4(`.chip-row > .dg-caption { align-self:center }`, B2 파일에 적음)로 가운데를 맞춘다. 없어도 넘치지는 않는다.
  - 캡션이 글꼴 차이로 칩 뒤에 들어가지 못하면 칩 줄 안에서 다음 줄로 넘어간다. 그래도 여유 25px로 기준 안이다.
  - 파랑 강조가 없는 장면이다(요지 밑줄만). `is-accent`나 `accent`를 더하지 않는다.
- 빌드 메모(S17): v0.3의 `scene-subtitle`, 목록 6개, `compare-note` 3개, `.callout`은 뺀다.

## S18 · Part Finder의 후보 검증 (v0.4 배치)

- section 클래스 `scene clip` · data-skill `split`
- 배치 순서: eyebrow → 제목 → `.thesis` → `.explain` → `.split-grid.wide-left` [ 왼쪽 `.stack`: `.dg-row`(배지 + 패널 제목) → `.data-table` | 오른쪽 `.dg`: `.dg-row.stretch`(질문 → 후보 검색) → `↓` → `.dg-row.stretch`(조건 검증 → 답변·없음) ] → `.source`
- 틀 결정
  - 작가 배치(왼쪽 `.split-panel`)는 왼쪽이 82 + 41 + 24 + 표 293 = 440px라 여유 8px로 기준에 못 미친다. **패널을 빼고** 배지·제목 줄과 표를 `.stack`으로 바로 쌓는다(−82px). S11과 같은 틀이다. 표가 흰 바탕·헤어라인·모서리를 가지고 있어 패널 없이도 묶여 보인다. 설명 문단을 늘리지 않아도 된다.
  - 오른쪽 흐름의 세로 상자는 `.dg-row`로 감싼다. 격자 칸 높이로 늘어난 `.dg`에서 상자가 세로로 커지지 않게 하려는 것이다(공통 cascade 사항).
- 요소 ↔ 클래스 ↔ 문구
  - 머리줄: `.concept-badge` "공개 Beta 설계 기반 개념 예시" + `.panel-title` "요청: 제조사 A의 로봇 블레이드"
  - 표: 머리줄 4칸, 후보 2행 `tr.is-dim`(판정 칸 `status-dot.warn`), 결과 줄 `td colspan="4"` 안 `<strong>`. 결과 줄은 **파랑 없이 굵은 글자만** 쓴다(작가 강조 메모 채택). `td.is-match`를 쓰지 않는다. `strong`은 기본 굵기만 받고 색은 `--ink`다.
  - 흐름: 질문 / 요청 조건 → 후보 검색 / 데이터 ↓ 조건 검증 / 코드(`accent`) → 답변·없음 / 모델이 설명(`muted`, B3 공통 톤: 모델 단계 = 오프화이트)

```html
<section id="s-19" class="scene clip" data-skill="split" data-scene-id="S18" data-start="90" data-duration="5" data-track-index="0">
  <div class="eyebrow"><span class="block-chip" data-editable="true">B3</span><span data-editable="true">SQL Tool과 Streamlit</span></div>
  <h2 class="scene-title" data-editable="true">Part Finder의 후보 검증</h2>
  <p class="thesis" data-editable="true">데이터에 있는 후보만, 조건이 다 맞을 때만 답한다.</p>
  <p class="explain" data-editable="true">공개 Beta 설계는 검색 후보에 있는 Part Number만 쓰고, 파트 종류·제조사·장비 조건을 코드로 검증한다. 맞는 후보가 없으면 '없음'이라고 답한다.</p>
  <div class="split-grid wide-left">
    <div class="stack">
      <div class="dg-row"><span class="concept-badge" data-editable="true">공개 Beta 설계 기반 개념 예시</span><div class="panel-title" data-editable="true">요청: 제조사 A의 로봇 블레이드</div></div>
      <table class="data-table">
        <thead>
          <tr><th data-editable="true">Part Number</th><th data-editable="true">파트 종류</th><th data-editable="true">제조사</th><th data-editable="true">판정</th></tr>
        </thead>
        <tbody>
          <tr class="is-dim"><td data-editable="true">PN-A-2001</td><td data-editable="true">O-ring</td><td data-editable="true">A</td><td><span class="status-dot warn" aria-hidden="true"></span><span data-editable="true">종류 불일치</span></td></tr>
          <tr class="is-dim"><td data-editable="true">PN-B-1001</td><td data-editable="true">로봇 블레이드</td><td data-editable="true">B</td><td><span class="status-dot warn" aria-hidden="true"></span><span data-editable="true">제조사 불일치</span></td></tr>
          <tr><td colspan="4"><strong data-editable="true">결과: 조건에 맞는 후보 없음</strong></td></tr>
        </tbody>
      </table>
    </div>
    <div class="dg">
      <div class="dg-row stretch">
        <div class="dg-box" data-editable="true">질문<span class="dg-sub">요청 조건</span></div>
        <span class="dg-arrow" aria-hidden="true">→</span>
        <div class="dg-box" data-editable="true">후보 검색<span class="dg-sub">데이터</span></div>
      </div>
      <span class="dg-arrow" aria-hidden="true">↓</span>
      <div class="dg-row stretch">
        <div class="dg-box accent" data-editable="true">조건 검증<span class="dg-sub">코드</span></div>
        <span class="dg-arrow" aria-hidden="true">→</span>
        <div class="dg-box muted" data-editable="true">답변·없음<span class="dg-sub">모델이 설명</span></div>
      </div>
    </div>
  </div>
  <p class="source" data-editable="true">출처: Part Number Finder Beta 공개 README (GitHub MyWay0104) · 표는 가상 데이터 · 사내 운영 구조와 같다고 단정하지 않음</p>
  <aside class="speaker-note">(slide_copy/B3.md S18 v0.4 발표자 노트 최종 전문)</aside>
  <div class="deck-footer" data-editable="true">AI Agent Guide · SK hynix 사내 교육</div>
  <div class="page-num">19 / 52</div>
</section>
```

- 높이 예산

| 요소 | px |
|---|---|
| eyebrow + 제목 | 145 |
| 요지 1줄 | 81 |
| 설명 2줄 | 109 |
| 본문 위 간격 | 32 |
| 격자 = 왼쪽(머리줄 41 + 24 + 표 2 + 67 + 75 + 75 + 74) | 358 |
| 출처 1줄 | 58 |
| **합계** | **783** |
| **출처 줄 위 여유** | **89** (설명이 3줄이어도 43) |

  - 오른쪽 흐름 = 121 + 24 + 화살표 40 + 24 + 123 = 332px(칸 358 안, 위쪽 정렬).
- 가로 배치와 줄바꿈
  - `wide-left` 914 : 703. 머리줄: 배지 ≈ 331 + 20 + 패널 제목 ≈ 393 = 744px → 1줄. 표 폭 914px, 칸별 최장(패딩 포함) PN-A-2001 181 + 로봇 블레이드 237 + 제조사 120 + "● 제조사 불일치" 265 ≈ 803px → 모든 칸 1줄.
  - 흐름 상자: (703 − 화살표 40 − gap 40)/2 ≈ 311px, 안쪽 261px(accent 259). 최장 "후보 검색"·"조건 검증"·"답변·없음" ≈ 138px, 보조 줄 최장 "모델이 설명" ≈ 127px → 모두 1줄.
  - 요지 ≈ 1,000px → 1줄. 설명 ≈ 2,080px → 2줄. 출처 ≈ 1,390px → 1줄.
- 글자 크기: 72 / 40 / 32(설명·상자) / 30(표) / 28(패널 제목) — **5종, 품질 기준(4종) 1개 초과.** 28px 패널 제목은 요청 문장 1줄이고 표(30px)와 2px 차이라 같은 층으로 읽힌다. 요청 문장이 표를 읽는 전제라서 24px 라벨 층으로 내리지 않았다. 해소가 필요하면 오케스트레이터가 결정한다.
- 주의점
  - 파랑 구조 강조는 "조건 검증" 상자 1곳이다. 노트가 "파란 상자인 조건 검증"을 가리킨다.
  - `↓`는 `.dg` 폭 가운데에 놓인다. 두 줄을 왼쪽에서 오른쪽, 위에서 아래(Z자)로 읽는다.
  - 가상 데이터 표시는 배지 "공개 Beta 설계 기반 개념 예시"와 출처 줄의 "표는 가상 데이터"로 화면에 보인다.
- 빌드 메모(S18): v0.3의 `.split-panel`, 표 3행 중 "PN-A-3001 밸브" 행, `td.is-match` 결과 줄, `.dg-caption`, 해석 `ul.bullets`는 뺀다.

## S19 · 실습 3 · SQL Tool 연결 (v0.4 배치)

- section 클래스 `scene clip` · data-skill `title-bullets`(실습 안내판)
- 오케스트레이터 결정(2026-09-16) 반영: 교육용 스키마 표를 넣지 않는다. 오른쪽 칸의 시각 요소는 안전 통제 4개 층 띠 하나다.
- 배치 순서(실습 안내판 공통 틀, S13과 같음): eyebrow → 제목 → `.board-meta` → `.explain`(관찰할 것) → `.practice-grid` [ 왼쪽 `.stack`: `.time-blocks.row` → `.done-box` | 오른쪽 `.stack`: `.dg-caption.is-left` → `.chip-row`(4칸 띠) → `.fallback` ] → `.source`
- 요소 ↔ 클래스 ↔ 문구
  - 안내 줄: 코드 위치를 끝에 붙인다(작가 제안 채택, 1줄 확인).
  - 시간 블록 `.time-blocks.row`: 3분 스키마 확인 / 7분 연결·조회(`is-accent`) / 4분 다른 입력 / 2분 결과 설명
  - 안전 통제 띠: 계획서 8절("한 줄 띠로"), 작가 `.chip-row`, 오케스트레이터 "4개 층 띠"가 모두 한 줄 띠를 지정했다. 28px 칩 4개는 오른쪽 칸 773px에 한 줄(≈698px)로 들어간다. 32px `.dg-box` 4개 한 줄은 상자 안쪽 128px라 "읽기 전용"(≈138px) 등이 2줄로 접혀 쓰지 않는다.
  - 캡션: 표가 빠졌으므로 작가 캡션에서 표를 가리키던 앞부분("교육용 가상 스키마 · 아래:")을 떼고 "실행 환경에서 막는 네 층(개념 정리)"만 둔다. 띠 위에 제목처럼 둔다. 작가 확인이 필요하다(문구 축소 요청 3).

```html
<section id="s-20" class="scene clip" data-skill="title-bullets" data-scene-id="S19" data-start="95" data-duration="5" data-track-index="0">
  <div class="eyebrow"><span class="block-chip" data-editable="true">B3</span><span data-editable="true">SQL Tool과 Streamlit</span></div>
  <h2 class="scene-title" data-editable="true">실습 3 · SQL Tool 연결</h2>
  <p class="board-meta" data-editable="true">11:14–11:30 · 16분 · 코드 위치: 교육용 DB 준비 부분 · SQL 조회 도구 연결 부분 — 실습 가이드 해당 장</p>
  <p class="explain" data-editable="true"><strong>관찰할 것</strong>조회 도구에 넘어간 인자와 실제 조회 결과를 나누고, 인자를 누가 정했는지 본다. 프롬프트의 '위험한 SQL 금지'는 접근 제한이 아니며, 권한은 실행 환경에서 막는다.</p>
  <div class="practice-grid">
    <div class="stack">
      <div class="time-blocks row">
        <div class="time-block"><div class="min" data-editable="true">3<small>분</small></div><div class="what" data-editable="true">스키마 확인</div></div>
        <div class="time-block is-accent"><div class="min" data-editable="true">7<small>분</small></div><div class="what" data-editable="true">연결·조회</div></div>
        <div class="time-block"><div class="min" data-editable="true">4<small>분</small></div><div class="what" data-editable="true">다른 입력</div></div>
        <div class="time-block"><div class="min" data-editable="true">2<small>분</small></div><div class="what" data-editable="true">결과 설명</div></div>
      </div>
      <div class="done-box"><div class="done-label" data-editable="true">완료 조건</div><div class="done-text" data-editable="true">정답을 아는 질문과 결과 없는 질문의 차이를 설명한다</div></div>
    </div>
    <div class="stack">
      <p class="dg-caption is-left" data-editable="true">실행 환경에서 막는 네 층(개념 정리)</p>
      <div class="chip-row">
        <span class="chip" data-editable="true">읽기 전용</span>
        <span class="chip" data-editable="true">허용 테이블</span>
        <span class="chip" data-editable="true">쿼리 검사</span>
        <span class="chip" data-editable="true">사람 승인</span>
      </div>
      <div class="fallback" data-editable="true"><strong>막히면</strong>DB 접속이 안 되면 같은 구조의 교육용 DB로 진행합니다</div>
    </div>
  </div>
  <p class="source" data-editable="true">출처: LangChain SQL Agent 가이드 · 조사정리 6-4 · 근거: 사용자 제공 강의안·시간표</p>
  <aside class="speaker-note">(slide_copy/B3.md S19 v0.4 발표자 노트 최종 전문 — 표를 가리키는 2·5문장은 문구 축소 요청 3 반영본)</aside>
  <div class="deck-footer" data-editable="true">AI Agent Guide · SK hynix 사내 교육</div>
  <div class="page-num">20 / 52</div>
</section>
```

- 높이 예산

| 요소 | px |
|---|---|
| eyebrow + 제목 | 145 |
| 안내 줄 | 51 |
| 관찰할 것 2줄 | 109 |
| 본문 위 간격 | 32 |
| 실습 격자(채움) | 477 |
| 출처 1줄 | 58 |
| **합계** | **872** |
| **출처 줄 위 여유** | **0 (격자 채움, v0.3과 같은 측정 방식)** |

  - 왼쪽 칸: 시간 블록 206("스키마 / 확인" 2줄) + 24 + 완료 조건 2줄 201 = **431px** → 칸 안 여유 46px.
  - 오른쪽 칸: 캡션 34 + 24 + 띠 57 + 24 + 막히면 2줄 116 = **255px** → 칸 안 여유 222px.
- 가로 배치와 줄바꿈
  - 왼쪽 851px. 블록 이름 안쪽 140px(강조 138): "스키마 확인"(148px)만 2줄이고 "연결·조회"·"다른 입력"·"결과 설명"(각 120px)은 1줄이다. 완료 조건 ≈ 868px/775px → 2줄.
  - 오른쪽 773px(본문 폭의 46%). 띠 칩 158 + 186 + 158 + 158 + gap 36 ≈ 698px → 1줄(여유 75px). 막히면 ≈ 785px/715px → 2줄.
  - 안내 줄 ≈ 1,270px → 1줄. 관찰할 것 ≈ 2,480px → 2줄.
- 글자 크기: 72(제목·분 숫자) / 36(완료 조건) / 32(관찰할 것) / 28(안내 줄·블록 이름·칩·막히면) — 4종
- 주의점
  - 작가 확인 요청(칩 4개가 오른쪽 773px에 한 줄로 드는지)의 답: 든다(≈698px).
  - 오른쪽 칸 아래가 222px 빈다. S13·S20은 캡처 자리가 칸을 채우지만, 이 장면은 표가 빠져 시각 요소가 띠 하나뿐이다. 무게를 맞추려면 **대안: 2×2 타일**을 쓴다. `.dg` 안에 `.dg-row` 2줄 × `.dg-box` 2개(32px, 상자 안쪽 327px)를 두면 오른쪽 칸이 34 + 24 + 192 + 24 + 116 = 390px로 차고, 추가 CSS는 필요 없다. 다만 "한 줄 띠" 지시와 다르므로 기본안은 칩 한 줄이다.
  - 강조: `.time-block.is-accent`(7분) + `.done-box` 테두리(컴포넌트 고정). 띠 칩은 모두 기본색이다(`chip.accent` 금지).
  - 출처 재확인 보류(작가 보류 10): "쿼리 검사"·"사람 승인"이 LangChain SQL Agent 가이드에 없으면 칩을 2개로 줄인다. 배치 변화는 없다(한 줄 ≈ 357px).
- 빌드 메모(S19): v0.3의 `ul.bullets` 3단계와 오른쪽 2×2 `.time-blocks`는 뺀다. 시간 블록은 왼쪽 `.time-blocks.row`로 옮긴다. 스키마 표·스키마 칩은 넣지 않는다.

## S20 · 실습 4 · Streamlit 화면 실행 (v0.4 배치)

- section 클래스 **`scene clip`**(`title-image` 클래스를 뺀다) · data-skill **`title-image` 유지**
- 작가 부족분 검산: 작가 배치(가운데 정렬, [캡처 자리 | ①–④ 목록] + 아래 `.done-row`)에서 캡처 자리 칸은 872 − 145 − 안내 줄 64 − 관찰할 것 109 − 32 − `.done-row`(24 + 완료 조건 2줄 201) − 58 = **241px**다. 플레이스홀더 v2 내용(265px)보다 **24px 부족**하다. 작가 계산과 같다.
- 두 대안 검토

| 안 | 필요한 것 | 결과 | 판정 |
|---|---|---|---|
| (1) `.done-row` 최대 폭 1400→1680 | CSS. `.done-row`는 B6 S40도 쓰므로 변형 클래스가 필요하다. 가운데 정렬 장면에는 [캡처 자리 \| 목록]을 폭 100%로 펴 줄 가로 컨테이너가 없어(`.practice-grid`·`.split-grid`는 가운데 정렬에서 내용 폭으로 줄어듦) 클래스가 하나 더 필요하다 | 완료 조건이 1줄(칸 안쪽 752 ≥ 문구 724px)이 되어 −49px → 캡처 칸 241→290px, 자리표시(265) **칸 안 여유 25px** | 가능하지만 CSS 2건 |
| (2) `.placeholder.is-spec` 패딩 40→24 | CSS. S08·S11·S13 등 모든 플레이스홀더 v2에 영향 | 자리표시 내용 265→233px, 칸 241px → **칸 안 여유 8px** | 빠듯하고 모든 장면에 영향 → 단독 채택 안 함 |
| **채택: 실습 안내판 공통 틀** | CSS 없음. 방향어 2곳 수정(문구 축소 요청 4) | 격자 477px 채움. 왼쪽 칸 안 여유 68, 캡처 자리 337px(내용 265) | **채택** |

- 채택 이유: 과제 지시("실습 안내판 3장은 서로 배치를 맞춘다")를 그대로 만족한다. S13·S19와 같은 `.practice-grid` 틀에서 왼쪽은 [①–④ 목록 → 완료 조건], 오른쪽은 [캡처 자리 → 막히면]이다. 캡처 자리는 337px로 v0.3(388px)에 가깝고, 작가안(241px)보다 크다. CSS 추가도, 완료 조건 문구 축소도 필요 없다.
  - `title-image` 클래스를 빼야 왼쪽 정렬과 `.practice-grid` 폭(1680px)이 산다. `validate_topic.py`는 data-skill 값만 보고, `sync_overview.py`·PPTX 변환 스크립트는 이 클래스를 참조하지 않는다(저장소 검색으로 확인).
  - 안내 줄은 `scene-subtitle`(34px) 대신 `.board-meta`(28px)로 쓴다. S13·S19와 같은 요소를 같은 클래스로 맞추는 결정이다. 글자는 v0.3 그대로다.
- 배치 순서: eyebrow → 제목 → `.board-meta` → `.explain`(관찰할 것) → `.practice-grid` [ 왼쪽 `.stack`: `ul.compare-list`(①–④) → `.done-box` | 오른쪽 `.stack`: `.image-frame > .placeholder.is-spec` → `.fallback` ] → `.source`
- 요소 ↔ 클래스 ↔ 문구
  - ①–④ 목록: `ul.compare-list`(32px, 작가 지정 크기). 목록 점(회색 10px)과 ① 번호가 함께 보인다. 번호가 캡처의 스티커와 짝을 이루므로 번호를 글자로 둔다.
  - 캡처 자리 P-S20: 오른쪽 773px(본문 폭의 46%, 화면 폭의 40%)
  - 완료 조건 `.done-box`, 막히면 `.fallback`: 글자는 v0.3 그대로. `.done-row`는 쓰지 않는다.
  - **방향어**: 캡처 자리가 오른쪽, 목록이 왼쪽으로 바뀌므로 플레이스홀더 "대체" 줄의 "오른쪽 ①–④"를 "왼쪽 ①–④"로 바꿔야 한다. 아래 골격은 바꾼 표기다(원문 "오른쪽"). 노트 4문장의 "왼쪽 자리에 … 오른쪽 네 줄이"도 좌우를 바꾼다. 둘 다 문구 축소 요청 4로 작가 확인을 받는다.

```html
<section id="s-21" class="scene clip" data-skill="title-image" data-scene-id="S20" data-start="100" data-duration="5" data-track-index="0">
  <div class="eyebrow"><span class="block-chip" data-editable="true">B3</span><span data-editable="true">SQL Tool과 Streamlit</span></div>
  <h2 class="scene-title" data-editable="true">실습 4 · Streamlit 화면 실행</h2>
  <p class="board-meta" data-editable="true">11:30–11:38 · 8분 · 실행 3분 → 질문·결과 3분 → 코드 위치 2분 · 실습 가이드 해당 장</p>
  <p class="explain" data-editable="true"><strong>관찰할 것</strong>입력 칸의 값이 어떤 함수로 들어가고, 결과가 어느 코드에서 화면에 그려지는지 짚는다. 처리 중 표시가 뜨는 동안 어느 단계가 실행되는지도 본다.</p>
  <div class="practice-grid">
    <div class="stack">
      <ul class="compare-list">
        <li data-editable="true">① 입력 칸 — 질문이 함수로 들어가는 곳</li>
        <li data-editable="true">② 처리 중 표시 — 모델·도구 호출 대기</li>
        <li data-editable="true">③ 결과 — 코드가 화면에 그리는 답</li>
        <li data-editable="true">④ 근거 표시 — 기사 문단·조회 결과</li>
      </ul>
      <div class="done-box"><div class="done-label" data-editable="true">완료 조건</div><div class="done-text" data-editable="true">입력 → 함수 → 화면 결과의 연결을 설명한다</div></div>
    </div>
    <div class="stack">
      <div class="image-frame">
        <div class="placeholder is-spec">
          <div class="placeholder-id" data-editable="true">[IMG-PLACEHOLDER · P-S20]</div>
          <div class="placeholder-desc" data-editable="true">넣을 자료: Streamlit 앱 화면 · 네 영역에 ①–④ 번호</div>
          <div class="placeholder-desc" data-editable="true">조건: 주소·키·모델명 가리기 · 1600×900 이상</div>
          <div class="placeholder-desc" data-editable="true">대체: 확보 전에는 왼쪽 ①–④로 진행</div>
        </div>
      </div>
      <div class="fallback" data-editable="true"><strong>막히면</strong>옆 사람 화면으로 함께 확인하고, 5분 넘게 막히면 강사 시연으로 봅니다</div>
    </div>
  </div>
  <p class="source" data-editable="true">출처: Streamlit Architecture 문서 · 근거: 사용자 제공 강의안·시간표</p>
  <aside class="speaker-note">(slide_copy/B3.md S20 v0.4 발표자 노트 최종 전문 — 4문장 좌우는 문구 축소 요청 4 반영본)</aside>
  <div class="deck-footer" data-editable="true">AI Agent Guide · SK hynix 사내 교육</div>
  <div class="page-num">21 / 52</div>
</section>
```

- 높이 예산

| 요소 | px |
|---|---|
| eyebrow + 제목 | 145 |
| 안내 줄 | 51 |
| 관찰할 것 2줄 | 109 |
| 본문 위 간격 | 32 |
| 실습 격자(채움) | 477 |
| 출처 1줄 | 58 |
| **합계** | **872** |
| **출처 줄 위 여유** | **0 (격자 채움, v0.3과 같은 측정 방식)** |

  - 왼쪽 칸: 목록 4줄(45×4 + 18×3) 234 + 24 + 완료 조건 1줄 152 = **410px** → 칸 안 여유 67px(완료 조건이 2줄이 돼도 459px, 여유 18).
  - 오른쪽 칸: 캡처 자리 477 − 24 − 막히면 116 = **337px**(내용 265, 여유 72) + 24 + 116 = 477px.
- 가로 배치와 줄바꿈
  - 왼쪽 851px, 목록 가용 821px. 최장 "① 입력 칸 — 질문이 함수로 들어가는 곳" ≈ 580px → 모두 1줄. 완료 조건 칸 안쪽 775px에 문구 ≈ 724px → 1줄.
  - 오른쪽 773px. 캡처 자리 안쪽 689px, 설명 최장 "넣을 자료: Streamlit 앱 화면 · 네 영역에 ①–④ 번호" ≈ 660px → 1줄(여유 29px, 넘치면 그 줄만 2줄이 되어 +39px, 캡처 자리 여유 안). 막히면 안쪽 715px에 ≈ 990px → 2줄.
  - 안내 줄 ≈ 1,040px → 1줄. 관찰할 것 ≈ 2,230px → 2줄.
- 글자 크기: 72(제목) / 36(완료 조건) / 32(관찰할 것·목록) / 28(안내 줄·막히면) — 4종
- 주의점
  - 문구 축소 요청 4(방향어)가 거절되면 **대안 배치**를 쓴다. 문구는 원문 그대로 두고 좌우만 바꾸는 방식으로, 왼쪽 칸 [캡처 자리 → 막히면], 오른쪽 칸 [①–④ 목록 → 완료 조건]이다. 캡처 자리는 851px(화면 폭의 44%, 본문 폭의 51%)이다. 오른쪽 칸은 234 + 24 + 완료 조건 2줄(칸 안쪽 697px) 201 = 459px로 여유가 18px밖에 없다. CSS는 필요 없지만 S13·S19와 좌우가 뒤집힌다.
  - 가운데 정렬(`title-image` 클래스)을 유지해야 한다면 표의 (1)안이다. CSS 요청 두 건(`.done-row.is-wide { max-width:none; }`, 폭 100% 2칸 가로 줄 클래스)이 필요하고, 캡처 칸 안 여유는 25px다. 이 문서의 CSS 요청 목록에는 넣지 않았다.
  - 강조: `.done-box` 파랑 테두리 1곳(컴포넌트 고정). 시간 블록이 없는 안내판이라 `is-accent`는 없다.
  - v0.3 `.scene.title-image .done-box { margin-top:24px }` 규칙은 클래스를 빼면 적용되지 않는다. 스택 gap 24가 대신한다.
- 빌드 메모(S20): v0.3의 `scene-subtitle`, 가운데 `.image-frame > .placeholder`(v1 라벨), `.done-row`는 뺀다. 같은 문구를 위 골격 위치로 옮긴다.

## S21 · 오전 실습의 전체 구조 (v0.4 배치)

- section 클래스 `scene clip` · data-skill `split`
- 배치 순서: eyebrow → 제목 → `.thesis` → `.explain` → `.split-grid.wide-left` [ 왼쪽 `.split-panel`: `.concept-badge` → `.dg > .dg-row.stretch`(상자 3 + `⇄` 2) → `.dg-caption` | 오른쪽 `.dg`: `.dg-row`(오전 상자) → `↓` → `.dg-row`(오후 상자) ] → `.source`
- 요소 ↔ 클래스 ↔ 문구
  - 세 층: `.dg-box.accent` "화면" / 보조 "Streamlit", `.dg-box` "모델·도구" / "LLM·검색·SQL", `.dg-box` "데이터" / "기사·교육용 DB"
  - 캡션(가운데, v0.3과 같음) "파랑 테두리: 오후 수정 후보 위치(예: 결과 없음 안내)"
  - 오전 → 오후: `.dg-box.muted` "오전 · 세 층을 연결한 앱" → `↓` → `.dg-box` "13:20 · 이 앱이 오후 작업 대상". 상자를 `.dg-row`로 한 번 감싸 격자 높이로 늘어나지 않게 한다(공통 cascade 사항).
  - 왼쪽 패널은 v0.3처럼 유지한다. 도식 패널이라 속이 차 있고(328px), 격자를 `.fill`로 늘리지 않으므로 빈 패널 문제가 없다.

```html
<section id="s-22" class="scene clip" data-skill="split" data-scene-id="S21" data-start="105" data-duration="5" data-track-index="0">
  <div class="eyebrow"><span class="block-chip" data-editable="true">B3</span><span data-editable="true">SQL Tool과 Streamlit</span></div>
  <h2 class="scene-title" data-editable="true">오전 실습의 전체 구조</h2>
  <p class="thesis" data-editable="true">오전에 연결한 세 층 중 한 곳을 오후에 AI에게 고치게 한다.</p>
  <p class="explain" data-editable="true">한 층의 작은 수정이 다른 층에 영향을 주므로, 오후에는 구조를 먼저 읽고 바꿀 범위를 정한다. 오후 Claude Code의 모델 연결은 오전 실습 API와 별개라 따로 확인한다.</p>
  <div class="split-grid wide-left">
    <div class="split-panel">
      <span class="concept-badge" data-editable="true">개념 예시</span>
      <div class="dg">
        <div class="dg-row stretch">
          <div class="dg-box accent" data-editable="true">화면<span class="dg-sub">Streamlit</span></div>
          <div class="dg-arrow" aria-hidden="true">⇄</div>
          <div class="dg-box" data-editable="true">모델·도구<span class="dg-sub">LLM·검색·SQL</span></div>
          <div class="dg-arrow" aria-hidden="true">⇄</div>
          <div class="dg-box" data-editable="true">데이터<span class="dg-sub">기사·교육용 DB</span></div>
        </div>
      </div>
      <p class="dg-caption" data-editable="true">파랑 테두리: 오후 수정 후보 위치(예: 결과 없음 안내)</p>
    </div>
    <div class="dg">
      <div class="dg-row"><div class="dg-box muted" data-editable="true">오전 · 세 층을 연결한 앱</div></div>
      <span class="dg-arrow" aria-hidden="true">↓</span>
      <div class="dg-row"><div class="dg-box" data-editable="true">13:20 · 이 앱이 오후 작업 대상</div></div>
    </div>
  </div>
  <p class="source" data-editable="true">근거: 사용자 제공 강의안 · Streamlit Architecture 문서</p>
  <aside class="speaker-note">(slide_copy/B3.md S21 v0.4 발표자 노트 최종 전문)</aside>
  <div class="deck-footer" data-editable="true">AI Agent Guide · SK hynix 사내 교육</div>
  <div class="page-num">22 / 52</div>
</section>
```

- 높이 예산

| 요소 | px |
|---|---|
| eyebrow + 제목 | 145 |
| 요지 1줄 | 81 |
| 설명 2줄 | 109 |
| 본문 위 간격 | 32 |
| 격자 = 왼쪽 패널(82 + 배지 41 + 24 + 상자 123 + 24 + 캡션 34) | 328 |
| 출처 1줄 | 58 |
| **합계** | **753** |
| **출처 줄 위 여유** | **119** (설명이 3줄이어도 73) |

  - 오른쪽 = 84 + 24 + 화살표 40 + 24 + 84 = 256px(칸 328 안, 위쪽 정렬).
- 가로 배치와 줄바꿈
  - `wide-left` 914 : 703. 패널 안쪽 832px, 상자 (832 − ⇄ 2×40 − gap 4×20)/3 ≈ 224px, 안쪽 174px(accent 172). 최장 "모델·도구" ≈ 138px, 보조 줄 최장 "기사·교육용 DB" ≈ 161px → 모두 1줄. 작가 추정(176px)과 같다.
  - 오른쪽 상자 안쪽 653px. "13:20 · 이 앱이 오후 작업 대상" ≈ 435px → 1줄.
  - 캡션 ≈ 552px → 1줄. 요지 ≈ 1,060px → 1줄. 설명 ≈ 2,390px → 2줄.
- 글자 크기: 72 / 40 / 32(설명·상자) — 3종(dg-sub·캡션 24, 배지 22는 라벨)
- 주의점
  - 파랑 구조 강조는 "화면" 상자 1곳이다. 캡션이 이 파랑의 뜻을 설명한다. 오른쪽 "13:20" 상자에는 파랑을 쓰지 않는다.
  - 오전 상자 `muted` / 오후 상자 흰색은 "지난 것 / 다음 것"의 톤 차이다. 가운데 층을 "Agent"로 부르지 않는다.
  - 보류 9("결과 없음 안내"가 앱에 이미 있는지)에 따라 캡션 예시가 "근거 표시"로 바뀌어도 폭 안에 든다.
- 빌드 메모(S21): v0.3의 상자 이름·보조 줄(긴 표기)과 해석 `ul.bullets`는 뺀다.

## CSS 요청

오케스트레이터가 `scene-styles` 끝(v0.4 텍스트 층 블록)에 추가한다. 색은 `:root` 토큰만 쓴다. 번호는 B2 파일(V4-1·V4-4)과 이어 쓴다.

| ID | 선택자와 속성 | 쓰는 장면 | 필요도 | 없을 때 |
|---|---|---|---|---|
| V4-2 | `.steps.row.is-compact .step { padding: 24px 36px; }` · `.steps.row.is-compact .step-body { font-size: var(--fs-thesis); }` | S16 | **필수** | 카드 244px, 출처 줄 위 여유 −1px(넘침). 이때는 문구 축소 요청 2로 대신한다. 이 장면 전용 변형이라 다른 `.steps.row`에는 영향이 없다 |
| V4-3 | `.steps.row .step.is-muted { background: var(--canvas); }` | S16 | 권장 | 모델 카드도 흰색이 된다. 계획서 7-3의 톤 구분(모델 = 오프화이트)이 빠지고, 라벨 글자로만 구분한다 |
| V4-4 | (B2 파일에 적음) `.chip-row > .dg-caption { align-self: center; }` 포함 | S17 | 선택 | 칩 줄 끝 캡션이 약 12px 위에 붙는다 |

## 문구 축소 요청

| # | 장면 · 요소 | 현재 문구(글자 수) | 요청 | 이유 |
|---|---|---|---|---|
| 1 | S15 · 흐름 상자 4개의 보조 줄 | 질문 / 최근 7일 장비별 건수?(2 / 13자), 조건 해석 / 모델(5 / 2자), 집계 / SQL·Python(2 / 10자), 설명 / 모델(2 / 2자) | 보조 줄을 없애고 상자 이름에 합친다: "질문"(2자) · "모델 · 조건 해석"(10자) · "SQL·Python · 집계"(15자) · "모델 · 설명"(7자). 상자 1에 예시를 남기려면 "질문 · 최근 7일 건수?"(13자, 258px로 상자 안쪽 310px 안)도 된다 | 현재 출처 줄 위 여유 20px < 24. 흐름 줄 123→86px(−37px)로 여유 57px. 예시 질문은 설명 문단 1문장에 이미 있다 |
| 2 | S16 · 카드 아래 작은 줄 4개 (**조건부**: CSS V4-2가 반영되지 않을 때만) | 도구 스키마 / tool_calls / ToolMessage / AIMessage | 화면에서 빼고 노트로만 말한다(노트 2문장의 "카드 아래 작은 이름은…"도 함께 수정) | V4-2가 없으면 여유 −1px. 작은 줄을 빼면 −45px로 여유 44px |
| 3 | S19 · 캡션과 노트 (오케스트레이터의 스키마 표 제외 결정에 따른 정리) | 캡션 "교육용 가상 스키마 · 아래: 실행 환경에서 막는 네 층(개념 정리)"(35자). 노트 2문장 "오른쪽 표는 교육용 가상 스키마 예시이고, …", 5문장 "표 아래 네 칸처럼" | 캡션 "실행 환경에서 막는 네 층(개념 정리)"(20자, 골격에 반영). 노트 2문장에서 표 소개 절을 빼고 "실제 테이블과 컬럼 이름은 실습 가이드 해당 장을 기준으로 봅니다." 정도로 둔다. 5문장은 "오른쪽 네 칸처럼"으로 바꾼다 | 화면에 없는 표를 가리키지 않게 한다. 높이 영향은 없다 |
| 4 | S20 · 방향어 (축소가 아니라 좌우 수정) | 플레이스홀더 "대체: 확보 전에는 오른쪽 ①–④로 진행". 노트 4문장 "왼쪽 자리에 실제 앱 캡처가 들어가면 … 오른쪽 네 줄이 …" | "대체: 확보 전에는 왼쪽 ①–④로 진행"(글자 수 같음, 골격에 반영). 노트 4문장은 "오른쪽 자리에 … 왼쪽 네 줄이 …"로 바꾼다 | 실습 안내판 3장(S13·S19·S20)을 같은 틀(캡처 자리 오른쪽)로 맞췄다. 거절되면 S20 주의점의 대안 배치(문구 원문 그대로, 좌우 반대)로 빌드한다 |
