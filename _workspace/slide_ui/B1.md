# B1 배치 지시 · 09:00–09:40 · LLM 원리와 첫 호출

작성: slide_ui_designer · 2026-09-14 · 대상: S01, G01, S02–S08 (9장)
입력: `_workspace/slide_copy/B1.md`, `00_component_catalog.md`, `index.html` `<style id="scene-styles">` 실측값, topic `DESIGN.md` 최소 적용 규칙, `ppt_visual_plan.md` v0.2

## 공통 배치 규칙 (B1·B2·B3 동일)

### 측정 기준 (scene-styles 실측)

| 항목 | 값 |
|---|---|
| 장면 내부 높이 | 1080 − 상 80 − 하 128 = **872px**, 폭 **1680px** |
| eyebrow(24px + 칩 여백) + 제목 1줄(20 + 72×1.15) | **145px** |
| `.scene-subtitle` (16 + 34×1.4) | **64px** |
| `.source` 1줄 (24 + 24×1.4) | **58px** (margin-top:auto로 바닥 고정) |
| 본문 가용 높이 (본문 margin-top 56 제외) | 제목+출처 **613px** / 부제 추가 시 **549px** |
| `.callout` 1줄 (28×2 + 34×1.4) | **104px** |
| `.split-grid` 1fr 1fr 한 칸 / `.wide-left` 좌·우 | 808 / 913·703px |
| `.split-panel` 안쪽 폭 | 칸 폭 − 82 (패딩 40×2 + 테두리) |
| `.compare-grid` 2열 / `.cols-3` 한 열 | 820 / 539px, 목록 가용 폭은 −80 패딩 −30 들여쓰기 |

글자 폭 추정: 한글 1.0em, 라틴·숫자 0.55em, 공백·기호 0.3em. 코드(`Consolas` 기준)는 0.55em × 28px = 15.4px/자, 한글은 28px/자. 행 높이: 글자 크기 × line-height(본문 1.4, step-body 1.35, 코드 1.55).

### 세로 간격 처리 (중요)

`.callout`, `.done-box`, `.chip-row`, `.code-card`에는 margin-top이 없다. 그리드 뒤에 바로 두면 0px로 붙는다. **여러 블록을 세로로 쌓을 때는 `<div class="stack">`(flex column, gap 24px) 래퍼를 쓴다**(카탈로그 5절 `.stack`).

- `.stack` 안에서는 `.compare-grid`·`.split-grid`의 `flex:1`이 무효가 되어 내용 높이만큼만 차지한다. 의도된 동작이다.
- `.stack` 안의 `.compare-grid`/`.steps`는 자기 margin-top 56px을 그대로 가진다.

### 글자 크기 세기

eyebrow·출처·풋터(24px 크롬)는 제외하고 센다. 장면당 4종 이하.

### 편집 속성

텍스트 leaf마다 `data-editable="true"`. 상태 점·화살표는 `aria-hidden="true"`. 상태 점을 제목과 함께 쓸 때는 `<div class="compare-heading"><span class="status-dot ok" aria-hidden="true"></span><span data-editable="true">열 제목</span></div>`처럼 글자만 편집 span으로 감싼다(heading 자체에는 data-editable을 두지 않음).

### 스플릿 패널 머리줄 (S05·S11·S18 공통)

배지와 패널 제목이 둘 다 있으면 `<div class="dg-row"><span class="concept-badge">…</span><div class="panel-title">…</div></div>` 한 줄로 둔다(세로 63px 절약, 세 장면 통일). 배지만 있으면 배지 단독 줄.

---

## S01 · AI Agent Guide

- data-skill: `title` · section 클래스: `scene clip title-scene night`
- 배치 순서: eyebrow(절대 위치, 좌상단) → `.hero-rule` → `.hero-kicker` → `h1.hero-title` → `p.hero-sub` → `.hero-meta`(span 2개) → `aside.speaker-note` → `.deck-footer` → `.page-num`
- 요소 ↔ 문구
  - eyebrow: `.block-chip` "B1" · span "LLM 원리와 첫 호출" · `.time-chip` "09:00–09:40"
  - `.hero-kicker`: SK hynix 사내 교육 · 하루 과정
  - `.hero-title`: AI Agent Guide
  - `.hero-sub`: 내 업무에 적용할 작은 AI 도구 만들기 (현재 index.html의 "작은 Agent 만들기"를 교체)
  - `.hero-meta`: "[강사명]" / "[교육일]"
  - 출처 "근거: 사용자 제공 강의안·시간표": `.source`로 넣는다. 카탈로그 추가 요청 B가 CSS에 반영되어 title-scene의 출처는 하단 좌측(bottom 96px)에 고정된다.
- 강조: 남색 반전 자체가 강조. `.hero-rule`(흰 막대)만. 추가 강조 없음.
- 글자 크기: 104 / 44 / 28 (3종)
- 넘침 근거
  - 폭: hero-sub 21자 ≈ 한글 17×44 + 라틴 2×24 + 공백 5×13 ≈ 860px < max-width 1400px → 1줄
  - 높이: rule 40 + kicker 39 + title 28+114 + sub 28+59 + meta 72+39 ≈ 419px < 872px
- 플레이스홀더: 없음
- 빌드 메모: `.title-scene`은 `justify-content:center`인데 `.source`의 `margin-top:auto`가 여유 공간을 먼저 흡수해 제목 묶음이 위로 붙는다. 요청 B 반영(2026-09-14)으로 CSS가 `.source`를 하단에 고정하므로 S01에도 `.source`를 둔다. `night`는 오전 범위에서 이 장면만.

## G01 · 하루 시간표

- data-skill: `steps` · section 클래스: `scene clip` (변형 없음)
- 배치 순서: eyebrow → `h2.scene-title` → **`.split-grid`(1fr 1fr)** [좌: `.stack` / 우: `.stack`] → `.source`
  - 좌 `.stack`: `.panel-title` "오전" → `ol.steps.timeline`(3단계) → `.chip-row` > `.chip` "점심 11:40–13:20" → `.callout`
  - 우 `.stack`: `.panel-title` "오후" → `ol.steps.timeline`(4단계)
- 단계 마크업(7개 모두 같은 형태):
  ```html
  <li class="step">
    <div class="step-time" data-editable="true">09:00</div>
    <div class="step-num" data-editable="true">B1</div>
    <div class="step-body" data-editable="true">LLM 원리와 첫 호출<span class="step-desc">산출물: 첫 호출 결과</span></div>
  </li>
  ```
- 요소 ↔ 문구
  - `.step-time`: 09:00 / 10:00 / 11:00 / 13:20 / 14:20 / 15:20 / 16:00
  - `.step-num`(원 안): B1 … B7 — 번호 대신 구간 ID를 넣어 본문을 짧게 한다
  - `.step-body`: 구간명 (LLM 원리와 첫 호출 / 외부 근거와 기사 RAG / SQL Tool과 Streamlit / Claude Code와 개발 계획 / Claude Code로 앱 개선 / 지식 축적과 Skill / 자유 실습과 Q&A)
  - `.step-desc`: "산출물: …" (B7만 "산출물: 나의 작은 도구 · 17:30 종료")
  - `.chip`: 점심 11:40–13:20 (구분선 대신. 오전 열 맨 아래에 두어 오후 열로 넘어가는 경계 역할)
  - `.callout`: 휴식 09:40 · 10:40 · 14:00 · 15:00 (각 20분)
  - `.panel-title` "오전"/"오후"는 배치용 라벨(문구 추가 아님, 편집 가능)
- 강조: `.step-num` 파랑 원(구조색) 외 추가 강조 없음. callout 틴트 1개.
- 글자 크기: 72 / 36 / 30(step-time) / 28 (4종, step-num 24는 원 안 라벨)
- 넘침 근거
  - 폭: 한 칸 808 − (step-time 120 + 28 + 원 56 + 28) = 본문 576px. 최장 "Claude Code와 개발 계획" ≈ 라틴 11×20 + 한글 5×36 + 공백 2×11 ≈ 420px → 1줄. 최장 desc "산출물: 나의 작은 도구 · 17:30 종료" ≈ 한글 11×28 + 기타 ≈ 460px → 1줄. callout ≈ 634 + 패딩 72 = 706 < 808 → 1줄
  - 높이: 단계 1개 = max(원 56, 본문 49 + 6 + desc 39) = 94px
    - 좌: 39 + 24 + (3×94 + 2×18) + 24 + 55 + 24 + 104 = **588px**
    - 우: 39 + 24 + (4×94 + 3×18) = 493px
    - 합계 145 + 56 + 588 + 58 = **847px ≤ 872** (여유 25px, 주의)
- 플레이스홀더: 없음
- 빌드 메모: 7단계를 한 줄로 쌓으면 7×94 + 6×18 = 766px로 넘친다. 반드시 오전/오후 2열로 나눈다. `split-panel`(흰 카드)로 감싸면 +82px로 넘치므로 감싸지 않는다.

## S02 · 공정 엔지니어의 개발 경험

- data-skill: `title-tags` · section 클래스: `scene clip`
- 배치 순서: eyebrow → `h2.scene-title` → `p.scene-subtitle` → `.tags`(4개) → `.source`
- 요소 ↔ 문구
  - 제목: 공정 엔지니어의 개발 경험
  - `.scene-subtitle`: 목표: 만들고, 설명하고, 검증하기
  - `.tag.accent`: NAND Implant 공정 Engineer / `.tag`: Part Finder · Dokmo · 기술 블로그 (이 순서)
  - `.source`: 근거: 사용자 제공 경력 · Part Number Finder Beta 공개 README · Dokmo README
- 강조: `.tag.accent` 1개만.
- 글자 크기: 72 / 36 / 34 (3종)
- 넘침 근거
  - 폭: 태그 합계 ≈ 574 + 260 + 180 + 250 + 간격 60 = 1,324px < 1680 → 태그 1줄
  - 높이: 145 + 64 + 48 + 태그 82 + 58 ≈ 397px. 아래 약 475px는 빈 여백(의도). 발표자가 경력을 말로 채우는 장면
- 플레이스홀더: 없음 (공개 화면 확보 시 `split` 전환은 보류 목록 3번)
- 빌드 메모: 태그를 `div`가 아닌 `span.tag`로 둔다(카탈로그 4-8). 성과 수치 태그를 추가하지 않는다.

## S03 · AI·머신러닝·LLM의 관계

- data-skill: `split` · section 클래스: `scene clip`
- 배치 순서: eyebrow → 제목 → `.split-grid`(1fr 1fr) [좌: `.split-panel` / 우: `ul.bullets`] → `.source`
  - 좌 `.split-panel`: `.concept-badge` "개념 예시" → `.dg` > 중첩 영역 → `.dg-caption`
  - 중첩: `.dg-zone`(라벨 "AI · 넓은 분야 전체") > `.dg-zone`(라벨 "머신러닝 · 데이터로 패턴을 학습") > `.dg-zone`(라벨 "딥러닝 · 신경망으로 학습") > `.dg-box.accent` "LLM" + `.dg-sub` "현대 LLM은 주로 여기"
- 요소 ↔ 문구
  - `.dg-zone-label` 3개: 영역 이름과 보조를 " · "로 이어 한 줄
  - `.dg-caption`: 포함 관계를 나타낸 그림 · 연표가 아님
  - `.bullets li` 1: AI 안에 머신러닝, 그 안에 딥러닝
  - `li` 2: 현대 LLM은 주로 딥러닝 기반
  - `li` 3: 2017년 Transformer 등장 + `span.sub` "문맥을 다루는 방식을 바꾼 구조"
- 강조: `.dg-box.accent`(LLM) 1개. bullets에 strong 없음.
- 글자 크기: 72 / 40 / 32 / 28 (4종, zone-label 22는 라벨)
- 넘침 근거
  - 폭: bullets 가용 808 − 52 = 756px. 최장 li 1 ≈ 한글 12×40 + 라틴 2×22 + 공백·쉼표 6×12 ≈ 600px → 1줄. 가장 안쪽 영역 폭 726 − 52×3 = 570px, LLM 상자 충분
  - 높이(좌): 패널 82 + 배지 41 + 24 + 영역 3겹 84×3 + 상자 126 + 24 + 캡션 34 = **583px ≤ 613** (여유 30px)
  - 높이(우): 3×56 + sub 45 + 2×28 = 269px
- 플레이스홀더: 없음
- 빌드 메모: 영역은 화살표 없이 겹겹 포함만. 연표처럼 가로 나열하지 않는다. zone-label은 absolute라 영역 상단 패딩 56px 안에 들어간다.

## S04 · LLM의 응답 생성

- data-skill: `steps` · section 클래스: `scene clip`
- 배치 순서: eyebrow → 제목 → `.stack` [ `ol.steps.row`(3단계) → `.chip-row`(예시) → `p.dg-caption` → `.callout` ] → `.source`
- 예시 줄 `.chip-row` 순서: `.concept-badge` "개념 예시" → `.chip` 회의 · 내용을 · 세 · 줄로 → `.dg-arrow` "→" → `.panel-title` "다음 후보" → `.chip.accent` 요약 → `.chip` 정리 → `.chip` …
- 요소 ↔ 문구
  - `.step-num` 1·2·3 / `.step-body`: 토큰으로 나누기 · 다음 토큰 분포 계산 · 선택하고 반복
  - `.step-desc`: 문장을 모델의 처리 단위로 분할 / 지금까지의 문맥으로 후보별 가능성 계산 / 하나를 골라 붙이고 다시 계산
  - `.dg-caption`: 실제 토큰 분할은 모델의 토크나이저마다 다름 · 확률 수치는 표시하지 않음
  - `.callout`: 학습은 매개변수를 바꾸고, 보통의 호출은 학습된 모델로 계산만 한다
- 강조: `.chip.accent` "요약"(3단계 "선택"의 예) 1개. `.step.is-accent`는 쓰지 않는다.
- 글자 크기: 72 / 38 / 34 / 28 (4종)
- 넘침 근거
  - 폭: 카드 폭 (1680 − 128)/3 = 517, 안쪽 445px. step-body 최장 "다음 토큰 분포 계산" ≈ 380px → 1줄. desc 최장 20자 × 28 ≈ 530px → 2줄. callout ≈ 37자 → 1줄(1608px에 47자)
  - 높이: 카드 40 + 72 + 24 + 51 + 6 + 78 + 40 = 311px. 145 + 56 + 311 + 24 + 칩 55 + 24 + 캡션 34 + 24 + 104 + 58 = **835px ≤ 872**
- 플레이스홀더: 없음
- 빌드 메모: 확률 숫자를 칩에 넣지 않는다. 캡션은 `p.dg-caption.is-left`로 칩 줄과 왼쪽 정렬을 맞춘다(요청 C 반영). `.concept-badge`는 `align-self:flex-start`라 칩보다 약간 위에 붙는 것은 정상.

## S05 · Attention과 문맥

- data-skill: `split` · section 클래스: `scene clip`
- 배치 순서: eyebrow → 제목 → `.split-grid`(1fr 1fr) [좌: `.split-panel` / 우: `ul.bullets`] → `.source`
  - 좌 패널: 머리줄 `.dg-row`[`.concept-badge` "개념 예시" + `.panel-title` "같은 '배', 다른 문맥"] → `.dg` [ `.dg-row`: `.dg-box` "디저트로 배를 먹었다" · `.dg-arrow` "→" · `.dg-box.accent` "배 = 과일" ] [ `.dg-row`: `.dg-box` "항구에서 배를 탔다" · `.dg-arrow` "→" · `.dg-box.accent` "배 = 선박" ] → `.dg-caption`
- 요소 ↔ 문구
  - `.dg-caption`: 주변 단어가 표현에 반영되는 개념도 · 측정값 아님
  - `li` 1: 같은 표현도 문맥에 따라 다름 / 2: 다른 위치의 정보를 반영하는 계산 / 3: 데이터·규모·학습 방법으로 활용 확장
- 강조: `.dg-box.accent` 2개(과일·선박, 같은 역할의 한 쌍).
- 글자 크기: 72 / 40 / 32 / 28 (4종)
- 넘침 근거
  - 폭: 행 안 상자 폭 (726 − 40 − 40)/2 = 323, 안쪽 273px. "디저트로 배를 먹었다" ≈ 한글 9×32 + 공백 2×10 = 308px → **2줄**(허용). bullets 756px에 최장 li 3 ≈ 한글 15×40 + 기호·공백 5×12 ≈ 660px → 1줄
  - 높이(좌): 82 + 머리줄 41 + 24 + 행 2개 (126×2 + 24) + 24 + 34 = **481px ≤ 613**
- 플레이스홀더: 없음
- 빌드 메모: attention 가중치 숫자·색 농도 표현을 넣지 않는다. 문장 상자가 2줄이 되는 것은 정상(`dg-row`가 세로 가운데 정렬).

## S06 · 모델·서버·API의 역할

- data-skill: `split` · section 클래스: `scene clip`
- 변형: 문구 담당 권장 `.wide-left` 대신 **`.split-grid` 1fr 1fr**. 이유: 코드 카드 최장 줄이 약 770px이라 `.wide-left` 오른쪽(703px)에 들어가지 않는다.
- 배치 순서: eyebrow → 제목 → `.split-grid` [좌: `.split-panel`(개념도) / 우: `.stack`(코드 카드 → bullets)] → `.source`
  - 좌 패널: `.concept-badge` "개념 예시" → `.dg` > `.dg-row`[
    `.dg-zone`(라벨 "수강생 프로그램") > `.dg-box` "실습 코드" + `.dg-sub` "SDK·LangChain으로 요청 작성"
    · `.dg-arrow` "⇄" + `.dg-sub` "API 요청·응답"
    · `.dg-zone.accent`(라벨 "사내 추론 서버") > `.dg-box` "vLLM" → `.dg-arrow` "↓" → `.dg-box.accent` "모델" ] → `.dg-caption`
  - 우 `.stack`: `.code-card`(`.code-title` + `pre.code-body`) → `ul.bullets`
- 요소 ↔ 문구
  - `.dg-caption`: 주소·모델명·키 대신 역할 이름으로 표시
  - `.code-title`: 요청 목적지를 정하는 곳 · 개념 예시
  - `pre.code-body`(5줄, 문구 축소 요청 2 반영 시): import 1줄 + `llm = ChatOpenAI(` + model 줄 + base_url 줄 + 닫는 괄호 줄. 주석은 `span.cm`, `model=MODEL`과 `base_url=BASE_URL`은 `span.hl`
  - `li` 1: 모델: 응답을 계산하는 핵심 / 2: vLLM: 모델을 실행하는 서버 / 3: API·SDK: 요청 약속과 호출 도구 (각 li 앞 단어 "모델:" 등은 strong 없이 그대로)
- 강조: `.dg-zone.accent` + `.dg-box.accent`(모델)은 한 묶음 1개, 코드 `.hl` 2곳(같은 "목적지" 의미). strong은 쓰지 않는다.
- 글자 크기: 72 / 40 / 32 / 28 (4종, dg-sub·zone-label은 라벨)
- 넘침 근거
  - 폭(코드): 우 칸 808 − 패딩 56 − 2 = 750px. 최장 `)⋯# 키는 환경변수에서 읽음` ≈ 라틴 26×15.4 + 한글 10×28 + 공백 2×15 = 711px → 잘림 없음(`white-space:pre`, `overflow:hidden`이라 넘치면 잘림)
  - 폭(개념도): 좌 패널 안쪽 726px에 영역1 · 화살표(약 110) · 영역2가 수축 배치. 영역1 보조는 2줄로 접힌다
  - 높이(좌): 영역2 = 56+24+4 + 상자 84 + 16 + 화살표 40 + 16 + 상자 84 = 324px. 패널 82 + 41 + 24 + 324 + 24 + 34 = **529px ≤ 613** — **문구 축소 요청 1 반영 전제**. 미반영 시 vLLM·모델 상자에 보조 2줄이 붙어 영역2 438px → 패널 643px, **위험(30px 초과)**
  - 높이(우): 코드 5줄 = 제목 67 + 48 + 5×43.4 + 2 = 334, + 24 + bullets 224 = **582px ≤ 613**. 6줄(빈 줄 유지)이면 625px, **위험(12px 초과)**
- 플레이스홀더: 없음
- 빌드 메모: 영역2 안의 두 상자는 세로로 쌓으므로 "vLLM → 모델"을 `↓`로 그린다. `api_key` 인자를 코드에 쓰지 않는다. 문구 요청 1·2가 거절되면 좌측 `.split-panel` 래퍼를 빼고(−82px) 코드 빈 줄만 지운다.

## S07 · Temperature와 출력 변화

- data-skill: `compare` · section 클래스: `scene clip`
- 배치 순서: eyebrow → 제목 → `.compare-grid.cols-3` [열1 · 열2 · 열3 `.compare-col.is-accent`] → `.source`
- 열 내부 순서: `.compare-kicker` → `.compare-heading` → `ul.compare-list` (메모 없음)
- 요소 ↔ 문구
  - 열1: kicker "temperature 낮게" / heading "낮은 값" / li: 선택의 다양성 감소 · 결과가 비슷해지기 쉬움
  - 열2: kicker "temperature 높게" / heading "높은 값" / li: 선택의 다양성 증가 · 표현이 자주 달라짐
  - 열3(is-accent): kicker "두 값 공통" / heading "별도 확인" / li: 새 정보를 더하지 않음 · 낮아도 틀릴 수 있음 · 완전 동일도 보장 안 됨
- 강조: 열3 `.is-accent` 1개. 상태 점 없음.
- 글자 크기: 72 / 40 / 32 / 24(kicker) (4종)
- 넘침 근거
  - 폭: 열 539 − 82 − 30 = 426px(약 13자). 최장 "결과가 비슷해지기 쉬움" ≈ 한글 10×32 + 공백 2×10 = 340px → 1줄
  - 높이: 열 = 80 + kicker 34 + 8 + heading 72 + 목록(24 + 3×45 + 2×18) = 389px < 그리드 613px(열은 늘어나 같은 높이)
- 플레이스홀더: 없음
- 빌드 메모: 세 열 높이는 grid로 같아진다. 열1·2의 항목 수(2)가 열3(3)보다 적은 것은 의도.

## S08 · 실습 1 · 호출과 비교

- data-skill: `title-bullets` · section 클래스: `scene clip`
- 배치 순서(실습 안내판 공통, S13·S19 동일): eyebrow → 제목 → `p.board-meta` → `.practice-grid` [좌: `.stack`(`ul.bullets` → `.done-box`) / 우: 일반 `div`(`.time-blocks` → `.fallback`)] → `.source`
- 변형: 문구의 `.time-blocks.row` 대신 **기본 2열 `.time-blocks`**(2×2). 이유: 우 칸 774px을 4칸으로 나누면 칸 안쪽이 120px이라 "반복 비교"(약 120px)도 줄이 넘어간다.
- 요소 ↔ 문구
  - `.board-meta`: 09:24–09:40 · 16분 · 실습 가이드 해당 장 (가이드 장 이름은 여기에만. 별도 `.guide-ref`를 두지 않음)
  - `.bullets li`: 준비된 호출로 첫 응답 확인 / temperature만 바꿔 같은 질문 반복 / 최신 자료 질문으로 한계 관찰
  - `.time-block` 4개(좌상→우상→좌하→우하): `.min` "4`<small>분</small>`" + `.what` "첫 호출" / "6분" "반복 비교"(`.is-accent`) / "4분" "최신 자료 질문" / "2분" "결과 확인"
  - `.done-box`: `.done-label` "완료 조건" · `.done-text` "바꾼 값과 관찰한 차이를 한 문장으로 설명한다"
  - `.fallback`: `<strong>막히면</strong>` + "접속이 안 되면 준비된 출력으로 같은 비교를 계속합니다"
  - `.source`: 근거: 사용자 제공 강의안·시간표 · vLLM Reproducibility 문서
- 강조: `.time-block.is-accent`(6분) 1개 + `.done-box` 파랑 테두리(컴포넌트 고정). bullets strong 없음.
- 글자 크기: 72(제목·분 숫자) / 40 / 36 / 28 (4종)
- 넘침 근거
  - 가용 높이: 872 − 145 − board-meta 51 − 48 − 58 = **570px**
  - 폭: 좌 850px(1.1fr), bullets 가용 798px. 최장 li 2 ≈ 라틴 11×22 + 한글 9×40 + 공백 4×12 ≈ 650px → 1줄. done-text ≈ 한글 19×36 + 공백 6×11 = 749 < 안쪽 774 → 1줄(빠듯). 우 774px, 칸 안쪽 319px에 `.what` 최장 "최신 자료 질문" 약 190px → 1줄. fallback 안쪽 716px에 약 800px → 2줄
  - 높이(좌): 224 + 24 + done-box 153 = **401px**
  - 높이(우): 칸 168×2 + 20 + fallback 20+36+78+2 = **492px ≤ 570**
- 플레이스홀더: 없음
- 빌드 메모: 원래 카탈로그 예시는 `.fallback`이 좌측이지만 좌측에 두면 561/570px로 빠듯하다. 세 안내판 모두 `.fallback`은 우측 시간 블록 아래에 둔다. 좌측 div는 `.stack`(bullets와 done-box 사이 24px).

---

## 카탈로그 추가 요청

| ID | 요청 | 이유 | 본문 대체안 |
|---|---|---|---|
| A | `.stack { display:flex; flex-direction:column; gap:24px; min-width:0; }` | callout·done-box·code-card·chip-row에 margin-top이 없어 그리드 뒤에 붙는다. G01·S04·S06·S08 등에서 세로 스택이 필요 | `.stack` 래퍼 사용(이 문서 전체) |
| B | `.scene.title-scene .source { position:absolute; left:120px; bottom:96px; }` | `title-scene`의 가운데 정렬을 `.source`의 margin-top:auto가 깨뜨림(S01, 이후 G02·quote 장면도 해당) | 반영됨(2026-09-14) |
| C | `.dg-caption.is-left { text-align:left; }` | S04 예시 칩 줄(왼쪽 정렬) 아래 캡션이 가운데 정렬로 어긋남. 우선순위 낮음 | 반영됨(2026-09-14) |

## 문구 축소 요청(slide_content_writer)

| # | 장면 · 요소 | 현재 문구 | 권장 문구 | 이유 |
|---|---|---|---|---|
| 1 | S06 · 개념도 `vLLM`·`모델` 상자의 `.dg-sub` | vLLM · 보조: 모델을 실행하는 서버 / 모델 · 보조: 응답 계산 | 두 상자의 보조 삭제(상자 이름만) | 오른쪽 해석 항목 1·2와 같은 내용 반복(split 스킬: 불릿이 그림을 반복하지 않음). 유지 시 좌 패널 643px > 613px |
| 2 | S06 · 코드 카드 | 6줄(import 뒤 빈 줄 포함) | 빈 줄을 지운 5줄 | 우측 코드 6줄 + 해석 3개 = 625px > 613px |
