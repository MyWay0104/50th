# 콘티 v1 · 묶음 P1 (도입 ~ 실습 03) — 2026-09-18

**확정: 2026-09-18 교차 검수 반영** (반영 내역: `_workspace/roadmap2/conti_fix_log_P1.md` · 확정 입력: `_workspace/roadmap2/conti_coordinator_decisions.md` 1절 · 2절 · 3절 · 3-1절 — 이 콘티와 다르면 그 문서가 우선한다)

- 모드 · 범위 · 버전: conti · 묶음 P1(장면 15개) · v1
- 장면 순서(스토리보드 그대로): S01 → G01 → S02 → G00 → **D01** → E01 → LAB01 → **D02** → S04 → N01 → LAB02 → S07 → L01 → LAB03 → T01
- 입력: 스토리보드 `docs/edu-storyboard-v1-2026-09-18.md`(전체 지도 · 구간 표지 설계 · 구간 0~2 · 실습 안내판 사양 · 추적 표 · 콘티 공통 규약 16항 · 확인 필요 사항 18건의 권고 기본값 **모두 채택**) · `ROADMAP2.md` 0절 · 0-0절(R2-1~R2-21) · 요약본 §실습01~§실습03 · 검수 리포트 3절(실습 01 · 02 · 03) · 9절 1~43행 · 덱 추출본 v0.5 · `_workspace/roadmap2/scenes/*.html` · `scene-styles.css` · `deck-rules.json`
- 표기 약속
  - 분은 각 블록의 "소요시간" 줄과 말미 「시간 합계표」에만 쓴다(문서 전용). 화면 문안과 강사 스크립트에는 시각 · 분을 쓰지 않는다(G01의 시각 세 개는 현재 값 유지).
  - "메모(문서 전용)" 줄의 내용(생략 가능 · 시연 대체 · 비상안)은 화면과 노트에 옮기지 않는다.
  - 보강 장면의 "현재" 문장은 추출본에서 글자 그대로 인용했다(치환 표에 그대로 쓸 수 있다). 괄호 안 숫자는 검수 리포트 9절의 행 번호다.
  - 화면 문안은 따옴표 없이 적은 그대로가 표기 문안이다. `{…}` · 백틱 안의 영문은 코드 표기다.
  - 축약 · 건너뛰기 · 시연 대체 같은 진행 지시는 "메모(문서 전용)"와 "소요시간" 줄에만 적는다. 예상 질문의 답과 강사 스크립트에는 대괄호 꼬리표를 넣지 않는다(빌더는 대괄호 꼬리표를 노트에 옮기지 않는다).
  - 긍정봇에게 거는 말과 봇의 답은 따옴표 안에 실습자료 그대로 둔다(L01 · LAB03). 안내판에서 화면 문장 속에 풀어 쓰는 질문 예시는 존댓말로 옮긴다(LAB02 — 스토리보드 확인 필요 15의 기본값).
  - 실습 안내판의 소단계 칸 작은 글씨(`.when`)에는 실습자료의 하위 번호를 짝지어 적는다(예: "실습자료 STEP 2" · "실습자료 2-1"). 파일 이름이 아니라 번호이므로 하우스 룰과 부딪치지 않는다. 하위 번호는 요약본 기준이며 원본과 같은지는 말미 5의 3번.
- 출처 문서 이름 확인(2026-09-18, 공식 사이트 열람): LangChain 공식 문서의 "Models" · "Messages" 페이지, LangChain 레퍼런스의 "ChatPromptTemplate", uv 공식 문서의 "Working on projects". `init_chat_model`에 `temperature`를 넘기는 예시와 `chain = prompt | model` 패턴이 현행 공식 문서에 있음을 확인했다.

---

## 공통 부품 (다른 묶음과 같은 그림이어야 하는 것 — P1이 처음 그리므로 여기서 사양을 고정한다)

### 공통 부품 A · 비유 축 띠 (G01 · D01~D07 · G02)

| 칸 | 아이콘 파일(`assets/img/icons/`) | 정거장 이름(굵게) | 작은 글씨 | 실습 번호(G01 · G02에서만) | Part |
|---|---|---|---|---|---|
| (띠 왼쪽 끝 표시) | `key.svg` | 준비 | 열쇠 | 01 | Part 1 |
| 1 | `brain.svg` | 두뇌 | LLM | 02 · 03 | Part 2 |
| 2 | `book-open.svg` | 책 | RAG | 04 | Part 2 |
| 3 | `hand.svg` | 손발 | Tool | 04b | Part 2 |
| 4 | `joystick.svg` | 조종 | Agent | 05 | Part 2 |
| 5 | `laptop.svg` | 내 PC의 에이전트 | Claude Code | 06 · 07 | Part 3 · Part 4 |
| 6 | `rocket.svg` | 적용 | 내 업무 | 08 | Part 4 |

- 아이콘 파일은 코디네이터 결정 C-1의 확정값이다(`joystick.svg` · `laptop.svg` · `door-open.svg`는 이미 `assets/img/icons/`에 들어와 있다). 대체 파일은 두지 않는다. UI 사양의 표지 예시 마크업에 조종 · 내 PC의 에이전트 칸이 다른 파일로 적혀 있어도 C-1의 파일로 빌드한다.
- 띠 문구(글자 그대로): **두뇌 → 책 → 손발 → 조종 → 내 PC의 에이전트 → 적용**. "준비"는 정거장이 아니므로 선으로 잇지 않고, 띠 왼쪽 끝 위의 작은 표시(열쇠 아이콘 + "준비")로만 둔다.
- 마크업 이름(코디네이터 결정 3절 · UI 사양 E절 확정): 띠 `ol.axis-strip` · 정거장 `li.axis-stop` · 아이콘 타일 `.axis-tile` · 아이콘 `.axis-icon` · 정거장 이름 `.axis-label` · 준비 표시 `li.axis-prep`(띠의 첫 자식 · 절대 위치라 칸을 차지하지 않는다). 밝은 배경과 반전 배경(`.scene.night`)에서 같은 마크업을 쓴다.
- 상태 세 가지(클래스 이름 고정): 지나옴 `is-done`(옅은 채움) · 현재 `is-current`(굵은 테두리 + 굵은 글씨 + 밑줄) · 남음 `is-todo`(흐림). 반전 배경 위에서도 테두리 대비 3:1 이상(R2-20).
- 장면별 상태
  - G01: 여섯 정거장 모두 **상태 클래스 없이**(기본 모양 = 보통). 준비 표시는 `axis-prep`에 글자 "준비 · 01"(상태 클래스 없음).
  - D01: 여섯 정거장 모두 `is-todo`, 준비 표시는 `axis-prep is-current`에 글자 "준비".
  - D02~D07: UI 사양 E-3의 상태 배치표대로(D02 = 첫째 `is-current`, D03 = 첫째 `is-done` · 둘째 `is-current` …). **준비 표시는 두지 않는다** — UI 사양에 `axis-prep`의 "지나옴" 모양이 없어서 남겨 두면 D01과 똑같이 강조되어 보인다.
  - G02: 여섯 정거장 모두 `is-done`, 준비 표시는 G01과 같게(코디네이터 결정 P4-6). G02가 반전 배경이면 준비 표시가 화면에서 가장 밝은 요소가 되므로 빌더가 스냅샷으로 확인하고, 거슬리면 G02에서만 준비 표시를 뺀다(실습 01 칩은 남긴다).
- 표지에서는 아이콘과 정거장 이름만 보인다(작은 글씨 · 실습 번호 없음. 실습 번호는 표지의 "아래 한 줄"이 맡는다). 작은 글씨와 실습 번호 칩은 G01 · G02에서만 정거장 안에 더한다(G01 구성 설명 참조).
- 아이콘 고정: 위 표의 파일을 이후 모든 장면(T01~T03 · S15 · S21 · S22 …)에서 같은 개념에 같은 파일로 쓴다. 열쇠는 `key.svg`, 문은 `door-open.svg`. `hand.svg`는 손발 비유 전용이다(손들기 표시에는 쓰지 않는다 — 코디네이터 결정 X-8).

### 공통 부품 B · 열쇠 3종 지도 (E01 전체판 · LAB01 · R01 · LAB04 · LAB04b · N02 · S23 · LAB06 축소판)

| 열쇠 | 이름(글자 그대로) | 여는 문 | 받는 곳 | 쓰는 곳 |
|---|---|---|---|---|
| 첫째 열쇠 | 채팅용 키 | 채팅 모델 | 사내 모델 운영 페이지 | 오늘 내내 · 오후 Claude Code에서도 |
| 둘째 열쇠 | 임베딩용 키 | 임베딩 모델 | 사내 모델 운영 페이지(따로 받음) | RAG 실습 |
| 셋째 열쇠 | 데이터 조회용 토큰 | 사내 데이터(사내 LLM 게이트웨이 밖 · 다른 플랫폼의 문) | 다른 플랫폼(이름을 쓰지 않는다) | Tool 실습에서 받음 · 만료됨 |

- 전체판(E01 한 곳): 세 줄. 줄마다 `[key.svg] 열쇠 이름 → [door-open.svg] 문 이름` + 아래 작은 글씨 한 줄. 셋째 줄은 점선 테두리(`.dg-box.is-dashed` — 채택됨).
- **축소판(다른 모든 장면 — 코디네이터 결정 X-6: 이 모양이 전 묶음의 기준이다. 상자 셋으로 그리지 않는다)**
  - 모양: **칩 한 줄.** `.chip-row` 하나 안에 맨 앞 `key.svg`(`.row-icon`) 하나 + `.chip` 세 개. 화살표 · 문 상자 · 줄바꿈 없음. 새 CSS 없음.
  - 칩의 글자(글자 그대로 · 순서 고정): `첫째 · 채팅용 키` / `둘째 · 임베딩용 키` / `셋째 · 데이터 조회용 토큰`
  - 강조: 그 장면에서 쓰거나 이야기하는 열쇠의 칩만 `.chip.accent`, 나머지는 `.chip`. 장면별 강조 — LAB01: 첫째 · 둘째 / R01 · LAB04: 둘째 / LAB04b · N02 · LAB05: 셋째 / S23 · LAB06: 첫째.
  - 꼬리(선택 · 한 개만): 칩 줄 끝에 `.dg-sub` 한 마디를 붙일 수 있다. 문이 보여야 하는 장면(401을 다루는 LAB04b · N02)은 "문 · 사내 데이터", 만료를 말하는 장면은 "만료되기도 합니다". 꼬리에 문장을 쓰지 않는다.
  - 자리: 안내판에서는 `.practice-grid` 오른쪽 `.stack`의 맨 위(자리표시 위). 높이가 모자라면 개념 칩 줄의 오른쪽 끝으로 올린다. 개념 장면에서는 설명문 바로 아래.
- 401의 고정 구절(코디네이터 결정 X-2 · 이 지도와 함께 쓰는 말): "401은 열쇠 문제입니다. 어느 문에서 났는지 보고 그 열쇠를 확인합니다." P1 범위에는 401 장면이 없고, E01이 "만료됩니다"로 첫 예고만 한다.
- 실제 값은 어디에도 쓰지 않는다. 예시가 꼭 필요하면 `sk-****`.

---

## 구간 0 · 도입

### [S01] AI Agent Guide (표지)
- S01 · 처리: 유지 · 바꿀 문장 없음 (검수 리포트 9절에 해당 행 없음)
- 소요시간: 1분 (문서 전용) · 스킵: 필수
- 메모(문서 전용): 노트의 끝 문장 "다음 장에서 오늘 하루 시간표를 보겠습니다."는 G01 재작성 뒤에도 뜻이 맞으므로 그대로 둔다.

### [G01] 하루 흐름
- 구간/위치: 도입 · S01 뒤
- 처리: 재작성 (9절 1~7행)
- 유형: 구조도 · data-skill: steps
- 학습목표 연결: 도입-LO1
- 핵심 메시지(1문장): 오늘은 두뇌에서 시작해 책과 손발을 붙이고 조종을 맡긴 뒤, 내 PC의 에이전트를 거쳐 내 업무에 적용합니다.
- 슬라이드 텍스트
  - 헤드라인: 하루 흐름
  - 시각 칩(현재 값 유지 · 네 개): 시작 09:00 / 점심 11:40–13:20 / 종료 17:30 / 휴식은 진행에 따라 안내합니다
  - 한 줄 요지(.thesis): 두뇌에서 시작해 책과 손발을 붙이고 조종을 맡긴 뒤, 내 PC의 에이전트를 거쳐 내 업무에 적용합니다.
  - 띠 위 작은 제목(.panel-title): 오늘의 여섯 정거장
  - 띠의 문안(정거장마다 위에서 아래로: 아이콘 / 정거장 이름 / 작은 글씨 / 실습 번호 칩)

    | 칸 | 정거장 이름(`.axis-label`) | 작은 글씨(`.dg-sub`) | 실습 번호 칩(`.chip`) |
    |---|---|---|---|
    | 띠 왼쪽 끝 위(`axis-prep`) | 준비 · 01 | — | — (표시 안의 "01"이 실습 번호다) |
    | 1 | 두뇌 | LLM | 실습 02 · 03 |
    | 2 | 책 | RAG | 실습 04 |
    | 3 | 손발 | Tool | 실습 04b |
    | 4 | 조종 | Agent | 실습 05 |
    | 5 | 내 PC의 에이전트 | Claude Code | 실습 06 · 07 |
    | 6 | 적용 | 내 업무 | 실습 08 |

  - 캡션(.dg-caption.is-left) 첫 줄: 정거장 아래 번호는 실습자료의 실습 번호와 같습니다 · 조종까지가 점심 전, 내 PC의 에이전트부터가 점심 뒤입니다
  - 캡션 둘째 줄(Part 묶음 · 이름은 실습자료와 같다): Part 1 개발환경 세팅 · 실습 01 / Part 2 LangChain 실습 · 실습 02부터 05 / Part 3 Claude Code 설치 · 실습 06 / Part 4 Claude Code 활용 · 실습 07 · 08
  - 화면에 두지 않는 것: 정거장마다의 "하는 일" 한 줄(노트가 말로 맡는다), Part 묶음 상자 줄, 띠 위의 "점심" 표시(캡션 첫 줄이 맡는다 · 시각은 위 칩에만)
  - 출처 줄: 없음
- 비주얼
  - 종류: 구조도(진행 띠 + 실습 번호 칩 줄)
  - 구성 설명(코디네이터 결정 P1-2 — 표지와 같은 띠를 밝은 배경에 그린다): 제목 아래 `.chip-row`(현재 마크업 그대로: `play.svg` + 칩 네 개) → `.thesis` → `.panel-title` → `ol.axis-strip` 하나. 띠의 첫 자식은 `li.axis-prep`(`key.svg` + 글자 "준비 · 01" · 상태 클래스 없음). 이어서 `li.axis-stop` 여섯 개를 공통 부품 A의 순서와 아이콘 파일로 넣되 **상태 클래스를 붙이지 않는다**(모두 보통). 정거장마다 `.axis-tile`(아이콘) → `.axis-label`(정거장 이름) → `.dg-sub`(작은 글씨) → `.chip`(실습 번호) 순서로 쌓는다. 칩을 정거장 안에 넣으므로 띠 아래에 **실습 번호 칩 한 줄**이 생기고, 정거장과 세로로 저절로 맞는다(새 CSS 없음). 빌더가 칩 줄을 띠 밖의 별도 줄로 빼려면 띠와 같은 6등분 폭으로 맞춘다. 띠 아래 `.dg-caption.is-left` 두 줄. 강조 칸은 두지 않는다. 현재의 "오전 · 다섯 구간 / 오후 · 세 구간" 두 줄과 1~8 번호, "→ 산출물" 표기는 모두 없앤다.
  - 표지와 다른 점: 표지의 띠는 절대 위치(장면 아래쪽 고정)지만 G01의 띠는 본문 흐름 안에 놓는다(UI 사양의 고정 위치 규칙은 `.title-scene`에만 걸린다). 준비 표시는 띠 위쪽으로 튀어나오므로 `.panel-title`과 겹치지 않는지 스냅샷으로 확인하고, 겹치면 `.panel-title`을 뺀다(띠의 `aria-label`이 같은 말을 갖고 있다).
  - 채택되지 않은 것: 콘티 초안의 9열 격자와 Part 묶음 상자 줄(새 CSS와 넘침 위험). 같은 그림을 G01 · 표지 · G02가 되풀이하는 효과는 그대로다.
  - 자산: 아이콘만(공통 부품 A). 자리표시 없음.
- 강사 스크립트: 오늘 하루를 한 장으로 보겠습니다. 가운데 띠의 여섯 정거장이 오늘의 줄거리입니다. 낱말은 지금 몰라도 됩니다. 정거장마다 하나씩 풉니다. 점심 전에는 두뇌인 LLM에게 첫 질문을 보내고, 기사 한 편을 책처럼 찾아보며 답하게 하고, 손발인 Tool로 계산과 사내 데이터 조회를 붙인 다음, 두뇌가 결과를 보고 다시 판단하며 손발을 조종하는 Agent까지 만듭니다. 점심 뒤에는 이 묶음이 이미 조립되어 있는 내 PC의 에이전트, Claude Code를 연결해 써 보고 마지막에 각자 업무에 적용합니다. 정거장 아래 번호는 실습자료의 실습 번호와 똑같으니 화면과 실습자료를 오갈 때 번호만 맞추시면 됩니다. 맨 왼쪽의 준비는 정거장에 가기 전에 열쇠를 받고 첫 응답까지 확인하는 순서입니다. 화면의 시각은 시작, 점심, 종료 세 개뿐이고 나머지는 여러분의 진행에 맞춰 조절하며 휴식도 그때그때 안내하겠습니다. 여섯 정거장 가운데 가장 궁금한 곳이 몇 번째인지 손가락으로 표시해 주세요.
- 예상 질문 & 답
  - "실습 04b는 왜 번호가 04b인가요?" → 실습자료의 번호를 그대로 옮겼습니다. RAG 실습 다음, Agent 실습 앞에 있는 Tool 실습입니다.
  - "오전에 다 못 따라가면 오후 실습을 못 하나요?" → 오후의 연결에는 오전에 받은 첫째 열쇠만 있으면 됩니다. 막히면 짝 화면으로 함께 진행합니다.
- 전환 문장: 다음 장에서 제 소개를 짧게 드리겠습니다.
- 소요시간: 3분 (문서 전용)
- 스킵 가능: 필수
- 메모(문서 전용): 첫 장에서 LLM · RAG · Tool · Agent · Claude Code 다섯 낱말이 라벨로 한꺼번에 보인다. 그래서 "하는 일" 줄을 화면에서 빼고 노트에 "낱말은 지금 몰라도 됩니다"를 넣었다. 띠의 글자는 공통 부품 A와 한 글자도 다르지 않게 한다(G02가 같은 띠를 다시 쓴다).
- 근거: 스토리보드 구간 0 · 검수 리포트 9절 1~7행 · ROADMAP2 R2-6 · R2-7 · R2-8 · R2-12(결정 1) · 요약본 전체 흐름 개요(Part 이름) · 코디네이터 결정 C-1 · P1-2 · 3절

### [S02] 강사 소개
- 처리: 유지(노트 한 곳만 바꿈 · 9절 8행)
- 바꿀 문장
  - 노트 · 현재: "Part Finder는 오전 조회 도구 구간에서 짧게," → 변경: "Part Finder는 오전 Tool 구간에서 짧게,"
- 그대로 두는 문장(P1 ↔ P4 이음매): 노트의 "잠깐 각자 줄이고 싶은 반복 업무 하나를 메모에 한 줄 적어 두세요. 오늘 마지막 자유 실습의 재료가 됩니다." — S39(P4)가 이 한 줄을 받는다.
- 자리표시 `[IMG-PLACEHOLDER · P-S02]`: 그대로(넣을 자료 · 조건 · 대체 변화 없음)
- 소요시간: 3분 (문서 전용) · 스킵: 축약 가능(카드 세 장을 읽지 않고 "반복 업무 한 줄"만 받는다)
- 근거: 검수 리포트 9절 8행 · ROADMAP1 결정 8

### [G00] AI를 잘 활용하는 공부법
- 처리: 보강(노트 한 곳 · 썸네일 모서리 · 9절 9행)
- 바꿀 문장
  - 노트 · 현재: "링크는 실습 가이드 후속 읽을거리에 있으니 수업 후에 보셔도 됩니다." → 변경: "링크는 화면 아래 출처 줄에 있으니 수업 후에 보셔도 됩니다. 이 네 단계는 오늘 마지막 장에서 한 번 더 돌아봅니다."
- 화면 문장: 바꿀 문장 없음. 네 단계의 글자(만들기 · 설명하기 · 조건 바꾸기 · 검증하기)는 G02 노트(P4)가 같은 순서 · 같은 낱말로 회수한다.
- 비주얼: 참고 영상 썸네일의 네 모서리를 모두 둥글게(R2-20). 그림 자체에 모서리 둥글림을 주는 규칙은 **채택됐다**(코디네이터 결정 3절 · UI 사양). 마크업은 그대로 두면 된다. 영상은 재생하지 않는다(썸네일과 요점만).
- 소요시간: 2분 (문서 전용) · 스킵: 축약 가능
- 근거: 검수 리포트 9절 9행 · ROADMAP2 R2-20 · R2-21("후속 읽을거리" 단정 금지) · 정의서 §2 콘티 규칙(참고 영상)

---

## 구간 1 · 준비 · 실습 01

### [D01] 구간 표지 · 준비
- 구간/위치: G00 뒤 · E01 앞
- 처리: 신설
- 유형: 브릿지(회고→예고) · data-skill: title (`title-scene night`, 표지 S01과 같은 짙은 주황 반전 배경)
- 슬라이드 텍스트(스토리보드 「구간 표지 설계」 표 그대로)

  | 항목 | 문안 |
  |---|---|
  | 머리말 | Part 1 · 개발환경 세팅 |
  | 큰 제목 | 준비 — 열쇠를 받고 문을 엽니다 |
  | 아래 한 줄 | 실습 01 |
  | 진행 띠 | 여섯 정거장 모두 흐림(`is-todo`), 띠 왼쪽 끝 위에 "준비" 표시만 강조(`axis-prep is-current`) |

- 비주얼: 표지 공통 틀(UI 사양 E-3 · E-4의 마크업 그대로). 머리말 = `.hero-kicker`, 큰 제목 = `.hero-title.is-section`(표지용 크기 변형 — 채택됨), 아래 한 줄 = `.hero-sub`, 그 아래 공통 부품 A의 띠 `ol.axis-strip`. 띠의 첫 자식으로 `li.axis-prep.is-current`(`key.svg` + "준비")를 넣고, 정거장 여섯 개는 모두 `li.axis-stop.is-todo`. 큰 제목은 줄이 넘치든 아니든 `<span class="nowrap">준비 —</span> <span class="nowrap">열쇠를 받고 문을 엽니다</span>`처럼 **`.nowrap` 두 덩어리**로 감싼다(코디네이터 결정 C-3 · `<br>`은 하우스 룰 위반). 시각 · 분 · 구간 코드 · 설명 문단 없음. 띠가 인포그래픽 요건을 채운다.
- 강사 스크립트(두 문장): 지금까지 오늘의 여섯 정거장과 공부 방법을 봤습니다. 여기서부터는 손을 움직여, 열쇠를 받고 첫 응답이 오는 데까지 갑니다.
- 소요시간: 0.5분 (문서 전용) · 스킵: 넘기기만
- 근거: 스토리보드 「구간 표지 설계」 · ROADMAP2 R2-13 · R2-20 · 코디네이터 결정 C-2 · C-3 · 3절

### [E01] 세 층과 열쇠 3종
- 구간/위치: 준비 · D01 뒤
- 처리: 재작성 (9절 10~16행)
- 유형: 구조도 · data-skill: split
- 학습목표 연결: 실습01-LO1 · 실습01-LO3
- 핵심 메시지(1문장): 코드는 내 PC에서 돌고 모델은 사내 LLM 게이트웨이 뒤에서 돌며, 환경 파일에 적은 열쇠가 둘을 잇습니다.
- 슬라이드 텍스트
  - 헤드라인: 세 층과 열쇠 3종
  - 한 줄 요지(.thesis): 코드는 내 PC에서 돌고 모델은 사내 LLM 게이트웨이 뒤에서 돌며, 환경 파일에 적은 열쇠가 둘을 잇습니다.
  - 설명문(.explain · 한 문장): 오늘은 열쇠를 받아 환경 파일(.env)에 적고 첫 응답을 확인합니다.
  - 왼쪽 · 세 층(개념 예시 배지 유지 · 상자의 작은 글씨는 한 줄씩, 게이트웨이 상자만 R2-17의 가장 작은 글씨 한 줄을 더 둔다)

    | 층 이름표 | 상자 제목 | 작은 글씨 | 가장 작은 글씨 |
    |---|---|---|---|
    | 내 PC | 실습 코드 | 요청을 보냄 | — |
    | 환경 파일 | 환경 파일(.env) | 주소와 열쇠를 적는 곳 | — |
    | 사내 LLM 게이트웨이(강조) | 사내 LLM 게이트웨이 | 열쇠를 확인하고 모델로 전달 | 뒤에서 추론 서버가 모델을 실행 |

  - 왼쪽 캡션: 주소·모델명·키는 역할 이름으로 표시 · 한 층만 어긋나도 첫 응답이 오지 않습니다
  - 오른쪽 · 열쇠 3종 지도(작은 제목 .panel-title: 어느 열쇠가 어느 문을 여는가)

    | 줄 | 열쇠 | → | 문 | 아래 작은 글씨 |
    |---|---|---|---|---|
    | 1 | 첫째 열쇠 · 채팅용 키 | → | 채팅 모델 | 사내 모델 운영 페이지에서 받습니다 · 오후에도 같은 열쇠를 씁니다 |
    | 2 | 둘째 열쇠 · 임베딩용 키 | → | 임베딩 모델 | 사내 모델 운영 페이지에서 따로 받습니다 · RAG 실습에서 씁니다 |
    | 3(점선) | 셋째 열쇠 · 데이터 조회용 토큰 | → | 사내 데이터 | 사내 LLM 게이트웨이 밖, 다른 플랫폼의 문입니다 · Tool 실습에서 받습니다 · 만료됩니다 |

  - 오른쪽 아래 강조 상자(.callout): 채팅용 키로는 **임베딩 모델의 문이 열리지 않습니다**
  - 코드 위치 줄(.guide-ref): 코드 위치: 실습자료 · 실습 01
  - 출처 줄: 없음(현재의 "vLLM Online Serving 문서 · LangChain Models 문서"는 코드 조각과 함께 뺀다)
- 없애는 것: 현재의 코드 카드("요청 목적지를 정하는 두 줄"), "코드에 OpenAI라는 이름이 있어도 …" 문장(12행 · R2-21), 칩 "첫 응답 = content" · "토큰 사용량도 함께"(14행), vLLM 로고(R2-17 — 낱말은 예상 질문의 답에서만).
- 교차 검수로 화면에서 노트로 내린 것(코디네이터 결정 X-14 — 넘침과 무관하게 지금 뺀다): 설명문의 "설치는 사전 과제로 마쳤습니다."(LAB01 노트의 첫 문장이 맡는다) · "열쇠는 세 개이고, 여는 문이 서로 다릅니다."(지도의 작은 제목이 같은 말이다) · 내 PC 상자의 "VS Code와 Python" · 환경 파일 상자의 "비밀은 코드 밖에"(LAB01 개념 칩 ②와 예상 질문이 맡는다). 게이트웨이 상자에 사용량 · 한도는 적지 않는다(코디네이터 결정 P1-9 — LAB06이 맡는다).
- 비주얼
  - 종류: 구조도 + 대응 지도
  - 구성 설명: `.split-grid`(좌우 비슷한 너비 — 현재의 `wide-left`는 떼어도 된다). 왼쪽 `.stack`: `.concept-badge` "개념 예시" → `.dg > .dg-row.even.stretch`에 `.dg-zone` 셋(셋째만 `.accent`)과 그 사이 `.dg-arrow` "⇄" 둘. 상자 아이콘은 내 PC = 기존 Python 로고, 환경 파일 = `key.svg`, 게이트웨이 = `server.svg`. "뒤에서 추론 서버가 모델을 실행"은 `.dg-sub`로 가장 작게. 오른쪽 `.stack`: `.panel-title` → `.dg` 안에 `.dg-row` 세 줄. 줄마다 `.dg-box`(왼쪽 · `key.svg` + 열쇠 이름) → `.dg-arrow` "→" → `.dg-box`(오른쪽 · `door-open.svg` + 문 이름), 줄 아래 `.dg-sub` 한 줄. 첫째 줄의 열쇠 상자는 `.accent`(오늘 가장 먼저, 가장 오래 쓰는 열쇠). 셋째 줄은 두 상자 모두 `.dg-box.is-dashed`(점선 테두리 · 채택됨 · 대비 3:1 이상) + `.muted`. 그 아래 `.callout`, 마지막에 `.guide-ref`.
  - 그림이 말해야 하는 것: 첫째 · 둘째 문은 왼쪽 그림의 게이트웨이에 달린 문이고, 셋째 문은 **게이트웨이 밖**이다. 점선 테두리와 셋째 줄의 작은 글씨("사내 LLM 게이트웨이 밖, 다른 플랫폼의 문입니다")가 그 구분을 맡는다.
  - 이 장면의 왼쪽 그림만 "세 층"이라 부른다(코디네이터 결정 X-5). 오후 S23이 같은 그림을 다시 쓰고, 앱의 그림(화면 · 처리 · 데이터)은 "세 부분"이라 부른다.
  - 자산: 아이콘만(`key.svg` · `server.svg` · `door-open.svg`). 자리표시 없음.
- 강사 스크립트: 실습 환경은 세 층으로 기억하시면 됩니다. 코드는 내 PC에서 돌고, 모델은 사내 LLM 게이트웨이 뒤에서 돕니다. 그 둘을 잇는 것이 환경 파일에 적는 주소와 열쇠입니다. 문은 게이트웨이에 달려 있고, 그 뒤에 모델이 있습니다.
  열쇠는 세 개입니다. 채팅용 키와 임베딩용 키는 둘 다 사내 모델 운영 페이지에서 받지만, 채팅용 키로는 임베딩 모델의 문이 열리지 않아서 따로 받습니다. 임베딩은 글의 뜻을 숫자로 바꾸는 일이고 RAG 실습에서 설명합니다. 셋째 열쇠인 데이터 조회용 토큰은 게이트웨이 밖, 다른 플랫폼의 문을 여는 열쇠입니다. 지금은 받지 않고 Tool 실습에서 받으며, 시간이 지나면 만료됩니다. 첫째 열쇠는 오후에 Claude Code를 연결할 때 그대로 다시 쓰니 잘 챙겨 두세요. 질문 하나 드리겠습니다. 환경 파일의 채팅용 키 칸이 비어 있으면 세 층 가운데 어디에서 막힐까요?
- 예상 질문 & 답
  - "키를 코드에 바로 적으면 안 되나요?" → 코드는 복사되고 공유됩니다. 비밀은 코드 밖의 환경 파일에 두고 코드는 읽어 가기만 합니다. 그래서 같은 코드를 옆 사람에게 줘도 열쇠는 따라가지 않습니다.
  - "임베딩용 키가 틀렸다면 언제 드러나나요?" → 오늘은 RAG 실습에서 처음 드러납니다. 지금의 검증 실행은 첫째 열쇠만 씁니다. (스토리보드 확인 질문 ②)
  - "추론 서버가 무엇인가요?" → 모델을 실제로 돌리는 서버입니다. 사내에서는 vLLM이라는 서빙 프로그램이 그 일을 하고, 우리 코드는 게이트웨이까지만 알면 됩니다.
  - (확인 질문 ①의 답) 내 PC에서 요청은 나가지만 게이트웨이의 문 앞에서 열쇠 확인에 걸립니다. 주소가 틀리면 문에 닿지도 못합니다.
- 전환 문장: 그러면 열쇠 두 개를 받아 첫 응답까지 직접 가 보겠습니다.
- 소요시간: 3분 (문서 전용)
- 스킵 가능: 필수
- 메모(문서 전용): 이 그림의 "만료됩니다"가 401의 첫 예고다(디브리핑은 N02 · P2 — 거기서는 고정 구절 "401은 열쇠 문제입니다. 어느 문에서 났는지 보고 그 열쇠를 확인합니다."를 쓴다). 열쇠 지도의 글자는 공통 부품 B와 한 글자도 다르지 않게 한다. 실습 직전 새 용어를 줄이려고 "추론 서버" · "vLLM"은 노트 본문에서 빼고 질문이 나올 때만 답한다. "임베딩"은 열쇠 이름 때문에 화면에 남을 수밖에 없어 노트에서 한 문장으로 뜻을 준다. uv는 이 장면에 넣지 않았다(LAB01 노트가 소단계 3에서 바로 풀어 준다 — 이 장면은 이미 글이 많다).
- 근거: 요약본 §실습01 · §실습04b(토큰과 401) · ROADMAP2 R2-5 · R2-6 · R2-17 · R2-21 · 검수 리포트 3절 실습 01 · 9절 10~16행 · 스토리보드 확인 필요 4(코드 조각 뺌) · 코디네이터 결정 C-1 · P1-9 · X-2 · X-5 · X-14

### [LAB01] 실습 01 · 열쇠를 받고 첫 응답까지
- 구간/위치: 준비 · E01 뒤
- 처리: 신설
- 유형: 실습 예고 · data-skill: title-bullets
- 학습목표 연결: 실습01-LO2 · 실습01-LO3
- 핵심 메시지(1문장): 열쇠 두 개를 받아 환경 파일에 적고 첫 응답을 확인합니다.
- 슬라이드 텍스트(스토리보드 「실습 안내판 사양」 그대로)
  - 제목: 실습 01 · 열쇠를 받고 첫 응답까지
  - 코드 위치 줄(.board-meta): 코드 위치: 실습자료 · 실습 01

  | 항목 | 내용 |
  |---|---|
  | 소단계와 개념 칩 | 1 열쇠 두 개 받기 — ① 열쇠 3종 / 2 실습 폴더를 열고 환경 파일 만들기 — ② 비밀은 코드 밖에 / 3 환경 맞추기 — ③ uv / 4 검증 실행 — ④ 세 층 |
  | 소단계 칸의 작은 글씨(`.when` · 실습자료 하위 번호) | 1 실습자료 STEP 2 / 2 실습자료 STEP 3 · 4 / 3 실습자료 STEP 3 / 4 실습자료 STEP 5 |
  | 관찰할 것 | "첫 응답이 오면 세 층이 이어진 것입니다. 오지 않으면 오류 글에서 어느 층인지 찾습니다. 셋째 열쇠는 Tool 실습에서 받습니다." |
  | 완료 조건 | "검증 실행에서 모델의 응답 문장이 출력됩니다. 환경 파일의 열쇠 칸 두 개가 채워졌는지 짝과 서로 확인합니다(값은 읽지 않습니다)." |
  | 막히면 | "연결이 안 된다는 글이면 주소와 사내망, 인증 글이면 열쇠 칸, 파일을 못 찾는다는 글이면 실행 위치(실습 폴더의 맨 위)를 봅니다. 계속 막히면 짝 화면으로 진행합니다." |
  | 강사 시연 대체(문서 전용 · 화면과 노트에 옮기지 않는다) | 대체하지 않는다(이후 모든 실습의 전제). 막힌 사람은 짝 화면으로 같이 가고 쉬는 동안 돕는다 |
  | 자리표시 | P-LAB01 · 넣을 자료: 검증 실행 성공 화면 / 조건: 키 · 주소 · 모델명 가리기 / 대체: 열쇠 지도 축소판(첫째 · 둘째 열쇠 강조) |

  - 출처 줄: 출처: uv 공식 문서(Working on projects)
- 비주얼
  - 종류: 실습 안내판(현재 S08과 같은 틀)
  - 구성 설명: `.scene-title` → `.board-meta` → `.explain`(맨 앞에 `<strong>관찰할 것</strong>`) → `.time-blocks.is-steps.row.time-band`에 `.time-block` 넷(`.min` 1~4, `.what` 소단계 이름, `.tb-icon`은 차례로 `key.svg` · `folder.svg` · `boxes.svg` · `play.svg`, 넷째만 `.is-accent`) → `.chip-row`에 개념 칩 ①~④(넷째 칩 `.accent`) → `.practice-grid`: 왼쪽 `.stack`에 `.done-box`(완료 조건)와 `.fallback`(막히면), 오른쪽 `.stack`에 공통 부품 B의 축소판(칩 한 줄 · 첫째 · 둘째 칩 `.accent`, 셋째 칩 보통 · 꼬리 없음)과 `.placeholder.is-spec`. 높이가 모자라면 축소판을 `.placeholder.is-spec` 위가 아니라 개념 칩 줄 오른쪽 끝으로 옮긴다. 소단계 칸마다 `.when`에 실습자료 하위 번호를 적는다.
  - 자산: `[IMG-PLACEHOLDER · P-LAB01]`
    - 넣을 자료: 검증 실행 성공 화면
    - 조건: 키 · 주소 · 모델명 가리기
    - 대체: 열쇠 지도 축소판(첫째 · 둘째 열쇠 강조)
- 강사 스크립트: 설치는 사전 과제로 마쳤으니 오늘은 열쇠부터 받습니다. 먼저 사내 모델 운영 페이지에서 채팅용 키와 임베딩용 키를 하나씩, 모두 두 개 받습니다. 어느 것을 골라 받는지는 실습자료 실습 01의 열쇠 발급 단계에 적힌 대로 고르세요. 이름이 비슷해서 가장 많이 틀리는 곳입니다. 이어서 실습 폴더를 열고 예시 파일을 복사해 환경 파일을 만든 뒤, 방금 받은 열쇠 두 개를 제자리에 적습니다. 다음으로 uv로 환경을 맞춥니다. uv는 정해 둔 목록대로 같은 환경을 누구의 PC에서든 똑같이 다시 만들어 주는 도구라서, 오늘의 실행 명령은 모두 uv로 시작합니다. 마지막으로 검증 실행을 돌립니다.
  응답이 나오지 않으면 화면의 막히면 칸처럼 오류 글이 연결 이야기인지, 인증 이야기인지, 파일을 못 찾는다는 이야기인지부터 읽어 보세요. 명령은 꼭 실습 폴더의 맨 위에서 실행해야 환경 파일을 찾습니다. 다 되신 분은 짝과 열쇠 칸 두 개가 채워졌는지 서로 봐 주세요. 값은 읽지 말고 칸만 확인합니다.
- 예상 질문 & 답
  - "환경을 맞추다가 설치가 실패합니다." → 먼저 실습 폴더의 맨 위인지, 사내망에 붙어 있는지 봅니다. 그다음 Python이 3.12 이상인지 확인합니다. 그보다 낮으면 뒤의 RAG 실습에 쓰는 검색 라이브러리가 설치되지 않습니다. 한 층씩 좁히는 순서는 부록의 실행 오류 점검 장에 있습니다.
  - "환경 파일에 빈칸이 많이 남는데 괜찮은가요?" → 항목은 많아도 세 묶음(채팅 · 임베딩 · 데이터 조회)입니다. 지금은 앞의 두 묶음만 채우고, 데이터 조회 묶음은 Tool 실습에서 채웁니다.
  - "pip으로 설치하면 안 되나요?" → 오늘은 모두 같은 환경이어야 옆 사람과 결과를 비교할 수 있어서 uv 하나로만 합니다.
- 전환 문장: 문이 열렸으니 이제 두뇌에게 첫 질문을 보냅니다. (D02로 잇는 고정 문장)
- 소요시간: 안내 2분 + 실습 18분 + 디브리핑 · 확인 2분 (문서 전용)
- 스킵 가능: 필수
- 메모(문서 전용): 강사 시연으로 대체하지 않는다. 오전에서 가장 불확실한 시간이 이 실습이다(여러 사람이 동시에 열쇠를 받고 패키지를 내려받는다). 디브리핑 2분에는 E01의 확인 질문 ① · ②를 다시 묻는다. 둘째 열쇠는 이 실습에서 검증되지 않아 잘못 받으면 RAG 실습에서 처음 드러난다 — 그래서 완료 조건에 "짝과 열쇠 칸 확인"을 넣었다(값은 읽지 않는다: 화면을 함께 볼 때도 열쇠 값은 드러내지 않는다는 오후의 규칙과 같은 말이다). 개념 칩 ③은 "uv"만 남기고 풀이("같은 환경 다시 만들기")는 노트로 내렸다. 소단계 ↔ 실습자료 하위 번호의 짝은 요약본 기준이다(소단계 2 · 3이 실습자료의 STEP 3 · 4에 걸쳐 있다 — 말미 5의 3번).
- 근거: 요약본 §실습01 · §실습02 해설 포인트(실습 폴더의 맨 위) · ROADMAP2 R2-5 · R2-6 · R2-7 · R2-21 · 검수 리포트 3절 실습 01 · 스토리보드 「실습 안내판 사양」 LAB01 · 확인 필요 14 · 코디네이터 결정 X-6 · X-14

---

## 구간 2 · 두뇌 · 실습 02 · 03

### [D02] 구간 표지 · 두뇌
- 구간/위치: LAB01 뒤 · S04 앞
- 처리: 신설
- 유형: 브릿지(회고→예고) · data-skill: title (`title-scene night`)
- 슬라이드 텍스트(스토리보드 「구간 표지 설계」 표 그대로)

  | 항목 | 문안 |
  |---|---|
  | 머리말 | Part 2 · LangChain 실습 |
  | 큰 제목 | 두뇌 — LLM에게 첫 질문을 보냅니다 |
  | 아래 한 줄 | 실습 02 · 실습 03 |
  | 진행 띠 | 두뇌 강조 |

- 비주얼: D01과 같은 표지 공통 틀(`.hero-kicker` · `.hero-title.is-section` · `.hero-sub` · `ol.axis-strip`). 띠에서 두뇌 칸만 `is-current`(굵은 테두리 + 굵은 글씨), 나머지 다섯 칸은 `is-todo`. **준비 표시(`axis-prep`)는 두지 않는다**(공통 부품 A · UI 사양 E-4). 큰 제목은 `<span class="nowrap">두뇌 —</span> <span class="nowrap">LLM에게 첫 질문을 보냅니다</span>` 두 덩어리.
- 강사 스크립트(한 문장): 여기서부터는 첫 정거장인 두뇌, LLM에게 질문을 보내고 같은 두뇌에 성격을 입혀 봅니다.
- 소요시간: 0.5분 (문서 전용) · 스킵: 넘기기만
- 메모(문서 전용): 바로 앞 LAB01의 고정 전환 문장("문이 열렸으니 이제 두뇌에게 첫 질문을 보냅니다.")이 "지금까지"의 구실을 하므로 노트는 "여기서부터" 한 문장만 둔다(R2-13의 "두 문장 이내"에 맞는다).
- 근거: 스토리보드 「구간 표지 설계」 · ROADMAP2 R2-13 · 코디네이터 결정 C-2 · C-3 · 3절

### [S04] LLM의 응답 생성
- 구간/위치: 두뇌 · D02 뒤
- 처리: 보강 (9절 17 · 18행)
- 유형: 개념 · 비유 · data-skill: steps
- 학습목표 연결: 실습02-LO1
- 핵심 메시지(1문장): LLM은 두뇌입니다. 사실을 조회하지 않고 다음 토큰을 골라 문장을 만들며, 아직 손발이 없습니다.
- 바꿀 문장(화면)
  1. 한 줄 요지(.thesis) (18행) · 현재: "LLM은 사실을 조회하지 않고 다음 토큰의 확률 분포를 계산합니다." → 변경: "LLM은 두뇌입니다. 판단하고 설명하지만 아직 손발이 없습니다." (추적 표의 고정 문장 그대로)
  2. 설명문 첫 문장 · 현재: "보통의 호출은 학습된 모델로 계산만 하므로, 질문을 더 보낸다고 모델이 재학습되지 않습니다." → 변경: "두뇌, 곧 대규모 언어모델(LLM)은 사실을 찾아보지 않고, 다음에 올 토큰을 골라 문장을 만듭니다."
  3. 설명문 셋째 문장 (17행) · 현재: "다음 실습에서 바꿀 temperature 값은 3단계의 선택에 영향을 줍니다." → 변경: 화면에서 뺀다(노트의 끝 질문 뒤에서 말로만 잇는다 — 아래 노트 3). S07이 첫 질문 실습 뒤로 갔으므로 여기서는 앞선 언급일 뿐이다.
  4. 그대로 두는 문장(P1 ↔ P2 이음매 — 컨텍스트 윈도우의 정의): "한 번에 넣을 수 있는 입력·출력 길이의 한도를 컨텍스트 윈도우라고 합니다."
  - 결과: 설명문은 두 문장이다(변경 2 + 그대로 두는 문장 4). "재학습되지 않는다"는 말은 현재 노트에 이미 있어("이미 학습된 모델로 결과를 계산할 뿐입니다") 화면에서만 뺀다. 비유 '두뇌'가 "알고 기억하는 존재"로 읽히지 않도록, 고정 문장 바로 아래 첫 문장이 "사실을 찾아보지 않고"로 시작한다.
- 바꿀 문장(노트)
  1. 맨 앞에 더함: "오늘의 첫 정거장은 두뇌입니다. LLM은 두뇌라서 판단하고 설명하지만, 아직 손발이 없어서 물어보면 대답만 합니다."
  2. 현재: "이 한도는 다음 RAG 구간에서 자료를 나누는 첫 이유로 다시 나옵니다." → 변경: "이 한도는 RAG 구간에서 자료를 나누는 첫 이유로 다시 나옵니다. Attention이 문맥을 어떻게 모으는지는 부록에 한 장 있으니 궁금한 분은 나중에 보세요."
  3. 맨 끝(질문 뒤 · 답을 들은 다음)에 더함: "갈리는 곳은 3단계, 고르는 단계입니다. 그 폭을 정하는 값이 temperature이고 첫 질문 실습 뒤에 직접 바꿔 봅니다. 이어서 이 두뇌에게 질문을 보내는 코드가 어떤 모양인지 보겠습니다."
- 비주얼: 현 세 단계 카드(`.steps.row`)와 되돌아가는 줄(`.chip-row`), 캡션, 출처 줄은 그대로. 제목 왼쪽에 두뇌 아이콘(`brain.svg` · `.head-icon`)을 더한다. 설명문이 현재보다 짧아지므로 넘침 걱정은 없다.
- 예상 질문 & 답
  - "그러면 LLM은 계산이나 검색을 못 하나요?" → 두뇌는 다음 토큰을 고를 뿐입니다. 정확한 계산과 조회는 손발, 곧 Tool 구간에서 코드에 맡깁니다.
  - (노트 끝 질문의 답) 3단계입니다. 고르는 단계에서 갈리고, 그 폭을 정하는 값이 temperature입니다.
- 전환 문장: 이어서 이 두뇌에게 질문을 보내는 코드가 어떤 모양인지 보겠습니다.
- 소요시간: 3분 (문서 전용) · 스킵: 필수
- 메모(문서 전용): 노트가 길다. 시간이 밀리면 맨 앞의 "AI ⊃ 머신러닝 ⊃ 딥러닝" 두 문장을 건너뛴다. 한 줄 요지를 비유의 고정 문장으로 바꾸고 원래 요지를 설명문 첫 문장으로 내린 것은 코디네이터 결정 P1-8로 확정됐다. 화면의 새 용어는 LLM · 토큰 · 컨텍스트 윈도우 셋으로 줄었다(확률 분포는 카드 제목에만, 재학습 · temperature는 노트로).
- 근거: 요약본 §실습02 개념 · ROADMAP2 R2-8 · 검수 리포트 9절 17 · 18행 · 스토리보드 추적 표(두뇌의 고정 문장 · 부록 안내 S04 → A01) · 정의서 §7(LLM이 검색하고 계산한다는 오해) · 코디네이터 결정 P1-8

### [N01] 프롬프트 템플릿과 체인
- 구간/위치: 두뇌 · S04 뒤 · LAB02 앞 (병합 때 정식 ID 부여)
- 처리: 신설
- 유형: 코드 하이라이트 · data-skill: split
- 학습목표 연결: 실습02-LO2
- 핵심 메시지(1문장): 역할이 붙은 메시지를 틀로 만들고, 변수만 갈아 끼워 모델에 잇는 것이 체인입니다.
- 슬라이드 텍스트
  - 헤드라인: 프롬프트 템플릿과 체인
  - 한 줄 요지(.thesis): 역할이 붙은 메시지를 틀로 만들고, 변수만 갈아 끼워 모델에 잇는 것이 체인입니다.
  - 설명문(.explain): 모델에는 역할이 붙은 메시지를 차례로 담은 메시지 목록을 보냅니다. system은 규칙을, user는 이번 질문을 담습니다. 바뀌는 부분만 변수로 비워 둔 틀이 프롬프트 템플릿이고, 이 틀을 모델에 이은 것이 체인입니다.
  - 왼쪽 흐름도 글자

    | 상자 | 제목 | 작은 글씨 |
    |---|---|---|
    | 템플릿 카드 | 프롬프트 템플릿 | system · 규칙과 말투 / user · {topic}에 대해 알려 주세요 |
    | 잇는 기호 | 세로 막대 기호 한 글자(파이프 · 코드의 ③과 같은 기호) | 앞의 출력이 뒤의 입력 |
    | 모델 | 모델 | 두뇌 · 답을 만듦 |
    | 응답 | 응답 | 글자만 꺼내 출력 |

  - 왼쪽 아래 칩 줄: 변수만 바꿉니다 / {topic} = 고양이 / → / {topic} = 사내 근무
  - 오른쪽 코드 카드 제목(.code-title): 템플릿을 모델에 잇는 체인 · 개념 예시
  - 오른쪽 코드(.code-body · 강조는 `.hl`, 주석은 `.cm`)

    ```python
    model = init_chat_model(MODEL_NAME, base_url=GATEWAY_URL)  # 주소·열쇠 같은 나머지 연결 값도 환경 파일에서

    prompt = ChatPromptTemplate.from_messages([
        ("system", "쉬운 말로 짧게 답합니다"),      # ① 역할
        ("user", "{topic}에 대해 알려 주세요"),     # ② 변수
    ])
    chain = prompt | model                          # ③ 잇기
    print(chain.invoke({"topic": "고양이"}).content)
    ```

  - 코드 아래 번호 칩: ① 역할 / ② 변수 / ③ 잇기
  - 코드 카드 제목 왼쪽: LangChain 로고(`assets/img/logos/langchain.svg`) — "왜 LangChain인가"는 화면 글자 없이 로고와 노트가 맡는다
  - 코드 위치 줄(.guide-ref): 코드 위치: 실습자료 · 실습 02
  - 출처 줄: 출처: LangChain Models 문서 · LangChain Messages 문서 · LangChain 레퍼런스(ChatPromptTemplate)
- 비주얼
  - 종류: 흐름도 + 코드 하이라이트
  - 구성 설명: `.split-grid`. 왼쪽 `.stack.is-vcenter`: `.dg > .dg-row.stretch`에 상자 넷을 가로로 — 템플릿 카드는 `.dg-box.is-wide.accent`(안에 `.dg-sub` 두 줄, 둘째 줄의 `{topic}`만 `.hl` 색), 그 뒤 `.dg-arrow.has-label`(기호 `|` + `.dg-sub` "앞의 출력이 뒤의 입력"), 모델 상자(`brain.svg`), `.dg-arrow` "→", 응답 상자(`message-square.svg`). 그 아래 `.chip-row`(넷째 칩만 `.accent`). 오른쪽 `.stack`: `.code-card`(제목 줄 왼쪽에 LangChain 로고 — 현재 덱이 코드 카드 제목에 로고를 두는 방식 그대로, 그런 쓰임새가 없으면 로고는 생략한다) → 번호 칩 `.chip-row` → `.guide-ref`. 코드의 ① ② ③은 주석 자리에 두고 `.hl`로 강조하는 곳은 `("system"`, `{topic}`, `prompt | model` 세 군데다.
  - 자산: 아이콘과 LangChain 로고. 자리표시 없음.
- 교차 검수로 화면에서 노트로 내린 것(코디네이터 결정 X-14 — 넘침과 무관하게 지금 뺀다): 아래 한 줄 강조 상자 "LangChain은 이런 부품을 같은 방식으로 잇는 연결 도구입니다."(노트가 같은 말을 한다) · 설명문의 "문장 하나가 아니라"(노트에 있다). 설명문은 "메시지 목록"이라는 낱말을 심는 첫 문장을 더해 짧은 세 문장이다.
- 강사 스크립트: 방금 본 두뇌에게 질문을 보내는 코드를 읽어 보겠습니다. 첫 줄은 준비 구간에서 확인한 그대로, 환경 파일의 주소와 열쇠로 사내 모델에 연결하는 줄입니다. 그다음이 프롬프트 템플릿입니다. 모델에 보내는 것은 문장 하나가 아니라 역할이 붙은 메시지를 차례로 담은 메시지 목록이라서, system 줄에는 규칙을, user 줄에는 이번 질문을 적습니다. user 줄에서 중괄호로 비워 둔 곳이 변수이고, 실행할 때 이 자리에 들어갈 말만 넘기면 같은 틀로 다른 질문을 보낼 수 있습니다. 세로 막대 하나가 앞의 출력을 뒤의 입력으로 넘겨 주는 연결이고, 이렇게 이은 것을 체인이라고 부릅니다. LangChain은 이런 부품을 같은 방식으로 잇게 해 주는 연결 도구라서, 오늘 뒤에 나오는 검색과 도구도 같은 모양으로 붙습니다. 그러면 변수를 고양이에서 사내 근무로 바꾸면 코드에서 고쳐야 할 곳은 몇 군데일까요?
- 예상 질문 & 답
  - "그냥 문자열을 이어 붙이면 되지 않나요?" → 한 번이면 됩니다. 같은 틀을 여러 질문에 다시 쓰고, 뒤에 검색이나 도구를 같은 방식으로 붙이려면 틀과 연결을 나눠 두는 편이 읽기도 고치기도 쉽습니다.
  - "assistant 역할은 없나요?" → 있습니다. 모델이 낸 답에 붙는 역할입니다. assistant 역할은 Tool 실습에서 봅니다. 도구 결과를 붙여 다시 보낼 때 메시지 목록이 자라는 모습으로 함께 나옵니다.
  - "LangChain 말고 다른 도구를 써도 되나요?" → 됩니다. 어떤 도구를 언제 고르는지는 부록에 정리해 두었으니 수업 뒤에 보세요.
  - (노트 끝 질문의 답) 한 군데, 실행할 때 넘기는 값뿐입니다.
- 전환 문장: 이제 이 코드를 직접 실행하고 변수를 바꿔 보겠습니다.
- 소요시간: 2.5분 (문서 전용)
- 스킵 가능: 필수
- 메모(문서 전용): 이 장면은 **"메시지 목록"이라는 낱말만 심는다**(코디네이터 결정 X-3). R2-16에 따라 "모델은 이번에 보낸 목록만 본다"는 원리와 대화 기억 이야기는 하지 않는다(S16 · S30의 몫 — S16 캡션이 "메시지 목록(질문과 지금까지의 요청 · 결과)"으로 이 낱말을 받는다). 예상 질문의 "assistant 역할은 Tool 실습에서 봅니다"는 약속이고, LAB04b(P2)의 대체 도식 "목록이 자라는 모습"이 그 약속을 지킨다. 코드는 개념 예시다. `MODEL_NAME` · `GATEWAY_URL`은 가상 이름이고, `{topic}`은 실습자료가 쓰는 일반 낱말이라 그대로 썼다(코디네이터 결정 P1-3으로 확정). 첫 줄만으로는 실제로 연결되지 않으므로 주석이 "나머지 연결 값도 환경 파일에서"라고 밝힌다. 실습 직전 새 용어는 역할 · 프롬프트 템플릿 · 변수 · 체인 넷에 낱말 하나(메시지 목록)다. 부록 안내(A03 · A07)는 노트 본문에서 빼고 예상 질문의 답으로 옮겼다.
- 근거: 요약본 §실습02(실습 2-1 · 2-2 · 시도해볼 값) · ROADMAP2 R2-16 · 검수 리포트 3절 실습 02(템플릿 · 변수 · 체인 · 왜 LangChain) · 9절 12행 · 스토리보드 부록 안내(N01 → A03 · A07) · 공식 문서 확인(`init_chat_model` · `prompt | model`) · 코디네이터 결정 P1-3 · X-3 · X-14

### [LAB02] 실습 02 · 첫 질문과 체인
- 구간/위치: 두뇌 · N01 뒤
- 처리: 재작성 (← S08 ① ② ⑤ · 9절 25 · 26 · 28 · 29 · 31 · 34행)
- 유형: 실습 예고 · data-skill: title-bullets
- 학습목표 연결: 실습02-LO1 · 실습02-LO2
- 핵심 메시지(1문장): 첫 질문을 보내고, 이어서 템플릿 체인으로 변수만 바꿔 가며 보냅니다.
- 슬라이드 텍스트(스토리보드 「실습 안내판 사양」 그대로)
  - 제목: 실습 02 · 첫 질문과 체인
  - 코드 위치 줄(.board-meta): 코드 위치: 실습자료 · 실습 02

  | 항목 | 내용 |
  |---|---|
  | 소단계와 개념 칩 | 1 첫 호출 — ① 두뇌 · 다음 토큰 / 2 값을 모르는 질문 던지기 — ② 모델이 모르는 것 / 3 템플릿 체인 실행 — ③ 템플릿 · 체인 / 4 변수와 system 언어 바꾸기 — ④ 변수 |
  | 소단계 칸의 작은 글씨(`.when` · 실습자료 하위 번호) | 1 · 2 실습자료 2-1 / 3 · 4 실습자료 2-2 |
  | 관찰할 것 | "값을 모르는 질문(예: 아메리카노가 얼마인가요?)에 어떤 답이 나오는지 봅니다. 변수 하나만 바꿨을 때 무엇이 그대로이고 무엇이 달라지는지도 봅니다." |
  | 완료 조건 | "체인의 두 부품이 각각 무엇을 하는지 한 문장으로 말합니다." |
  | 막히면 | "고친 뒤 오류가 나면 되돌리기로 원래대로 돌린 다음 다시 실행합니다. 실행이 안 되면 실습 폴더의 맨 위인지 봅니다. 접속이 안 되면 준비된 출력으로 진행합니다." |
  | 강사 시연 대체(문서 전용 · 화면과 노트에 옮기지 않는다) | 필요 없음(짧다) |
  | 자리표시 | P-LAB02 · 넣을 자료: 첫 호출 출력과 체인 실행 출력 / 조건: 키 · 주소 · 모델명 가리기 / 대체: 체인 미니 도식(템플릿 → 모델 → 응답) |
  | 비고 | "환각"이라는 낱말은 이 안내판에 쓰지 않는다. 이름은 T01에서 붙인다. 값을 모르는 질문의 예시는 실습자료의 예시를 존댓말로 옮긴다("아메리카노가 얼마인가요?"). 화면과 노트 어디에서도 "우리 가게"라고 말하지 않는다(수강생은 어느 가게인지 말하지 않고 묻는다) |

  - 질문 예시의 자리: 소단계 2 칸의 `.when`은 실습자료 하위 번호가 차지하므로, 예시는 "관찰할 것"의 괄호 안에 둔다.
  - 출처 줄: 없음(개념의 출처는 N01에 있다)
- 비주얼
  - 종류: 실습 안내판
  - 구성 설명: LAB01과 같은 틀. `.time-block` 넷(`.tb-icon`은 차례로 `play.svg` · `circle-help.svg` · `link.svg` · `pencil.svg`, 둘째 칸 `.is-accent` — 이 실습에서 가장 눈여겨볼 관찰). 개념 칩 ①~④(둘째 칩 `.accent`). `.practice-grid` 오른쪽: 체인 미니 도식(`.dg-row.is-compact`에 상자 셋 "템플릿" → "모델" → "응답", N01의 왼쪽 그림을 줄인 것)과 `.placeholder.is-spec`.
  - 자산: `[IMG-PLACEHOLDER · P-LAB02]`
    - 넣을 자료: 첫 호출 출력과 체인 실행 출력
    - 조건: 키 · 주소 · 모델명 가리기
    - 대체: 체인 미니 도식(템플릿 → 모델 → 응답)
- 강사 스크립트: 먼저 첫 호출을 실행합니다. 질문 한 줄을 보내고 답에서 글자만 꺼내 출력하는 가장 단순한 호출입니다. 이어서 질문을 "아메리카노가 얼마인가요?"처럼 모델이 값을 알 수 없는 것으로 바꿔 보냅니다. 어느 가게인지 말하지 않았는데도 답이 나옵니다. 맞다 틀리다를 가리지 말고, 답이 어디까지 구체적인지만 적어 두세요. 다음으로 앞 장에서 읽은 템플릿 체인을 실행하고, 변수에 넣는 말을 바꿔 다시 실행한 뒤, system 줄의 언어도 한 번 바꿔 봅니다. 무엇이 그대로이고 무엇이 달라졌는지가 관찰 포인트입니다.
  코드를 고친 뒤 오류가 나면 당황하지 말고 되돌리기로 원래대로 돌린 다음 다시 실행하세요. 끝나면 하나 여쭙겠습니다. 어느 가게인지 모르는 두뇌는 왜 '보통 얼마'까지만 말할 수 있을까요?
- 예상 질문 & 답
  - "답이 옆 사람과 다릅니다." → 정상입니다. 같은 질문에도 표현이 달라질 수 있고, 왜 그런지는 바로 다음 장의 temperature에서 봅니다.
  - "가격을 꽤 그럴듯하게 말해 주는데요?" → 그 값이 어디서 왔는지 모델은 보여 주지 못합니다. 이 점은 실습이 끝난 뒤 한 장으로 정리합니다.
  - (노트 끝 질문의 답) 두뇌는 값을 찾아보지 않고 다음 토큰을 고를 뿐이라서, 학습한 글에 흔히 나오던 일반적인 말까지만 할 수 있습니다. 특정 가게의 오늘 값은 학습한 자료에 없습니다.
- 전환 문장: 넘기기 전에 하나만 예측해 보세요. 답이 흔들리지 않게 조이는 설정값이 있다면, 그 값을 조였을 때 틀린 답이 맞는 답으로 바뀔까요? 바뀐다면 손가락 하나, 안 바뀐다면 둘입니다. 같은 질문에도 답의 표현이 달라지는 이유와 함께 확인하겠습니다.
- 소요시간: 안내 1.5분 + 실습 약 7분 + 디브리핑 · 확인 1분 (문서 전용 · 실습 02와 03의 18분을 7 : 11로 나눈 어림값)
- 스킵 가능: 필수
- 메모(문서 전용): 요약본에 따르면 실습 01의 검증 실행과 이 실습의 첫 호출은 같은 코드다. 그래서 소단계 1은 "다시 돌려 보고 읽는" 단계로 짧게 끝난다(말미 5의 1번). "준비된 출력"은 강사가 미리 받아 둔 출력 파일을 뜻한다(코디네이터 결정 P1-10 — 아침 확인 항목). **전환 문장의 예측 질문은 S07의 확인 질문을 앞으로 당긴 것이다**(코디네이터 결정 X-13): S07 화면의 한 줄 요지("정확성 스위치가 아닙니다")가 곧 답이라, S07을 띄우기 전에 묻고 S07에서 답을 확인한다. 이 시점에는 temperature라는 낱말을 아직 화면에서 보지 않았으므로 "조이는 설정값"이라고만 말한다. 화면에 완료 조건이 있으므로 노트에서 다시 읽지 않는다.
- 근거: 요약본 §실습01 STEP 5 · §실습02 · ROADMAP2 R2-1 · R2-7 · 검수 리포트 3절 실습 02(값을 모르는 질문) · 9절 25~34행 · 스토리보드 「실습 안내판 사양」 LAB02 · 확인 필요 15 · 코디네이터 결정 P1-10 · X-13

### [S07] Temperature와 출력 변화
- 구간/위치: 두뇌 · LAB02 뒤로 **이동**(현재는 S04 바로 뒤) · L01 앞
- 처리: 보강 · 이동
- 유형: 비교 · data-skill: compare
- 학습목표 연결: 실습03-LO1
- 핵심 메시지(1문장): temperature는 다양성을 조절할 뿐 정확성 스위치가 아닙니다.
- 바꿀 문장(화면)
  1. 설명문 · 현재: "2단계에서 계산한 분포를 3단계에서 고를 때 얼마나 뾰족하게 볼지 정합니다. 바뀌는 것은 선택의 폭뿐입니다." → 변경: "두뇌가 다음 토큰을 고를 때, 계산해 둔 분포를 얼마나 뾰족하게 볼지 정하는 값입니다. 바뀌는 것은 선택의 폭뿐입니다." (이동으로 S04의 "2단계 · 3단계"가 세 장 앞이 되어 지시어를 풀어 썼다)
  2. 캡션 · 현재: "같은 후보 5개의 분포 모양 · 개념 예시 · 수치 없음" → 변경: "같은 후보 5개의 분포 모양 · 개념 예시 · 값의 범위는 0부터 2까지입니다"
  3. 한 줄 요지와 세 열의 글자: 바꿀 문장 없음
- 더하는 것(화면): 세 열 아래 값 범위 눈금 한 줄 — 왼쪽 끝 "0" · 오른쪽 끝 "2" · 눈금 아래 왼쪽 "비슷한 답" · 오른쪽 "다양한 답". 실습 기본값은 화면에 쓰지 않는다(스토리보드 확인 필요 16).
- 바꿀 문장(노트)
  1. 맨 앞에 더함: "방금 예측하신 것부터 확인하겠습니다. 답을 조이는 그 설정값의 이름이 temperature이고, 화면 첫 줄이 답입니다. 다양성을 조절할 뿐 정확성 스위치가 아닙니다. 방금 같은 질문에도 옆 사람과 답의 표현이 달랐던 것도 이 값 때문이고, 이어지는 캐릭터 봇 실습에서 직접 바꿔 봅니다."
  2. 현재: "오늘 실습에서는 temperature 하나만 바꿔 봅니다." → 변경: "오늘 실습에서는 temperature 하나만 바꿔 봅니다. 값의 범위는 0부터 2까지이고, 실습 코드는 0.7에서 시작합니다."
  3. 현재(끝 문장): "같은 질문을 여러 번 보내면 어떤 차이가 날지 먼저 예측해 보세요." → 변경: "그래서 값을 낮추면 같은 답이 되풀이될 뿐이고, 그 답이 맞는지는 따로 확인해야 합니다. 이어서 같은 두뇌에 성격과 답의 틀을 입히는 방법을 보겠습니다."
- 확인 질문의 자리(코디네이터 결정 X-13): 질문("조였을 때 틀린 답이 맞는 답으로 바뀔까요?")은 **앞 장면 LAB02의 전환 문장**에서 먼저 묻고, 이 장면은 답을 확인하는 자리다. 이 장면의 노트 안에서는 묻지 않는다(한 줄 요지가 답을 먼저 보여 주기 때문이다). 같은 질문을 LAB03 디브리핑에서 실습 결과를 놓고 한 번 더 묻는다.
- 비주얼: 현 분포 막대 세 열(`.compare-grid.cols-3` + `.dist`) 그대로. 눈금 한 줄은 `.scale-line`(양 끝 숫자 "0" · "2"와 가운데 가는 선 · 대비 3:1 이상 — 코디네이터 결정 3절에서 채택됨)으로 그리고, 눈금 아래 왼쪽 "비슷한 답" · 오른쪽 "다양한 답"은 `.dg-sub`. 넘치면 눈금 줄을 빼고 캡션의 문장만으로 대신한다(이미 범위가 들어 있다).
- 예상 질문 & 답
  - "0으로 두면 항상 같은 답이 나오나요?" → 거의 같아지지만 서버 환경에 따라 완전히 같다는 보장은 없습니다. 화면 셋째 열의 마지막 줄이 그 말입니다.
  - "업무에는 몇으로 두는 것이 좋나요?" → 정해진 형식의 답이 필요하면 낮게, 표현을 여러 개 받아 고르고 싶으면 높게 둡니다. 어느 쪽이든 사실 확인은 별도입니다.
- 전환 문장: 이어서 같은 두뇌에 성격과 답의 틀을 입히는 방법을 보겠습니다.
- 소요시간: 2.5분 (문서 전용) · 스킵: 필수
- 메모(문서 전용): 출처 줄 "vLLM Reproducibility 문서 · LangChain Models 문서"는 공식 문서라 그대로 둔다(코디네이터 결정 C-4 · C-5로 확정). 9절에 S07의 충돌 행은 없고, 바꾸는 이유는 위치 이동과 스토리보드의 "값 범위 눈금 한 줄", 그리고 확인 질문의 자리 옮김(X-13)이다. 검토자가 제안한 예측 질문("값을 끝까지 올리면 긍정봇의 정해진 마무리 한마디도 바뀔까요?")은 이 시점에 긍정봇의 마무리가 아직 소개되지 않아 이 장면에 두지 않고, LAB03 노트의 관찰 한 줄로 옮겼다.
- 근거: 요약본 §실습03 코드 포인트(temperature 0~2 · 실습 기본값) · ROADMAP2 R2-4 ① · 검수 리포트 3절 실습 03 · 스토리보드 구간 2(12번) · 확인 필요 16 · 정의서 §7("temperature 0 = 항상 정답") · 코디네이터 결정 C-4 · X-13 · 3절

### [L01] 성격과 답의 틀 정하기
- 구간/위치: 두뇌 · S07 뒤로 **이동** · LAB03 앞
- 처리: 재작성 · 이동 (9절 19~24행)
- 유형: 비교 · data-skill: split
- 학습목표 연결: 실습03-LO2
- 핵심 메시지(1문장): system 프롬프트에 성격과 답의 틀을 적어 두면 같은 두뇌가 다른 봇이 됩니다.
- 슬라이드 텍스트
  - 헤드라인: 성격과 답의 틀 정하기
  - 한 줄 요지(.thesis): system 프롬프트에 성격과 답의 틀을 적어 두면 같은 두뇌가 다른 봇이 됩니다.
  - 설명문(.explain): system 프롬프트는 모든 질문 앞에 먼저 놓이는 지침입니다. 누구처럼 말할지가 성격(페르소나)이고, 어떤 순서로 답하고 어떻게 끝낼지가 답의 틀(format)입니다. 모델은 그대로이고 지침만 다릅니다.
  - 왼쪽 카드 제목(.code-title): system 프롬프트 · 개념 예시
  - 왼쪽 카드 본문(세 줄, 줄 앞에 번호 마커)
    - ① 당신은 무엇이든 긍정적으로 바꿔 말하는 봇입니다.
    - ② 먼저 공감하고, 이어서 좋은 면을 찾아 줍니다.
    - ③ 마지막은 항상 "이거 완전 럭키비키자나"로 끝냅니다.
  - 왼쪽 카드 아래 번호 칩: ① 성격 / ② 답의 순서 / ③ 정해진 마무리
  - 오른쪽 위 입력 줄(.chip): 같은 입력 · "야근하게 됐어"
  - 오른쪽 답 카드 A(제목: system 없음): 야근이 잦으면 피로가 쌓일 수 있습니다. 일정을 조정하거나 충분히 쉬는 것이 좋습니다.
  - 오른쪽 답 카드 B(제목: system 있음 · 강조): ② 아이고, 야근이라니 많이 지치시겠어요. 그래도 조용한 사무실에서 밀린 일을 끝낼 기회예요. ③ 이거 완전 럭키비키자나! — 카드 제목 옆에 "① 말투 전체"
  - 오른쪽 캡션: 개념 예시 · 실제 답은 실행할 때마다 다릅니다
  - 코드 위치 줄(.guide-ref): 코드 위치: 실습자료 · 실습 03
  - 출처 줄: 출처: LangChain Messages 문서
- 없애는 것(R2-4 · R2-16): 역할 세 상자(system · user · assistant), "앞의 답을 다시 넣지 않으면 이전 대화를 모릅니다", "다음 요청에 다시 넣어야 기억", "예시 몇 개를 함께 넣으면 답의 모양이 예시를 따릅니다", 메시지 목록 코드(assistant 줄 포함), 노트의 긍정봇 기억 확인과 few-shot 설명. 역할 소개는 N01이 맡았다.
- 비주얼
  - 종류: 코드 하이라이트 + 전후 비교
  - 구성 설명: `.split-grid`. 왼쪽 `.stack`: `.code-card`(본문 세 줄, 번호 ① ② ③은 `.hl`) → `.chip-row` 번호 칩 셋 → `.guide-ref`. 오른쪽 `.stack`: `.chip-row`(입력 줄) → `.dg > .dg-row.stretch`에 `.dg-box` 둘을 위아래가 아니라 좌우로(A는 보통, B는 `.accent`). 상자 안 글은 `.dg-sub`, B의 ② ③ 번호는 왼쪽 카드와 같은 `.hl` 색으로 같은 번호임을 보인다. ①은 문장 한 곳이 아니라 말투 전체에 드러나므로 B 카드의 제목 옆에 붙인다. 아래 `.dg-caption`.
  - 자산: 아이콘은 두뇌(`brain.svg` — 두 답 카드 사이 위쪽에 하나, "같은 두뇌"를 뜻함). 자리표시 없음.
- 강사 스크립트: 앞에서 system 줄에는 규칙을 적는다고 했는데, 그 한 줄이 얼마나 많은 것을 정하는지 보겠습니다. 왼쪽이 이어지는 실습의 긍정봇에 들어 있는 것과 같은 짜임의 system 프롬프트입니다. 럭키비키는 나쁜 일도 운 좋게 받아들인다는 뜻의 유행어입니다. ①번 줄은 누구처럼 말할지, 곧 성격을 정하고, ②번과 ③번 줄은 어떤 순서로 답하고 어떻게 끝낼지, 곧 답의 틀을 정합니다. 오른쪽은 같은 입력에 대한 답 두 개입니다. system이 없을 때는 일반적인 조언이 나오고, 있을 때는 공감하고 좋은 면을 찾은 뒤 정해진 한마디로 끝납니다. 모델은 하나도 바뀌지 않았고 바뀐 것은 지침뿐입니다. 예측 질문 하나 드리겠습니다. 왼쪽에서 ③번 줄만 지우면 오른쪽 답의 어디가 달라질까요? 실습에서 직접 지워 보고 확인해 보세요.
- 예상 질문 & 답
  - "system에 적으면 반드시 지키나요?" → 대체로 따르지만 보장은 아닙니다. 지침일 뿐이라서, 꼭 지켜야 하는 제한은 코드와 권한에서 막습니다. 그 이야기는 Agent 구간의 안전장치에서 다시 합니다.
  - "성격과 답의 틀을 나눠 적어야 하나요?" → 한 덩어리로 적어도 동작합니다. 나눠 읽는 이유는 나중에 고칠 때 어디를 고칠지 알기 위해서입니다.
- 전환 문장: 이제 같은 두뇌에 성격을 입히고, temperature도 직접 바꿔 보겠습니다.
- 소요시간: 2.5분 (문서 전용)
- 스킵 가능: 필수
- 메모(문서 전용): 왼쪽 세 줄은 요약본의 "공감 → 긍정 전환 → 정해진 마무리 규칙"을 옮긴 개념 예시이고 실제 프롬프트의 글자가 아니다(스토리보드 확인 필요 18 · 요청서 문항 B3 · B4). 마무리 문구는 화면에 그대로 쓴다(코디네이터 결정 P1-4로 확정 — 실습자료와 같은 이름 · 같은 문구라 수강생이 헷갈리지 않는다). 입력 예 "야근하게 됐어"와 봇의 답은 따옴표 안에 실습자료의 말투 그대로 둔다(머리말의 표기 약속). 이 장면의 "예측 → 실습에서 확인" 짜임은 그대로 둔다(예측 질문의 답이 화면에 먼저 보이지 않는다).
- 근거: 요약본 §실습03 코드 포인트 · 테스트 입력 예시 · ROADMAP2 R2-4 ② ③ · R2-16 · 검수 리포트 3절 실습 03 · 9절 19~24행 · 정의서 §7("system prompt = 보안장치"는 05 · 07에서) · 코디네이터 결정 P1-4

### [LAB03] 실습 03 · 럭키비키 긍정봇
- 구간/위치: 두뇌 · L01 뒤
- 처리: 신설 (← S08 ③ ④ · 9절 25~27 · 29 · 30 · 32 · 33 · 35행)
- 유형: 실습 예고 · data-skill: title-bullets
- 학습목표 연결: 실습03-LO1 · 실습03-LO2
- 핵심 메시지(1문장): 같은 두뇌에 성격과 답의 틀을 입히고, temperature로 다양성을 바꿔 봅니다.
- 슬라이드 텍스트(스토리보드 「실습 안내판 사양」 그대로)
  - 제목: 실습 03 · 럭키비키 긍정봇
  - 코드 위치 줄(.board-meta): 코드 위치: 실습자료 · 실습 03

  | 항목 | 내용 |
  |---|---|
  | 소단계와 개념 칩 | 1 봇 실행 · 입력 하나 — ① 성격 / 2 temperature를 바꿔 같은 입력 여러 번 — ② temperature / 3 system 프롬프트에서 성격과 답의 틀 찾기 — ③ 답의 틀 / 4 대화 루프로 이어 말하기 — ④ 같은 체인 다시 부르기 / 5 같은 봇을 웹 화면으로 — ⑤ Streamlit · 실행만 |
  | 소단계 칸의 작은 글씨(`.when` · 실습자료 하위 번호) | 5 실습자료 3-1 (소단계 1~4는 요약본에 하위 번호가 없어 비워 둔다) |
  | 관찰할 것 | "세 가지를 봅니다. temperature에 따라 답이 얼마나 달라지는지, 봇의 성격이 어디서 정해지는지, 답의 순서와 마무리가 어디서 정해지는지입니다." |
  | 완료 조건 | "바꾼 것 하나와 달라진 것 하나를 한 문장으로 말합니다." |
  | 막히면 | "고친 뒤 오류가 나면 되돌리기로 원래대로 돌린 다음 다시 실행합니다. 접속이 안 되면 준비된 출력으로 같은 비교를 합니다. 웹 화면이 뜨지 않으면 짝 화면으로 봅니다." |
  | 강사 시연 대체(문서 전용 · 화면과 노트에 옮기지 않는다) | 소단계 4는 생략할 수 있다. 소단계 5는 실행만 하고 구조 설명은 하지 않는다("구조는 Agent 실습 앞에서 봅니다") |
  | 자리표시 | P-LAB03(← P-S08) · 넣을 자료: temperature 두 값에서 같은 입력의 답 비교 / 조건: 주소 · 키 가리기 / 대체: 관찰 세 가지 체크 카드 |
  | 비고 | 기억 여부는 관찰하지 않는다(R2-16). 테스트 입력은 실습자료의 예시(일상 속 속상한 일 세 가지)를 그대로 쓴다 |

  - 출처 줄: 없음(개념의 출처는 S07 · L01에 있다)
- 비주얼
  - 종류: 실습 안내판
  - 구성 설명: LAB01과 같은 틀. `.time-block` 다섯(`.tb-icon`은 차례로 `play.svg` · `thermometer.svg` · `list-checks.svg` · `message-square.svg` · `globe.svg`, 둘째 칸 `.is-accent` · 다섯째 칸의 `.when`에 "실습자료 3-1"). 소단계 아이콘은 비유 축의 아이콘 파일(공통 부품 A)과 겹치지 않는 것만 쓴다. 개념 칩 ①~⑤(둘째 칩 `.accent`). `.practice-grid` 오른쪽: 관찰 세 가지 체크 카드(`.dg-check` 세 줄 — "temperature에 따른 차이" · "성격이 정해지는 곳" · "답의 순서와 마무리가 정해지는 곳")와 `.placeholder.is-spec`.
  - 자산: `[IMG-PLACEHOLDER · P-LAB03]` (현재 덱의 P-S08 자리를 이어받는다 — 자리는 그대로, ID와 "넣을 자료"만 바뀐다 · 9절 30행)
    - 넣을 자료: temperature 두 값에서 같은 입력의 답 비교
    - 조건: 주소 · 키 가리기
    - 대체: 관찰 세 가지 체크 카드
- 강사 스크립트: 먼저 긍정봇을 실행하고 속상한 일 하나를 입력해 봅니다. "오늘 시험을 망쳤어", "면접에서 떨어졌어", "야근하게 됐어" 가운데 하나를 그대로 쓰시면 됩니다. 이어서 코드에서 temperature가 적힌 줄을 찾아 0에 가깝게 한 번, 1을 넘겨 한 번 바꾸고, 값마다 같은 입력을 세 번씩 보내 답이 얼마나 달라지는지 비교합니다. 값을 높여도 정해진 마무리 한마디는 그대로인지도 보세요. 차이가 없었다면 그것도 관찰 결과입니다. 다음으로 system 프롬프트를 열어 성격을 정한 곳과 답의 순서, 마무리를 정한 곳을 각각 짚어 보세요. 앞 장에서 예측한 대로 마무리 줄을 지우고 다시 실행해 보셔도 좋습니다.
  그다음은 대화 루프로 이어서 말을 걸어 보고, 마지막으로 같은 봇을 웹 화면으로 띄워 봅니다. 대화 루프는 실습자료에 적힌 종료 낱말로 끝내고, 웹 화면은 터미널에서 Ctrl과 C를 함께 눌러 멈춘 뒤 다음으로 갑니다. 웹 화면은 지금은 실행만 하고, 구조는 Agent 실습 앞에서 봅니다. 코드를 고친 뒤 오류가 나면 되돌리기로 원래대로 돌린 다음 다시 실행하세요.
- 예상 질문 & 답
  - "temperature를 바꿨는데 답이 거의 같습니다." → 입력이 짧고 답의 틀이 단단하면 차이가 작습니다. 값을 양 끝으로 더 벌려 보고, 그래도 같으면 그대로 적어 두세요.
  - "웹 화면은 어떻게 만들어진 건가요?" → 같은 봇에 화면을 입힌 것입니다. 화면 · 처리 · 데이터 세 부분으로 나눠 읽는 법은 Agent 실습 앞에서 봅니다.
  - "봇이 앞에 한 말을 기억하나요?" → 이번 실습에서는 보지 않습니다. 모델이 무엇을 보고 답하는지는 Tool 실습에서 다룹니다.
- 전환 문장: 말투와 틀은 마음대로 바꿨습니다. 그러면 두뇌가 끝내 하지 못한 것은 무엇이었는지 정리해 보겠습니다.
- 소요시간: 안내 1.5분 + 실습 약 11분 + 디브리핑 · 확인 2분 (문서 전용 · 어림값)
- 스킵 가능: 필수(소단계 4는 생략 가능)
- 메모(문서 전용): 시간이 밀리면 소단계 4를 건너뛴다(비상 사다리 2단 · −2분). 소단계 5는 실행만 한다(R2-10). 디브리핑에서는 LAB02 전환 문장에서 예측하고 S07에서 확인한 질문("낮추면 틀린 답이 맞는 답으로 바뀔까요?")을 실습 결과를 놓고 다시 묻고, 두세 명의 "바꾼 것 하나 · 달라진 것 하나"를 듣는다. temperature를 바꾸는 위치는 코드 미확인이라 노트에서 "적힌 줄을 찾아"까지만 말한다(요청서 문항 B2). "0에 가깝게 · 1을 넘겨 · 세 번씩"은 0.6과 0.8처럼 가까운 두 값을 고르면 차이가 보이지 않기 때문에 넣었다(범위 0~2는 요약본의 값). 대화 루프의 종료 낱말은 화면과 노트에 글자로 쓰지 않고 "실습자료에 적힌 종료 낱말"이라고만 말한다. 웹 화면을 멈추지 않으면 터미널이 잡혀 다음 실습의 명령이 먹지 않는다 — 전형적인 막힘이라 노트에 넣었다. 개념 칩 ④는 "입력 반복"(개념이 아님)에서 "같은 체인 다시 부르기"로 바꿨다(기억은 관찰하지 않는다 — R2-16). "준비된 출력"은 강사가 미리 받아 둔다(코디네이터 결정 P1-10). 화면에 완료 조건이 있으므로 노트에서 다시 읽지 않는다.
- 근거: 요약본 §실습03 · ROADMAP2 R2-4 · R2-10 · R2-16 · 검수 리포트 3절 실습 03 · 9절 25~35행 · 스토리보드 「실습 안내판 사양」 LAB03 · 자리표시 계승 표(P-S08 → P-LAB03) · 코디네이터 결정 P1-10 · X-5 · X-13

### [T01] 두뇌의 한계, 그래서 책
- 구간/위치: 두뇌의 마지막 · LAB03 뒤 · D03(P2) 앞
- 처리: 재작성 (S09 합침 · 9절 36~43행)
- 유형: 브릿지(회고→예고) · data-skill: compare
- 학습목표 연결: 실습02-LO1 · (다음 구간) 실습04-LO1
- 핵심 메시지(1문장): 말투와 틀은 바꿀 수 있지만 모델이 모르는 자료는 답하지 못합니다. 그래서 다음은 책을 찾아보며 답하는 RAG입니다.
- 슬라이드 텍스트
  - 헤드라인: 두뇌의 한계, 그래서 책
  - 한 줄 요지(.thesis): 말투와 틀은 바꿀 수 있지만, 모델이 모르는 자료는 답하지 못합니다. 그래서 다음은 책을 찾아보며 답하는 RAG입니다.
  - 설명문(.explain · 세 문장): 캐릭터 봇 실습에서 성격과 답의 틀, temperature로 답의 모양은 바꿨습니다. 하지만 값을 알 수 없는 질문에는 일반적인 답이 나왔고, 특정한 값을 자신 있게 말한 경우도 있었습니다. 다음 실습은 질문과 가까운 조각을 찾아 질문과 함께 넘겨 그 빈자리를 채웁니다.
  - 영문 병기: 이 장면은 **"RAG"만 쓴다**(한 줄 요지 · 셋째 열 · 아래 띠). "검색 증강 생성(RAG)" 병기는 S10(P2) 한 곳이다(코디네이터 결정 C-7 · X-4).
  - 세 열

    | 열 | 작은 머리말 | 제목 | 항목 |
    |---|---|---|---|
    | 1 | ① 지금 되는 것 | 실습 02 · 03 · 두뇌 | 변수만 바꿔 다시 묻기 / 성격과 답의 틀 정하기 / temperature로 다양성 조절 |
    | 2(강조) | ② 아직 안 되는 것 | 모델만으로는 한계 | 학습 이후의 최신 자료 / 사내 문서·우리 부서 자료 / 출처를 대며 답하기 |
    | 3 | ③ 그래서 다음 | 실습 04 · 책 찾아보며 답하기 | 질문과 가까운 조각을 찾아 붙이기 / 답을 찾은 조각과 대조하기 / 없으면 없다고 답하기 |

  - 둘째 열 아래 한 줄(.compare-note): 그럴듯하지만 근거 없는 답을 환각(hallucination)이라 부릅니다.
  - 아래 띠(.chip-row): [두뇌 아이콘] 두뇌 · 실습 02 · 03 → [책 아이콘] 책 · 실습 04 (강조 칩) / 띠 끝 작은 글씨: RAG는 책을 찾아보며 답하기입니다.
  - 출처 줄: 출처: Lewis et al. 2020
- S09에서 가져오는 것과 넘기는 것(R2-15): 환각의 이름과 정의 한 줄, 한계 세 가지(이미 둘째 열에 있음), 노트의 "모델이 알 수 없는 업무 자료 떠올리기"는 T01이 받는다. "근거를 붙여도 확인은 필요합니다"와 "근거를 붙이면 환각이 완전히 사라질까요?"는 S14(P2)로 넘긴다. S09 장면은 없어진다.
- 비주얼
  - 종류: 비교표(세 열) + 비유 띠
  - 구성 설명: 현재 T01의 마크업 그대로(`.compare-grid.cols-3` · 둘째 열 `.is-accent`). 열 아이콘(`.col-icon`)을 첫째 열 `brain.svg`, 둘째 열 `triangle-alert.svg`(현재 그대로), 셋째 열 `book-open.svg`로 바꾼다. 둘째 열 목록 아래 `.compare-note` 한 줄(환각의 정의). 아래 `.chip-row`: `brain.svg`(`.row-icon`) + `.chip` → `.dg-arrow` "→" → `book-open.svg` + `.chip.accent` → `.dg-sub`(고정 문장).
  - 자산: 아이콘만. 자리표시 없음.
- 강사 스크립트: 두뇌 구간을 정리하겠습니다. 왼쪽 열처럼 변수만 바꿔 다시 묻고, system 프롬프트로 성격과 답의 틀을 정하고, temperature로 다양성을 조절하는 것까지는 됐습니다. 그런데 아메리카노 값을 물었을 때는 어느 가게인지도 모르는 채 일반적인 답이 나왔고, 어떤 분은 특정 값을 단정한 답을 받으셨을 겁니다. '보통 이 정도'라는 답은 환각이 아닙니다. 모르는 것을 아는 것처럼 하나의 값으로 말했을 때가 환각입니다. 가운데 열의 세 가지, 학습 이후의 자료와 사내 자료와 출처는 모델 안에 없습니다. 두뇌가 다음 토큰을 고르는 방식이라 생기는 부작용이고, 말투나 temperature를 바꿔서는 없어지지 않습니다.
  그래서 다음 정거장은 책입니다. 질문과 가까운 조각을 찾아 질문과 함께 넘겨서, 두뇌가 책을 찾아보며 답하게 만듭니다. 여러분 업무에서 모델이 절대 알 수 없는 자료 하나를 떠올려 보세요. 다음 실습에서는 기사 한 편이 그 자료의 자리에 들어갑니다.
- 예상 질문 & 답
  - "그러면 그 자료로 모델을 다시 학습시키는 건가요?" → 아닙니다. 모델은 그대로 두고, 찾은 조각을 질문과 함께 넘길 뿐입니다. 바로 다음 장에서 그 흐름을 봅니다.
  - "'보통 얼마쯤'이라는 답도 환각인가요?" → 아닙니다. 모르는 값을 아는 것처럼 단정했을 때가 환각입니다.
  - "모른다고 답하면 환각이 아닌가요?" → 네, 모른다고 답했다면 환각이 아닙니다. 없는 것을 자신 있게 채웠을 때를 환각이라고 부릅니다.
- 전환 문장: 다음 정거장은 책, 찾아보며 답하는 RAG입니다.
- 소요시간: 2.5분 (문서 전용)
- 스킵 가능: 필수
- 메모(문서 전용): 없애는 문장 — "규칙·예시로 답 모양 정하기"(37행), "최신 자료를 물었을 때는 …"(38 · 40행), "기사 본문을 …"(39행), 노트의 "규칙과 예시"(41행)와 "긍정봇이 두 번째 말을 기억하지 못한 것은 …"(42행), "실습 1 · 실습 2" 표기 전부(36행). "환각"이라는 낱말은 이 장면이 처음이다(LAB02에서는 관찰만 했다). 수강생이 실제로 보낸 질문은 어느 가게인지 말하지 않은 값 질문이므로 화면과 노트에서 "우리 가게"라고 말하지 않고, "일반적인 답"과 "단정한 답"을 갈라 뒤쪽에만 환각의 이름을 붙인다. 둘째 열 아래의 환각 정의 한 줄은 P2(S14 · LAB04b)가 받아 쓰는 글자라 그대로 둔다. 설명문은 세 문장으로 줄어 넘침 걱정이 없다.
- 근거: 요약본 §실습02(값 질문) · §실습04 개념 · ROADMAP2 R2-2 · R2-4 · R2-7 · R2-8 · R2-15 · 검수 리포트 9절 36~43행 · 스토리보드 추적 표(책의 고정 문장 · 순서 문제 4번) · 정의서 §7("RAG = 모델을 학습시키는 것") · 코디네이터 결정 C-1 · C-7 · X-4

---

## 말미 1 · 구간별 시간 합계표 (문서 전용 — 화면과 노트에 옮기지 않는다)

| 구간 | 장면별 분(강의 · 안내) | 강의 합 | 실습 | 디브리핑 · 확인 | 구간 합 | 스토리보드 수요 | 배정안 | 차이 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 0 도입 | S01 1 · G01 3 · S02 3 · G00 2 | 9 | 0 | 0 | 9 | 9 | 10 | −1 |
| 1 준비 | D01 0.5 · E01 3 · LAB01 2 | 5.5 | 18 | 2 | 25.5 | 25.5 | 20 | +5.5 |
| 2 두뇌 | D02 0.5 · S04 3 · N01 2.5 · LAB02 1.5 · S07 2.5 · L01 2.5 · LAB03 1.5 · T01 2.5 | 16.5 | 18(실습 02 약 7 · 실습 03 약 11) | 3(1 + 2) | 37.5 | 37.5 | 30 | +7.5 |
| **P1 합** | | **31** | **36** | **5** | **72** | 72 | 60 | **+12** |

- 스토리보드의 수요와 같다(장면을 더하거나 빼지 않았다). 초과분 +12분의 처리는 스토리보드의 비상 사다리를 그대로 따른다. P1 범위에서 쓸 수 있는 것은 1단의 S02 · G00 축약(각 −1분)과 2단의 LAB03 소단계 4 생략(−2분)으로 모두 −4분이다.
- 나머지 +8분은 실습 01의 실제 소요에 달려 있다(R2-6이 유지되므로 설계로 줄일 수 없다 · 스토리보드 확인 필요 17). 실습 02와 03의 7 : 11 분할은 요약본의 단계 수로 어림한 값이다.
- 교차 검수 반영으로 장면 수와 분은 바뀌지 않았다(화면 글자를 줄이고 노트를 짧게 했을 뿐이다). 실습 03은 소단계 다섯에 어림 11분이라 코드 수정 오류가 한 번만 나도 넘친다 — "막히면"의 되돌리기 문장과 소단계 4 생략이 그 대비다. 당일의 단계 판단은 덱 밖의 강사 진행표(코디네이터 결정 X-18 · 메인 에이전트가 만든다)가 맡고, 화면과 노트에는 옮기지 않는다.
- 밀도 점검: 개념 장면의 연속은 S04 → N01(두 장) 뒤 LAB02, S07 → L01(두 장) 뒤 LAB03이라 다섯 장을 넘지 않는다. 구간 1 · 2 모두 첫 장면에서 "이 구간에서 만들 것"이 표지와 핵심 메시지로 보인다.

## 말미 2 · 자리표시 목록 (이미지 확보 to-do)

| 자리표시 | 장면 | 넣을 자료 | 조건(가릴 것) | 대체(캡처가 없어도 빌드가 막히지 않게) | 계승 |
|---|---|---|---|---|---|
| P-S02 | S02 | 프로젝트 1개의 공개 화면 | 계정·사내 정보 가리기 | 확보 전엔 카드 설명으로 진행 | 그대로 |
| P-LAB01 | LAB01 | 검증 실행 성공 화면 | 키 · 주소 · 모델명 가리기 | 열쇠 지도 축소판(첫째 · 둘째 열쇠 강조) | 신규 |
| P-LAB02 | LAB02 | 첫 호출 출력과 체인 실행 출력 | 키 · 주소 · 모델명 가리기 | 체인 미니 도식(템플릿 → 모델 → 응답) | 신규 |
| P-LAB03 | LAB03 | temperature 두 값에서 같은 입력의 답 비교 | 주소 · 키 가리기 | 관찰 세 가지 체크 카드 | ← P-S08(자리는 그대로, ID와 넣을 자료만 바뀜) |

- 자리표시가 없는 장면(G01 · G00 · D01 · E01 · D02 · S04 · N01 · S07 · L01 · T01)은 모두 도식 · 아이콘 · 썸네일이 시각 요소다.

### 비유 축 아이콘 파일(코디네이터 결정 C-1 확정 · 모두 `assets/img/icons/`에 있다 · 대체 파일 없음)

| 파일 | 개념 | P1에서 쓰는 곳 |
|---|---|---|
| `brain.svg` | 두뇌(LLM) | 띠 · S04 제목 · N01 모델 상자 · L01 · T01 |
| `book-open.svg` | 책(RAG) | 띠 · T01 셋째 열과 아래 띠 |
| `hand.svg` | 손발(Tool) — 이 개념 전용 | 띠 |
| `joystick.svg` | 조종(Agent) | 띠 |
| `laptop.svg` | 내 PC의 에이전트(Claude Code) | 띠 |
| `rocket.svg` | 적용 | 띠 |
| `key.svg` | 열쇠 | 띠의 준비 표시 · E01 · 열쇠 지도 축소판 · LAB01 |
| `door-open.svg` | 문 | E01 열쇠 지도 |

- 안내판 소단계 아이콘(`.tb-icon`)에는 위 여덟 파일 가운데 `key.svg`(LAB01 소단계 1 — 같은 개념)만 쓰고, 나머지는 비유와 상관없는 파일을 쓴다(LAB03은 `play` · `thermometer` · `list-checks` · `message-square` · `globe`).

### CSS 요청의 처리 결과(코디네이터 결정 3절)

| # | 콘티 초안의 요청 | 처리 | 콘티에 반영한 값 |
|---:|---|---|---|
| 1 | 비유 축 진행 띠(밝은 배경 · 반전 배경 · 상태 세 가지) | 채택 · 필수 | 이름은 UI 사양대로 `ol.axis-strip` · `.axis-stop` · `.axis-tile` · `.axis-icon` · `.axis-label` · `.axis-prep`, 상태 `is-done / is-current / is-todo`(공통 부품 A · D01 · D02 · G01) |
| 2 | G01용 9열 격자와 Part 묶음 상자 줄 | **채택하지 않음** | G01을 `ol.axis-strip` 6칸 + 실습 번호 칩 줄 + `axis-prep`("준비 · 01")로 다시 썼다. Part 묶음은 캡션 둘째 줄 |
| 3 | 썸네일 그림의 네 모서리 둥글림 | 채택 | G00 — 마크업 변화 없음 |
| 4 | `.dg-box.is-dashed`(점선 테두리) | 채택 | E01 셋째 열쇠 줄 |
| 5 | `.scale-line`(양 끝 숫자와 가는 선) | 채택 | S07 눈금 한 줄 |
| — | 표지용 제목 크기 변형 | 채택 | D01 · D02의 큰 제목은 `.hero-title.is-section`, 아래 한 줄은 `.hero-sub` |

### 빌드할 때 넘침을 살필 곳
- E01: 글을 줄였지만 여전히 좌우 두 그림이 함께 있다 → 넘치면 왼쪽 캡션의 앞 절("주소·모델명·키는 역할 이름으로 표시")을 뺀다. 열쇠 지도의 글자는 빼지 않는다.
- G01: 준비 표시가 띠 위로 튀어나온다 → `.panel-title`과 겹치면 `.panel-title`을 뺀다. 다섯째 정거장의 이름("내 PC의 에이전트")과 칩("실습 06 · 07")이 칸 폭을 넘지 않는지 본다.
- N01: 강조 상자를 뺐으므로 오른쪽 열은 코드 카드 · 번호 칩 · 코드 위치 줄 셋이다 → 그래도 넘치면 왼쪽 아래 칩 줄("변수만 바꿉니다 …")을 뺀다(노트의 끝 질문이 같은 구실을 한다).
- LAB01 · LAB03: `.practice-grid` 오른쪽에 대체 도식과 자리표시가 함께 들어가고 "막히면"이 세 문장으로 늘었다 → 높이가 모자라면 대체 도식(LAB01은 열쇠 지도 축소판)을 개념 칩 줄로 올린다.
- D01 · D02: 큰 제목은 `.nowrap` 두 덩어리로 감싼다(`<br>` 금지).
- S04 · T01: 설명문이 현재보다 짧아져 넘침 걱정이 없다.

## 말미 3 · 용어집 갱신분 (P1에서 처음 쓰거나 표기를 정한 말)

| 표기(이후 이렇게만 쓴다) | 최초 병기 | 처음 나오는 곳 | 비고 |
|---|---|---|---|
| LLM | 대규모 언어모델(LLM) | S04 설명문 | G01 띠의 작은 글씨 "LLM"은 라벨이라 병기하지 않는다 |
| 정거장 | — | G01 | 비유 축의 여섯 칸을 부르는 말. "준비"는 정거장이 아니다 |
| 환경 파일 | 환경 파일(.env) | E01 | 현재 덱의 "환경변수"를 대체한다. 오후에는 설정 파일(P3) |
| 세 층 | — | E01 | **연결의 그림에만 쓴다**(내 PC · 환경 파일 · 사내 LLM 게이트웨이 — E01 · LAB01 · 오후 S23). 앱의 그림은 "화면 · 처리 · 데이터 세 부분"이라 부른다(코디네이터 결정 X-5) |
| 사내 LLM 게이트웨이 | — | E01 | 작은 글씨 "뒤에서 추론 서버가 모델을 실행". vLLM은 노트(예상 질문의 답)에서만 |
| 문 | — | E01 | 첫째 · 둘째 문은 게이트웨이에 달려 있고 그 뒤에 모델이 있다. 셋째 문은 게이트웨이 밖, 다른 플랫폼의 문이다 |
| 메시지 목록 | — | N01 설명문 | 낱말만 심는다("역할이 붙은 메시지를 차례로 담은 메시지 목록"). 원리("모델은 이번에 보낸 목록만 봅니다")는 S16 · S30의 몫(코디네이터 결정 X-3 · C-8) |
| 첫째 열쇠 · 채팅용 키 / 둘째 열쇠 · 임베딩용 키 / 셋째 열쇠 · 데이터 조회용 토큰 | — | E01 | 문 이름: 채팅 모델 · 임베딩 모델 · 사내 데이터 |
| 검증 실행 | — | LAB01 | 실습 01의 마지막 실행을 부르는 역할 이름 |
| 토큰 · 컨텍스트 윈도우 · temperature | — | S04 · S07 | 현재 덱 표기 유지 |
| 프롬프트 템플릿 · 변수 · 체인 | — | N01 | 파이프 기호는 "세로 막대"라고 말로 풀어 준다(노트) |
| system 프롬프트 | — | N01(system 줄) · L01 | 현재 덱의 "system 한 줄"을 대체한다 |
| 성격 | 성격(페르소나) | L01 | LAB03 · T01에서는 "성격"만 |
| 답의 틀 | 답의 틀(format) | L01 | 세부 이름: 답의 순서 · 정해진 마무리 |
| 환각 | 환각(hallucination) | T01 | LAB02에서는 낱말을 쓰지 않는다 |
| 환각이 아닌 것 | — | T01 노트 | "보통 이 정도"라는 일반적인 답, "모른다"는 답은 환각이 아니다. 모르는 것을 아는 것처럼 하나의 값으로 말했을 때만 환각이라 부른다 |
| RAG | 검색 증강 생성(RAG) — **병기는 S10(P2) 한 곳** | P1에서는 T01 | G01의 작은 글씨 "RAG"는 라벨. T01은 "RAG"만 쓴다(코디네이터 결정 C-7 · X-4) |
| 럭키비키 긍정봇 | — | LAB03 | "캐릭터 봇 실습" = 실습 03을 본문에서 가리키는 이름 |

## 말미 4 · 다른 묶음과 맞춰야 할 이음매에서 P1이 확정한 문장

### P1 ↔ P2
| 이음매 | P1이 확정한 글자 | P2가 받을 곳 |
|---|---|---|
| T01 셋째 열 | 제목 "실습 04 · 책 찾아보며 답하기" · 항목 "질문과 가까운 조각을 찾아 붙이기 / 답을 찾은 조각과 대조하기 / 없으면 없다고 답하기" | D03 · S10의 첫 문장("… 질문과 가까운 조각을 찾아 질문과 함께 넘기는 것입니다")과 낱말이 같다(조각 · 찾아 · 함께 넘겨) |
| T01 설명문 끝 | "다음 실습은 질문과 가까운 조각을 찾아 질문과 함께 넘겨 그 빈자리를 채웁니다." (여기서 끝난다) | **T01은 "RAG"만 쓴다. "검색 증강 생성(RAG)" 병기는 S10 한 곳**(코디네이터 결정 C-7 · X-4) |
| 환각의 정의 | 화면: "그럴듯하지만 근거 없는 답을 환각(hallucination)이라 부릅니다." · 노트: "'보통 이 정도'라는 답은 환각이 아닙니다. 모르는 것을 아는 것처럼 하나의 값으로 말했을 때가 환각입니다." | S14의 "근거를 붙여도 확인은 필요합니다" · LAB04b 개념 칩 ①의 "환각" — LAB04b 노트도 "지어낼 수도 있고, 모른다고 할 수도 있습니다"처럼 단정하지 않는 쪽으로 맞춘다 |
| 낱말 "메시지 목록" | N01 설명문 첫 문장 "모델에는 역할이 붙은 메시지를 차례로 담은 메시지 목록을 보냅니다." (낱말만 · 원리는 말하지 않는다) | S16 캡션 "메시지 목록(질문과 지금까지의 요청 · 결과)" · S30 · T03 노트의 첫 언급(코디네이터 결정 X-3 · C-8) |
| assistant 역할의 약속 | N01 예상 질문의 답 "assistant 역할은 Tool 실습에서 봅니다. 도구 결과를 붙여 다시 보낼 때 메시지 목록이 자라는 모습으로 함께 나옵니다." | LAB04b의 대체 도식 "목록이 자라는 모습"(user · assistant · tool 역할이 보이게) |
| T01이 넘긴 질문 | "근거를 붙이면 환각이 완전히 사라질까요?"는 T01에서 묻지 않는다 | S14 |
| T01 노트의 끝 | "여러분 업무에서 모델이 절대 알 수 없는 자료 하나를 떠올려 보세요. 다음 실습에서는 기사 한 편이 그 자료의 자리에 들어갑니다." | S10 노트가 이 말을 받아 시작할 수 있다 |
| 컨텍스트 윈도우의 정의 | S04 화면 "한 번에 넣을 수 있는 입력·출력 길이의 한도를 컨텍스트 윈도우라고 합니다." · 노트 "이 한도는 RAG 구간에서 자료를 나누는 첫 이유로 다시 나옵니다." | R01의 "나누는 첫 이유" |
| 열쇠 지도 | 공통 부품 B(이름 · 문 · 받는 곳). **축소판은 칩 한 줄이 기준이다**(`.chip-row` 하나 · 맨 앞 `key.svg` · 칩 셋 "첫째 · 채팅용 키" / "둘째 · 임베딩용 키" / "셋째 · 데이터 조회용 토큰" · 강조할 칩만 `.chip.accent` · 꼬리는 `.dg-sub` 한 마디) — 코디네이터 결정 X-6 | R01(둘째 강조 — 카드 번호는 ③ 임베딩 · 코디네이터 결정 X-12) · LAB04 "막히면" · LAB04b · N02(셋째 강조 · 꼬리 "문 · 사내 데이터"). 상자 셋으로 그리지 않는다 |
| 첫 예고 | E01 셋째 줄 "사내 LLM 게이트웨이 밖, 다른 플랫폼의 문입니다 · Tool 실습에서 받습니다 · 만료됩니다" | LAB04b 소단계 4 · N02의 401 — 고정 구절 "401은 열쇠 문제입니다. 어느 문에서 났는지 보고 그 열쇠를 확인합니다."(코디네이터 결정 X-2). E01이 "문"을 셋으로 갈라 두었으므로 "어느 문"이라는 말이 통한다 |

### P1 ↔ P4
| 이음매 | P1이 확정한 글자 | P4가 받을 곳 |
|---|---|---|
| 띠 | 공통 부품 A(칸 이름 · 작은 글씨 · 실습 번호 · 아이콘 파일). 띠 문구 "두뇌 → 책 → 손발 → 조종 → 내 PC의 에이전트 → 적용" | G02는 G01과 같은 `ol.axis-strip` + 정거장 안의 실습 번호 칩 줄을 쓰고 여섯 정거장 모두 `is-done`. "준비 · 01" 표시도 G01을 따른다(코디네이터 결정 P4-6 · 반전 배경에서의 밝기는 공통 부품 A 참조). "하는 일" 줄은 G01에서도 화면에 없다 |
| G01의 한 줄 요지 | "두뇌에서 시작해 책과 손발을 붙이고 조종을 맡긴 뒤, 내 PC의 에이전트를 거쳐 내 업무에 적용합니다." | G02의 "두뇌에서 내 업무까지 왔습니다." |
| 공부 방법 네 단계 | 만들기 · 설명하기 · 조건 바꾸기 · 검증하기(G00 화면 · 바꾸지 않음). G00 노트 끝 "이 네 단계는 오늘 마지막 장에서 한 번 더 돌아봅니다." | G02 노트 |
| 반복 업무 한 줄 | S02 노트 "잠깐 각자 줄이고 싶은 반복 업무 하나를 메모에 한 줄 적어 두세요. 오늘 마지막 자유 실습의 재료가 됩니다."(바꾸지 않음) | S39 |

### P1 ↔ P3 (스토리보드 이음매 표에는 없지만 글자가 맞아야 하는 곳)
| 이음매 | P1이 확정한 글자 | P3가 받을 곳 |
|---|---|---|
| 세 층 그림 | E01 왼쪽 그림(내 PC ⇄ 환경 파일 ⇄ 사내 LLM 게이트웨이 · 아이콘 · 작은 글씨는 상자마다 한 줄: "요청을 보냄" / "주소와 열쇠를 적는 곳" / "열쇠를 확인하고 모델로 전달" + 가장 작게 "뒤에서 추론 서버가 모델을 실행") | S23이 같은 그림을 다시 그린다(가운데만 "설정 파일"). **"세 층"이라는 말은 이 그림에만 쓴다.** S29 · S21 · LAB05의 앱 그림은 "화면 · 처리 · 데이터 세 부분"(코디네이터 결정 X-5) |
| 첫째 열쇠의 재등장 | E01 "오후에도 같은 열쇠를 씁니다" · 노트 "첫째 열쇠는 오후에 Claude Code를 연결할 때 그대로 다시 쓰니 잘 챙겨 두세요." | G03 준비물 칩 "오전에 받은 채팅용 키" · S23 · LAB06 |
| Streamlit | LAB03 노트 "웹 화면은 지금은 실행만 하고, 구조는 Agent 실습 앞에서 봅니다." · "웹 화면은 터미널에서 Ctrl과 C를 함께 눌러 멈춘 뒤 다음으로 갑니다." | S29 노트의 시작 "캐릭터 봇 실습에서 실행만 해 본 그 도구" · LAB05의 웹 화면판에도 멈추는 법 한 문장을 같은 말로 |
| 열쇠 값을 드러내지 않기 | LAB01 완료 조건 "… 짝과 서로 확인합니다(값은 읽지 않습니다)." | LAB07의 "환경 파일에는 열쇠가 들어 있으므로 읽히지 않습니다. 화면을 함께 볼 때도 열지 않습니다."(코디네이터 결정 X-10)와 같은 규칙이다 |
| system 프롬프트의 한계 | L01 예상 질문의 답 "지침일 뿐이라서, 꼭 지켜야 하는 제한은 코드와 권한에서 막습니다. 그 이야기는 Agent 구간의 안전장치에서 다시 합니다." | N03 안전장치 칸 |

### 전 묶음 (표지 D01~D07의 공통 틀에 P1이 덧붙인 세부 — D03~D07도 같게)
| 항목 | P1이 정한 것 |
|---|---|
| 마크업 자리(UI 사양 E-3과 같다) | 머리말 = `.hero-kicker` · 큰 제목 = `.hero-title.is-section` · 아래 한 줄 = `.hero-sub` · 그 아래 띠 `ol.axis-strip`(정거장 `li.axis-stop` · 상태 `is-done / is-current / is-todo`) |
| 큰 제목의 줄바꿈 | "정거장 이름 —"과 "하는 일"을 각각 `.nowrap`으로 감싼다(두 덩어리 · 코디네이터 결정 C-3). `<br>`은 쓰지 않는다 |
| 띠 왼쪽 끝 "준비" 표시 | **D01에만 둔다**(`li.axis-prep.is-current`). D02~D07에는 두지 않는다 — UI 사양에 "지나옴" 모양이 없어 남겨 두면 D01과 똑같이 강조된다 |
| 띠에 보이는 글자 | 아이콘과 정거장 이름만(작은 글씨 · 실습 번호 없음) |
| 아이콘 파일 | 코디네이터 결정 C-1의 파일(조종 `joystick.svg` · 내 PC의 에이전트 `laptop.svg`). UI 사양 예시 마크업의 다른 파일 이름은 따르지 않는다 |
| 노트 | 두 문장 이내. 기본은 "지금까지 … / 여기서부터 …" 꼴이고, 바로 앞 장면이 전환 장면(T01~T03)이거나 고정 전환 문장(LAB01 → D02)이면 "여기서부터 …" 한 문장만 말한다(같은 말을 세 번 되풀이하지 않는다) |

## 말미 5 · 확인 필요 사항 (새 질문 · 권고 기본값 포함)

스토리보드 확인 필요 1~18은 권고 기본값대로 썼다(이 묶음에 해당: 3 · 4 · 14 · 15 · 16 · 17 · 18). 아래는 콘티를 쓰면서 새로 생긴 것이다.

| # | 질문 | 권고 기본값(이 콘티에 적용한 값) | 누가 풀 수 있나 |
|---:|---|---|---|
| 1 | 요약본으로는 실습 01의 검증 실행과 실습 02의 첫 호출이 같은 코드로 보인다. 맞다면 LAB02 소단계 1은 같은 실행의 되풀이다 | 맞다고 보고 LAB02 메모에 "다시 돌려 보고 읽는 단계"로 적었다(화면과 노트는 어느 쪽이어도 어긋나지 않는 문장). 다르면 메모만 고친다 | 사내 확인(요청서에 문항 추가 권고) |
| 2 | 실습 02와 03의 시간 분할 · 실습 01의 실제 소요 | 약 7 : 11(문서 전용 어림값). 강사의 첫 회차 경험으로 고친다. 실습 01의 소요는 오전 시간 추정의 가장 큰 변수다(코디네이터 아침 확인 3번) | 사용자 |
| 3 | 안내판 `.when`에 적은 실습자료 하위 번호(실습 01의 STEP 2~5 · 2-1 · 2-2 · 3-1)가 원본과 같은가 | 요약본의 번호대로 적었다. 다르면 짝짓는 글자만 바꾼다. LAB01 소단계 2 · 3은 실습자료 STEP 3 · 4에 걸쳐 있어 "STEP 3 · 4" / "STEP 3"으로 나눠 적었다 | 사내 확인(요청서에 문항 추가 권고) |
| 4 | 열쇠 발급 화면에서 채팅용 · 임베딩용으로 무엇을 골라야 하는지는 역할 이름만으로 알 수 없다 | 화면에는 쓰지 않고(사내 고유명), LAB01 노트가 "실습자료 실습 01의 열쇠 발급 단계에 적힌 대로 고르세요"라고 가리킨다. 강사는 당일 자기 화면으로 고르는 곳을 한 번 보여 준다(값은 가린다) | 사용자(아침 확인) |
| 5 | "준비된 출력"(LAB02 · LAB03 "막히면")이 실제로 준비되어 있는지 | 강사가 첫 호출 · 체인 · temperature 두 값의 출력을 미리 받아 둔다(코디네이터 결정 P1-10 · 아침 확인 1번) | 사용자 |
| 6 | LAB01 완료 조건의 "짝과 열쇠 칸 확인"이 사내 보안 기준에 맞는가(값은 읽지 않고 칸이 채워졌는지만 본다) | 맞다고 보고 넣었다. 꺼려지면 "각자 열쇠 칸 두 개가 채워졌는지 확인합니다"로 바꾼다(둘째 열쇠가 검증되지 않는 문제는 남는다) | 사용자 |

코디네이터 결정으로 닫힌 질문(콘티 초안의 1~5 · 8~10번): 아이콘 파일 = C-1 확정 / G01의 그림 = P1-2(`ol.axis-strip` + 칩 줄) / N01의 `{topic}` = P1-3 그대로 / L01의 마무리 문구 = P1-4 그대로 / S07 출처 줄 = C-4 유지 / S04 한 줄 요지 = P1-8 적용 / E01 게이트웨이 상자 = P1-9 한도 적지 않음 / 준비된 출력 = P1-10.

교차 검수의 "제안"(확정 사실을 건드리는 의견)은 본문에 반영하지 않았다. P1에 닿는 것은 하나다 — 열쇠 발급을 사전 과제로 옮기자는 의견(R2-6에 닿음). 차선으로, 사전 안내문에 "발급 화면을 미리 열어 로그인까지만 해 두기"를 넣을 수 있다(발급은 당일 그대로 · 사용자 판단).

요청서(`docs/handoff-inhouse-claude-lab-check.md`)에 추가를 권하는 문항
- 실습 01의 검증 실행과 실습 02의 첫 호출이 같은 파일인가(위 1번).
- 실습자료 원본의 하위 번호가 요약본과 같은가(위 3번).
- 긍정봇 코드에서 temperature 값은 어디에 적혀 있는가(코드 안의 숫자 / 환경 파일 / 웹 화면의 조절 막대) — 요청서 문항 B2를 구체화. LAB03 노트의 "temperature가 적힌 줄을 찾아"가 맞는지 가린다.
- 실습 02의 템플릿 코드에서 역할 이름이 "user"인가 "human"인가 — N01 개념 예시의 글자를 실습 코드와 같게 맞추기 위해서다(어느 쪽이든 동작은 같다).

## 말미 6 · 품질 게이트 (정의서 §10 · P1 범위)

| 항목 | 판정 | 근거 |
|---|---|---|
| 실습마다 Why 장면 한 장 이상 | 통과 | 01 E01 · 02 S04 + N01 · 03 S07 + L01 |
| 지정 주제 다섯 개 가운데 P1 몫 | 통과 | LLM 기초 원리 = S04(+ S07). "RAG가 필요한 이유"의 앞 절반 = T01(나머지는 S10 · P2) |
| 내용 축 · 실습 없는 축의 표시 | 통과(P1 몫) | 축 ① = S04 · S07, 부록 A01 안내는 S04 노트. 축 ③ 안내(A03 · A07)는 N01 예상 질문의 답 한 줄(노트 본문에서는 뺐다) |
| 서사가 구간 경계에서 끊기지 않음 | 통과 | G00 → D01(노트) · LAB01의 고정 전환 문장 → D02 · T01 → D03. 띠는 G01 · D01 · D02에서 같은 그림 |
| 모든 개념이 첫 사용 전에 설명됨 | 통과 | 열쇠 · 환경 파일(E01 → LAB01) · 템플릿 · 변수 · 체인(N01 → LAB02) · temperature(S07 → LAB03) · 성격 · 답의 틀(L01 → LAB03) · 환각(LAB02 관찰 → T01 이름) · uv(LAB01 개념 칩과 노트) |
| 의도적 에러의 예고와 디브리핑 | 통과(P1 몫) | P1 범위에 의도적 에러는 없다. 401의 첫 예고를 E01 지도("만료됩니다")와 노트에 두었다 |
| 구간마다 확인 질문 하나 이상 | 통과 | 도입 G01 노트 · 준비 E01 노트(① · ②는 예상 질문과 LAB01 디브리핑) · 두뇌 LAB02 노트 ① · ②는 LAB02 전환 문장에서 묻고 S07에서 확인(답이 화면에 먼저 보이지 않게 — X-13) · L01의 예측 질문은 LAB03에서 확인 |
| 실습 직전 새 용어(X-14) | 통과 | E01 → LAB01: 화면의 새 말은 환경 파일 · 사내 LLM 게이트웨이 · 열쇠 3종(한 묶음)으로 줄였고 추론 서버 · vLLM · uv의 풀이는 노트로 내렸다. N01 → LAB02: 역할 · 프롬프트 템플릿 · 변수 · 체인 + 낱말 "메시지 목록". 강조 상자와 부록 안내는 화면 · 노트 본문에서 뺐다 |
| 모든 장면에 비주얼, 참고 영상은 썸네일과 요점만 | 통과 | 15장 모두 도식 · 아이콘 · 자리표시 지정. G00은 재생하지 않음 |
| 장면별 분 · 구간 합계 ≤ 배정 | **조건부 통과** | 도입 −1 · 준비 +5.5 · 두뇌 +7.5. 스토리보드의 비상 사다리를 그대로 따르며 P1에서 −4분까지 줄일 수 있다. 분은 문서에만 |
| 본문 50장 안팎 | 통과(P1 몫) | 본문 13장 + 표지 2장. 스토리보드의 장수와 같다(S09는 T01에 합침) |
| 실제 키 · 토큰 값 없음, 고유명은 역할 이름 | 통과 | 사내 주소 · 모델명 · 키 이름 · 팀 이름 · 폴더와 파일 이름을 쓰지 않았다. `.env` 표기는 공통 규약 3이 허용한 "환경 파일(.env)" 한 곳뿐 |
| 하우스 룰 위반 없음 | 통과 | 시각은 G01의 세 개뿐(현재 값) · 분 없음 · 구간 코드와 막 표현 없음 · 화면 문장 존댓말 · 출처 줄은 공식 문서와 논문 · `<br>`과 인라인 style을 요구하지 않음 · 자리표시 P-S02 · P-S08(→ P-LAB03) 자리 유지 |
| ROADMAP2 0절 · 0-0절과 충돌 없음 | 통과 | R2-4(few-shot · 기억 · 인젝션 없음) · R2-5(열쇠 3종) · R2-6(당일 범위) · R2-7(번호) · R2-8(비유) · R2-10(Streamlit 실행만) · R2-13 · R2-15(S09 합침) · R2-16 · R2-17 · R2-20 · R2-21(토큰 사용량 · 공급사 이름 · 후속 읽을거리를 쓰지 않음). 검수 리포트 9절 1~43행을 모두 다뤘다 |
| 모든 제안에 근거 표기 | 통과 | 장면마다 근거 줄 |
| 예시가 반도체 현업 맥락 | 해당 적음 | P1은 환경과 LLM 기초라 실습자료의 예시(아메리카노 · 긍정봇 입력)를 따랐다. 현업 맥락은 T01 노트("여러분 업무에서 모델이 절대 알 수 없는 자료")와 S02의 공정 업무 도구로 잇는다 |
| 용어 표기가 용어집과 일치 | 통과 | 말미 3. 최초 병기: LLM(S04) · 환경 파일(E01) · 성격 · 답의 틀(L01) · 환각(T01). RAG의 병기는 S10(P2) 한 곳이고 T01은 "RAG"만 쓴다. "세 층"은 E01의 연결 그림에만 쓴다 |
| 교차 검수 마지막 점검(금지 낱말 0건) | 통과 | 옛 띠 · 격자 클래스 이름, 옛 상태 이름, 비유 아이콘으로 잘못 쓰인 파일 이름, 옛 흐름의 낱말을 이 파일에서 검색해 "현재" 인용 칸 밖에서는 0건임을 확인했다 |
| 참고자료 문장을 복사하지 않음 | 통과 | 교안 PDF는 열지 않았다. 공식 문서는 페이지 이름과 API 모양만 확인했다 |

